# Trust Ledger Lane Log

## 2026-08-10 - L0 direct snapshot restore proof summary - Codex

- Scope: reconciled the paused L0 worker backlog and closed one real operator-facing gap without changing snapshot, receipt, Merkle, restore, or Treeship schemas.
- Behavior changed: direct `zmem restore <snapshot> --summary-only` now surfaces verified snapshot write-receipt and chain counts, transition count, provenance-anchor verification, linked Treeship artifacts, and trusted-provenance status before reporting the separate restore receipt.
- Reconciliation: `zmem lineage <memory-id> --summary-only` already provides ordered mutation-chain UX, so that stale tracker item is now marked shipped instead of reimplemented.
- Tests: focused restore, CLI onboarding (`224/224`), snapshot (`43/43`), supported-runtime full suite (`1,387` with two expected skips), eval (`11/11`), strict release smoke, and diff checks pass.
- Proof boundary: the summary exposes verification already performed by `verify_snapshot`; it does not claim semantic truth or add a new trust source.
- Automation state: broad swarms remain paused. Any future L0 run needs a fresh bounded acceptance target against current `main`.

## 2026-07-20 - L0 digest-bound memory context - Codex

- Scope: added one canonical commitment for the exact `zerker.memory_context.v1` artifact supplied to an agent, without changing retrieval, SQLite schema, Merkle v1, or agent authority.
- Behavior changed: inject persists a compact context commitment; `why`, wrapped runs, CLI summaries, Treeship action proof, and ActiveGraph read proof carry the same digest and policy reference.
- Tests: full Python suite passes `1,266` tests with two expected optional skips; eval `11/11`, site/docs builds, release smoke, strict prelaunch, public proof, assets, and return-packet verification pass.
- Proof boundary: the digest proves exact ZMem context at the admission boundary. It does not prove memory truth, model reasoning, or prompts outside that context artifact.
- Next safe slice: carry this proof into the scheduled-agent cold-start/gap-audit demo before starting isolated dense retrieval work.

## 2026-07-16 - L0 review hardening and versioned-root boundary - Codex

- Scope: fixed the confirmed event-chain concurrency, user-path, and MCP boolean issues without changing receipt schemas, artifact types, signing logic, or the published Merkle contract.
- Behavior changed: direct event appends acquire SQLite's writer lock before reading and advancing the chain head; user paths expand consistently; MCP boolean fields require actual JSON booleans.
- Tests: deterministic two-connection append contention, store path expansion, CLI path parsing, and MCP string-boolean refusal pass; the full Python suite passes `1,250` tests with two expected optional skips and eval passes `11/11`.
- Proof boundary: `binary-sha256-v1` remains unchanged for compatibility. A correct successor needs leaf/node domain separation plus explicit leaf-count or tree-shape binding and legacy-v1 verification; it will be designed and reviewed as a separate versioned migration.
- Review boundary: operator-profile capabilities are not exposed to the default agent profile, and direct store methods are in-process primitives. Future findings must be classified against those boundaries before severity is assigned.
- Next safe slice: specify Merkle `v2` with fixtures and migration tests in an isolated branch after `v0.1.6`, while an independent reviewer re-tests the clean release tag read-only.

## 2026-07-06T12:57:26Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable bundle/export regression for already-shipped duplicate `supporting_memories[*].id` refusal behavior, without changing store semantics, receipt schemas, snapshot payloads, CLI wording, or Treeship dependency boundaries.
- Files touched: `tests/test_snapshot.py`; `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. A self-rehashed receipt bundle that duplicates the same bundled memory entry under `supporting_memories` is now regression-locked to fail local `verify_bundle(...)` and portable `export_receipt(..., fmt="treeship")` before any `.treeship.json` artifact is written.
- Tests: focused bundle/export verification `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_bundle_rejects_duplicate_supporting_memory_entries tests.test_exporter.ExporterTest.test_rejects_duplicate_supporting_memory_entries_in_bundle_treeship_file_export -q` -> passed (`Ran 2 tests`); `git diff --check -- tests/test_snapshot.py tests/test_exporter.py` -> passed; `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 37 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 20 tests`).
- Artifacts/receipts: this is a regression-lock only. It keeps portable bundle lineage honest by refusing duplicated convenience entries even when the underlying supporting write receipt chain still verifies, while preserving the honest wording that verified provenance does not guarantee semantic truth.
- Blockers: this slice intentionally stops at duplicate `supporting_memories` entries on the existing bundle/export path. It does not widen into new schema, new verifier logic, or the unrelated dirty-worktree temporal/store failures.
- Next safe slice: lock one similarly bounded `trusted_only` wording or portable bundle/export refusal contract that is already implemented but not yet regression-covered, only if that matters more than clearing the unrelated shared-store overlap first.

## 2026-07-06T09:02:05Z - L0 trust-ledger - Codex

- Scope: hardened one bounded portable bundle/export seam by making bundled `supporting_memories[*].id` stay aligned with declared `supporting_memory_ids`, without changing receipt schemas, snapshot payloads, CLI wording, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `zerker_memory/treeship.py`; `tests/test_snapshot.py`; `tests/test_treeship.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_bundle(...)` and `to_treeship_statement(bundle)` now reject self-rehashed portable bundles whose `supporting_memories` list relabels a bundled memory under an undeclared id, even when the supporting write receipt chain remains internally valid.
- Tests: focused bundle/export regressions `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_bundle_rejects_supporting_memory_entry_missing_from_declared_supporting_ids tests.test_treeship.TreeshipAdapterTest.test_rejects_bundle_with_supporting_memory_entry_missing_from_declared_supporting_ids -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py zerker_memory/treeship.py tests/test_snapshot.py tests/test_treeship.py docs/CONTINUOUS_BUILD/trust-ledger.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` -> passed; `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 36 tests`); `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 44 tests`); required `python3 -m unittest tests.test_store -q` -> failed on unrelated temporal identity rank assertions in `tests.test_store.MemoryStoreTest.test_query_at_no_search_selected_current_scope_preserves_identity_bitemporal_fields` and `tests.test_store.MemoryStoreTest.test_query_at_no_search_selected_current_scope_keeps_pre_activation_identity_metadata_explicit`; `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: this keeps portable bundle lineage and Treeship-ready export honest from local data only. Verified provenance still means the bundled memory list matches the declared proof set; it does not guarantee semantic truth of the memory content.
- Blockers: the required full `tests.test_store -q` run is currently red on two temporal identity rank expectations outside this bundle/export seam, so this lane run leaves that pre-existing overlap unresolved.
- Next safe slice: lock one similarly bounded bundle/export contract such as duplicate `supporting_memories` ids or one remaining `trusted_only` wording surface, but only if that matters more than the unrelated temporal-store failures now blocking a clean full-store pass.

## 2026-07-06T04:56:52Z - L0 trust-ledger - Codex

- Scope: locked one bounded `forget()` portable export-verification seam that was already implemented on shipped receipts, without changing store semantics, receipt schemas, export shapes, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. A self-rehashed forgotten mutation receipt that forges `treeship_statement.object.actor_id` is now regression-locked to fail `export_receipt(..., fmt="treeship")` before any portable `.treeship.json` artifact is written, with the same `source event actor_id mismatch` contract already enforced by the shipped local verifier and Treeship statement export path.
- Tests: focused receipt/export verification `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_forget_actor_identity tests.test_exporter.ExporterTest.test_rejects_tampered_forget_actor_identity_memory_write_treeship_file_export -q` -> passed (`Ran 2 tests`); `git diff --check -- tests/test_exporter.py docs/CONTINUOUS_BUILD/trust-ledger.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` -> passed.
- Artifacts/receipts: this is a regression-lock only. It keeps the portable file export path honest for forgotten mutation receipts by refusing to mint a provenance artifact when actor identity verification fails, while preserving the honest wording that verified provenance does not guarantee semantic truth.
- Blockers: this slice intentionally stops at one already-shipped `forget()` export seam. Other L0 sibling file-export and bundle-continuity seams still need separate bounded coverage if operators want broader portable proof hardening before schema work.
- Next safe slice: lock one similarly bounded exporter or bundle refusal contract on another already-shipped mutation seam, such as a remaining prior/new-root continuity or portable-file rejection path, only if that risk now matters more than non-L0 lane work.

## 2026-07-06T00:56:31Z - L0 trust-ledger - Codex

- Scope: locked one bounded `revoke()` actor-identity proof seam that was already implemented on shipped receipts, without changing store semantics, receipt schemas, export shapes, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_store.py`; `tests/test_treeship.py`; `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. A self-rehashed revoked mutation receipt that forges `treeship_statement.object.actor_id` is now regression-locked to fail direct local verification, `to_treeship_statement(...)` export verification, and raw `snapshot verify --summary-only` as `source event actor_id mismatch`.
- Tests: focused regression lock `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_revoke_actor_identity tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_revoke_actor_identity_memory_write_statement tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_revoke_actor_identity_as_untrusted -q` -> passed (`Ran 3 tests`).
- Artifacts/receipts: this is a regression-lock only. It keeps revoked mutation receipts independently checkable for actor identity across local, export, and snapshot-summary proof surfaces while preserving the honest wording that verified provenance does not guarantee semantic truth.
- Blockers: this slice intentionally stops at revoked mutation actor identity. Other L0 sibling seams still need separate bounded coverage if operators want broader receipt hardening before schema work.
- Next safe slice: lock one similarly bounded regression on another already-shipped mutation/export/bundle seam, such as a remaining prior/new-root or export-continuity contract, only if that risk now matters more than non-L0 lane work.

## 2026-07-05T20:57:55Z - L0 trust-ledger - Codex

- Scope: hardened one bounded `revoke()` previous-status continuity seam so the shipped mutation receipt can prove its prior state across local verification, Treeship export, and snapshot-summary verification without making Treeship a user-facing dependency.
- Files touched: `zerker_memory/store.py`; `zerker_memory/treeship.py`; `tests/test_store.py`; `tests/test_treeship.py`; `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `revoke()` source events now persist `previous_status`, `verify_memory_write_receipt(...)` now re-checks `zerker.memory.mutation_receipt` `revoke()` statements so `object.previous_status` must match the embedded `REVOKED` source event payload when present, and Treeship export now carries a minimal prior-receipt `status` stub so older prior-linked revoke receipts still get the same continuity check when possible. A self-rehashed tampered revoke receipt that changes `treeship_statement.object.previous_status` now fails direct local verification, `to_treeship_statement(...)` export verification, and raw `snapshot verify --summary-only`.
- Tests: focused gap repro `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_revoke_previous_status tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_revoke_previous_status_memory_write_statement tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_revoke_previous_status_as_untrusted -q` -> failed (`3` failures), then the same focused cluster -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 330 tests`); `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 42 tests`); `python3 -m unittest tests.test_trust_ledger_cli -q` -> passed (`Ran 11 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed; `git diff --check -- zerker_memory/store.py zerker_memory/treeship.py tests/test_store.py tests/test_treeship.py tests/test_trust_ledger_cli.py docs/CONTINUOUS_BUILD/trust-ledger.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` -> passed.
- Artifacts/receipts: this keeps revoke mutation receipts independently checkable for prior-status continuity on the existing local, export, and snapshot-summary proof surfaces while preserving the honest wording that verified provenance does not guarantee semantic truth.
- Blockers: this slice intentionally stops at `revoke()` previous-status continuity. Other L0 seams such as broader actor-identity coverage or additional export/bundle continuity contracts still need separate bounded follow-ons if operators want more hardening before schema work.
- Next safe slice: lock one similarly bounded actor-identity or prior/new-root contract on another already-shipped mutation/export/bundle seam only if that risk now matters more than non-L0 lane work.

## 2026-07-05T16:56:04Z - L0 trust-ledger - Codex

- Scope: locked one bounded forgotten-mutation actor-identity proof seam that was already implemented on shipped receipts, without changing store semantics, receipt schemas, export shapes, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_store.py`; `tests/test_treeship.py`; `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. A self-rehashed forgotten mutation receipt that forges `treeship_statement.object.actor_id` is now regression-locked to fail direct local verification, `to_treeship_statement(...)` export verification, and raw `snapshot verify --summary-only` as `source event actor_id mismatch`.
- Tests: focused regression lock `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_forget_actor_identity -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_forget_actor_identity_memory_write_statement -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_forget_actor_identity_as_untrusted -q` -> passed (`Ran 1 test`); `git diff --check -- tests/test_store.py tests/test_treeship.py tests/test_trust_ledger_cli.py docs/CONTINUOUS_BUILD/trust-ledger.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` -> passed.
- Artifacts/receipts: this is a regression-lock only. It keeps forgotten mutation receipts independently checkable for actor identity across local, export, and snapshot-summary proof surfaces while preserving the honest wording that verified provenance does not guarantee semantic truth.
- Blockers: this slice intentionally stops at forgotten mutation actor identity. Other L0 sibling seams still need separate bounded coverage if operators want broader receipt hardening before schema work.
- Next safe slice: lock one similarly bounded regression on another already-shipped mutation/export/bundle proof seam, such as `revoke()` actor identity or another prior/new-root continuity surface, only if that risk now matters more than non-L0 lane work.

## 2026-07-05T12:53:40Z - L0 trust-ledger - Codex

- Scope: locked one bounded rejected-mutation actor-identity proof seam that was already implemented on shipped receipts, without changing store semantics, receipt schemas, export shapes, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_store.py`; `tests/test_treeship.py`; `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. A self-rehashed rejected mutation receipt that forges `treeship_statement.object.actor_id` is now regression-locked to fail direct local verification, `to_treeship_statement(...)` export verification, and raw `snapshot verify --summary-only` as `source event actor_id mismatch`.
- Tests: focused regression lock `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_reject_actor_identity -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_reject_actor_identity_memory_write_statement -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_reject_actor_identity_as_untrusted -q` -> passed (`Ran 1 test`); `git diff --check -- tests/test_store.py tests/test_treeship.py tests/test_trust_ledger_cli.py docs/CONTINUOUS_BUILD/trust-ledger.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` -> passed.
- Artifacts/receipts: this is a regression-lock only. It keeps rejected mutation receipts independently checkable for actor identity across local, export, and snapshot-summary proof surfaces while preserving the honest wording that verified provenance does not guarantee semantic truth.
- Blockers: this slice intentionally stops at rejected mutation actor identity. Other L0 sibling seams still need separate bounded coverage if operators want broader receipt hardening before schema work.
- Next safe slice: lock one similarly bounded regression on another already-shipped mutation/export/bundle proof seam, such as `forget()` or `revoke()` actor identity or another prior/new-root continuity surface, only if that risk now matters more than non-L0 lane work.

## 2026-07-05T08:54:38Z - L0 trust-ledger - Codex

- Scope: locked one bounded promoted-mutation actor-identity proof seam that was already implemented on shipped receipts, without changing store semantics, receipt schemas, export shapes, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_store.py`; `tests/test_treeship.py`; `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. A self-rehashed promoted mutation receipt that forges `treeship_statement.object.actor_id` is now regression-locked to fail direct local verification, `to_treeship_statement(...)` export verification, and raw `snapshot verify --summary-only` as `source event actor_id mismatch`.
- Tests: baseline adjacent actor/proof cluster `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_mutation_actor_identity tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_promote_authority_memory_write_statement tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_promote_authority_as_untrusted -q` -> passed (`Ran 3 tests`); focused regression lock `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_promote_actor_identity tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_promote_actor_identity_memory_write_statement tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_promote_actor_identity_as_untrusted -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` -> passed; `python3 -m unittest tests.test_treeship -q` -> passed; `python3 -m unittest tests.test_trust_ledger_cli -q` -> passed; `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed; `git diff --check -- tests/test_store.py tests/test_treeship.py tests/test_trust_ledger_cli.py docs/CONTINUOUS_BUILD/trust-ledger.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` -> passed.
- Artifacts/receipts: this is a regression-lock only. It keeps promoted mutation receipts independently checkable for actor identity across local, export, and snapshot-summary proof surfaces while preserving the honest wording that verified provenance does not guarantee semantic truth.
- Blockers: this slice intentionally stops at promoted mutation actor identity. Other L0 portable proof seams still need separate bounded coverage if operators want broader receipt hardening before schema work.
- Next safe slice: lock one similarly bounded regression on another already-shipped mutation/export/bundle proof seam, such as a sibling mutation actor-identity or prior/new-root continuity surface, only if that risk now matters more than non-L0 lane work.

## 2026-07-05T04:54:02Z - L0 trust-ledger - Codex

- Scope: hardened one bounded `promote()` mutation verifier seam for authority continuity already present on shipped receipts, without changing receipt schemas, export shapes, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `tests/test_treeship.py`; `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_memory_write_receipt(...)` now re-checks `zerker.memory.mutation_receipt` `promote()` statements so `object.authority` must match the embedded `PROMOTED` source event payload. A self-rehashed tampered promote receipt now fails direct local verification, `to_treeship_statement(...)` export verification, and raw `snapshot verify --summary-only`.
- Tests: focused gap repro `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_promote_authority tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_promote_authority_memory_write_statement tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_promote_authority_as_untrusted -q` -> failed (`3` failures), then the same focused cluster -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 323 tests`); `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 38 tests`); `python3 -m unittest tests.test_trust_ledger_cli -q` -> passed (`Ran 7 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed; `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_treeship.py tests/test_trust_ledger_cli.py` -> passed.
- Artifacts/receipts: this keeps promote mutation receipts independently checkable from local data only. Verified provenance now includes promoted-authority continuity, while wording remains honest that semantic truth is not guaranteed.
- Blockers: this slice intentionally stops at `promote()` authority continuity. Other portable proof-hardening seams still need separate bounded contracts if operators want more L0 coverage before broader receipt or schema work.
- Next safe slice: add one similarly bounded verifier contract for another already-persisted mutation/export/bundle seam only if that risk matters more than non-L0 lane work, without widening Treeship into a user-facing dependency.

## 2026-07-05T00:53:29Z - L0 trust-ledger - Codex

- Scope: hardened one bounded `forget()` mutation verifier seam so previous-status continuity is independently checkable on the shipped local receipt, Treeship export, and snapshot-summary proof paths, without widening Treeship into a user-facing dependency.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `tests/test_treeship.py`; `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `forget()` source events now persist `previous_status`, and `verify_memory_write_receipt(...)` now re-checks `zerker.memory.mutation_receipt` `forget()` statements so `object.previous_status` must match the embedded `FORGOTTEN` source event payload. A self-rehashed tampered forget receipt now fails direct local verification, `to_treeship_statement(...)` export verification, and raw `snapshot verify --summary-only`.
- Tests: focused gap repro `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_forget_previous_status tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_forget_previous_status_memory_write_statement tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_forget_previous_status_as_untrusted -q` -> failed (`3` failures), then the same focused cluster -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 321 tests`); `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 37 tests`); `python3 -m unittest tests.test_trust_ledger_cli -q` -> passed (`Ran 6 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed; `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_treeship.py tests/test_trust_ledger_cli.py` -> passed.
- Artifacts/receipts: this keeps forgotten mutation receipts independently checkable from local data only. Verified provenance now includes prior-status continuity for `forget()`, while wording remains honest that semantic truth is not guaranteed.
- Blockers: this slice intentionally stops at the shipped forget previous-status field. Other portable proof-hardening seams still rely on separate receipt/export/bundle contracts.
- Next safe slice: lock one similarly bounded portable proof contract for another remaining mutation/export/bundle seam only if operators still need more L0 hardening before broader receipt or schema work.

## 2026-07-04T20:52:54Z - L0 trust-ledger - Codex

- Scope: locked one bounded revoke-mutation verifier seam that was already implemented on shipped receipts, without changing store semantics, receipt schemas, export shapes, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_store.py`; `tests/test_treeship.py`; `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `verify_memory_write_receipt(...)` revoke-reason check is now regression-locked end to end: a self-rehashed revoke receipt that changes `object.reason` can no longer quietly pass direct local verification, `to_treeship_statement(...)` export verification, or raw `snapshot verify --summary-only`.
- Tests: focused receipt/proof coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_revoke_reason -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_revoke_reason_memory_write_statement -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_revoke_reason_as_untrusted -q` -> passed (`Ran 1 test`).
- Artifacts/receipts: this is a regression-lock only. It keeps revoke mutation receipts independently checkable on existing local, export, and snapshot-summary proof surfaces while preserving the honest wording that verified provenance does not guarantee semantic truth.
- Blockers: this slice intentionally stops at the already-shipped revoke reason field. `forget()` previous-status continuity still lacks its own bounded verifier contract.
- Next safe slice: add one similarly bounded `forget()` previous-status verifier regression or implementation contract, only if operators still need that remaining mutation sibling before broader receipt/schema work.

## 2026-07-04T16:53:26Z - L0 trust-ledger - Codex

- Scope: hardened one bounded revoke-mutation verifier seam for descendant metadata already present on shipped receipts, without changing receipt schemas, export shapes, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `tests/test_treeship.py`; `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_memory_write_receipt(...)` now re-checks `zerker.memory.mutation_receipt` `revoke()` descendant metadata so `object.revoked_ids`, `object.descendant_ids`, and `object.descendant_count` must match the embedded `REVOKED` source event payload. A self-rehashed tampered revoke receipt now fails direct local verification, `to_treeship_statement(...)` export verification, and `snapshot verify --summary-only`.
- Tests: focused gap repro `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_revoke_descendant_metadata tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_revoke_memory_write_statement tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_revoke_descendant_metadata_as_untrusted -q` -> failed (`3` failures), then the same focused cluster -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 313 tests`); `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 35 tests`); `python3 -m unittest tests.test_trust_ledger_cli -q` -> passed (`Ran 4 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed; `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_treeship.py tests/test_trust_ledger_cli.py` -> passed.
- Artifacts/receipts: this keeps revoke mutation receipts independently checkable from local data only. Verified provenance now includes descendant-set continuity, while wording remains honest that semantic truth is not guaranteed.
- Blockers: this slice intentionally stops at revoke descendant metadata. `revoke()` reason and `forget()` previous-status continuity still need separate bounded verifier contracts.
- Next safe slice: add one similarly bounded event-payload verifier contract for one remaining mutation sibling field such as revoke reason or forget previous-status continuity, without widening receipt schemas or turning Treeship into a user-facing dependency.

## 2026-07-04T12:53:10Z - L0 trust-ledger - Codex

- Scope: hardened one bounded reject-mutation verifier seam for event-payload fields already present on shipped receipts, without changing receipt schemas, export shapes, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `tests/test_treeship.py`; `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_memory_write_receipt(...)` now re-checks `zerker.memory.mutation_receipt` `reject()` statements so `object.reason` and `object.previous_status` must match the embedded `REJECTED` source event payload. A self-rehashed tampered reject receipt now fails direct local verification, `to_treeship_statement(...)` export verification, and `snapshot verify --summary-only`.
- Tests: focused gap repro before the fix `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_reject_reason_and_previous_status tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_reject_memory_write_statement tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_reject_reason_as_untrusted -q` -> failed (`3` failures), then the same focused cluster -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 310 tests`); `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 34 tests`); `python3 -m unittest tests.test_trust_ledger_cli -q` -> passed (`Ran 3 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed; `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_treeship.py tests/test_trust_ledger_cli.py` -> passed.
- Artifacts/receipts: this keeps reject mutation receipts independently checkable from local data only. Verified provenance now includes reject reason and prior-status continuity, while wording remains honest that semantic truth is not guaranteed.
- Blockers: this slice intentionally stops at `reject()` payload fields. The verifier still does not re-check sibling event-payload mutation fields such as `revoke()` reason and descendant metadata or `forget()` previous-status continuity.
- Next safe slice: add one similarly bounded verifier contract for one remaining mutation sibling payload on the existing snapshot/export verification path without widening receipt schemas or turning Treeship into a user-facing dependency.

## 2026-07-04T04:52:19Z - L0 trust-ledger - Codex

- Scope: hardened one bounded mutation-receipt verification seam for exported and snapshotted write-receipt chains by rejecting self-rehashed mutation status drift, without changing receipt schemas, export shapes, summary wording, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_memory_write_receipt(...)` now checks mutation-receipt `object.mutation` and `object.status` against the embedded source event type (`PROMOTED` -> `promote`/`active`, `REJECTED` -> `reject`/`deprecated`, `REVOKED` -> `revoke`/`revoked`, `FORGOTTEN` -> `forget`/`forgotten`), so a self-rehashed tampered mutation statement no longer passes `verify_memory_write_receipt_chain(...)` or `snapshot verify`. The existing `zmem snapshot verify <snapshot> --summary-only` failure surface is now regression-locked to keep `Ready: no`, failed verification, `Trusted provenance: not verified`, `Semantic truth: not guaranteed`, and the concrete mutation-status mismatch together.
- Tests: focused regression cluster `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_mutation_status tests.test_trust_ledger_cli.TrustLedgerCliTest.test_snapshot_verify_summary_only_marks_tampered_mutation_receipt_as_untrusted tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_surfaces_verified_export_provenance -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 303 tests`); `python3 -m unittest tests.test_trust_ledger_cli -q` -> passed (`Ran 2 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed; `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_trust_ledger_cli.py` -> passed.
- Artifacts/receipts: this keeps durable mutation receipts independently checkable across live, snapshot, and export verification surfaces by rejecting forged mutation status claims while preserving the honest wording that provenance/integrity can verify even though semantic truth is not guaranteed.
- Blockers: this slice intentionally stops at mutation name/status derived from the source event type; other event-payload-derived mutation fields such as reject/revoke reasons, previous status, or revoke descendant metadata are still not re-checked on this verification path.
- Next safe slice: if operators need another L0 mutation-verifier hardening pass, add one similarly bounded event-payload contract for remaining mutation fields on snapshot/export verification without widening Treeship into a user-facing dependency.

## 2026-07-04T00:49:30Z - L0 trust-ledger - Codex

- Scope: locked one bounded bundle-verification honesty regression for an already-shipped tampered supporting write-receipt failure path, without changing store semantics, receipt schemas, export logic, summary wording, or Treeship dependency boundaries.
- Files touched: `tests/test_trust_ledger_cli.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: none. `zmem bundle verify <bundle> --summary-only` already reported untrusted supporting provenance on tampered supporting write receipts; this slice makes that contract durable by asserting `Ready: no`, `Supporting provenance verify: failed (0/1 verified)`, `Trusted provenance: not verified`, `Semantic truth: not guaranteed`, and the underlying write-receipt verification error together.
- Tests: focused receipt/proof baseline plus new regression `python3 -m unittest tests.test_store.MemoryStoreTest.test_memory_write_emits_provenance_receipt tests.test_treeship.TreeshipAdapterTest.test_bundle_statement_surfaces_verified_supporting_provenance_summary tests.test_cli_onboarding.CliOnboardingTest.test_bundle_verify_summary_only_surfaces_verified_supporting_provenance tests.test_trust_ledger_cli.TrustLedgerCliTest.test_bundle_verify_summary_only_marks_tampered_supporting_receipt_as_untrusted -q` -> passed (`Ran 4 tests`).
- Artifacts/receipts: this is a verification-surface lock only. It keeps bundle verification wording honest that provenance/integrity checks failed for the supporting receipt while semantic truth remains unguaranteed either way.
- Blockers: wider export/bundle failure surfaces still rely on older mixed coverage; this slice only locks the tampered supporting write-receipt summary path and does not add any new portable artifact shape.
- Next safe slice: only if operators need another L0 verification contract, lock one similarly bounded snapshot-verify or export-summary failure surface that distinguishes trusted provenance verification from semantic truth without widening Treeship into a user-facing dependency.

## 2026-07-03T17:10:32Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped non-attested `forget()` mutation receipt, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for a verified forgotten `zerker.memory_write.v1` mutation receipt without an optional Treeship attestation: the exported statement must preserve reviewer actor identity, forgotten status/authority, previous status, content digest, prior-receipt id/hash anchors, prior/new Merkle roots, absence of an `attestation` block, and `semantic_truth_guaranteed=False`.
- Tests: baseline focused receipt/store checks `python3 -m unittest tests.test_store.MemoryStoreTest.test_forget_persists_mutation_receipt_without_overwriting_original_write_provenance tests.test_treeship.TreeshipAdapterTest.test_exports_verified_forget_memory_mutation_statement -q` -> passed (`Ran 2 tests`); post-change export cluster `python3 -m unittest tests.test_store.MemoryStoreTest.test_forget_persists_mutation_receipt_without_overwriting_original_write_provenance tests.test_treeship.TreeshipAdapterTest.test_exports_verified_forget_memory_mutation_statement tests.test_exporter.ExporterTest.test_exports_verified_forget_memory_write_treeship_statement_to_file -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 18 tests`); `git diff --check -- tests/test_exporter.py` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for a non-attested forget receipt is now regression-locked as a locally reproducible provenance/integrity artifact that stays usable without any Treeship artifact id. It stays honest that the exported file proves receipt lineage and forgetting provenance, not semantic truth of the forgotten claim.
- Blockers: this remains contract-only exporter coverage; non-attested file-path coverage is now locked for promote/reject/revoke/forget mutation receipts, but wider bundle/export verification and any `trusted_only` wording surfaces still live on separate proof surfaces.
- Next safe slice: only if operators need another L0 proof contract, lock one bounded `trusted_only` wording surface or one bounded bundle/export verification surface for existing provenance/mutation receipts without widening Treeship into a user-facing dependency.

## 2026-07-03T16:48:04Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped non-attested `promote()` mutation receipt, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for a verified promoted `zerker.memory_write.v1` mutation receipt without an optional Treeship attestation: the exported statement must preserve reviewer actor identity, promoted status/authority, content digest, prior-receipt id/hash anchors, prior/new Merkle roots, absence of an `attestation` block, and `semantic_truth_guaranteed=False`.
- Tests: baseline focused receipt/store checks `python3 -m unittest tests.test_store.MemoryStoreTest.test_promote_persists_mutation_receipt_without_overwriting_original_write_provenance tests.test_treeship.TreeshipAdapterTest.test_exports_verified_memory_mutation_statement -q` -> passed (`Ran 2 tests`); post-change export cluster `python3 -m unittest tests.test_store.MemoryStoreTest.test_promote_persists_mutation_receipt_without_overwriting_original_write_provenance tests.test_treeship.TreeshipAdapterTest.test_exports_verified_memory_mutation_statement tests.test_exporter.ExporterTest.test_exports_verified_promote_memory_write_treeship_statement_to_file -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 17 tests`); `git diff --check -- tests/test_exporter.py` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for a non-attested promote receipt is now regression-locked as a locally reproducible provenance/integrity artifact that stays usable without any Treeship artifact id. It stays honest that the exported file proves receipt lineage and promotion provenance, not semantic truth of the promoted claim.
- Blockers: this remains contract-only exporter coverage; promote file-path coverage is now locked for both attested and non-attested receipts, but other non-attested mutation siblings, wider bundle/export verification, and any `trusted_only` wording surfaces still live on separate proof surfaces.
- Next safe slice: only if operators need more no-attestation proof coverage, add one similarly bounded exporter regression for another existing mutation sibling such as `forget()`, or lock one `trusted_only` wording surface without widening Treeship into a user-facing dependency.

## 2026-07-03T12:46:23Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped non-attested `revoke()` mutation receipt, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for a verified revoked `zerker.memory_write.v1` mutation receipt without an optional Treeship attestation: the exported statement must preserve reviewer actor identity, revoke reason, revoked/descendant ids and counts, content digest, prior-receipt id/hash anchors, prior/new Merkle roots, absence of an `attestation` block, and `semantic_truth_guaranteed=False`.
- Tests: baseline focused receipt/store checks `python3 -m unittest tests.test_store.MemoryStoreTest.test_revoke_persists_root_mutation_receipt_without_overwriting_original_write_provenance tests.test_treeship.TreeshipAdapterTest.test_exports_verified_revoke_memory_mutation_statement -q` -> passed (`Ran 2 tests`); post-change export cluster `python3 -m unittest tests.test_store.MemoryStoreTest.test_revoke_persists_root_mutation_receipt_without_overwriting_original_write_provenance tests.test_treeship.TreeshipAdapterTest.test_exports_verified_revoke_memory_mutation_statement tests.test_exporter.ExporterTest.test_exports_verified_revoke_memory_write_treeship_statement_to_file -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 16 tests`); `git diff --check -- tests/test_exporter.py` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for a non-attested revoke receipt is now regression-locked as a locally reproducible provenance/integrity artifact that stays usable without any Treeship artifact id. It stays honest that the exported file proves receipt lineage and descendant-revocation integrity, not semantic truth of the revoked claim.
- Blockers: this remains contract-only exporter coverage; revoke file-path coverage is now locked for both attested and non-attested receipts, but other non-attested mutation siblings, wider bundle/export verification, and any `trusted_only` wording surfaces still live on separate proof surfaces.
- Next safe slice: only if operators need more no-attestation proof coverage, add one similarly bounded exporter regression for another existing mutation sibling such as `promote()` or `forget()`, or lock one `trusted_only` wording surface without widening Treeship into a user-facing dependency.

## 2026-07-03T08:47:30Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped non-attested `reject()` mutation receipt, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for a verified reject `zerker.memory_write.v1` mutation receipt without an optional Treeship attestation: the exported statement must preserve reviewer actor identity, reject reason, previous status, content digest, prior-receipt id/hash anchors, prior/new Merkle roots, absence of an `attestation` block, and `semantic_truth_guaranteed=False`.
- Tests: baseline focused receipt/store checks `python3 -m unittest tests.test_store.MemoryStoreTest.test_reject_persists_mutation_receipt_without_overwriting_original_write_provenance tests.test_treeship.TreeshipAdapterTest.test_exports_verified_reject_memory_mutation_statement -q` -> passed (`Ran 2 tests`); post-change export cluster `python3 -m unittest tests.test_store.MemoryStoreTest.test_reject_persists_mutation_receipt_without_overwriting_original_write_provenance tests.test_treeship.TreeshipAdapterTest.test_exports_verified_reject_memory_mutation_statement tests.test_exporter.ExporterTest.test_exports_verified_reject_memory_write_treeship_statement_to_file -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 15 tests`); `git diff --check -- tests/test_exporter.py` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for a non-attested reject receipt is now regression-locked as a locally reproducible provenance/integrity artifact that stays usable without any Treeship artifact id. It stays honest that the exported file proves receipt lineage and integrity, not semantic truth of the rejected claim.
- Blockers: this remains contract-only exporter coverage; reject file-path coverage is now locked for both attested and non-attested receipts, but wider bundle/export verification, `trusted_only` wording, and other non-attested mutation siblings still live on separate proof surfaces.
- Next safe slice: only if operators need more no-attestation proof coverage, add one similarly bounded exporter regression for another existing mutation sibling such as `promote()`, `revoke()`, or `forget()`, or lock one `trusted_only` wording surface without widening Treeship into a user-facing dependency.

## 2026-07-03T04:45:09Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped attested add/quarantine provenance receipt, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for an attested quarantined `zerker.memory_write.v1` provenance receipt: the exported statement must preserve actor identity, quarantined status, low-authority provenance fields, content digest, prior/new Merkle roots, Treeship attestation artifact id/subject, and `semantic_truth_guaranteed=False`.
- Tests: baseline focused receipt/store checks `python3 -m unittest tests.test_store.MemoryStoreTest.test_quarantined_provenance_receipt_can_carry_compact_treeship_attestation_when_enabled tests.test_treeship.TreeshipAdapterTest.test_exports_attested_quarantined_memory_write_provenance_statement_honestly -q` -> passed (`Ran 2 tests`); post-change export cluster `python3 -m unittest tests.test_store.MemoryStoreTest.test_quarantined_provenance_receipt_can_carry_compact_treeship_attestation_when_enabled tests.test_treeship.TreeshipAdapterTest.test_exports_attested_quarantined_memory_write_provenance_statement_honestly tests.test_exporter.ExporterTest.test_exports_attested_quarantined_memory_write_provenance_statement_to_file -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 14 tests`); `git diff --check -- tests/test_exporter.py docs/CONTINUOUS_BUILD/trust-ledger.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for an attested add/quarantine provenance receipt is now regression-locked as a locally reproducible provenance/integrity artifact. It stays honest that the exported file proves provenance lineage and optional attestation metadata, not semantic truth of the quarantined claim.
- Blockers: this remains contract-only exporter coverage; initial-write file-path coverage is now locked for active and attested quarantined provenance receipts, but wider bundle/export verification and any `trusted_only` wording surfaces still live on separate proof surfaces.
- Next safe slice: only if operators need more initial-write proof coverage, add one similarly bounded bundle/export verification or `trusted_only` wording regression for existing provenance receipts without widening Treeship into a user-facing dependency.

## 2026-07-03T00:44:31Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped `end_session()` lifecycle receipt, without changing store semantics, lifecycle payloads, receipt schemas, export logic, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for a `zerker.lifecycle_receipt.v1` session-end receipt: the exported statement must preserve actor identity, session end mutation id, session id, summary, snapshot hash, content digest, prior/new Merkle roots, null optional `treeship_artifact_id`, and `semantic_truth_guaranteed=False`.
- Tests: baseline focused receipt/store checks `python3 -m unittest tests.test_store.MemoryStoreTest.test_end_session_emits_receipt_visible_roots_and_memory_type_summary tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_end_receipt tests.test_treeship.TreeshipAdapterTest.test_exports_embedded_session_end_lifecycle_statement -q` -> passed (`Ran 3 tests`); post-change export cluster `python3 -m unittest tests.test_store.MemoryStoreTest.test_end_session_emits_receipt_visible_roots_and_memory_type_summary tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_end_receipt tests.test_treeship.TreeshipAdapterTest.test_exports_embedded_session_end_lifecycle_statement tests.test_exporter.ExporterTest.test_exports_embedded_session_end_lifecycle_statement_to_file -q` -> passed (`Ran 4 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 13 tests`); `git diff --check -- tests/test_exporter.py` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for a session-end lifecycle receipt is now regression-locked as a locally reproducible provenance/integrity artifact. It stays honest that the exported file proves receipt lineage and session-end linkage, not semantic truth of the session summary or memory contents.
- Blockers: lifecycle file-path coverage is now locked for restore/start/checkpoint/snapshot/end, but wider lifecycle aggregate/export verification still lives on separate proof surfaces.
- Next safe slice: only if operators need more lifecycle proof coverage, add one similarly bounded aggregate/export verification regression for an existing lifecycle receipt surface without widening Treeship into a user-facing dependency.

## 2026-07-02T20:44:30Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped `start_session()` lifecycle receipt, without changing store semantics, lifecycle payloads, receipt schemas, export logic, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for a `zerker.lifecycle_receipt.v1` session-start receipt: the exported statement must preserve actor identity, session start mutation id, session id, snapshot hash, content digest, context budget hint, prior/new Merkle roots, null optional `treeship_artifact_id`, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_embedded_session_start_lifecycle_statement tests.test_exporter.ExporterTest.test_exports_embedded_session_start_lifecycle_statement_to_file -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 12 tests`); `git diff --check -- tests/test_exporter.py` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for a session-start lifecycle receipt is now regression-locked as a locally reproducible provenance/integrity artifact. It stays honest that the exported file proves receipt lineage and session-start linkage, not semantic truth of the session summary or future session behavior.
- Blockers: this remains contract-only exporter coverage; portable file-path regressions for the remaining lifecycle sibling `end_session()` and wider lifecycle aggregate/export verification still live on separate proof surfaces.
- Next safe slice: only if operators need one more portable lifecycle file contract, add one similarly bounded exporter regression for `end_session()` without widening Treeship into a user-facing dependency.

## 2026-07-02T16:43:59Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped `snapshot_session()` lifecycle receipt, without changing store semantics, lifecycle payloads, receipt schemas, export logic, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for a `zerker.lifecycle_receipt.v1` session snapshot receipt: the exported statement must preserve actor identity, session snapshot mutation id, session id, snapshot hash, content digest, prior/new Merkle roots, null optional `treeship_artifact_id`, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_embedded_session_snapshot_lifecycle_statement tests.test_exporter.ExporterTest.test_exports_embedded_session_snapshot_lifecycle_statement_to_file -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 11 tests`); `git diff --check -- tests/test_exporter.py` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for a session snapshot lifecycle receipt is now regression-locked as a locally reproducible provenance/integrity artifact. It stays honest that the exported file proves receipt lineage and snapshot linkage, not semantic truth of the snapshot summary or memory contents.
- Blockers: this remains contract-only exporter coverage; portable file-path regressions for lifecycle siblings such as session start or session end still live on separate proof surfaces.
- Next safe slice: only if operators need more portable lifecycle file coverage, add one similarly bounded exporter regression for one existing lifecycle sibling such as `start_session()` or `end_session()` without widening Treeship into a user-facing dependency.

## 2026-07-01T11:31:38Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped `checkpoint_session()` lifecycle receipt, without changing store semantics, receipt schemas, lifecycle payloads, export logic, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for a `zerker.lifecycle_receipt.v1` checkpoint receipt: the exported statement must preserve session actor identity, checkpoint mutation id, session id, snapshot hash, content digest, prior/new Merkle roots, null optional `treeship_artifact_id`, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_embedded_session_checkpoint_lifecycle_statement tests.test_exporter.ExporterTest.test_exports_embedded_session_checkpoint_lifecycle_statement_to_file -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 10 tests`); `git diff --check -- tests/test_exporter.py` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for checkpoint lifecycle receipts is now regression-locked as a locally reproducible provenance/integrity artifact. It stays honest that the exported file proves receipt lineage and snapshot linkage, not semantic truth of the checkpoint summary or memory contents.
- Blockers: this remains contract-only exporter coverage; portable file-path regressions for other lifecycle siblings such as session snapshot or session start/end still live on separate proof surfaces.
- Next safe slice: only if operators need more portable lifecycle file coverage, add one similarly bounded exporter regression for one existing lifecycle sibling without widening Treeship into a user-facing dependency.

## 2026-07-01T07:30:13Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped attested `forget()` mutation receipt, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for an attested forgotten `zerker.memory_write.v1` receipt: the exported statement must preserve reviewer actor identity, previous status, content digest, prior-receipt id/hash anchors, prior/new Merkle roots, Treeship attestation artifact id/subject, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_forget_persists_mutation_receipt_without_overwriting_original_write_provenance tests.test_store.MemoryStoreTest.test_forget_mutation_receipt_can_carry_compact_treeship_attestation_when_enabled -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_forget_memory_mutation_statement tests.test_treeship.TreeshipAdapterTest.test_exports_verified_attested_forget_memory_mutation_statement tests.test_exporter.ExporterTest.test_exports_attested_forget_memory_write_treeship_statement_to_file -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 9 tests`); `git diff --check -- tests/test_exporter.py` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for an attested forget receipt is now regression-locked as a locally reproducible provenance/integrity artifact. It stays honest that the exported file proves receipt lineage and optional attestation metadata, not semantic truth of the forgotten claim.
- Blockers: this remains contract-only exporter coverage; other mutation siblings, checkpoint/snapshot/export receipt surfaces, and wider bundle/snapshot external re-check evidence still live on separate proof surfaces.
- Next safe slice: only if operators need more portable file-level coverage, add one similarly bounded exporter regression for another existing attested mutation or lifecycle receipt without widening Treeship into a user-facing dependency.

## 2026-07-01T03:31:05Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped attested `promote()` mutation receipt, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for an attested promoted `zerker.memory_write.v1` receipt: the exported statement must preserve reviewer actor identity, content digest, prior-receipt id/hash anchors, prior/new Merkle roots, Treeship attestation artifact id/subject, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_promote_persists_mutation_receipt_without_overwriting_original_write_provenance tests.test_store.MemoryStoreTest.test_promote_mutation_receipt_can_carry_compact_treeship_attestation_when_enabled -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_memory_mutation_statement tests.test_treeship.TreeshipAdapterTest.test_exports_verified_attested_memory_mutation_statement tests.test_exporter.ExporterTest.test_exports_attested_promote_memory_write_treeship_statement_to_file -q` -> passed; `python3 -m unittest tests.test_exporter -q` -> passed; `git diff --check -- tests/test_exporter.py docs/CONTINUOUS_BUILD/trust-ledger.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for an attested promote receipt is now regression-locked as a locally reproducible provenance/integrity artifact. It stays honest that the exported file proves receipt lineage and optional attestation metadata, not semantic truth of the promoted claim.
- Blockers: this remains contract-only exporter coverage; portable file-path regressions for other attested mutation siblings and wider bundle/snapshot external re-check evidence still live on separate proof surfaces.
- Next safe slice: only if operators need more portable file-level coverage, add one similarly bounded exporter regression for another existing attested provenance or mutation receipt without widening Treeship into a user-facing dependency.

## 2026-06-30T23:30:01Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped attested `reject()` mutation receipt, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for an attested rejected `zerker.memory_write.v1` receipt: the exported statement must preserve reviewer actor identity, reject reason, prior-receipt id/hash anchors, prior/new Merkle roots, Treeship attestation artifact id/subject, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_reject_mutation_receipt_can_carry_compact_treeship_attestation_when_enabled tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_reject_chain_with_intervening_write -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_reject_memory_mutation_statement tests.test_treeship.TreeshipAdapterTest.test_exports_verified_attested_reject_memory_mutation_statement tests.test_exporter.ExporterTest.test_exports_attested_reject_memory_write_treeship_statement_to_file -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 7 tests`); `git diff --check -- tests/test_exporter.py` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for an attested reject receipt is now regression-locked as a locally reproducible provenance/integrity artifact. It stays honest that the exported file proves receipt lineage and optional attestation metadata, not semantic truth of the rejected claim.
- Blockers: this remains contract-only exporter coverage; other attested mutation siblings and broader bundle/snapshot external re-check evidence still live on separate proof surfaces.
- Next safe slice: only if operators need more portable file-level coverage, add one similarly bounded exporter regression for another existing attested provenance or mutation receipt without widening Treeship into a user-facing dependency.

## 2026-06-30T19:29:51Z - L0 trust-ledger - Codex

- Scope: locked one bounded portable export regression for the already-shipped attested `revoke()` mutation receipt, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_exporter.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped `export_receipt(..., fmt="treeship")` file path is now explicitly covered for an attested revoked `zerker.memory_write.v1` receipt: the exported statement must preserve reviewer actor identity, revoke reason, prior-receipt anchors, prior/new Merkle roots, Treeship attestation artifact id/subject, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_exporter.ExporterTest.test_exports_attested_revoke_memory_write_treeship_statement_to_file tests.test_treeship.TreeshipAdapterTest.test_exports_verified_attested_revoke_memory_mutation_statement tests.test_store.MemoryStoreTest.test_revoke_mutation_receipt_can_carry_compact_treeship_attestation_when_enabled -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_exporter -q` -> passed (`Ran 6 tests`); `git diff --check -- tests/test_exporter.py` -> passed.
- Artifacts/receipts: portable `.treeship.json` file export for an attested revoke receipt is now regression-locked as a locally reproducible provenance/integrity artifact. It stays honest that the exported file proves receipt lineage and optional attestation metadata, not semantic truth of the revoked claim.
- Blockers: this remains contract-only exporter coverage; portable file-path regressions for other attested mutation siblings and broader bundle/snapshot external re-check evidence still live on separate proof surfaces.
- Next safe slice: only if operators need more portable file-level coverage, add one similarly bounded exporter regression for another existing attested mutation or provenance receipt without widening Treeship into a user-facing dependency.

## 2026-06-30T15:28:27Z - L0 trust-ledger - Codex

- Scope: locked one bounded add/quarantine provenance regression for the already-shipped initial write receipt path when optional Treeship digest attestation is present, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped quarantined provenance receipt is now explicitly covered when an optional Treeship write attestation is present: `verify_memory_write_receipt(...)` must accept the persisted quarantined receipt plus its mirrored attestation block, and `to_treeship_statement(...)` must preserve quarantined status, low-authority provenance fields, attestation artifact id/subject, prior/new Merkle roots, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_quarantined_memory_write_provenance_statement_honestly tests.test_treeship.TreeshipAdapterTest.test_exports_attested_quarantined_memory_write_provenance_statement_honestly tests.test_store.MemoryStoreTest.test_quarantined_provenance_receipt_surfaces_created_state_and_verifies_locally tests.test_store.MemoryStoreTest.test_quarantined_provenance_receipt_can_carry_compact_treeship_attestation_when_enabled -q` -> passed (`Ran 4 tests`); `git diff --check -- tests/test_treeship.py tests/test_store.py` -> passed.
- Artifacts/receipts: attested add/quarantine provenance receipts are now regression-locked as independently checkable provenance/integrity artifacts for both local verification and portable Treeship export, while staying honest that verified provenance does not guarantee semantic truth of the quarantined claim.
- Blockers: this remains contract-only coverage; standalone exported write-provenance statements still do not bundle broader bundle/snapshot evidence, so external re-checks beyond the single receipt still need the existing local bundle or snapshot proof surfaces.
- Next safe slice: only if operators need one more narrow initial-write proof contract, add a similarly bounded attested regression for another existing add/quarantine surface such as bundle/export verification or trusted-only wording without widening Treeship into a user-facing dependency.

## 2026-06-30T11:30:50Z - L0 trust-ledger - Codex

- Scope: locked one bounded attested mutation-receipt regression for the already-shipped `revoke()` path, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped revoke mutation receipt is now explicitly covered when an optional Treeship write attestation is present: `to_treeship_statement(...)` must preserve the attestation artifact id/subject plus revoked/descendant ids, prior-receipt anchors, prior/new Merkle roots, and `semantic_truth_guaranteed=False`, while `verify_memory_write_receipt_chain(...)` must accept the attested revoke chain even when a descendant write changes the live Merkle root between the original source write and the later revoke.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_revoke_memory_mutation_statement tests.test_store.MemoryStoreTest.test_revoke_mutation_receipt_can_carry_compact_treeship_attestation_when_enabled -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_revoke_memory_mutation_statement tests.test_treeship.TreeshipAdapterTest.test_exports_verified_attested_revoke_memory_mutation_statement tests.test_store.MemoryStoreTest.test_revoke_mutation_receipt_can_carry_compact_treeship_attestation_when_enabled -q` -> passed (`Ran 3 tests`); `git diff --check -- tests/test_treeship.py tests/test_store.py` -> passed.
- Artifacts/receipts: attested revoke mutation receipts are now regression-locked as independently checkable provenance/integrity artifacts for both portable Treeship export and local ordered-chain verification, while staying honest that verified provenance does not guarantee semantic truth of the revoked claim or its descendants.
- Blockers: this remains contract-only coverage; standalone exported mutation statements still do not bundle the full prior receipt needed to re-check ordered-chain continuity from the exported artifact alone, so the honest local verification surface for revoke remains the ordered receipt chain.
- Next safe slice: only if operators need another narrow optional-artifact mutation contract, add a similarly bounded attested export or ordered-chain verifier regression for another existing receipt surface such as quarantine/add lineage or aggregate export verification without widening Treeship into a user-facing dependency.

## 2026-06-30T07:28:04Z - L0 trust-ledger - Codex

- Scope: locked one bounded attested mutation-receipt regression for the already-shipped `forget()` path, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped forget mutation receipt is now explicitly covered when an optional Treeship write attestation is present: `verify_memory_write_receipt(...)` must accept the persisted forgotten receipt plus its mirrored attestation block, and `to_treeship_statement(...)` must preserve the attestation artifact id/subject alongside prior-receipt anchors, prior/new Merkle roots, previous status, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_forget_memory_mutation_statement tests.test_store.MemoryStoreTest.test_forget_persists_mutation_receipt_without_overwriting_original_write_provenance -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_forget_memory_mutation_statement tests.test_treeship.TreeshipAdapterTest.test_exports_verified_attested_forget_memory_mutation_statement tests.test_store.MemoryStoreTest.test_forget_persists_mutation_receipt_without_overwriting_original_write_provenance tests.test_store.MemoryStoreTest.test_forget_mutation_receipt_can_carry_compact_treeship_attestation_when_enabled -q` -> passed (`Ran 4 tests`); `git diff --check -- tests/test_treeship.py tests/test_store.py` -> passed.
- Artifacts/receipts: attested forget mutation receipts are now regression-locked as independently checkable provenance/integrity artifacts for both local verification and portable Treeship export, while staying honest that the attestation proves signed receipt lineage rather than semantic truth of the forgotten claim.
- Blockers: this remains contract-only coverage; standalone exported mutation statements still do not bundle the full prior receipt needed to re-check ordered-chain continuity from the exported artifact alone.
- Next safe slice: only if operators need one more narrow trust contract beyond the now-locked attested promote/reject/forget export surfaces, add a similarly bounded attested export or verifier regression for another existing mutation receipt without widening Treeship into a user-facing dependency.

## 2026-06-30T03:26:12Z - L0 trust-ledger - Codex

- Scope: locked one bounded attested mutation-receipt regression for the already-shipped `reject()` path, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped reject mutation receipt is now explicitly covered when an optional Treeship write attestation is present: `verify_memory_write_receipt(...)` must accept the persisted rejected receipt plus its mirrored attestation block, and `to_treeship_statement(...)` must preserve the attestation artifact id/subject alongside prior-receipt anchors, prior/new Merkle roots, rejection reason, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_reject_memory_mutation_statement tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_reject_chain_with_intervening_write -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_reject_memory_mutation_statement tests.test_treeship.TreeshipAdapterTest.test_exports_verified_attested_reject_memory_mutation_statement tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_reject_chain_with_intervening_write tests.test_store.MemoryStoreTest.test_reject_mutation_receipt_can_carry_compact_treeship_attestation_when_enabled -q` -> passed (`Ran 4 tests`); `git diff --check -- tests/test_treeship.py tests/test_store.py docs/CONTINUOUS_BUILD/trust-ledger.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` -> passed.
- Artifacts/receipts: attested reject mutation receipts are now regression-locked as independently checkable provenance/integrity artifacts for both local verification and portable Treeship export, while staying honest that the attestation proves signed receipt lineage rather than semantic truth of the rejected claim.
- Blockers: this remains contract-only coverage; standalone exported mutation statements still do not bundle the full prior receipt needed to re-check ordered-chain continuity from the exported artifact alone.
- Next safe slice: only if operators need one more narrow trust contract beyond the now-locked attested promote/reject export surfaces, add a similarly bounded attested export or verifier regression for another existing mutation receipt without widening Treeship into a user-facing dependency.

## 2026-06-29T23:26:19Z - L0 trust-ledger - Codex

- Scope: locked one bounded attested mutation-receipt regression for the already-shipped `promote()` path, without changing store semantics, receipt schemas, export logic, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped promote mutation receipt is now explicitly covered when an optional Treeship write attestation is present: `verify_memory_write_receipt(...)` must accept the persisted promoted receipt plus its mirrored attestation block, and `to_treeship_statement(...)` must preserve the attestation artifact id/subject alongside prior-receipt anchors, prior/new Merkle roots, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_memory_mutation_statement tests.test_treeship.TreeshipAdapterTest.test_exports_verified_attested_memory_mutation_statement tests.test_store.MemoryStoreTest.test_promote_persists_mutation_receipt_without_overwriting_original_write_provenance tests.test_store.MemoryStoreTest.test_promote_mutation_receipt_can_carry_compact_treeship_attestation_when_enabled -q` -> passed (`Ran 4 tests`); `git diff --check -- tests/test_treeship.py tests/test_store.py` -> passed.
- Artifacts/receipts: attested promote mutation receipts are now regression-locked as independently checkable provenance/integrity artifacts for both local verification and portable Treeship export, while staying honest that the attestation proves signed receipt lineage rather than semantic truth of the promoted claim.
- Blockers: this remains contract-only coverage; standalone exported mutation statements still do not bundle the full prior receipt needed to re-check ordered-chain continuity from the exported artifact alone.
- Next safe slice: only if operators need one more narrow trust contract beyond the now-locked promote/reject/revoke/forget direct mutation exports, add a similarly bounded attested export or verifier regression for another existing mutation receipt without widening Treeship into a user-facing dependency.

## 2026-06-29T19:24:29Z - L0 trust-ledger - Codex

- Scope: locked one bounded lifecycle receipt regression for the already-shipped `end_session()` mutation, without changing store semantics, receipt schemas, snapshot payloads, export logic, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped end-session lifecycle receipt is now explicitly covered for both local verification and direct Treeship export: `verify_lifecycle_receipt(...)` must accept the persisted `end_session` receipt, and `to_treeship_statement(...)` must preserve actor identity, content digest, snapshot hash, source-event linkage, prior/new Merkle roots, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_embedded_session_end_lifecycle_statement tests.test_store.MemoryStoreTest.test_end_session_emits_receipt_visible_roots_and_memory_type_summary tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_end_receipt -q` -> passed (`Ran 3 tests`); `git diff --check -- tests/test_treeship.py tests/test_store.py` -> passed.
- Artifacts/receipts: end-session lifecycle receipts are now regression-locked as independently checkable provenance/integrity artifacts for the session-closeout mutation and its portable Treeship statement, while staying honest that semantic truth of the session summary is not guaranteed.
- Blockers: this remains contract-only coverage; the end-session receipt still carries a compact snapshot summary rather than a full source snapshot artifact, so the local verifier proves provenance/integrity of the lifecycle receipt itself rather than semantic truth of the closed session state.
- Next safe slice: with `start_session()` / `end_session()` / `checkpoint_session()` / `snapshot_session()` / `restore_snapshot()` lifecycle sibling coverage now locked, add another bounded trust-ledger contract only if operators need a different durable proof surface such as aggregate lifecycle rollup/export verification without widening Treeship into a user-facing dependency.

## 2026-06-29T15:23:01Z - L0 trust-ledger - Codex

- Scope: locked one bounded lifecycle receipt regression for the already-shipped `start_session()` mutation, without changing store semantics, receipt schemas, snapshot payloads, export logic, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped start-session lifecycle receipt is now explicitly covered for both local verification and direct Treeship export: `verify_lifecycle_receipt(...)` must accept the persisted `start_session` receipt, and `to_treeship_statement(...)` must preserve actor identity, content digest, context budget hint, source-event linkage, prior/new Merkle roots, and `semantic_truth_guaranteed=False`.
- Tests: `git diff --check -- tests/test_treeship.py tests/test_store.py` -> passed; `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_embedded_session_start_lifecycle_statement tests.test_store.MemoryStoreTest.test_start_session_emits_receipt_visible_roots_and_budget_hint tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_start_receipt -q` -> passed (`Ran 3 tests`).
- Artifacts/receipts: start-session lifecycle receipts are now regression-locked as independently checkable provenance/integrity artifacts for the session-resume mutation and its portable Treeship statement, while staying honest that semantic truth of the session summary is not guaranteed.
- Blockers: this remains contract-only coverage; the start-session receipt still carries a compact snapshot summary rather than a full source snapshot artifact, so the local verifier proves provenance/integrity of the lifecycle receipt itself rather than the semantic truth of the resumed session state.
- Next safe slice: only if operators need the remaining lifecycle sibling, add one similarly bounded direct export or verifier contract for `end_session()` without widening Treeship into a user-facing dependency.

## 2026-06-29T11:23:54Z - L0 trust-ledger - Codex

- Scope: locked one bounded lifecycle receipt regression for the already-shipped `checkpoint_session()` mutation, without changing store semantics, receipt schemas, snapshot payloads, export logic, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped checkpoint lifecycle receipt is now explicitly covered for both local verification and direct Treeship export: `verify_lifecycle_receipt(...)` must accept the persisted `checkpoint_session` receipt, and `to_treeship_statement(...)` must preserve actor identity, content digest, snapshot hash, source-event linkage, prior/new Merkle roots, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_embedded_session_checkpoint_lifecycle_statement tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_checkpoint_receipt tests.test_treeship.TreeshipAdapterTest.test_exports_embedded_session_snapshot_lifecycle_statement tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_snapshot_receipt -q` -> passed (`Ran 4 tests`); `git diff --check -- tests/test_treeship.py tests/test_store.py` -> passed; `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 25 tests`); `python3 -m unittest tests.test_store.MemoryStoreTest.test_checkpoint_session_emits_receipt_visible_roots_and_memory_type_summary tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_checkpoint_receipt tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_snapshot_receipt tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_reports_tampered_actor_identity -q` -> passed (`Ran 4 tests`).
- Artifacts/receipts: checkpoint lifecycle receipts are now regression-locked as independently checkable provenance/integrity artifacts for the checkpoint mutation and its portable Treeship statement, while staying honest that semantic truth of the checkpoint summary is not guaranteed.
- Blockers: the checkpoint result still carries only a compact snapshot summary, not the full `zerker.memory_snapshot.v1` artifact, so a verifier that wants to re-check the checkpoint's source snapshot must use a separate snapshot export.
- Next safe slice: only if operators need more sibling lifecycle export coverage than checkpoint/snapshot/restore, add one similarly bounded direct export or verifier contract for `start_session()` or `end_session()` without widening Treeship into a user-facing dependency.

## 2026-06-29T07:22:35Z - L0 trust-ledger - Codex

- Scope: locked one bounded direct Treeship export regression for the already-shipped `reject()` mutation receipt, without changing store semantics, export logic, receipt schemas, bundle/snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped adapter contract is now explicitly covered for a rejected `zerker.memory.mutation_receipt`: `to_treeship_statement(...)` must preserve reviewer actor identity, rejected status/authority, rejection reason, previous status, content digest, prior/new Merkle roots, prior receipt anchors, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_reject_memory_mutation_statement tests.test_store.MemoryStoreTest.test_reject_persists_mutation_receipt_without_overwriting_original_write_provenance -q` -> passed (`Ran 2 tests`); `git diff --check -- tests/test_treeship.py` -> passed; `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 24 tests`).
- Artifacts/receipts: direct exported reject mutation statements are now regression-locked as independently checkable provenance/integrity artifacts for a rejected memory transition, while staying honest that semantic truth of the rejected claim is not guaranteed.
- Blockers: standalone exported mutation statements still do not bundle the prior receipt needed to re-check ordered-chain continuity from the exported artifact alone.
- Next safe slice: only if operators need coverage beyond the now-locked promote/reject/revoke/forget direct mutation exports, add one similarly bounded export or verify contract for another existing durable receipt surface without widening Treeship into a user-facing dependency.

## 2026-06-29T03:21:11Z - L0 trust-ledger - Codex

- Scope: locked one bounded direct Treeship export regression for the already-shipped `forget()` mutation receipt, without changing store semantics, export logic, receipt schemas, bundle/snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped adapter contract is now explicitly covered for a forgotten `zerker.memory.mutation_receipt`: `to_treeship_statement(...)` must preserve reviewer actor identity, forgotten status/authority, previous status, content digest, prior/new Merkle roots, prior receipt anchors, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_forget_memory_mutation_statement tests.test_store.MemoryStoreTest.test_forget_persists_mutation_receipt_without_overwriting_original_write_provenance -q` -> passed (`Ran 2 tests`); `git diff --check -- tests/test_treeship.py docs/CONTINUOUS_BUILD/trust-ledger.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` -> passed; `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 23 tests`).
- Artifacts/receipts: direct exported forget mutation statements are now regression-locked as independently checkable provenance/integrity artifacts for a forgotten memory transition, while staying honest that semantic truth of the forgotten claim is not guaranteed.
- Blockers: standalone exported mutation statements still do not bundle the prior receipt needed to re-check ordered-chain continuity from the exported artifact alone.
- Next safe slice: only if operators need more sibling mutation coverage than revoke/forget, add one similarly bounded direct export regression for another existing mutation receipt such as `reject()` without widening Treeship into a user-facing dependency.

## 2026-06-28T23:21:00Z - L0 trust-ledger - Codex

- Scope: locked one bounded direct Treeship export regression for `revoke()` mutation receipts on top of the already-shipped write-receipt adapter, without changing store semantics, export logic, receipt schemas, bundle/snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped adapter contract is now explicitly covered for a revoked `zerker.memory.mutation_receipt`: `to_treeship_statement(...)` must preserve actor identity, revoked root+descendant ids, descendant count, prior/new Merkle roots, prior receipt anchors, and `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_revoke_memory_mutation_statement tests.test_store.MemoryStoreTest.test_revoke_persists_root_mutation_receipt_without_overwriting_original_write_provenance -q` -> passed (`Ran 2 tests`); `git diff --check -- tests/test_treeship.py` -> passed; `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 22 tests`).
- Artifacts/receipts: direct exported revoke mutation statements are now regression-locked as independently checkable provenance/integrity artifacts for descendant revocation lineage, while staying honest that semantic truth of the revoked claim is not guaranteed.
- Blockers: standalone exported mutation statements still do not bundle the prior receipt needed to re-check ordered-chain continuity from the exported artifact alone.
- Next safe slice: only if operators need more sibling mutation coverage than revoke, add one similarly bounded direct export regression for another existing mutation receipt such as `forget()` without widening Treeship into a user-facing dependency.

## 2026-06-28T19:20:49Z - L0 trust-ledger - Codex

- Scope: locked one bounded quarantine export-contract slice on top of the already-shipped write-receipt Treeship adapter, without changing store semantics, export logic, receipt schemas, bundle/snapshot payloads, or Treeship dependency boundaries.
- Files touched: `tests/test_treeship.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. The shipped adapter behavior is now regression-locked for quarantined provenance receipts: `to_treeship_statement(...)` must export a quarantined `zerker.memory.write_provenance` statement with the created-state `status="quarantined"`, low-authority provenance fields, prior/new Merkle roots, and explicit `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_quarantined_memory_write_provenance_statement_honestly tests.test_store.MemoryStoreTest.test_quarantined_provenance_receipt_surfaces_created_state_and_verifies_locally -q` -> passed (`Ran 2 tests`); `git diff --check -- tests/test_treeship.py` -> passed; `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 21 tests`).
- Artifacts/receipts: quarantined add/provenance receipts are now explicitly covered as portable Treeship-exportable proof artifacts, and the locked wording stays honest: exported receipts verify provenance/integrity of the quarantined memory write, not semantic truth of the claim.
- Blockers: this slice adds contract coverage only; standalone exported mutation receipts still do not carry the full prior receipt needed to re-check ordered-chain continuity by themselves.
- Next safe slice: only if operators need one more withheld/pre-active proof contract, add one similarly bounded regression for a quarantined receipt inside a wider export/verify surface such as bundle or file export without widening Treeship into a user-facing dependency.

## 2026-06-28T15:22:04Z - L0 trust-ledger - Codex

- Scope: landed one bounded shared-adapter export slice so existing per-memory write receipts can be emitted as portable Treeship statements, without changing store/write semantics, receipt schemas, bundle/snapshot payloads, or Treeship dependency boundaries.
- Files touched: `zerker_memory/treeship.py`; `tests/test_treeship.py`; `tests/test_exporter.py`; `docs/PROOF_SCHEMA.md`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `to_treeship_statement(...)` now accepts embedded `zerker.memory_write.v1` provenance and mutation receipts, locally re-verifies receipt hash, source event linkage, optional write attestation metadata, and any signed prior receipt id/hash/root anchors already carried on the receipt, then exports the embedded statement with explicit `object.semantic_truth_guaranteed=False`. Tampered write-receipt exports are rejected before statement emission.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_verified_memory_write_provenance_statement tests.test_treeship.TreeshipAdapterTest.test_exports_verified_memory_mutation_statement tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_memory_write_statement tests.test_exporter.ExporterTest.test_exports_memory_write_treeship_statement_to_file -q` -> passed (`Ran 4 tests`); `python3 -m unittest tests.test_treeship tests.test_exporter -q` -> passed (`Ran 25 tests`); `git diff --check -- zerker_memory/treeship.py tests/test_treeship.py tests/test_exporter.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 268 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: external verifiers can now export one more narrow proof artifact directly from add/promote/reject/revoke-class write receipts without a live store: the exported statement proves the current receipt's provenance, content digest, actor identity, Merkle-root transition, optional attestation artifact id, and any signed prior-link anchors already embedded in the receipt. This verifies provenance and integrity, not semantic truth, and a standalone mutation statement still does not prove full ordered-chain continuity by itself.
- Blockers: direct write-receipt export now covers current receipt integrity and signed prior-link anchors, but it still does not bundle the full prior receipt needed to re-check ordered mutation-chain continuity from the exported statement alone.
- Next safe slice: only if external verifiers need standalone ordered-chain continuity beyond the current receipt plus signed prior-link anchors, add one bounded export contract for carrying the prior receipt or a compact chain summary without widening Treeship into a user-facing dependency.

## 2026-06-28T07:18:44Z - L0 trust-ledger - Codex

- Scope: landed one bounded Treeship-export proof-contract slice so `zmem export --format treeship` preserves the already-verified supporting provenance facts from receipt bundles, without changing bundle schemas, store/write paths, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `zerker_memory/treeship.py`; `tests/test_treeship.py`; `docs/PROOF_SCHEMA.md`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `to_treeship_statement(...)` now keeps exported action-proof wording honest by always surfacing `object.semantic_truth_guaranteed=False`, and when given a receipt bundle it now independently re-verifies embedded `supporting_memory_write_receipts`, rejects tampered supporting provenance anchors, and carries additive `supporting_write_receipt_count`, `verified_supporting_write_receipt_count`, `trusted_provenance_verified`, compact `supporting_provenance_receipts`, and optional attestation artifact ids into the portable Treeship statement.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_converts_memory_action_receipt_dict_to_statement tests.test_treeship.TreeshipAdapterTest.test_embeds_bundle_proof_when_exporting_from_receipt_bundle tests.test_treeship.TreeshipAdapterTest.test_bundle_statement_surfaces_verified_supporting_provenance_summary tests.test_treeship.TreeshipAdapterTest.test_rejects_bundle_with_tampered_supporting_write_receipt -q` -> passed (`Ran 4 tests`); `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 14 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 264 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: Treeship-exported proof statements can now prove one more narrow external fact from the bundle alone: any carried original supporting provenance receipt still matches its embedded source event, content digest, Merkle-root transition, and optional Treeship artifact id, so trusted provenance survives into the portable statement while semantic truth remains explicitly unguaranteed.
- Blockers: Treeship-exported statements still carry only original supporting provenance anchors, not ordered supporting mutation-receipt chains or bundle-side intervening-write continuity counts.
- Next safe slice: only if external verifiers need more than the original supporting provenance anchors, add one bounded Treeship-statement continuity summary or ordered supporting-chain contract without widening Treeship into a user-facing dependency.

## 2026-06-28T03:20:05Z - L0 trust-ledger - Codex

- Scope: landed one bounded bundle-verification provenance-summary slice so exported receipt bundles verify their embedded supporting write receipts directly and expose compact provenance anchors, without changing bundle schemas, write paths, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `zerker_memory/cli.py`; `tests/test_snapshot.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_bundle(...)` now verifies `supporting_memory_write_receipts` when present, rejects tampered embedded provenance receipts, and returns additive `supporting_write_receipt_count`, `verified_supporting_write_receipt_count`, `supporting_provenance_receipts`, `attestation_artifacts`, and `trusted_provenance_verified` summary data. `zmem bundle verify --summary-only` now surfaces compact provenance memory/actor/content-digest/root-transition/Treeship-artifact lines for single-memory bundles while keeping the wording explicit that verified provenance does not guarantee semantic truth.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_bundle_accepts_valid_bundle tests.test_snapshot.SnapshotTest.test_verify_bundle_surfaces_supporting_provenance_anchor_summary tests.test_snapshot.SnapshotTest.test_verify_bundle_rejects_tampered_supporting_write_receipt tests.test_cli_onboarding.CliOnboardingTest.test_bundle_verify_summary_only_surfaces_verified_supporting_provenance -q` -> passed (`Ran 4 tests`); `git diff --check -- zerker_memory/store.py zerker_memory/cli.py tests/test_snapshot.py tests/test_cli_onboarding.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 262 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: receipt bundle verification can now independently prove one more narrow export fact from the bundle alone: the embedded supporting provenance receipt still matches its source event, content digest, Merkle-root transition, and optional Treeship artifact, so trusted provenance stays checkable outside the live DB. This verifies provenance and integrity, not semantic truth.
- Blockers: bundle verification still carries only original supporting provenance anchors, not full per-memory mutation-chain continuity counts, because receipt bundles do not yet export ordered supporting write-receipt chains.
- Next safe slice: if operators need mutation-chain continuity from bundles, add one bounded export contract for ordered supporting write-receipt chains or a separate bundle-side continuity proof summary without widening Treeship into a user-facing dependency.

## 2026-06-27T23:15:42Z - L0 trust-ledger - Codex

- Scope: landed one bounded snapshot-verification continuity slice so exported write-receipt chains can explain intervening unrelated writes from snapshot data alone, without changing receipt schemas, snapshot payloads, write paths, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `zerker_memory/cli.py`; `tests/test_snapshot.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_snapshot(...)` now returns additive `total_intervening_event_count`, `total_intervening_other_memory_event_count`, and per-memory `write_receipt_chains` summaries derived from snapshot `events[*].seq` ordering. Snapshot transition summaries now surface an explicit `continuity_basis=prior_receipt_link_plus_snapshot_previous_event_root`, plus snapshot-local prior event hash/root anchors, so `zmem snapshot verify --summary-only` can explain why a verified mutation chain still holds after unrelated writes in the exported event stream.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_snapshot_surfaces_intervening_write_continuity_counts -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_explains_intervening_write_continuity -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_reject_chain_with_intervening_write tests.test_snapshot.SnapshotTest.test_verify_snapshot_accepts_revoke_receipt_with_intervening_events tests.test_snapshot.SnapshotTest.test_verify_snapshot_surfaces_intervening_write_continuity_counts tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_explains_intervening_write_continuity -q` -> passed (`Ran 4 tests`); `git diff --check -- zerker_memory/store.py zerker_memory/cli.py tests/test_snapshot.py tests/test_cli_onboarding.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 260 tests`); `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 31 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: snapshot verification can now prove one more narrow receipt fact from exported data only: a durable mutation chain can stay trusted even when unrelated writes intervened, because the per-memory receipt link still matches and the snapshot event log preserves the prior-event Merkle anchor. This verifies provenance and continuity, not semantic truth.
- Blockers: receipt bundle verification and other proof-facing summaries still do not mirror the same intervening-write counts, and multi-chain snapshot summaries still compress continuity detail down to aggregate counts in terminal output.
- Next safe slice: mirror the same snapshot-local continuity counts into one bounded bundle/export proof summary, or expose one more compact per-chain snapshot detail line only if operators need more than the new aggregate counts.

## 2026-06-27T19:15:53Z - L0 trust-ledger - Codex

- Scope: landed one bounded read-only continuity-summary slice on top of the existing live per-memory receipt-chain verifier so operators can see why a mutation chain still verifies after unrelated writes intervene, without changing receipt schemas, snapshot/export payloads, write paths, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `zerker_memory/cli.py`; `tests/test_store.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_memory_write_receipt_chain(...)` now returns additive per-transition continuity metadata and aggregate `total_intervening_event_count` / `total_intervening_other_memory_event_count` values derived from the live event log `seq` ordering, including the current receipt's live previous-event hash/root anchor and an explicit `continuity_basis=prior_receipt_link_plus_live_previous_event_root`. `zmem lineage --summary-only` now surfaces that continuity basis plus intervening-event counts so trusted provenance remains readable even when the prior per-memory receipt is not adjacent in the global event stream.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_reject_chain_with_intervening_write -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_lineage_summary_only_explains_intervening_write_continuity -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_reject_chain_with_intervening_write tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_prior_root tests.test_cli_onboarding.CliOnboardingTest.test_lineage_summary_only_surfaces_verified_write_receipt_chain tests.test_cli_onboarding.CliOnboardingTest.test_lineage_summary_only_explains_intervening_write_continuity -q` -> passed (`Ran 4 tests`); `git diff --check -- zerker_memory/store.py zerker_memory/cli.py tests/test_store.py tests/test_cli_onboarding.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 258 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: the lineage summary now makes one more narrow proof fact human-readable from local data only: a verified mutation chain can be anchored by the prior per-memory receipt link plus the current event's live previous-event root even if unrelated writes happened in between. This verifies provenance and continuity, not semantic truth.
- Blockers: snapshot verify, bundle verify, and other proof-facing summaries still do not surface the same intervening-write continuity counts; this slice only improves the existing live lineage/report surface.
- Next safe slice: mirror the same additive continuity counts into one bounded snapshot/export or bundle-proof summary only if operators need that detail outside `zmem lineage`, without widening receipt schemas or turning Treeship into a user-facing dependency.

## 2026-06-27T15:14:50Z - L0 trust-ledger - Codex

- Scope: hardened one bounded live receipt-verification slice so existing per-memory mutation chains remain independently verifiable even when unrelated writes land between the original add/quarantine receipt and a later mutation, without changing receipt schemas, snapshot payloads, CLI wording, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_memory_write_receipt_chain(...)` now treats `prior_receipt_id` / `prior_receipt_hash` as the per-memory linkage and verifies `prior_merkle_root` against the current receipt's actual previous event root in the live event log, instead of assuming the prior receipt was adjacent in the global Merkle/event sequence. Legitimate `reject` and other mutation chains with intervening unrelated writes now verify locally while still rejecting tampered roots.
- Tests: `git diff --check -- zerker_memory/store.py tests/test_store.py` -> passed; `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_reject_chain_with_intervening_write tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_prior_root tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_attested_promote_chain -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_snapshot_accepts_revoke_receipt_with_intervening_events -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 256 tests`); `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 30 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: live lineage verification for durable mutation receipts is now honest about what is being proven. Operators can verify provenance and Merkle continuity for mutation chains even when other memories changed in between, while semantic truth of the memory content still remains unproven.
- Blockers: this slice only hardens the live per-memory verifier; it does not yet add a dedicated read-only CLI/export summary that explicitly explains intervening global writes when showing a verified chain.
- Next safe slice: add one bounded lineage/report regression or summary that makes the now-verified intervening-write continuity readable without widening receipt schemas or turning Treeship into a user-facing dependency.

## 2026-06-27T11:12:26Z - L0 trust-ledger - Codex

- Scope: landed one bounded snapshot-verification supersession slice for same-provenance subject-lookup restatement transitions without parent links, without changing write schemas, lifecycle paths, CLI proof wording, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_snapshot(...)` now also returns additive `supersession_transition_count`, `verified_supersession_transition_count`, and `supersession_transitions` summaries for same-provenance `subject-lookup-restatement` supersessions that are anchored to the verified activation receipt of the chosen current memory. The new derived summaries reuse snapshot event ordering plus query-time serial rules to identify the stale/current pair and surface superseded/superseding ids, superseded-at timestamp, anchor receipt id/hash/kind, anchor event hash, actor identity, content digest, prior/new Merkle roots, optional Treeship artifact id, and explicit `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_snapshot_surfaces_subject_lookup_restatement_supersession_transition_summary tests.test_snapshot.SnapshotTest.test_verify_snapshot_surfaces_explicit_update_supersession_transition_summary tests.test_snapshot.SnapshotTest.test_verify_snapshot_surfaces_parent_supersession_transition_summary tests.test_store.MemoryStoreTest.test_query_at_same_provenance_restatement_supersedes_older_subject_fact_without_parent_link -q` -> passed; `git diff --check -- zerker_memory/store.py tests/test_snapshot.py` -> passed; `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 30 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 253 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: exported snapshots can now prove one more narrow implicit current-vs-history transition class without introducing a new durable write receipt: same-provenance restatement supersession is derived from snapshot memory/event state but anchored to an already verified activation receipt of the chosen current memory. That verifies provenance and lineage of the transition anchor, not semantic truth of either memory claim.
- Blockers: cross-provenance subject-lookup conflicts and exact-tie restatements remain intentional abstentions or ambiguous history rather than receipt-ready supersession summaries.
- Next safe slice: add one bounded read-only snapshot/export-facing summary that groups the now-derived restatement supersession anchors for operators without widening write schemas or turning Treeship into a user-facing dependency.

## 2026-06-27T07:14:26Z - L0 trust-ledger - Codex

- Scope: landed one bounded snapshot-verification supersession slice for explicit update current-vs-history transitions without parent links, without changing write schemas, lifecycle paths, CLI proof wording, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_snapshot(...)` now also returns additive `supersession_transition_count`, `verified_supersession_transition_count`, and `supersession_transitions` summaries for explicit update supersessions that are anchored to the verified activation receipt of the chosen current memory. The new derived summaries reuse snapshot event ordering plus query-time serial rules to identify the stale/current pair and surface superseded/superseding ids, superseded-at timestamp, anchor receipt id/hash/kind, anchor event hash, actor identity, content digest, prior/new Merkle roots, optional Treeship artifact id, and explicit `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_snapshot_surfaces_explicit_update_supersession_transition_summary tests.test_snapshot.SnapshotTest.test_verify_snapshot_surfaces_parent_supersession_transition_summary tests.test_store.MemoryStoreTest.test_query_at_explicit_update_supersedes_older_same_subject_memory_without_parent_link -q` -> passed; `git diff --check -- zerker_memory/store.py tests/test_snapshot.py` -> passed; `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 29 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 247 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: exported snapshots can now prove one more narrow implicit current-vs-history transition class without introducing a new durable write receipt: explicit update supersession is derived from snapshot memory/event state but anchored to an already verified activation receipt of the chosen current memory. That verifies provenance and lineage of the transition anchor, not semantic truth of either memory claim.
- Blockers: subject-lookup/restatement supersessions without a parent link or explicit update pattern still remain implicit temporal state rather than receipt-ready proof summaries.
- Next safe slice: extend the same bounded derived-anchor contract to one subject-lookup restatement supersession path without widening write schemas or turning Treeship into a user-facing dependency.

## 2026-06-27T03:13:50Z - L0 trust-ledger - Codex

- Scope: landed one bounded snapshot-verification supersession slice for parent-linked current-vs-history transitions, without changing write schemas, lifecycle paths, CLI proof wording, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_snapshot(...)` now returns additive `supersession_transition_count`, `verified_supersession_transition_count`, and `supersession_transitions` summaries for parent-linked supersessions that are anchored to the verified activation receipt of the superseding child memory. Each summary carries superseded/superseding memory ids, superseded-at timestamp, anchor receipt id/hash/kind, anchor event hash, actor identity, content digest, prior/new Merkle roots, optional Treeship artifact id, and explicit `semantic_truth_guaranteed=False`.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_snapshot_surfaces_parent_supersession_transition_summary tests.test_snapshot.SnapshotTest.test_verify_snapshot_surfaces_provenance_anchor_summary tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_attested_promote_chain -q` -> passed; `git diff --check -- zerker_memory/store.py tests/test_snapshot.py` -> passed; `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 28 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 246 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: exported snapshots can now prove one narrow implicit current-vs-history transition class without introducing a new durable write receipt: parent supersession is derived from snapshot memory/event state but anchored to an already verified superseding child receipt. That verifies provenance and lineage of the transition anchor, not semantic truth of either memory claim.
- Blockers: this new receipt-ready supersession contract only covers parent-linked supersessions. Explicit update/restatement supersessions without a parent link still remain implicit temporal state rather than receipt-ready proof summaries.
- Next safe slice: extend the same bounded derived-anchor contract to one explicit update/current-vs-history supersession path without widening write schemas or turning Treeship into a user-facing dependency.

## 2026-06-26T23:12:33Z - L0 trust-ledger - Codex

- Scope: landed one bounded export-verification summary slice for original per-memory provenance anchors in snapshot verification, without changing write paths, receipt schemas, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `zerker_memory/cli.py`; `tests/test_snapshot.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_snapshot(...)` now returns additive `provenance_receipt_count`, `verified_provenance_receipt_count`, and `provenance_receipts` summaries for the original `zerker.memory.write_provenance` receipt in each exported write-receipt chain, including actor identity, content digest, prior/new Merkle roots, local verification status, and optional Treeship artifact id. `zmem snapshot verify --summary-only` now surfaces that compact provenance-anchor block and stays explicit that verified provenance/integrity does not guarantee semantic truth.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_snapshot_surfaces_provenance_anchor_summary -q` -> passed; `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_surfaces_verified_export_provenance -q` -> passed; `git diff --check -- zerker_memory/store.py zerker_memory/cli.py tests/test_snapshot.py tests/test_cli_onboarding.py` -> passed; `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 27 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 244 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: exported snapshot verification now exposes the original add/quarantine provenance anchor for each memory chain as local proof-readable data only: actor/agent identity, content digest, prior/new Merkle-root transition, and optional Treeship artifact id. Provenance is verifiable; semantic truth is still not guaranteed.
- Blockers: explicit durable mutation receipts still do not exist for implicit supersession/current-vs-history state changes, and multi-memory human snapshot summaries still compress provenance anchors instead of listing every chain in terminal output.
- Next safe slice: add one narrow supersession provenance contract, or expand multi-memory snapshot-verify terminal detail only if operators actually need more than the new compact counts-plus-anchor summary.

## 2026-06-26T19:12:02Z - L0 trust-ledger - Codex

- Scope: hardened the original add/quarantine provenance receipt root transition so first-write memory receipts are more independently verifiable in both local receipt checks and snapshot/export verification, without widening into new CLI surfaces, lifecycle writes, or Treeship dependency changes.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `_append_event(...)` now carries additive `prior_merkle_root` metadata, and original `zerker.memory.write_provenance` receipts now surface `prior_event_hash`, `prior_merkle_root`, and `new_merkle_root` by default. `verify_memory_write_receipt(...)` now cross-checks those root-transition fields against the embedded source event when present, and `verify_snapshot(...)` now rejects forged first-write provenance roots in exported snapshots instead of only validating later mutation transitions.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_memory_write_emits_provenance_receipt tests.test_store.MemoryStoreTest.test_quarantined_provenance_receipt_surfaces_created_state_and_verifies_locally tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_provenance_status tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_provenance_prior_merkle_root tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_attested_promote_chain tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_prior_root -q` -> passed; `python3 -m unittest tests.test_snapshot.SnapshotTest.test_snapshot_captures_memory_events_receipts_and_hash tests.test_snapshot.SnapshotTest.test_verify_snapshot_accepts_revoke_receipt_with_intervening_events tests.test_snapshot.SnapshotTest.test_verify_snapshot_rejects_tampered_write_receipt_chain tests.test_snapshot.SnapshotTest.test_verify_snapshot_rejects_tampered_first_provenance_prior_merkle_root tests.test_snapshot.SnapshotTest.test_restore_rejects_snapshot_with_tampered_write_receipt_chain -q` -> passed; `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_snapshot.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 243 tests`); `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 26 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: add/quarantine provenance receipts now expose an independently checkable prior/new Merkle-root transition from local receipt data, and exported snapshots now prove that first-write root transition instead of trusting only later mutation receipts. Verified provenance/integrity still does not guarantee semantic truth.
- Blockers: explicit durable mutation receipts still do not exist for implicit supersession/current-vs-history state changes, and provenance receipt verification still relies on embedded source-event evidence rather than a separate public export/report surface.
- Next safe slice: add one bounded export/read summary that surfaces the richer first-write provenance root transition, or one narrow supersession provenance contract, without widening receipt schemas again.

## 2026-06-26T15:09:17Z - L0 trust-ledger - Codex

- Scope: hardened the original add/quarantine write provenance receipt so newly remembered memories carry independently checkable created-state metadata without widening into new CLI/read-only proof surfaces, lifecycle write paths, or Treeship dependency changes.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `remember(...)` now records additive provenance fields on the original `zerker.memory.write_provenance` receipt for `actor_id`, `memory_type`, `scope`, `source_kind`, `trust`, `authority`, created `status`, and explicit `semantic_truth_guaranteed=False`. `verify_memory_write_receipt(...)` now cross-checks those surfaced provenance fields against the durable source event payload and rejects recomputed receipts whose created-state status/type/source-kind/trust/authority no longer match the original event, including quarantined adds.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_memory_write_emits_provenance_receipt tests.test_store.MemoryStoreTest.test_quarantined_provenance_receipt_surfaces_created_state_and_verifies_locally tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_provenance_status tests.test_store.MemoryStoreTest.test_memory_write_can_emit_compact_treeship_attestation_when_enabled tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_attested_promote_chain tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_attestation_subject -q` -> passed; `git diff --check -- zerker_memory/store.py tests/test_store.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 241 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: add/quarantine provenance receipts are now more receipt-ready from local data only: operators can verify who wrote the memory, what state it entered, and that verified provenance/integrity still does not guarantee semantic truth.
- Blockers: explicit durable mutation receipts still do not exist for implicit supersession/current-vs-history state changes, and provenance receipts still do not surface an independently checkable prior-Merkle-root field for the very first add event.
- Next safe slice: add one bounded receipt-ready contract for supersession provenance or one verifier-backed export/read surface that consumes the richer add/quarantine provenance fields without widening receipt schemas further.

## 2026-06-26T11:06:35Z - L0 trust-ledger - Codex

- Scope: landed one bounded read-only lifecycle receipt summary slice for the aggregate session timeline without changing lifecycle write semantics, receipt schemas, snapshot artifacts, or Treeship dependency boundaries.
- Files touched: `zerker_memory/cli.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `build_session_timeline_report(...)` now verifies each persisted timeline lifecycle receipt through `verify_lifecycle_receipt(...)`, attaches additive `receipt_verification` plus compact `receipt_summary` metadata to every entry, and reports aggregate verified/failed provenance counts plus linked Treeship artifact counts. `render_session_timeline_summary(...)` now surfaces trusted provenance status, content digest, prior/new Merkle roots, source-event hash plus actor identity, optional Treeship artifact id, and explicit `semantic truth: not guaranteed` wording for start, checkpoint, snapshot, snapshot-soft-delete, and end timeline entries.
- Tests: `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_session_timeline_summary_surfaces_mixed_lifecycle_entries_and_retention_state tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_snapshot_receipt tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_reports_tampered_actor_identity tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_reports_tampered_source_event_identity -q` -> passed; `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` -> passed; `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: the aggregate session timeline is now proof-readable from local data only, and each listed lifecycle transition remains independently verifiable while staying honest that verified provenance/integrity does not guarantee semantic truth.
- Blockers: single-session timeline summaries now match the compact receipt contract, but there is still no equivalent multi-session lifecycle rollup that combines receipt-verification counts across sessions, and `restore_snapshot()` remains snapshot-linked rather than source-event-linked by design.
- Next safe slice: add one bounded multi-session lifecycle rollup that combines starts/checkpoints/snapshots/retention/ends per session and reuses the same additive receipt-summary contract without widening any lifecycle write path or Treeship dependency boundary.

## 2026-06-26T07:07:32Z - L0 trust-ledger - Codex

- Scope: landed one bounded read-only lifecycle receipt summary slice for persisted session starts/ends without changing lifecycle write semantics, receipt schemas, snapshot artifacts, or Treeship dependency boundaries.
- Files touched: `zerker_memory/cli.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `build_session_starts_report(...)` and `build_session_ends_report(...)` now verify each persisted lifecycle receipt through `verify_lifecycle_receipt(...)` and attach additive `receipt_verification` plus compact `receipt_summary` metadata. `render_session_starts_summary(...)` and `render_session_ends_summary(...)` now surface trusted provenance status, content digest, prior/new Merkle roots, source-event hash plus actor identity, optional Treeship artifact id, and explicit `semantic truth: not guaranteed` wording.
- Tests: `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_session_starts_summary_surfaces_latest_start_root_and_budget_hints tests.test_cli_onboarding.CliOnboardingTest.test_session_ends_summary_surfaces_latest_end_root_and_counts tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_snapshot_receipt tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_reports_tampered_actor_identity tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_reports_tampered_source_event_identity -q` -> passed; `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` -> passed; `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: persisted session start/end lifecycle receipts are now readable and independently verifiable from the existing read-only report surfaces, while staying honest that verified provenance/integrity does not guarantee semantic truth.
- Blockers: the aggregate lifecycle timeline still does not surface the same compact receipt-verification block, and `restore_snapshot()` remains snapshot-linked rather than source-event-linked by design.
- Next safe slice: extend the same bounded receipt-summary contract to one aggregate lifecycle timeline summary without changing lifecycle write paths or Treeship dependency boundaries.

## 2026-06-26T03:08:13Z - L0 trust-ledger - Codex

- Scope: landed one bounded read-only lifecycle receipt summary slice for persisted checkpoint/snapshot receipts without changing lifecycle write semantics, receipt schemas, snapshot artifacts, or Treeship dependency boundaries.
- Files touched: `zerker_memory/cli.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `build_session_checkpoints_report(...)` and `build_session_snapshots_report(...)` now verify each persisted lifecycle receipt through `verify_lifecycle_receipt(...)` and attach additive `receipt_verification` plus compact `receipt_summary` metadata. `render_session_checkpoints_summary(...)` and `render_session_snapshots_summary(...)` now surface trusted provenance status, content digest, prior/new Merkle roots, source-event hash plus actor identity, optional Treeship artifact id, and explicit `semantic truth: not guaranteed` wording.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_snapshot_receipt tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_reports_tampered_actor_identity tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_reports_tampered_source_event_identity -q` -> passed; `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_session_checkpoints_summary_surfaces_checkpoint_root_and_memory_type_counts tests.test_cli_onboarding.CliOnboardingTest.test_session_snapshots_summary_surfaces_soft_deleted_retention_state -q` -> passed; `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (227 tests); `python3 -m zerker_memory eval` -> passed (11/11); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: persisted checkpoint/snapshot lifecycle receipts are now readable and independently verifiable from the existing read-only report surfaces, while staying honest that verified provenance/integrity does not guarantee semantic truth.
- Blockers: session starts/ends and the aggregate lifecycle timeline do not yet surface the same compact receipt-verification block, and `restore_snapshot()` remains snapshot-linked rather than source-event-linked by design.
- Next safe slice: extend the same bounded receipt-summary contract to `session starts` / `session ends` or one aggregate lifecycle timeline summary without changing lifecycle write paths or Treeship dependency boundaries.

## 2026-06-25T23:08:10Z - L0 trust-ledger - Codex

- Scope: hardened persisted lifecycle receipt provenance by adding an additive source-event identity echo for existing session start/end/checkpoint/snapshot receipts, without changing write-path semantics, snapshot artifacts, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: derived lifecycle receipts backed by durable session events now embed a fuller `treeship_statement.source.event` block with event schema/hash algorithm, payload hash, prior event hash, actor id/URI, and created-at metadata. `verify_lifecycle_receipt(...)` now recomputes that embedded source-event hash for persisted lifecycle receipts and rejects actor or event-identity rewrites that no longer match the original durable event hash. `restore_snapshot()` receipts stay unchanged because they are derived from a verified source snapshot, not a newly appended lifecycle event.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_snapshot_receipt tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_reports_tampered_actor_identity tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_reports_tampered_source_event_identity -q` -> passed; `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_lifecycle_receipt_accepts_restore_snapshot_receipt_with_source_snapshot tests.test_snapshot.SnapshotTest.test_verify_lifecycle_receipt_reports_tampered_restore_receipt -q` -> passed; `python3 -m unittest tests.test_store -q` -> passed (224 tests); `python3 -m unittest tests.test_snapshot -q` -> passed (25 tests); `python3 -m zerker_memory eval` -> passed (11/11); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: persisted lifecycle receipts are now independently checkable against their own embedded source-event identity and hash inputs, which strengthens provenance/integrity for start/end/checkpoint/snapshot receipts without claiming semantic truth of the underlying memories or summaries.
- Blockers: `restore_snapshot()` still has no durable source event by design, so rollback receipts remain snapshot-linked rather than event-hash-linked.
- Next safe slice: add one bounded read-only lifecycle receipt summary/report that surfaces the new source-event identity block for persisted session lifecycle receipts without widening into write-path or Treeship-hosted changes.

## 2026-06-22 - coordinator

- Scope: seeded lane for memory receipt, Merkle lineage, rollback/export, and Treeship proof integration.
- Files touched: lane log only.
- Behavior changed: none.
- Tests: not applicable.
- Artifacts/receipts: none.
- Blockers: worker must first inventory existing mutation paths and receipt fields.
- Next safe slice: add a failing or clarifying test for receipt emission on one non-add mutation.

## 2026-06-22T18:50:00Z - L0 trust-ledger - Codex

- Scope: inventoried durable mutation paths and strengthened receipt-adjacent lineage coverage for one non-add mutation without changing store behavior.
- Files touched: `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`.
- Behavior changed: none. Existing durable mutations observed: `remember`/`import_external` create memory rows, Merkle events, and `memory_write_receipts`; `promote`, `reject`, `revoke`, and `forget` update memory status/authority and append Merkle events; `inject` creates action receipts; `snapshot`/`restore_snapshot` export and restore memories, events, action receipts, and write receipts.
- Tests: `python3 -m unittest tests.test_snapshot` -> passed, 14 tests.
- Artifacts/receipts: strengthened `test_snapshot_captures_promote_event_lineage` proves a `PROMOTED` event is snapshot-visible, chained via `prev_event_hash`, and reflected in the snapshot Merkle root.
- Blockers: non-add transitions still do not create their own `memory_write_receipts`; this slice only locks the existing event-ledger coverage.
- Next safe slice: define the minimal transition receipt envelope for `promote` or `revoke`, then add a focused failing test before touching `zerker_memory/store.py`.

## 2026-06-22T22:54:56Z - L0 trust-ledger - Codex

- Scope: landed the first durable non-add mutation receipt for `promote` without turning Treeship into a user-facing dependency.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`; `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: `promote()` now appends a second ordered `memory_write_receipt` for the mutation itself; `memory_write_receipt(memory_id)` stays pinned to the original source-provenance receipt for existing bundle/why/workspace surfaces, and new `memory_write_receipts(memory_id)` exposes the full event-ordered receipt chain.
- Artifacts/receipts: the new promote mutation receipt carries actor identity, content digest, prior receipt link, prior Merkle root, new Merkle root, and event hash in its embedded Treeship statement; this proves mutation provenance and integrity, not semantic truth of the promoted claim.
- Snapshot/export: snapshot write receipts are now exported in event order, so the original provenance receipt and the promote mutation receipt remain independently restorable and verifiable.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_snapshot_captures_promote_mutation_write_receipt tests.test_snapshot.SnapshotTest.test_snapshot_captures_promote_event_lineage -q` -> passed; `python3 -m unittest tests.test_store.MemoryStoreTest.test_promote_persists_mutation_receipt_without_overwriting_original_write_provenance -q` -> passed; `python3 -m unittest tests.test_snapshot -q` -> passed (15 tests); `python3 -m unittest tests.test_store -q` -> passed (161 tests); `python3 -m zerker_memory eval` -> passed (11/11); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Blockers: `reject`, `revoke`, `forget`, and lifecycle/export-only mutations still do not emit their own mutation receipts, and no CLI/report surface summarizes the ordered receipt chain yet.
- Next safe slice: add the same explicit mutation-receipt envelope for `reject` or `revoke`, then extend one proof-facing surface to display ordered mutation receipts without changing current source-provenance semantics.

## 2026-06-22T23:20:00Z - L0 trust-ledger - Codex

- Scope: advanced non-add mutation receipt coverage by adding the ordered mutation receipt for `reject()`.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`.
- Behavior changed: `reject()` now appends a `zerker.memory.mutation_receipt` after the original source-provenance write receipt, carrying actor identity, rejection reason, previous status, resulting status/authority, prior receipt link, prior Merkle root, new Merkle root, and event hash. `memory_write_receipt(memory_id)` still returns the original provenance receipt; `memory_write_receipts(memory_id)` exposes the ordered chain. Receipts prove provenance/integrity of the rejection event, not semantic truth.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_reject_persists_mutation_receipt_without_overwriting_original_write_provenance -q` -> passed; `python3 -m unittest tests.test_store.MemoryStoreTest.test_promote_persists_mutation_receipt_without_overwriting_original_write_provenance tests.test_store.MemoryStoreTest.test_queue_and_reject_memory -q` -> passed; `python3 -m unittest tests.test_snapshot -q` -> passed (15 tests); `python3 -m unittest tests.test_store -q` -> passed (164 tests).
- Artifacts/receipts: new focused test verifies the reject mutation receipt is ordered after the original receipt, preserves the original receipt lookup, links to the prior receipt hash/id, and avoids semantic-truth language.
- Blockers: `revoke()` and `forget()` still do not emit mutation receipts; proof-facing CLI/report summaries for ordered write receipt chains remain absent.
- Next safe slice: add mutation receipt coverage for `revoke()` root events, including affected descendant ids, without attempting per-descendant receipts unless a separate test defines that behavior.

## 2026-06-23T06:54:59Z - L0 trust-ledger - Codex

- Scope: landed the root `revoke()` mutation receipt and snapshot export coverage without widening into descendant receipt emission or new proof-facing CLI surfaces.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `revoke()` now appends an ordered `zerker.memory.mutation_receipt` for the revoked root memory while preserving `memory_write_receipt(memory_id)` as the original source-provenance anchor. The new mutation receipt records actor identity, reason, previous status, resulting status/authority, revoked root+descendant ids, descendant count, prior receipt link, and prior/new Merkle roots. Descendants are still revoked without their own mutation receipts in this slice.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_revoke_persists_root_mutation_receipt_without_overwriting_original_write_provenance -q` -> passed; `python3 -m unittest tests.test_snapshot.SnapshotTest.test_snapshot_captures_revoke_mutation_write_receipt -q` -> passed; `python3 -m unittest tests.test_store -q` -> passed (173 tests); `python3 -m unittest tests.test_snapshot -q` -> passed (16 tests); `python3 -m zerker_memory eval` -> passed (11/11).
- Artifacts/receipts: snapshot export now preserves the revoke mutation receipt in the ordered root receipt chain, so revoke provenance and lineage remain independently verifiable outside the live DB. The receipt proves provenance/integrity of the revocation event, not semantic truth of the revoked claim.
- Blockers: `forget()` and lifecycle/export-only mutations still do not emit their own mutation receipts, and no read-only proof/report surface summarizes ordered mutation chains yet.
- Next safe slice: add the same bounded mutation-receipt envelope for `forget()`, or expose one read-only receipt-chain summary surface without changing existing source-provenance lookups.

## 2026-06-23T10:55:33Z - L0 trust-ledger - Codex

- Scope: landed the `forget()` mutation receipt and snapshot export coverage without widening into new proof/report read surfaces.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `forget()` now appends an ordered `zerker.memory.mutation_receipt` while preserving `memory_write_receipt(memory_id)` as the original source-provenance anchor. The new mutation receipt records actor identity, previous status, resulting status/authority, prior receipt link, and prior/new Merkle roots.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_forget_persists_mutation_receipt_without_overwriting_original_write_provenance -q` -> passed; `python3 -m unittest tests.test_snapshot.SnapshotTest.test_snapshot_captures_forget_mutation_write_receipt -q` -> passed; `python3 -m unittest tests.test_snapshot -q` -> passed (17 tests); `python3 -m unittest tests.test_store -q` -> passed (177 tests); `python3 -m zerker_memory eval` -> passed (11/11); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Artifacts/receipts: snapshot export now preserves the forget mutation receipt in the ordered write-receipt chain, so forgotten-memory provenance and lineage remain independently verifiable outside the live DB. The receipt proves verified provenance and mutation integrity, not semantic truth of the forgotten claim.
- Blockers: lifecycle/export-only mutations still do not emit their own mutation receipts, and no read-only proof/report surface summarizes ordered mutation chains yet.
- Next safe slice: expose one read-only receipt-chain summary surface for the existing ordered mutation receipts, or define the first checkpoint/snapshot/export mutation-receipt contract before changing store behavior again.

## 2026-06-23T14:59:10Z - L0 trust-ledger - Codex

- Scope: landed the first explicit lifecycle mutation-receipt envelope for `checkpoint_session()` and `snapshot_session()` without adding a new table, CLI surface, or Treeship dependency.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `session_checkpoints()` and `session_snapshots()` now return a derived `zerker.lifecycle_receipt.v1` envelope for each persisted `SESSION_CHECKPOINTED` / `SESSION_SNAPSHOTTED` event. Each receipt is deterministically reconstructed from the durable event row plus stored payload, carries actor identity, session id, payload content digest, prior/new Merkle roots, snapshot hash, a `zerker.memory.mutation_receipt` Treeship statement, and explicit `semantic_truth_guaranteed: false` wording.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_checkpoint_session_emits_receipt_visible_roots_and_memory_type_summary tests.test_store.MemoryStoreTest.test_session_checkpoints_reads_back_persisted_checkpoint_events tests.test_store.MemoryStoreTest.test_snapshot_session_persists_snapshot_payload_and_receipt_visible_roots tests.test_store.MemoryStoreTest.test_session_snapshots_reads_back_persisted_snapshot_events_and_payloads -q` -> passed; `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_snapshot_accepts_valid_snapshot tests.test_snapshot.SnapshotTest.test_restore_snapshot_round_trips_to_empty_store -q` -> passed; `python3 -m unittest tests.test_store -q` -> passed (182 tests); `python3 -m zerker_memory eval` -> passed (11/11).
- Artifacts/receipts: the lifecycle receipt hashes are independently recomputable from the returned payload, and the embedded Treeship statements prove verifiable lineage/integrity of the checkpoint or snapshot event only. They do not claim semantic truth of the stored memories or summaries.
- Blockers: `restore_snapshot()` still restores state without emitting its own rollback receipt, and there is still no read-only CLI/proof summary for ordered mutation or lifecycle receipt chains.
- Next safe slice: extend the same derived lifecycle receipt contract to `restore_snapshot()` so rollback/export verification has an explicit post-restore receipt without changing snapshot artifact format.

## 2026-06-23T19:04:20Z - L0 trust-ledger - Codex

- Scope: landed the first explicit rollback/export receipt for `restore_snapshot()` without adding a new table, appending a restore event, or changing snapshot artifact format.
- Files touched: `zerker_memory/store.py`; `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `restore_snapshot(snapshot, *, actor_id=\"snapshot_restore\")` now returns a derived `zerker.lifecycle_receipt.v1` receipt in its result payload. The receipt carries actor identity, snapshot hash, payload content digest, prior/new Merkle roots, optional `treeship_artifact_id: null`, and an embedded `zerker.memory.mutation_receipt` statement with `semantic_truth_guaranteed: false`. No restore event is appended, so the restored store Merkle root still matches the imported snapshot Merkle root exactly.
- Artifacts/receipts: the restore receipt is independently recomputable from the returned receipt payload plus the verified source snapshot summary. It proves verified provenance/integrity of the restore operation and imported snapshot lineage, not semantic truth of the restored memories.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_restore_snapshot_returns_deterministic_restore_receipt_without_changing_snapshot_root tests.test_snapshot.SnapshotTest.test_restore_snapshot_round_trips_to_empty_store -q` -> passed; `python3 -m unittest tests.test_snapshot -q` -> passed (18 tests); `python3 -m unittest tests.test_store -q` -> passed (186 tests); `python3 -m zerker_memory eval` -> passed (11/11); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Blockers: the restore receipt is returned but not yet summarized by any read-only CLI/proof surface, and export/handoff restore flows still rely on callers to surface the returned receipt explicitly.
- Next safe slice: expose one read-only receipt-chain or restore-summary surface that shows the returned lifecycle/rollback receipt without changing `memory_write_receipt(memory_id)` provenance-anchor semantics.

## 2026-06-23T23:02:26Z - L0 trust-ledger - Codex

- Scope: added one shared read-only verifier for existing lifecycle receipts so rollback/export and persisted session snapshot receipts can be checked independently without changing receipt formats or adding a Treeship dependency.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.verify_lifecycle_receipt(receipt, *, source_snapshot=None)` now verifies `zerker.lifecycle_receipt.v1` payloads by recomputing `receipt_hash`, `content_digest`, and embedded Treeship statement consistency, plus optional snapshot-hash/root/count linkage when a source snapshot is supplied. This covers the derived `restore_snapshot()` rollback receipt and persisted `snapshot_session()` receipts without appending events or altering storage.
- Artifacts/receipts: lifecycle receipts are now independently checkable through a shared API instead of only via ad hoc test-side hash recomputation. Verification proves provenance/integrity and snapshot lineage consistency only; `semantic_truth_guaranteed` remains explicitly false.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_lifecycle_receipt_accepts_restore_snapshot_receipt_with_source_snapshot tests.test_snapshot.SnapshotTest.test_verify_lifecycle_receipt_reports_tampered_restore_receipt -q` -> passed; `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_snapshot_receipt -q` -> passed; `python3 -m unittest tests.test_snapshot -q` -> passed (20 tests); `python3 -m unittest tests.test_store -q` -> passed (190 tests); `python3 -m zerker_memory eval` -> passed (11/11).
- Blockers: the new verifier is still store-level only, so there is still no CLI/proof summary surface for restore receipts or ordered lifecycle/mutation receipt chains.
- Next safe slice: expose one read-only restore/receipt summary surface on top of `verify_lifecycle_receipt(...)` without changing `memory_write_receipt(memory_id)` provenance-anchor semantics.

## 2026-06-24T03:05:29Z - L0 trust-ledger - Codex

- Scope: exposed one read-only rollback/export proof summary for handoff restores on top of the existing lifecycle verifier, without changing receipt formats, snapshot payloads, or mutation behavior.
- Files touched: `zerker_memory/cli.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `restore_handoff_package(...)` now verifies the derived `restore_snapshot()` lifecycle receipt against the source snapshot before reporting success, and `render_restore_summary(...)` now surfaces restore receipt verification status, receipt id/hash, snapshot hash, prior/new Merkle roots, optional Treeship artifact id, and explicit `Semantic truth: not guaranteed` wording.
- Artifacts/receipts: the returned restore receipt is now summarized as verified provenance/integrity for the rollback/export action itself; no Treeship CLI or hosted dependency is required, and the summary stays explicit that semantic truth of restored memories is not guaranteed.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_lifecycle_receipt_accepts_restore_snapshot_receipt_with_source_snapshot tests.test_snapshot.SnapshotTest.test_verify_lifecycle_receipt_reports_tampered_restore_receipt -q` -> passed; `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_restore_handoff_package_verifies_and_restores_snapshot -q` -> passed; `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Blockers: there is still no analogous read-only CLI/proof summary for ordered per-memory mutation receipt chains, and direct `restore <snapshot>` JSON restore still prints raw JSON instead of the compact receipt-aware summary used by handoff restore.
- Next safe slice: expose one bounded read-only receipt-chain summary for `memory_write_receipts(memory_id)` or extend the same receipt-aware summary path to direct snapshot restore without changing snapshot/import semantics.

## 2026-06-24T07:04:24Z - L0 trust-ledger - Codex

- Scope: locked snapshot/export and restore coverage for the existing optional Treeship digest-attestation path on ordered write-receipt chains, without changing store behavior or receipt schemas.
- Files touched: `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: none. This slice proves that the already-shipped `treeship_statement.attestation` payload survives snapshot export and `restore_snapshot(...)` for both the original provenance receipt and a later mutation receipt in the same ordered chain.
- Artifacts/receipts: the new test locks optional Treeship artifact ids and digest-only attestation payloads as portable receipt metadata only. Provenance/integrity are externally attributable; semantic truth is still not guaranteed.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_snapshot_restore_preserves_attested_ordered_write_receipt_chain tests.test_store.MemoryStoreTest.test_memory_write_can_emit_compact_treeship_attestation_when_enabled tests.test_store.MemoryStoreTest.test_memory_write_treeship_attestation_failure_is_non_fatal_without_strict_mode -q` -> passed (`Ran 3 tests`).
- Blockers: ordered mutation receipt chains are now attestation-round-trip tested, but there is still no read-only CLI/proof summary for `memory_write_receipts(memory_id)` and no direct restore summary surface outside handoff restore.
- Next safe slice: add one bounded read-only receipt-chain summary for `memory_write_receipts(memory_id)` that exposes optional attestation artifact ids without changing any write semantics.

## 2026-06-24T11:05:45Z - L0 trust-ledger - Codex

- Scope: added one shared independent verifier for surfaced `memory_write_receipts(...)` chains so existing provenance-plus-mutation receipts can be checked outside the live write path and after snapshot restore, without adding a Treeship dependency or changing receipt storage.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.verify_memory_write_receipt(...)` now verifies a surfaced `zerker.memory_write.v1` receipt by recomputing its hash from the embedded canonical `treeship_statement.source.receipt`, checking Treeship statement consistency, optional digest-attestation linkage, and mutation root fields. `MemoryStore.verify_memory_write_receipt_chain(...)` now verifies the ordered prior-receipt link, prior/new Merkle-root transition, and optional attestation artifacts across a per-memory receipt chain.
- Artifacts/receipts: the same verifier accepts live and snapshot-restored ordered receipt chains, including optional Treeship artifact ids and `sha256:<receipt_hash>` attestations. Verification proves provenance, lineage continuity, and integrity of the recorded mutation chain only; semantic truth of the memory content is not guaranteed.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_attested_promote_chain tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_prior_root -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_snapshot.SnapshotTest.test_snapshot_restore_preserves_attested_ordered_write_receipt_chain -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 197 tests`); `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 21 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Blockers: the verifier is store-level only, so operators still do not have a compact read-only CLI/proof summary for ordered `memory_write_receipts(memory_id)` chains or direct `lineage` output built on top of it.
- Next safe slice: add one bounded read-only lineage/CLI summary surface on top of `verify_memory_write_receipt_chain(...)` without changing existing receipt schemas or provenance-anchor lookups.

## 2026-06-24T15:05:33Z - L0 trust-ledger - Codex

- Scope: added one bounded read-only lineage summary for ordered per-memory write-receipt chains without changing write-path behavior, receipt storage, or Treeship dependency boundaries.
- Files touched: `zerker_memory/cli.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `zmem lineage --summary-only` now verifies the existing ordered `memory_write_receipts(memory_id)` chain through `MemoryStore.verify_memory_write_receipt_chain(...)` and surfaces original/latest receipt ids and kinds, actor URIs, content digest, Merkle-root transition, optional Treeship artifact ids, verified provenance status, and explicit `Semantic truth: not guaranteed` wording. Default `zmem lineage` JSON output is unchanged.
- Artifacts/receipts: the new summary is proof-facing only. It makes provenance/integrity and receipt-chain continuity readable from local data without claiming semantic truth of the memory content.
- Tests: `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_lineage_parser_accepts_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_lineage_summary_only_surfaces_verified_write_receipt_chain tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_attested_promote_chain tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_prior_root -q` -> passed (`Ran 4 tests`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed; `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` -> passed.
- Blockers: direct `zmem restore <snapshot>` still returns raw JSON instead of the compact receipt-aware summary style now used by handoff restore and lineage.
- Next safe slice: extend the same bounded receipt-aware summary contract to direct snapshot restore without changing snapshot import semantics or receipt schemas.

## 2026-06-24T19:06:03Z - L0 trust-ledger - Codex

- Scope: extended the existing rollback/export receipt summary contract to direct `zmem restore <snapshot>` without changing store receipts, snapshot payloads, or Treeship dependency boundaries.
- Files touched: `zerker_memory/cli.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: direct snapshot restore now verifies the derived `restore_snapshot()` lifecycle receipt against the source snapshot, and both `zmem restore <snapshot> --summary-only` and default `zmem restore <snapshot>` reuse the same compact receipt-aware summary path already used by handoff restore before optional JSON output.
- Artifacts/receipts: the direct-restore summary now surfaces restore receipt verification status, receipt id/hash, snapshot hash, prior/new Merkle roots, optional Treeship artifact id, and explicit `Semantic truth: not guaranteed` wording from local data only.
- Tests: `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_restore_parser_accepts_handoff_dir_and_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_restore_parser_accepts_snapshot_path_and_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_restore_handoff_package_verifies_and_restores_snapshot tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_verifies_and_restores_snapshot tests.test_snapshot.SnapshotTest.test_verify_lifecycle_receipt_accepts_restore_snapshot_receipt_with_source_snapshot tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_snapshot_receipt -q` -> passed (`Ran 6 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` -> passed; `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Blockers: restore summaries are now consistent, but there is still no comparable compact proof summary for bundle/export verification outside the existing handoff and lineage surfaces.
- Next safe slice: add one bounded read-only bundle/export summary that reuses the local verifier outputs without changing receipt schemas or write paths.

## 2026-06-24T23:06:11Z - L0 trust-ledger - Codex

- Scope: added one bounded read-only bundle/export proof summary surface on top of the existing local bundle verifier without changing receipt storage, store write behavior, or Treeship dependency boundaries.
- Files touched: `zerker_memory/cli.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `zmem bundle verify <bundle> --summary-only` now prints a compact proof-facing summary with action id, bundle hash, supporting memory/event/write-receipt counts, bundle verification, memory-tree verification, optional Treeship artifact ids from bundled provenance receipts, trusted provenance status, and explicit `Semantic truth: not guaranteed` wording. The default JSON verifier result now also includes additive nested `bundle_verify` and `supporting_provenance` fields, and the CLI exits non-zero if an included supporting write receipt fails independent provenance verification.
- Artifacts/receipts: bundled `supporting_memory_write_receipts` are now verified individually through the existing local write-receipt verifier and summarized as provenance/integrity evidence only. This proves bundle integrity plus bundled supporting provenance when present; it does not claim semantic truth of the retrieved memories.
- Tests: `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_bundle_verify_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_bundle_verify_summary_only_surfaces_verified_supporting_provenance tests.test_store.MemoryStoreTest.test_memory_write_emits_provenance_receipt tests.test_store.MemoryStoreTest.test_memory_write_can_emit_compact_treeship_attestation_when_enabled -q` -> passed (`Ran 4 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` -> passed; `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Blockers: `zmem treeship publish --dry-run` and `zmem snapshot verify` still remain JSON-first proof surfaces, so bundle/export verification is not yet summarized consistently across every verifier entry point.
- Next safe slice: extend the same bounded proof-summary contract to `zmem treeship publish --dry-run` without changing receipt formats, Treeship dependency boundaries, or write paths.

## 2026-06-25T03:08:03Z - L0 trust-ledger - Codex

- Scope: extended the bounded proof-summary contract to `zmem treeship publish --dry-run` without changing receipt formats, store write behavior, or Treeship dependency boundaries.
- Files touched: `zerker_memory/cli.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `zmem treeship publish <action-id> --summary-only` now prints a compact proof-facing summary for the already-verified statement export and publish handoff. The summary surfaces action id, statement path/hash, statement kind/subject, verified bundle hash, Merkle root, resolved executable, rendered command, trusted provenance status, and explicit `Semantic truth: not guaranteed` wording. Default JSON `treeship publish` output is unchanged.
- Artifacts/receipts: this is read-only on top of the existing verified Treeship statement export. It makes provenance/integrity of the export-and-publish handoff readable from local data, but still does not claim semantic truth of the underlying memories.
- Tests: `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_treeship_publish_parser tests.test_cli_onboarding.CliOnboardingTest.test_treeship_publish_summary_only_surfaces_verified_export_and_command tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_bundle_verify_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_bundle_verify_summary_only_surfaces_verified_supporting_provenance tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_verifies_and_restores_snapshot tests.test_cli_onboarding.CliOnboardingTest.test_lineage_summary_only_surfaces_verified_write_receipt_chain tests.test_treeship -q` -> passed (`Ran 18 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` -> passed; `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Blockers: `zmem snapshot verify` still remains a JSON-first proof surface, so proof-summary readability is still not uniform across every verifier entry point.
- Next safe slice: extend the same bounded proof-summary contract to `zmem snapshot verify` without changing snapshot schemas, store behavior, or Treeship dependency boundaries.

## 2026-06-25T07:09:11Z - L0 trust-ledger - Codex

- Scope: strengthened snapshot/export verification so exported `write_receipts` are verified against the snapshot's own event chain before restore, without changing snapshot schema, receipt schema, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_snapshot.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_snapshot(...)` and `restore_snapshot(...)` now reject recomputed snapshots whose embedded write receipt lineage or prior/new Merkle-root transitions were forged, while still accepting valid interleaved-event cases such as revoke-after-descendant-create by checking `prior_merkle_root` against the exported prior event root instead of only the previous write receipt root. Snapshot verification now also reports verified write-receipt chain and transition counts.
- Artifacts/receipts: this is export/rollback proof hardening on top of existing receipts. It proves the exported receipt payloads remain consistent with the exported event log and Merkle history; it still does not claim semantic truth of the underlying memories.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_snapshot_accepts_revoke_receipt_with_intervening_events tests.test_snapshot.SnapshotTest.test_verify_snapshot_rejects_tampered_write_receipt_chain tests.test_snapshot.SnapshotTest.test_restore_rejects_snapshot_with_tampered_write_receipt_chain tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_prior_root -q` -> passed (`Ran 4 tests`); `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 24 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 213 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed; `git diff --check -- zerker_memory/store.py tests/test_snapshot.py` -> passed.
- Blockers: `zmem snapshot verify` still remains JSON-first, so the strengthened export verifier is not yet exposed through the same compact proof-summary contract used by lineage, restore, bundle verify, and treeship publish.
- Next safe slice: extend the same bounded proof-summary contract to `zmem snapshot verify` on top of the strengthened store/export verifier without changing snapshot schema, Treeship dependency boundaries, or write paths.

## 2026-06-25T11:06:39Z - L0 trust-ledger - Codex

- Scope: tightened independent verification for optional Treeship digest attestations on existing write receipts without changing receipt format, write paths, snapshot artifacts, or user-facing Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_memory_write_receipt(...)` and `verify_memory_write_receipt_chain(...)` now reject attestation blobs whose `schema`, `system`, `kind`, `subject`, `status`, `artifact_id`, or `signed_at` shape no longer matches the verified local receipt they claim to attest. This keeps optional Treeship artifact metadata bound to the actual receipt id and digest instead of treating the attestation blob as opaque passthrough.
- Artifacts/receipts: the local verifier still proves receipt provenance/integrity and digest-binding only. It does not claim semantic truth of the memory content, and it still cannot independently prove that a remote Treeship artifact id itself exists without external Treeship evidence.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_memory_write_can_emit_compact_treeship_attestation_when_enabled tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_attested_promote_chain tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_attestation_subject -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 214 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Blockers: lifecycle receipts still have no equivalent optional attestation verifier because they currently carry `treeship_artifact_id: null` only, and read-only proof summaries still rely on the store verifier rather than surfacing this stricter attestation contract explicitly.
- Next safe slice: extend the same bounded attestation-shape verification to snapshot-exported and lifecycle receipt summaries only if those surfaces start carrying non-null optional Treeship artifact metadata.

## 2026-06-25T15:06:32Z - L0 trust-ledger - Codex

- Scope: extended the bounded proof-summary contract to `zmem snapshot verify --summary-only` and surfaced snapshot-exported write-receipt Treeship artifact ids from the existing local verifier without changing snapshot or receipt schemas.
- Files touched: `zerker_memory/store.py`; `zerker_memory/cli.py`; `tests/test_snapshot.py`; `tests/test_cli_onboarding.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_snapshot(...)` now keeps an additive `attestation_artifacts` list beside the existing write-receipt counts, and `zmem snapshot verify <snapshot> --summary-only` now prints a compact proof-facing summary with snapshot path/hash, memory/event/receipt counts, write-receipt chain verification counts, Merkle root recomputation, optional Treeship artifact ids from embedded write receipts, trusted provenance status, and explicit `Semantic truth: not guaranteed` wording. Default JSON output is unchanged except for the additive attestation list.
- Artifacts/receipts: this is read-only export verification on top of the existing snapshot verifier. It makes embedded write-receipt provenance and optional Treeship artifact ids readable from local data only; it does not require Treeship to be installed and does not claim semantic truth of the snapshot contents.
- Tests: `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_snapshot_accepts_valid_snapshot tests.test_snapshot.SnapshotTest.test_verify_snapshot_surfaces_attested_write_receipt_artifacts tests.test_snapshot.SnapshotTest.test_verify_snapshot_rejects_tampered_write_receipt_chain -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_parser_accepts_verify_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_surfaces_verified_export_provenance -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 25 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 218 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed; `git diff --check -- zerker_memory/store.py zerker_memory/cli.py tests/test_snapshot.py tests/test_cli_onboarding.py docs/CONTINUOUS_BUILD/trust-ledger.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` -> passed.
- Blockers: snapshot verify summary parity is now closed, but lifecycle receipts still carry only `treeship_artifact_id: null`, so there is still no equivalent non-null artifact verification path on lifecycle receipts themselves.
- Next safe slice: only if lifecycle or export receipts begin carrying non-null optional Treeship artifact ids, extend the same bounded local verifier-plus-summary contract there without widening into hosted Treeship dependency or write-path changes.

## 2026-06-25T19:06:35Z - L0 trust-ledger - Codex

- Scope: hardened independent actor-identity verification for existing mutation and lifecycle receipts without changing write paths, receipt schemas, snapshot artifacts, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `tests/test_store.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_memory_write_receipt(...)` and `verify_memory_write_receipt_chain(...)` now reject recomputed mutation receipts whose embedded `actor_id` no longer matches the source event actor or whose embedded source event no longer matches the surfaced `event_hash`. `verify_lifecycle_receipt(...)` now rejects lifecycle receipts whose embedded `actor_id` no longer maps to the surfaced `actor_uri`, and restore receipts still require any embedded `actor_uri` to match too.
- Artifacts/receipts: this is verifier-only proof hardening on top of existing receipts. It proves provenance/integrity plus tighter actor-identity binding for the recorded mutation, but still does not claim semantic truth of the memory content.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_mutation_actor_identity tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_accepts_attested_promote_chain tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_chain_reports_tampered_prior_root tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_snapshot_receipt tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_reports_tampered_actor_identity -q` -> passed (`Ran 5 tests`); `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_lifecycle_receipt_accepts_restore_snapshot_receipt_with_source_snapshot tests.test_snapshot.SnapshotTest.test_verify_lifecycle_receipt_reports_tampered_restore_receipt -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 223 tests`); `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 25 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed; `git diff --check -- zerker_memory/store.py tests/test_store.py` -> passed.
- Blockers: lifecycle receipts still do not carry a durable source-event `actor_id`, so a coherently rewritten lifecycle `actor_id`+`actor_uri` pair would need an additive source-identity field rather than verifier-only checks.
- Next safe slice: add one additive lifecycle source-identity echo only if we want lifecycle actor identity to be as tamper-evident as per-memory mutation receipts, without widening into hosted Treeship dependency or write-path changes.

## 2026-06-28T11:21:35Z - L0 trust-ledger - Codex

- Scope: extended the shared Treeship export adapter to accept existing lifecycle mutation receipts without changing store writes, receipt schemas, snapshot payloads, or CLI behavior.
- Files touched: `zerker_memory/treeship.py`; `tests/test_treeship.py`; `tests/test_exporter.py`; `docs/PROOF_SCHEMA.md`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `to_treeship_statement(...)` now accepts lifecycle receipts that already embed `treeship_statement`, recomputes the lifecycle `receipt_hash`, checks the embedded `source.receipt` linkage, preserves actor identity plus content digest and prior/new Merkle roots, and returns the recorded mutation statement for session snapshot and snapshot restore receipts instead of failing on missing action-receipt fields.
- Artifacts/receipts: this is export-only proof hardening on top of existing lifecycle receipts. It proves that the exported statement still matches the recorded lifecycle receipt and its embedded lineage fields; it does not claim semantic truth, and restore statements still need the original snapshot artifact if an external verifier wants to re-check `source_snapshot_verified`.
- Tests: `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_exports_embedded_session_snapshot_lifecycle_statement tests.test_treeship.TreeshipAdapterTest.test_exports_embedded_restore_lifecycle_statement tests.test_treeship.TreeshipAdapterTest.test_rejects_tampered_embedded_restore_lifecycle_statement tests.test_exporter.ExporterTest.test_exports_embedded_lifecycle_treeship_statement_to_file -q` -> passed (`Ran 4 tests`); `python3 -m unittest tests.test_treeship tests.test_exporter -q` -> passed (`Ran 21 tests`); `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_lifecycle_receipt_accepts_restore_snapshot_receipt_with_source_snapshot tests.test_store.MemoryStoreTest.test_verify_lifecycle_receipt_accepts_persisted_session_snapshot_receipt -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/treeship.py tests/test_treeship.py tests/test_exporter.py` -> passed; `python3 scripts/release_smoke.py --summary-only` -> passed; `python3 -m zerker_memory status --summary-only` -> passed.
- Blockers: this export path is still lifecycle-only; direct shared-adapter Treeship export for per-memory write receipts with optional attestation metadata is not yet covered.
- Next safe slice: add one similarly bounded per-memory write-receipt export contract only if external verifiers need direct add/promote/reject/revoke statement export beyond the existing lineage and snapshot proofs.

## 2026-07-04T08:51:22Z - L0 trust-ledger - Codex

- Scope: hardened one bounded bundle verification seam for supporting write-receipt key alignment, without changing receipt schemas, snapshot payloads, CLI wording, or Treeship dependency boundaries.
- Files touched: `zerker_memory/store.py`; `zerker_memory/treeship.py`; `tests/test_snapshot.py`; `tests/test_treeship.py`; `docs/CONTINUOUS_BUILD/trust-ledger.log.md`; `docs/SWARM_OPERATION_TRACKER.md`; `docs/CURRENT_STATE.md`; `docs/BUILD_LOG.md`.
- Behavior changed: `verify_bundle(...)` and `to_treeship_statement(bundle)` now reject portable bundles whose `supporting_memory_write_receipts` map keys are not declared in `supporting_memory_ids` or whose embedded `memory_id` no longer matches the receipt-map key, even when the embedded write receipt is otherwise internally self-consistent.
- Artifacts/receipts: this keeps bundle/export provenance independently verifiable and honest from local data only. The verifier now proves bundled supporting write receipts have not been relabeled in transit, while still stating that provenance integrity is verified but semantic truth is not guaranteed.
- Tests: focused receipt-first checks `python3 -m unittest tests.test_store.MemoryStoreTest.test_memory_write_emits_provenance_receipt tests.test_store.MemoryStoreTest.test_quarantined_provenance_receipt_surfaces_created_state_and_verifies_locally tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_provenance_status tests.test_store.MemoryStoreTest.test_verify_memory_write_receipt_reports_tampered_provenance_prior_merkle_root -q` -> passed (`Ran 4 tests`); focused bundle/export checks `python3 -m unittest tests.test_snapshot.SnapshotTest.test_verify_bundle_rejects_supporting_write_receipt_key_missing_from_declared_supporting_ids tests.test_snapshot.SnapshotTest.test_verify_bundle_rejects_supporting_write_receipt_key_memory_id_mismatch tests.test_snapshot.SnapshotTest.test_verify_bundle_accepts_valid_bundle tests.test_snapshot.SnapshotTest.test_verify_bundle_rejects_tampered_supporting_write_receipt -q` -> passed (`Ran 4 tests`), `python3 -m unittest tests.test_treeship.TreeshipAdapterTest.test_rejects_bundle_with_rekeyed_supporting_write_receipt tests.test_treeship.TreeshipAdapterTest.test_rejects_bundle_with_tampered_supporting_write_receipt tests.test_treeship.TreeshipAdapterTest.test_embeds_bundle_proof_when_exporting_from_receipt_bundle -q` -> passed (`Ran 3 tests`), and `git diff --check -- zerker_memory/store.py zerker_memory/treeship.py tests/test_snapshot.py tests/test_treeship.py` -> passed; required broader verification `python3 -m unittest tests.test_snapshot -q` -> passed (`Ran 35 tests`), `python3 -m unittest tests.test_treeship -q` -> passed (`Ran 33 tests`), `python3 -m unittest tests.test_store -q` -> passed (`Ran 306 tests`), `python3 -m zerker_memory eval` -> passed (`11/11`), `python3 scripts/release_smoke.py --summary-only` -> passed, and `python3 -m zerker_memory status --summary-only` -> passed.
- Blockers: this remains a narrow bundle/export verification slice only; `supporting_memories` list alignment and any remaining `trusted_only` wording surfaces still live on separate proof surfaces.
- Next safe slice: add one similarly bounded bundle verifier contract for `supporting_memories` versus `supporting_memory_ids`, or lock one `trusted_only` wording surface without widening Treeship into a user-facing dependency.
