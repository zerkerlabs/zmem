# Lifecycle Compaction Lane Log

## 2026-08-01 - reviewable explicit-expiry maintenance

- Scope: bounded L2 operator maintenance for objective expiry only; did not automate conflict resolution, duplicate selection, provenance repair, high-risk judgment, descendant revocation, retention pruning, or MCP mutation.
- Files touched: `zerker_memory/maintenance.py`, `zerker_memory/health.py`, `zerker_memory/store.py`, `zerker_memory/cli.py`, `zerker_memory/dashboard.py`, `tests/test_maintenance.py`, and factual public/control-room docs.
- Behavior changed: `zmem maintain preview` creates a content-free state-bound plan, `apply` performs one confirmed `active -> expired` transition, and `verify` proves the historical receipt while reporting current-state drift.
- Safety: preview stays read-only; apply rejects stale plans and targets, future expiry, altered receipts, and unverified receipt chains; the transition does not cascade or delete the row; replay is idempotent.
- Artifacts/receipts: `zerker.memory_maintenance_plan.v1`, `zerker.memory_maintenance_result.v1`, `zerker.memory_maintenance_verification.v1`, `EXPIRED`, and the existing `zerker.memory.mutation_receipt` contract with maintenance plan/action binding.
- Tests: focused maintenance suite passes `20/20`; the combined integration suite passes `824/824`; the full repository suite passes `1,314` tests with two expected optional skips; eval passes `11/11`; fresh-workspace and strict release smoke, wheel build/import, docs typecheck/build, compilation, and diff checks pass.
- Next safe slice: connect consolidation to live store candidates with source coverage and reversible summaries after this branch is reviewed and landed.

## 2026-07-05 - raw snapshot continuity receipt counts must be non-negative

- Scope: bounded L2 slice on raw continuity sidecar lifecycle receipt-provenance count validation only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now reject `zerker.snapshot_continuity.v1` sidecars when aggregate or per-session lifecycle receipt-provenance counts go negative for `verified_receipt_count`, `failed_receipt_count`, or `linked_treeship_artifact_count`.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_negative_lifecycle_receipt_provenance_counts_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_negative_session_lifecycle_receipt_provenance_counts_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_negative_lifecycle_receipt_provenance_counts_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_negative_session_lifecycle_receipt_provenance_counts_in_session_continuity_sidecar -q` passed (`Ran 4 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 711 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive validation only on existing raw `*.continuity.json` sidecars.
- Blockers: sidecar verification still proves only local structure plus snapshot hash/root coherence; there is still no signature or receipt-backed authenticity proof for the sidecar itself.
- Next safe slice: add one similarly bounded raw-sidecar coherence contract on another rollup scalar only if operators still need stricter local continuity honesty before authenticity work.

## 2026-06-22 - coordinator

- Scope: seeded lane for Working/Episodic/Semantic/Procedural memory, session lifecycle, checkpoints, snapshots, and context-boundary compaction.
- Files touched: lane log only.
- Behavior changed: none.
- Tests: not applicable.
- Artifacts/receipts: none.
- Blockers: worker must inventory existing session, handoff, restore, inject, and propose flows.
- Next safe slice: add a minimal checkpoint/session contract without widening setup UX.

## 2026-06-22 - procedural-vs-recall context contract

- Scope: bounded L2 slice on context-boundary packing only; did not add `start_session`, `checkpoint_session`, `snapshot_session`, or `end_session` commands.
- Files touched: `zerker_memory/store.py`, `zerker_memory/runner.py`, `tests/test_store.py`, `tests/test_runner.py`.
- Behavior changed: packing receipts now expose `memory_type_summary` for injected/withheld/budget-dropped ids by memory type, and runner context payloads now expose `memory_classes`, `memory_type_summary`, and `budget_dropped` so policy/procedural rules stay explicitly separate from episodic/semantic recall.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_packing_receipt_summarizes_instructional_recall_withheld_and_budget_dropped_types -q`, `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_separates_instructional_and_recall_memory_and_surfaces_budget_receipts -q`, `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q`, and `python3 -m zerker_memory eval`.
- Artifacts/receipts: `retrieval.packing.memory_type_summary`, context `memory_classes`, and context `budget_dropped`.
- Blockers: working memory and session lifecycle are still implicit; no receipt-visible checkpoint root or session command surface exists yet.
- Next safe slice: add one minimal checkpoint contract that writes a receipt-visible checkpoint root without widening snapshot/handoff UX.

## 2026-06-23 - session checkpoint root contract

- Scope: bounded L2 slice on store-level lifecycle checkpoints only; did not add `start_session`, `snapshot_session`, or `end_session` commands.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`.
- Behavior changed: `checkpoint_session` now persists `SESSION_CHECKPOINTED` events with session id, scope, snapshot hash/root summary, active-memory tree root, and memory-type-separated active ids/counts. `session_checkpoints` now reads those persisted checkpoint receipts back with receipt-visible prior/current Merkle roots.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_checkpoint_session_emits_receipt_visible_roots_and_memory_type_summary -q` and `python3 -m unittest tests.test_store.MemoryStoreTest.test_session_checkpoints_reads_back_persisted_checkpoint_events -q` passed. `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` failed on unrelated existing `tests.test_cli_onboarding.CliOnboardingTest.test_run_launch_proof_writes_transcript_and_artifacts` and `tests.test_store.MemoryStoreTest.test_revoke_persists_root_mutation_receipt_without_overwriting_original_write_provenance`. `python3 -m zerker_memory eval` passed.
- Artifacts/receipts: `zerker.session_checkpoint.v1`, `SESSION_CHECKPOINTED` event payloads, `checkpoint_merkle_root`, `snapshot.snapshot_hash`, `snapshot.snapshot_merkle_root`, `memory_tree.root`, and `memory_type_summary.active_ids_by_type`.
- Blockers: no first-class `start_session`, `snapshot_session`, or `end_session` command surface yet. Broad verification is currently blocked outside this slice by the dirty `zerker_memory/cli.py` finalize-script string mismatch and missing revoke mutation receipt behavior.
- Next safe slice: add `snapshot_session` or a thin read-only CLI surface for `session_checkpoints` after the unrelated red-suite failures are isolated.

## 2026-06-23 - session snapshot payload contract

- Scope: bounded L2 slice on store-level lifecycle snapshots only; did not add `start_session`, `checkpoint_session` CLI wrappers, or `end_session` commands.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`.
- Behavior changed: `snapshot_session` now persists `SESSION_SNAPSHOTTED` events plus durable `session_snapshot_payloads` rows keyed by `session_snapshot_id` and `snapshot_hash`. `session_snapshots` now reads those persisted snapshot receipts back with receipt-visible prior/current Merkle roots, active-memory tree summary, memory-type-separated active ids/counts, and the stored pre-event full snapshot payload.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_snapshot_session_persists_snapshot_payload_and_receipt_visible_roots tests.test_store.MemoryStoreTest.test_session_snapshots_reads_back_persisted_snapshot_events_and_payloads -q` passed. `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` failed only on unrelated existing `tests.test_cli_onboarding.CliOnboardingTest.test_run_launch_proof_writes_transcript_and_artifacts`. `python3 -m zerker_memory eval` passed.
- Artifacts/receipts: `zerker.session_snapshot.v1`, `SESSION_SNAPSHOTTED` event payloads, `session_snapshot_merkle_root`, `snapshot_hash`, and `session_snapshot_payloads.snapshot_json`.
- Blockers: no CLI surface yet for `snapshot_session`/`session_snapshots`, and no retention or export/restore policy exists yet for stored session snapshot payloads.
- Next safe slice: add a thin read-only CLI surface for `session_snapshots`, or add explicit retention metadata/soft-delete rules for stored session snapshot payloads without widening `start_session` or `end_session`.

## 2026-06-23 - session snapshot payload retention tombstones

- Scope: bounded L2 slice on retention/soft-delete for stored session snapshot payloads only; did not add `start_session`, `checkpoint_session`, or `end_session` commands.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`.
- Behavior changed: `soft_delete_session_snapshot_payload(...)` now marks durable `session_snapshot_payloads` rows with retention metadata, appends receipt-visible `SESSION_SNAPSHOT_PAYLOAD_SOFT_DELETED` events, and makes `session_snapshots()` return `payload_status` plus a retention tombstone instead of the full snapshot payload once a snapshot has been soft-deleted.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_soft_delete_session_snapshot_payload_preserves_receipt_visible_summary tests.test_store.MemoryStoreTest.test_session_snapshots_reports_soft_deleted_payload_without_returning_snapshot_json tests.test_store.MemoryStoreTest.test_snapshot_session_persists_snapshot_payload_and_receipt_visible_roots tests.test_store.MemoryStoreTest.test_session_snapshots_reads_back_persisted_snapshot_events_and_payloads -q` passed; `python3 -m unittest tests.test_store tests.test_runner` passed (`Ran 265 tests`); `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner` failed only on unrelated existing `tests.test_cli_onboarding.CliOnboardingTest.test_run_launch_proof_writes_transcript_and_artifacts`; `python3 -m zerker_memory eval` passed.
- Artifacts/receipts: `zerker.session_snapshot_retention.v1`, `SESSION_SNAPSHOT_PAYLOAD_SOFT_DELETED`, `payload_status`, `retention.deleted_event_hash`, and `retention.soft_delete_merkle_root`.
- Blockers: no read-only CLI/report surface summarizes snapshot retention state yet, and no automatic retention/pruning policy exists beyond the new explicit soft-delete API.
- Next safe slice: add a thin read-only CLI summary for `session_snapshots` retention state, or add one bounded retention-pruning policy without widening `start_session` or `end_session`.

## 2026-06-24 - session lifecycle read-only CLI summary

- Scope: bounded L2 slice on read-only lifecycle/session inspection only; did not add `start_session`, `checkpoint_session`, `snapshot_session`, or `end_session` write commands.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `zmem session checkpoints` and `zmem session snapshots` now expose persisted lifecycle state through JSON or `--summary-only` terminal summaries. The summaries surface checkpoint roots, snapshot roots/hashes, active memory-type counts, snapshot payload availability, and soft-delete retention tombstones without widening any lifecycle mutation path.
- Tests: focused CLI/store/runner lifecycle cluster passed (`python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_snapshot_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_session_checkpoints_summary_surfaces_checkpoint_root_and_memory_type_counts tests.test_cli_onboarding.CliOnboardingTest.test_session_snapshots_summary_surfaces_soft_deleted_retention_state tests.test_store.MemoryStoreTest.test_checkpoint_session_emits_receipt_visible_roots_and_memory_type_summary tests.test_store.MemoryStoreTest.test_session_checkpoints_reads_back_persisted_checkpoint_events tests.test_store.MemoryStoreTest.test_snapshot_session_persists_snapshot_payload_and_receipt_visible_roots tests.test_store.MemoryStoreTest.test_session_snapshots_reports_soft_deleted_payload_without_returning_snapshot_json tests.test_runner.RunnerTest.test_build_context_separates_instructional_and_recall_memory_and_surfaces_budget_receipts -q`); broad `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 384 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: read-only summaries for `checkpoint_merkle_root`, `session_snapshot_merkle_root`, `snapshot_hash`, `payload_status`, and `retention.soft_delete_merkle_root`.
- Blockers: there is still no CLI write surface for `checkpoint_session` / `snapshot_session`, and no automatic retention-pruning policy exists beyond explicit soft-delete.
- Next safe slice: add one thin `zmem session checkpoint` write wrapper around the existing store contract, or add one bounded retention-pruning policy without widening `start_session` or `end_session`.

## 2026-06-24 - session checkpoint CLI write wrapper

- Scope: bounded L2 slice on checkpoint writes only; did not add `snapshot_session`, `start_session`, or `end_session` commands.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `zmem session checkpoint` now writes a persisted checkpoint through the existing store contract. Default output returns the full checkpoint JSON payload; `--summary-only` surfaces checkpoint id, session id, actor id, checkpoint root, snapshot hash/root, active-memory count, and memory-type counts.
- Tests: focused CLI checkpoint coverage passed (`python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_checkpoint_write_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_session_checkpoint_summary_surfaces_written_checkpoint_root_and_counts tests.test_cli_onboarding.CliOnboardingTest.test_session_checkpoints_summary_surfaces_checkpoint_root_and_memory_type_counts tests.test_cli_onboarding.CliOnboardingTest.test_session_snapshots_summary_surfaces_soft_deleted_retention_state -q`); broad `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 393 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: write-path CLI access to persisted `zerker.session_checkpoint.v1` payloads, including `checkpoint_merkle_root`, `snapshot.snapshot_hash`, `snapshot.snapshot_merkle_root`, and `memory_type_summary.active_counts_by_type`.
- Blockers: there is still no CLI write surface for `snapshot_session`, no `start_session` / `end_session` surface, and no automatic retention-pruning policy beyond explicit soft-delete.
- Next safe slice: add one thin `zmem session snapshot` write wrapper around the existing store contract, or add one bounded `start_session` contract without widening retention or restore UX.

## 2026-06-24 - session snapshot CLI write wrapper

- Scope: bounded L2 slice on snapshot writes only; did not add `start_session` or `end_session` commands and did not change retention behavior.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `zmem session snapshot` now writes a persisted session snapshot through the existing store contract. Default output returns the full session snapshot JSON payload; `--summary-only` surfaces session snapshot id, session id, actor id, payload status, session snapshot root, snapshot hash, active-memory count, and memory-type counts.
- Tests: focused snapshot CLI coverage passed (`python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_snapshot_write_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_session_snapshot_summary_surfaces_written_snapshot_root_and_counts tests.test_cli_onboarding.CliOnboardingTest.test_session_checkpoint_summary_surfaces_written_checkpoint_root_and_counts tests.test_cli_onboarding.CliOnboardingTest.test_session_snapshots_summary_surfaces_soft_deleted_retention_state -q`); broad `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 401 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: write-path CLI access to persisted `zerker.session_snapshot.v1` payloads, including `session_snapshot_merkle_root`, `snapshot_hash`, `payload_status`, and `memory_type_summary.active_counts_by_type`.
- Blockers: there is still no `start_session` / `end_session` command surface, and no automatic retention-pruning policy beyond explicit soft-delete.
- Next safe slice: add one bounded `start_session` contract, or add one thin soft-delete write wrapper for snapshot retention without widening restore UX.

## 2026-06-24 - session snapshot soft-delete CLI write wrapper

- Scope: bounded L2 slice on snapshot retention writes only; did not add `start_session` or `end_session` commands and did not change store retention semantics.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `zmem session delete-snapshot` now exposes the existing `soft_delete_session_snapshot_payload(...)` store contract as a thin CLI write surface. Default output returns the full soft-deleted session snapshot JSON payload; `--summary-only` surfaces session snapshot id, session id, deleted-by/at/reason, payload status, snapshot hash, original session snapshot root, soft-delete root, and memory-type counts.
- Tests: focused retention CLI coverage passed (`python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_delete_snapshot_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_session_snapshot_soft_delete_summary_surfaces_retention_roots -q`, `python3 -m unittest tests.test_store.MemoryStoreTest.test_soft_delete_session_snapshot_payload_preserves_receipt_visible_summary tests.test_store.MemoryStoreTest.test_session_snapshots_reports_soft_deleted_payload_without_returning_snapshot_json -q`, `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_separates_instructional_and_recall_memory_and_surfaces_budget_receipts -q`); broad `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 407 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: write-path CLI access to persisted `zerker.session_snapshot_retention.v1` tombstones, including `payload_status`, `retention.deleted_reason`, and `retention.soft_delete_merkle_root`.
- Blockers: there is still no `start_session` / `end_session` command surface, and no automatic retention-pruning policy beyond explicit soft-delete.
- Next safe slice: add one bounded `start_session` contract so lifecycle continuity has an explicit begin-state before adding any pruning automation.

## 2026-06-25 - session start CLI/store contract

- Scope: bounded L2 slice on explicit session begin-state only; did not add `end_session` or any new retention/report surface.
- Files touched: `zerker_memory/store.py`, `zerker_memory/cli.py`, `tests/test_store.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `start_session(...)` now persists `SESSION_STARTED` lifecycle events with prior/new Merkle roots, snapshot hash/root summary, active-memory tree summary, memory-type-separated active ids/counts, and an optional `context_budget_tokens` hint. `zmem session start` now exposes that contract through JSON or `--summary-only`.
- Tests: focused `python3 -m unittest tests.test_store.MemoryStoreTest.test_start_session_emits_receipt_visible_roots_and_budget_hint tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_start_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_session_start_summary_surfaces_written_start_root_and_budget_hint -q` passed; broad `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 422 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: `zerker.session_start.v1`, `SESSION_STARTED`, `session_start_merkle_root`, `token_budget_hint.context_budget_tokens`, and lifecycle receipt `mutation=start_session`.
- Blockers: there is still no `end_session` command surface, and there is still no read-only `session starts` report for later inspection.
- Next safe slice: add one bounded `end_session` contract, or add one thin read-only `session starts` report without widening restore or retention behavior.

## 2026-06-25 - session end CLI/store contract

- Scope: bounded L2 slice on explicit session closeout only; did not add any new retention/report surface or change checkpoint/snapshot behavior.
- Files touched: `zerker_memory/store.py`, `zerker_memory/cli.py`, `tests/test_store.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `end_session(...)` now persists `SESSION_ENDED` lifecycle events with prior/new Merkle roots, snapshot hash/root summary, active-memory tree summary, and memory-type-separated active ids/counts. `zmem session end` now exposes that persisted lifecycle write path through JSON or `--summary-only`.
- Tests: focused `python3 -m unittest tests.test_store.MemoryStoreTest.test_end_session_emits_receipt_visible_roots_and_memory_type_summary tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_end_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_session_end_summary_surfaces_written_end_root_and_counts -q` passed; broad `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 431 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: `zerker.session_end.v1`, `SESSION_ENDED`, `session_end_merkle_root`, and lifecycle receipt `mutation=end_session`.
- Blockers: there is still no read-only `session starts` or `session ends` report for later inspection, and there is still no automatic retention-pruning policy beyond explicit snapshot soft-delete.
- Next safe slice: add one thin read-only `session starts` / `session ends` report without widening restore or retention behavior.

## 2026-06-25 - session start/end read-only reports

- Scope: bounded L2 slice on read-only session lifecycle inspection only; did not change any lifecycle write path, retention semantics, or restore behavior.
- Files touched: `zerker_memory/store.py`, `zerker_memory/cli.py`, `tests/test_store.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `session_starts(...)` and `session_ends(...)` now read back persisted `SESSION_STARTED` and `SESSION_ENDED` events, and `zmem session starts` / `zmem session ends` now expose compact JSON or `--summary-only` inspection for later session lifecycle review. Start summaries include context-budget hint visibility; end summaries include the same root and memory-type counts as the write result.
- Tests: focused `python3 -m unittest tests.test_store.MemoryStoreTest.test_session_starts_reads_back_persisted_start_events tests.test_store.MemoryStoreTest.test_session_ends_reads_back_persisted_end_events tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_starts_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_ends_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_session_starts_summary_surfaces_latest_start_root_and_budget_hints tests.test_cli_onboarding.CliOnboardingTest.test_session_ends_summary_surfaces_latest_end_root_and_counts -q` passed; lifecycle cluster `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_start_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_starts_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_session_start_summary_surfaces_written_start_root_and_budget_hint tests.test_cli_onboarding.CliOnboardingTest.test_session_starts_summary_surfaces_latest_start_root_and_budget_hints tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_end_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_ends_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_session_end_summary_surfaces_written_end_root_and_counts tests.test_cli_onboarding.CliOnboardingTest.test_session_ends_summary_surfaces_latest_end_root_and_counts tests.test_store.MemoryStoreTest.test_start_session_emits_receipt_visible_roots_and_budget_hint tests.test_store.MemoryStoreTest.test_session_starts_reads_back_persisted_start_events tests.test_store.MemoryStoreTest.test_end_session_emits_receipt_visible_roots_and_memory_type_summary tests.test_store.MemoryStoreTest.test_session_ends_reads_back_persisted_end_events tests.test_store.MemoryStoreTest.test_checkpoint_session_emits_receipt_visible_roots_and_memory_type_summary tests.test_store.MemoryStoreTest.test_session_checkpoints_reads_back_persisted_checkpoint_events tests.test_store.MemoryStoreTest.test_snapshot_session_persists_snapshot_payload_and_receipt_visible_roots tests.test_store.MemoryStoreTest.test_session_snapshots_reads_back_persisted_snapshot_events_and_payloads tests.test_runner.RunnerTest.test_build_context_separates_instructional_and_recall_memory_and_surfaces_budget_receipts -q` passed (`Ran 17 tests`); broad `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 438 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: read-only summaries for `session_start_merkle_root`, `session_end_merkle_root`, `token_budget_hint.context_budget_tokens`, and persisted lifecycle receipt ids/hashes returned from `session_starts()` / `session_ends()`.
- Blockers: there is still no automatic retention-pruning policy beyond explicit snapshot soft-delete, and lifecycle inspection is still split across separate start/end/checkpoint/snapshot report surfaces.
- Next safe slice: add one bounded retention-pruning policy or summary without widening restore UX or changing start/end receipt semantics.

## 2026-06-25 - session snapshot prune keep-latest contract

- Scope: bounded L2 slice on snapshot retention pruning only; did not change session start/end/checkpoint/snapshot write semantics, restore behavior, or runner context packing.
- Files touched: `zerker_memory/store.py`, `zerker_memory/cli.py`, `tests/test_store.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `prune_session_snapshot_payloads(...)` now soft-deletes older available snapshot payloads for one session while keeping the latest `N`, reusing the existing `soft_delete_session_snapshot_payload(...)` receipt/tombstone path instead of introducing a second retention mechanism. `zmem session prune-snapshots` now exposes the same bounded contract through JSON or `--summary-only`, including kept/pruned ids plus before/after available vs soft-deleted counts.
- Tests: focused retention coverage passed (`python3 -m unittest tests.test_store.MemoryStoreTest.test_soft_delete_session_snapshot_payload_preserves_receipt_visible_summary tests.test_store.MemoryStoreTest.test_session_snapshots_reports_soft_deleted_payload_without_returning_snapshot_json tests.test_store.MemoryStoreTest.test_prune_session_snapshot_payloads_soft_deletes_older_available_snapshots tests.test_store.MemoryStoreTest.test_prune_session_snapshot_payloads_skips_already_soft_deleted_entries tests.test_cli_onboarding.CliOnboardingTest.test_session_snapshots_summary_surfaces_soft_deleted_retention_state tests.test_cli_onboarding.CliOnboardingTest.test_session_snapshot_soft_delete_summary_surfaces_retention_roots tests.test_cli_onboarding.CliOnboardingTest.test_session_snapshot_prune_summary_surfaces_kept_and_pruned_counts tests.test_runner.RunnerTest.test_build_context_separates_instructional_and_recall_memory_and_surfaces_budget_receipts -q`) and `python3 -m zerker_memory eval` passed (`11/11`); required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` failed only on unrelated temporal assertions in `tests.test_store.MemoryStoreTest.test_query_at_unlearned_history_surfaces_forgotten_resolution_reason` and `tests.test_store.MemoryStoreTest.test_query_at_unlearned_history_surfaces_deprecated_resolution_reason_for_rejected_memory`.
- Artifacts/receipts: bounded reuse of `zerker.session_snapshot_retention.v1` via per-snapshot soft-delete receipts, plus read-only `zerker.session_snapshot_prune.v1` / `zerker.session_snapshot_prune_result.v1` summaries.
- Blockers: full lifecycle verification is currently blocked by the unrelated L1 temporal red-suite failures above, and lifecycle inspection/retention is still split across separate start/end/checkpoint/snapshot/prune surfaces rather than one aggregate timeline.
- Next safe slice: add one read-only aggregated lifecycle timeline or retention-status summary on top of the existing session surfaces without changing receipt semantics or restore behavior.

## 2026-06-26 - session lifecycle aggregate timeline

- Scope: bounded L2 slice on read-only lifecycle aggregation only; did not change lifecycle write semantics, retention behavior, restore behavior, or runner context packing.
- Files touched: `zerker_memory/store.py`, `zerker_memory/cli.py`, `tests/test_store.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `session_lifecycle_timeline(...)` now merges persisted session starts, checkpoints, snapshots, snapshot-payload soft-delete tombstones, and session ends into one event-ordered read-only view. `zmem session timeline` now exposes that aggregate lifecycle surface through JSON or `--summary-only`, including latest timeline root, context-budget hints, snapshot payload availability, and retention tombstone details in one report. The timeline query filters before applying the returned-entry limit so busy stores do not hide older matching session events.
- Tests: focused lifecycle aggregation and limit/filter regressions passed (`Ran 9 tests`); required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 456 tests`); `python3 -m zerker_memory eval` passed (`11/11`); `git diff --check -- zerker_memory/store.py zerker_memory/cli.py tests/test_store.py tests/test_cli_onboarding.py` passed.
- Artifacts/receipts: read-only `zerker.session_timeline_report.v1` plus `zerker.session_lifecycle_timeline_entry.v1` entries with event-kind counts, latest timeline root, start budget hints, snapshot payload states, and soft-delete tombstone details.
- Blockers: aggregate lifecycle inspection still remains session-by-session and terminal/JSON-first; there is not yet a compact retention rollup for latest-retained snapshot state across many sessions.
- Next safe slice: add one bounded retention-status rollup on top of the new aggregate timeline so operators can see latest available vs soft-deleted snapshot state per session without widening lifecycle write semantics.

## 2026-06-26 - session snapshot retention rollup

- Scope: bounded L2 slice on read-only cross-session snapshot retention status only; did not change lifecycle write semantics, retention tombstone semantics, restore behavior, or runner context packing.
- Files touched: `zerker_memory/store.py`, `zerker_memory/cli.py`, `tests/test_store.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `session_snapshot_retention_rollup(...)` now groups persisted session snapshots per session/scope with latest snapshot status, available vs soft-deleted counts, latest available snapshot roots, and latest soft-delete reasons/roots. `zmem session retention` now exposes the same rollup through JSON or `--summary-only`.
- Tests: focused new store rollup tests passed (`Ran 2 tests`); focused new CLI retention tests passed (`Ran 2 tests`); focused lifecycle cluster passed (`Ran 7 tests`); `git diff --check -- zerker_memory/store.py zerker_memory/cli.py tests/test_store.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 465 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: read-only `zerker.session_snapshot_retention_rollup_report.v1` plus per-session `zerker.session_snapshot_retention_rollup_entry.v1` summaries with `retention_state`, latest snapshot status root, latest available snapshot id/root, and latest soft-delete reason/root.
- Blockers: the new rollup is snapshot-state-only; it does not yet merge starts, checkpoints, or ends into one multi-session lifecycle surface, and it does not yet mirror lifecycle receipt-verification summaries.
- Next safe slice: add one bounded multi-session lifecycle rollup that combines start/end/checkpoint/snapshot counts per session, or extend lifecycle receipt summaries to the existing timeline, without widening any write path.

## 2026-06-26 - session lifecycle rollup

- Scope: bounded L2 slice on read-only cross-session lifecycle status only; did not change lifecycle write semantics, retention semantics, restore behavior, or runner context packing.
- Files touched: `zerker_memory/store.py`, `zerker_memory/cli.py`, `tests/test_store.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `session_lifecycle_rollup(...)` now groups persisted starts, checkpoints, snapshots, snapshot soft-deletes, and ends per session/scope with latest lifecycle kind/root, per-kind event counts, latest snapshot payload status, latest end root, and latest start `context_budget_tokens` hint. `zmem session rollup` now exposes the same grouped view through JSON or `--summary-only`.
- Tests: focused lifecycle-rollup contract coverage passed (`python3 -m unittest tests.test_store.MemoryStoreTest.test_session_lifecycle_rollup_groups_sessions_and_latest_event_state tests.test_store.MemoryStoreTest.test_session_lifecycle_rollup_filters_before_session_limit tests.test_cli_onboarding.CliOnboardingTest.test_build_parser_parses_session_rollup_summary_only tests.test_cli_onboarding.CliOnboardingTest.test_session_lifecycle_rollup_summary_groups_sessions_and_latest_status -q`, `Ran 4 tests`); `git diff --check -- zerker_memory/store.py zerker_memory/cli.py tests/test_store.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 474 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: read-only `zerker.session_lifecycle_rollup_report.v1` plus per-session `zerker.session_lifecycle_rollup_entry.v1` summaries with `latest_event_kind`, `latest_status_root`, event-kind counts, payload-status counts, and `latest_start_token_budget_hint`.
- Blockers: the new rollup still does not carry lifecycle receipt-verification summaries across sessions, and it remains terminal/JSON-first rather than being mirrored into a dashboard or handoff artifact.
- Next safe slice: add one bounded receipt-summary extension to the new multi-session rollup, or mirror the same grouped lifecycle state into one existing dashboard/handoff surface without widening any write path.

## 2026-06-26 - session lifecycle rollup receipt summaries

- Scope: bounded L2 slice on read-only lifecycle proof visibility only; did not change lifecycle write semantics, retention semantics, restore behavior, or runner context packing.
- Files touched: `zerker_memory/store.py`, `zerker_memory/cli.py`, `tests/test_store.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `session_lifecycle_rollup(...)` now records per-session `verified_receipt_count`, `failed_receipt_count`, `linked_treeship_artifact_count`, and `latest_receipt_summary` derived from local `verify_lifecycle_receipt(...)` checks over the already-persisted lifecycle receipts. `zmem session rollup --summary-only` now surfaces aggregate receipt provenance counts plus one per-session receipt-status line alongside lifecycle counts, roots, payload retention state, and latest start budget hints.
- Tests: focused rollup receipt-summary coverage passed (`python3 -m unittest tests.test_store.MemoryStoreTest.test_session_lifecycle_rollup_groups_sessions_and_latest_event_state -q`, `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_session_lifecycle_rollup_summary_groups_sessions_and_latest_status -q`); `git diff --check -- zerker_memory/store.py zerker_memory/cli.py tests/test_store.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 479 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: additive read-only receipt provenance counts and `latest_receipt_summary` on `zerker.session_lifecycle_rollup_entry.v1`, plus aggregate receipt provenance counts on `zerker.session_lifecycle_rollup_report.v1`.
- Blockers: the grouped lifecycle surface is still terminal/JSON-first and is not yet mirrored into a dashboard or handoff artifact; lifecycle receipts also still carry no non-null Treeship artifact ids in the ordinary local path.
- Next safe slice: mirror the same grouped lifecycle-plus-receipt state into one existing dashboard or handoff surface without widening any write path.

## 2026-06-27 - handoff lifecycle continuity mirror

- Scope: bounded L2 slice on handoff continuity visibility only; did not change lifecycle write semantics, retention semantics, restore behavior, runner context packing, or snapshot/bundle export formats.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `create_handoff_package(...)` now embeds the existing read-only `session_lifecycle_rollup(...)` report into the handoff result and `zerker.handoff_manifest.v1`, and the generated handoff README now includes a `Session Lifecycle Continuity` section with the same grouped lifecycle-plus-receipt summary. `render_handoff_summary(...)` now surfaces lifecycle session counts, per-kind event counts, and aggregate receipt provenance so context-boundary transfers carry start/checkpoint/snapshot/soft-delete/end state instead of only snapshot/bundle paths.
- Tests: focused handoff coverage passed (`python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_create_handoff_package_exports_snapshot_and_latest_bundle tests.test_cli_onboarding.CliOnboardingTest.test_create_handoff_package_handles_snapshot_only_state tests.test_cli_onboarding.CliOnboardingTest.test_create_handoff_package_includes_session_lifecycle_rollup_for_context_boundary_review -q`, `Ran 3 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 489 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: additive `session_lifecycle_rollup` and `session_lifecycle_rollup_summary` on `zerker.handoff.v1` / `zerker.handoff_manifest.v1`, plus the README `Session Lifecycle Continuity` handoff section.
- Blockers: the lifecycle continuity mirror now exists only on generated handoff artifacts; receive-side restore output and launch-proof/operator packet surfaces still do not repeat the same grouped lifecycle summary.
- Next safe slice: mirror the same grouped lifecycle-plus-receipt state into one bounded restore-side or launch-proof handoff summary without widening lifecycle writes or retention behavior.

## 2026-06-27 - restore lifecycle continuity mirror

- Scope: bounded L2 slice on receive-side restore visibility only; did not change lifecycle write semantics, retention semantics, handoff export behavior, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `restore_handoff_package(...)` now carries any manifest-backed `session_lifecycle_rollup` and `session_lifecycle_rollup_summary` through `zerker.restore_handoff.v1`, and `render_restore_summary(...)` now surfaces lifecycle session counts, per-kind event counts, receipt provenance, and the latest restored session payload/budget/retention state.
- Tests: `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_restore_handoff_package_verifies_and_restores_snapshot tests.test_cli_onboarding.CliOnboardingTest.test_restore_handoff_package_surfaces_session_lifecycle_rollup_for_receive_side_review tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_verifies_and_restores_snapshot tests.test_cli_onboarding.CliOnboardingTest.test_create_handoff_package_includes_session_lifecycle_rollup_for_context_boundary_review -q` passed (`Ran 4 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 492 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: additive `session_lifecycle_rollup` and `session_lifecycle_rollup_summary` on `zerker.restore_handoff.v1`, plus restore terminal summaries for lifecycle event counts, receipt provenance, latest payload state, budget hint, and soft-delete reason.
- Blockers: the grouped lifecycle continuity summary still is not mirrored into launch-proof/operator packet receive-side surfaces, and direct `restore <snapshot>` still has no equivalent lifecycle continuity source because raw snapshot files do not include handoff manifests.
- Next safe slice: mirror the same grouped lifecycle-plus-receipt state into one bounded launch-proof/operator-packet receive-side summary without widening any write path, or add one explicitly separate snapshot-side continuity export only if operators need parity outside handoff packages.

## 2026-06-27 - handoff/restore retention continuity mirror

- Scope: bounded L2 slice on retention-state continuity in handoff and restore artifacts only; did not change lifecycle write semantics, retention tombstone semantics, snapshot/bundle export formats, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `create_handoff_package(...)` now carries `session_retention_rollup` and a rendered retention summary alongside the existing lifecycle rollup in `zerker.handoff.v1`, `zerker.handoff_manifest.v1`, and the generated README. `restore_handoff_package(...)` and `render_restore_summary(...)` now surface the same retention counts, payload-status totals, and latest soft-delete reason for receive-side continuity review.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_create_handoff_package_includes_session_lifecycle_rollup_for_context_boundary_review tests.test_cli_onboarding.CliOnboardingTest.test_restore_handoff_package_surfaces_session_lifecycle_rollup_for_receive_side_review -q` passed (`Ran 2 tests`); required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 499 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: additive `session_retention_rollup` and `session_retention_rollup_summary` on `zerker.handoff.v1`, `zerker.handoff_manifest.v1`, and `zerker.restore_handoff.v1`, plus a new README `Session Snapshot Retention` section and restore terminal retention summary lines.
- Blockers: launch-proof/operator-packet receive-side surfaces still do not repeat the same retention summary, and raw snapshot restore still has no lifecycle/retention continuity source outside handoff manifests.
- Next safe slice: mirror the same grouped retention summary into one bounded launch-proof/operator-packet receive-side handoff surface without widening lifecycle writes, retention semantics, or raw snapshot format.

## 2026-06-27 - launch-proof/operator-packet continuity mirror

- Scope: bounded L2 slice on launch-proof/operator-packet receive-side continuity visibility only; did not change lifecycle write semantics, retention semantics, restore semantics, runner context packing, or snapshot/bundle formats.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: launch-proof manifests now pass through any handoff-backed `session_lifecycle_rollup` / `session_retention_rollup` plus their rendered summaries, and the generated `public-verify-summary.md` plus `zmem verify-operator-packet --summary-only` now repeat lifecycle session/event counts, receipt provenance, latest context-budget hint, retention totals, and latest soft-delete reason when that continuity exists.
- Tests: focused lifecycle continuity cluster passed (`python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_create_handoff_package_includes_session_lifecycle_rollup_for_context_boundary_review tests.test_cli_onboarding.CliOnboardingTest.test_restore_handoff_package_surfaces_session_lifecycle_rollup_for_receive_side_review tests.test_cli_onboarding.CliOnboardingTest.test_run_launch_proof_mirrors_handoff_session_continuity_into_operator_packet_surfaces tests.test_store.MemoryStoreTest.test_session_lifecycle_rollup_groups_sessions_and_latest_event_state tests.test_store.MemoryStoreTest.test_session_snapshot_retention_rollup_groups_sessions_and_latest_retention_state tests.test_runner.RunnerTest.test_build_context_separates_instructional_and_recall_memory_and_surfaces_budget_receipts -q`, `Ran 6 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 505 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: additive `session_lifecycle_rollup`, `session_lifecycle_rollup_summary`, `session_retention_rollup`, and `session_retention_rollup_summary` on `zerker.launch_proof_manifest.v1`, `zerker.operator_packet_verify.v1`, and the generated `public-verify-summary.md`.
- Blockers: raw `restore <snapshot>` and standalone snapshot/return-packet paths still have no lifecycle continuity source outside handoff-backed manifests.
- Next safe slice: mirror the same grouped lifecycle-plus-retention continuity into one bounded `verify-return-packet` / return-packet summary surface, or keep raw snapshot restore explicitly continuity-free without widening any write path.

## 2026-06-28 - return-packet continuity mirror

- Scope: bounded L2 slice on return-packet receive-side continuity visibility only; did not change lifecycle write semantics, retention semantics, restore semantics, runner context packing, or snapshot/bundle formats.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `verify_return_packet_archive(...)` now carries any handoff-backed `session_lifecycle_rollup` / `session_retention_rollup` state through `zerker.return_packet_verify.v1`, and `render_return_packet_summary(...)` now repeats lifecycle session/event counts, receipt provenance, latest context-budget hint, retention totals, and the latest soft-delete reason when that continuity exists in the archived launch-proof manifest.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_verify_return_packet_archive_mirrors_handoff_session_continuity tests.test_cli_onboarding.CliOnboardingTest.test_verify_return_packet_archive_reports_ready_packet -q` passed (`Ran 2 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 515 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: additive `session_lifecycle_rollup`, `session_lifecycle_rollup_summary`, `session_retention_rollup`, and `session_retention_rollup_summary` on `zerker.return_packet_verify.v1` and `zmem verify-return-packet --summary-only`.
- Blockers: standalone raw snapshot restore is still intentionally continuity-free outside handoff-backed manifests, so raw snapshot receives still cannot surface lifecycle/retention history.
- Next safe slice: either mirror the same grouped lifecycle-plus-retention continuity into one bounded raw-snapshot-side export/receive artifact, or explicitly document that standalone snapshot restore remains continuity-free outside handoff packages.

## 2026-06-28 - raw snapshot continuity-free restore contract

- Scope: bounded L2 slice on raw `restore <snapshot>` receive-side wording only; did not change lifecycle write semantics, retention semantics, restore mechanics, runner context packing, or snapshot file formats.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `render_restore_summary(...)` now states that standalone raw snapshot restores carry no lifecycle or retention continuity, while handoff-backed restore summaries keep the richer `session_lifecycle_rollup` / `session_retention_rollup` view without falling back to the continuity-free note.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_restore_handoff_package_surfaces_session_lifecycle_rollup_for_receive_side_review tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_verifies_and_restores_snapshot -q` passed (`Ran 2 tests`); required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 519 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: restore terminal summary now explicitly distinguishes raw snapshot restore from handoff-manifest continuity; no new receipt or snapshot artifact schema landed.
- Blockers: raw snapshot restore is still continuity-free by design, so operators still cannot recover lifecycle/retention history from a bare snapshot alone.
- Next safe slice: if raw snapshot parity is worth the extra surface area, add one explicitly separate snapshot-side continuity artifact instead of overloading the snapshot restore path.

## 2026-07-05 - raw snapshot continuity latest-event-created-at coherence

- Scope: bounded L2 slice on raw snapshot continuity sidecar timestamp validation only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or sidecar generation.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now reject `zerker.snapshot_continuity.v1` sidecars whose rollup-level `latest_event_created_at` disagrees with the event-specific latest timestamp implied by `latest_event_kind`.
- Tests: pre-fix `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_mismatched_latest_event_created_at_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_mismatched_latest_event_created_at_in_session_continuity_sidecar -q` failed (`10` subtest failures); post-fix focused pair passed; adjacent eight-test continuity cluster passed; `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 695 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new artifacts or receipt schema; additive validation only on existing raw `*.continuity.json` sidecars.
- Blockers: sidecar authenticity is still trust-on-local-file presence only; there is still no signature or receipt-backed proof for the sidecar itself.
- Next safe slice: add one similarly bounded raw-sidecar coherence contract on another rollup headline field only if operators still need stricter local continuity honesty before authenticity work.

## 2026-06-28 - raw snapshot continuity sidecar parity

- Scope: bounded L2 slice on standalone raw snapshot continuity only; did not change lifecycle write semantics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `zmem snapshot --out ...` now writes an adjacent `*.continuity.json` sidecar keyed to the exported snapshot hash/root. `zmem snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now load that sidecar when present and surface lifecycle session/event counts, receipt provenance, latest start budget hint, retention totals, and latest soft-delete reason without treating the bare snapshot itself as continuity-bearing.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_export_writes_adjacent_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_surfaces_adjacent_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_surfaces_adjacent_session_continuity_sidecar -q` passed (`Ran 3 tests`); focused lifecycle parity cluster passed (`Ran 6 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 530 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: additive `zerker.snapshot_continuity.v1` sidecar artifacts beside raw snapshot exports, plus raw snapshot verify/restore summaries that now distinguish continuity sidecar presence from continuity-free bare snapshots.
- Blockers: the new sidecar is optional and currently trust-on-local-file presence only; there is not yet a dedicated tamper/mismatch contract test proving raw restore/verify wording stays honest when a copied or stale sidecar does not match the snapshot.

## 2026-07-01 - raw snapshot payload-status count coherence validation

- Scope: bounded L2 slice on raw snapshot continuity sidecar per-session payload-status coherence only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose per-session `latest_payload_status` disagrees with that same session row's concrete payload counts. `latest_payload_status=available` now requires `available_payload_count > 0`, and `latest_payload_status=soft_deleted` now requires `soft_deleted_payload_count > 0`. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit count-coherence wording instead of accepting a tampered latest-status row because the rollup totals still reconcile.
- Tests: pre-fix focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_incoherent_session_payload_status_counts_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_incoherent_session_payload_status_counts_in_session_continuity_sidecar -q` failed (`2` failures); post-fix the same focused contract passed (`Ran 2 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 598 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive per-session latest-status/count coherence validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still proves only local structure plus snapshot hash/root coherence; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded per-session `retention_state` coherence contract versus the same session row's available/soft-deleted counts, or stop and leave deeper authenticity work explicitly deferred.
- Next safe slice: add one bounded invalid-sidecar summary contract so raw snapshot verify/restore can explain hash/root mismatches without widening snapshot restore mechanics or manifest schemas.

## 2026-07-01 - raw snapshot retention-state count coherence validation

- Scope: bounded L2 slice on raw snapshot continuity sidecar per-session `retention_state` coherence only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose per-session `retention_state` disagrees with that same session row's `available_payload_count` and `soft_deleted_payload_count`. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit expected-state wording instead of accepting a tampered retention-state row because both the enum and the aggregate totals still reconcile.
- Tests: pre-fix focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_incoherent_session_retention_state_counts_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_incoherent_session_retention_state_counts_in_session_continuity_sidecar -q` failed (`2` failures); post-fix focused four-test cluster including the existing unknown-state contracts passed (`Ran 4 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 600 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive per-session `retention_state`/count coherence validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still proves only local structure plus snapshot hash/root coherence; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded impossible zero-payload-count session-row contract for raw continuity sidecars, or stop here and explicitly defer deeper sidecar-authenticity work.

## 2026-06-28 - raw snapshot invalid-sidecar summary contract

- Scope: bounded L2 slice on invalid raw snapshot continuity sidecar summaries only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `load_snapshot_continuity_sidecar(...)` now reports expected-vs-found values for raw snapshot continuity hash/root mismatches, and raw `snapshot verify <snapshot> --summary-only` plus raw `restore <snapshot> --summary-only` now keep that mismatch explicit instead of falling back to the continuity-free standalone-restore note when a copied or stale sidecar is present.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_mismatched_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_mismatched_session_continuity_sidecar -q` passed (`Ran 2 tests`); focused sidecar lifecycle cluster passed (`Ran 5 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 536 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; raw snapshot verify/restore summaries now carry explicit invalid-sidecar mismatch wording for `zerker.snapshot_continuity.v1` sidecars.
- Blockers: copied or stale raw snapshot sidecars are still treated as local-file mismatches only; no signature or receipt-backed authenticity check exists for the sidecar itself beyond snapshot hash/root matching.
- Next safe slice: add one bounded invalid-sidecar contract for malformed or schema-bad continuity files if operators need the same explicit summary wording beyond hash/root mismatch cases.

## 2026-06-28 - raw snapshot malformed-sidecar summary contract

- Scope: bounded L2 slice on malformed raw snapshot continuity sidecar summaries only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `load_snapshot_continuity_sidecar(...)` now rejects `zerker.snapshot_continuity.v1` sidecars that are missing `session_lifecycle_rollup` / `session_retention_rollup` or provide those rollups with the wrong type, and raw `snapshot verify <snapshot> --summary-only` plus raw `restore <snapshot> --summary-only` now keep that invalid-sidecar failure explicit instead of quietly rendering zero-session lifecycle/retention summaries.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_missing_lifecycle_rollup_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_invalid_retention_rollup_in_session_continuity_sidecar -q` passed (`Ran 2 tests`); focused sidecar lifecycle cluster passed (`Ran 6 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 545 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; raw snapshot verify/restore summaries now distinguish malformed `zerker.snapshot_continuity.v1` sidecars from valid zero-count continuity state.
- Blockers: sidecar validation still proves only local structure plus snapshot hash/root coherence; there is still no signature, receipt-backed authenticity check, or deeper nested rollup schema validation for the sidecar itself.
- Next safe slice: add one bounded nested-rollup validation contract only if operators need explicit invalid-sidecar wording for structurally present but internally malformed lifecycle/retention entries beyond missing or wrong-type top-level rollups.

## 2026-06-29 - raw snapshot nested-rollup sidecar entry validation

- Scope: bounded L2 slice on nested raw snapshot continuity sidecar validation only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects structurally present but internally malformed `zerker.snapshot_continuity.v1` sidecars when `session_lifecycle_rollup.sessions` or `session_retention_rollup.sessions` is not a list of objects. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit nested-path wording instead of accepting placeholder session entries as valid continuity.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_invalid_lifecycle_rollup_session_entry_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_invalid_retention_rollup_session_entry_in_session_continuity_sidecar -q` passed (`Ran 2 tests`); focused sidecar lifecycle cluster passed (`Ran 7 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 552 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive nested entry validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still checks local structure plus snapshot hash/root coherence only; there is still no signature, receipt-backed authenticity check, or broader per-field rollup coherence validation.
- Next safe slice: add one bounded count/summary coherence check, such as `count` vs `sessions` length, only if operators need stricter invalid-sidecar wording before authenticity work.

## 2026-06-29 - raw snapshot rollup-count coherence validation

- Scope: bounded L2 slice on raw snapshot continuity sidecar rollup coherence only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_lifecycle_rollup.count` or `session_retention_rollup.count` disagrees with the actual `sessions` array length. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit count-vs-length wording instead of accepting incoherent rollup totals as valid continuity.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_incoherent_lifecycle_rollup_count_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_incoherent_retention_rollup_count_in_session_continuity_sidecar -q` passed (`Ran 2 tests`); focused sidecar lifecycle cluster passed (`Ran 9 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 561 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive rollup-count coherence validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still checks local structure plus snapshot hash/root coherence only; there is still no signature, receipt-backed authenticity check, or deeper per-field coherence work beyond the `count`/`sessions` contract.
- Next safe slice: add one similarly bounded coherence check for another rollup aggregate that is already present in the sidecar, such as event or payload-status totals versus the concrete session rows, without widening restore mechanics or sidecar authenticity.

## 2026-06-29 - raw snapshot lifecycle event-total coherence validation

- Scope: bounded L2 slice on raw snapshot continuity sidecar lifecycle aggregate coherence only; did not change lifecycle write semantics, retention semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_lifecycle_rollup.event_kind_counts` disagree with the summed per-session `start_count`, `checkpoint_count`, `snapshot_count`, `snapshot_soft_delete_count`, or `end_count` rows. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit `event_kind_counts.*` mismatch wording instead of rendering misleading lifecycle totals from tampered sidecars.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_incoherent_lifecycle_event_totals_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_incoherent_lifecycle_event_totals_in_session_continuity_sidecar -q` passed (`Ran 2 tests`); focused sidecar lifecycle cluster passed (`Ran 11 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 567 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive lifecycle event-total coherence validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still checks local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check, and retention payload-status aggregates are not yet cross-checked against the concrete session rows.
- Next safe slice: add one similarly bounded retention payload-status aggregate coherence check versus per-session `available_payload_count` / `soft_deleted_payload_count`, without widening restore mechanics or sidecar authenticity.

## 2026-06-29 - raw snapshot retention payload-total coherence validation

- Scope: bounded L2 slice on raw snapshot continuity sidecar retention aggregate coherence only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_retention_rollup.payload_status_counts.available` or `.soft_deleted` disagree with the summed per-session `available_payload_count` / `soft_deleted_payload_count` rows. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit `payload_status_counts.*` mismatch wording instead of rendering misleading retention totals from tampered sidecars.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_incoherent_retention_payload_totals_in_session_continuity_sidecar -q` passed (`Ran 1 test`); focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_incoherent_retention_payload_totals_in_session_continuity_sidecar -q` passed (`Ran 1 test`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 573 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive retention payload-total coherence validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check, and `retention_state_counts` is not yet cross-checked against per-session `retention_state`.
- Next safe slice: add one similarly bounded `session_retention_rollup.retention_state_counts` coherence contract versus per-session `retention_state`, without widening restore mechanics or sidecar authenticity.

## 2026-06-30 - raw snapshot retention-state coherence validation

- Scope: bounded L2 slice on raw snapshot continuity sidecar retention-state aggregate coherence only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_retention_rollup.retention_state_counts.all_available`, `.mixed`, or `.soft_deleted_only` disagree with counted per-session `retention_state` rows. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit `retention_state_counts.*` mismatch wording instead of rendering misleading retention-state totals from tampered sidecars.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_incoherent_retention_state_totals_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_incoherent_retention_state_totals_in_session_continuity_sidecar -q` passed (`Ran 2 tests`); focused sidecar retention cluster passed (`Ran 4 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 577 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive retention-state coherence validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded per-session `retention_state` enum-validation contract if operators need stricter invalid-sidecar wording before any authenticity work.

## 2026-06-30 - raw snapshot retention-state enum validation

- Scope: bounded L2 slice on raw snapshot continuity sidecar per-session retention-state validation only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_retention_rollup.sessions[*].retention_state` is not one of `all_available`, `mixed`, or `soft_deleted_only`. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with an explicit invalid-enum error instead of only surfacing a derived `retention_state_counts.*` mismatch.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_unknown_session_retention_state_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_unknown_session_retention_state_in_session_continuity_sidecar -q` passed (`Ran 2 tests`); adjacent six-test retention cluster passed (`Ran 6 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 586 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive per-session retention-state enum validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded per-session snapshot payload-status enum-validation contract only if operators need more explicit invalid-sidecar wording before any authenticity work.

## 2026-06-30 - raw snapshot payload-status enum validation

- Scope: bounded L2 slice on raw snapshot continuity sidecar per-session payload-status validation only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_retention_rollup.sessions[*].latest_payload_status` is not one of `available` or `soft_deleted`. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit invalid-enum wording instead of accepting a tampered per-session payload-status row as valid continuity.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_unknown_session_payload_status_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_unknown_session_payload_status_in_session_continuity_sidecar -q` passed (`Ran 2 tests`); adjacent six-test retention cluster passed (`Ran 6 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 592 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive per-session payload-status enum validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded per-session `latest_payload_status` coherence contract versus per-session available/soft-deleted counts only if operators need stricter invalid-sidecar wording before any authenticity work.

## 2026-07-01 - raw snapshot zero-payload session-row validation

- Scope: bounded L2 slice on impossible raw snapshot continuity sidecar session rows only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_retention_rollup.sessions[*]` rows claim zero `available_payload_count` and zero `soft_deleted_payload_count`. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit zero-payload session-row wording instead of only failing indirectly through `latest_payload_status` count mismatch.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_zero_payload_session_row_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_zero_payload_session_row_in_session_continuity_sidecar -q` failed first (`2` failures), then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 607 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive zero-payload session-row validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded non-negative per-session payload-count contract for raw continuity sidecars, without widening restore mechanics or sidecar authenticity.

## 2026-07-02 - raw snapshot negative payload-count validation

- Scope: bounded L2 slice on impossible negative per-session raw snapshot continuity counts only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_retention_rollup.sessions[*].available_payload_count` or `.soft_deleted_payload_count` is negative. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit non-negative count wording instead of accepting tampered sidecars and rendering nonsensical negative retention totals.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_negative_available_payload_count_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_negative_soft_deleted_payload_count_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_negative_available_payload_count_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_negative_soft_deleted_payload_count_in_session_continuity_sidecar -q` failed first (`4` failures), then passed (`Ran 4 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 613 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive non-negative per-session payload-count validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded non-negative aggregate `session_retention_rollup.payload_status_counts.*` contract for raw continuity sidecars, without widening restore mechanics or sidecar authenticity.

## 2026-07-02 - raw snapshot negative aggregate payload-total validation

- Scope: bounded L2 slice on impossible negative aggregate raw snapshot continuity retention totals only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_retention_rollup.payload_status_counts.available` or `.soft_deleted` is negative. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit non-negative aggregate wording instead of only surfacing a weaker summed-total mismatch. Existing per-session negative-count precedence stays intact when both a session row and the aggregate are invalid.
- Tests: focused new aggregate-total contracts failed first (`4` failures), then the eight-test negative-count cluster passed (`Ran 8 tests`); required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 622 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive non-negative aggregate retention-total validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded non-negative aggregate `session_retention_rollup.retention_state_counts.*` contract for raw continuity sidecars, without widening restore mechanics or sidecar authenticity.

## 2026-07-03 - raw snapshot negative aggregate retention-state-total validation

- Scope: bounded L2 slice on impossible negative aggregate raw snapshot continuity retention-state totals only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_retention_rollup.retention_state_counts.all_available`, `.mixed`, or `.soft_deleted_only` is negative. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit non-negative aggregate retention-state wording instead of only surfacing a weaker counted-state mismatch.
- Tests: focused new aggregate retention-state contracts failed first (`2` failures), then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 626 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive non-negative aggregate retention-state validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded non-negative aggregate `session_lifecycle_rollup.event_kind_counts.*` contract for raw continuity sidecars, without widening restore mechanics or sidecar authenticity.

## 2026-07-03 - raw snapshot negative aggregate lifecycle-event-total validation

- Scope: bounded L2 slice on impossible negative aggregate raw snapshot continuity lifecycle totals only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose aggregate `session_lifecycle_rollup.event_kind_counts.start`, `.checkpoint`, `.snapshot`, `.snapshot_soft_delete`, or `.end` is negative. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit non-negative aggregate lifecycle wording instead of only surfacing a weaker summed-total mismatch.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_negative_lifecycle_event_totals_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_negative_lifecycle_event_totals_in_session_continuity_sidecar -q` failed first (`10` subtest failures), then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 636 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive non-negative aggregate lifecycle-total validation for `zerker.snapshot_continuity.v1`.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded non-negative per-session `session_lifecycle_rollup.sessions[*].*_count` contract for raw continuity sidecars, without widening restore mechanics or sidecar authenticity.

## 2026-07-03 - raw snapshot retention-state negative aggregate coverage expansion

- Scope: bounded L2 slice on contract coverage only for the already-shipped raw snapshot continuity retention-state validator; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, runner context packing, or validator behavior.
- Files touched: `tests/test_cli_onboarding.py`.
- Behavior changed: none in product behavior. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` were already rejecting negative aggregate `session_retention_rollup.retention_state_counts.all_available`, `.mixed`, and `.soft_deleted_only`; this slice locks the previously untested `mixed` and `soft_deleted_only` paths so copied or hand-edited sidecars cannot silently regress on those aggregate keys.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_negative_retention_state_total_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_negative_mixed_retention_state_total_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_negative_soft_deleted_only_retention_state_total_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_negative_retention_state_total_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_negative_mixed_retention_state_total_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_negative_soft_deleted_only_retention_state_total_in_session_continuity_sidecar -q` passed (`Ran 6 tests`).
- Artifacts/receipts: no new schema; stronger regression coverage for existing `zerker.snapshot_continuity.v1` aggregate retention-state validation.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded non-negative aggregate `session_lifecycle_rollup.event_kind_counts.*` contract for raw continuity sidecars, without widening restore mechanics or sidecar authenticity.

## 2026-07-03 - raw snapshot negative per-session lifecycle-count validation

- Scope: bounded L2 slice on impossible negative per-session raw snapshot continuity lifecycle counts only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_lifecycle_rollup.sessions[*].start_count`, `.checkpoint_count`, `.snapshot_count`, `.snapshot_soft_delete_count`, or `.end_count` is negative. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now fail with explicit per-session non-negative lifecycle-count wording before falling through to a weaker aggregate mismatch.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_negative_session_lifecycle_counts_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_negative_session_lifecycle_counts_in_session_continuity_sidecar -q` passed (`Ran 2 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 642 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive per-session lifecycle-count validation for existing `zerker.snapshot_continuity.v1` sidecars.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded per-session lifecycle-row coherence contract such as `latest_event_kind` versus the positive `*_count` fields, without widening restore mechanics or sidecar authenticity.

## 2026-07-04 - raw snapshot latest-event lifecycle-row coherence validation

- Scope: bounded L2 slice on impossible per-session raw snapshot continuity lifecycle rows only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_lifecycle_rollup.sessions[*].latest_event_kind` points at `start`, `checkpoint`, `snapshot`, `snapshot_soft_delete`, or `end` while the matching per-session `*_count` is zero. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit latest-event coherence wording instead of rendering a copied or hand-edited lifecycle row as valid continuity.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_incoherent_session_latest_event_kind_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_incoherent_session_latest_event_kind_in_session_continuity_sidecar -q` failed first (`10` subtest failures), then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 646 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive per-session `latest_event_kind` coherence validation for existing `zerker.snapshot_continuity.v1` sidecars.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded latest-event enum/type contract for raw continuity sidecars only if operators need stricter invalid-sidecar wording beyond the new positive-count coherence check, without widening restore mechanics or sidecar authenticity.

## 2026-07-04 - raw snapshot latest-event identifier coherence validation

- Scope: bounded L2 slice on impossible per-session raw snapshot continuity lifecycle identifiers only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_lifecycle_rollup.sessions[*].latest_event_kind` points at `start`, `checkpoint`, `snapshot`, `snapshot_soft_delete`, or `end` while the matching `latest_*` identifier field is missing. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit latest-event identifier wording instead of accepting a copied or hand-edited lifecycle row whose headline event has no matching lifecycle id anchor.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_missing_latest_event_identifier_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_missing_latest_event_identifier_in_session_continuity_sidecar -q` failed first (`10` subtest failures), then the focused four-test cluster including the existing latest-event-count contracts passed (`Ran 4 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 659 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive per-session latest-event identifier coherence validation for existing `zerker.snapshot_continuity.v1` sidecars.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded latest-event root coherence contract so a raw continuity sidecar cannot claim `latest_event_kind` plus id without the matching latest Merkle-root field, without widening restore mechanics or sidecar authenticity.

## 2026-07-04 - raw snapshot latest-event root coherence validation

- Scope: bounded L2 slice on impossible per-session raw snapshot continuity lifecycle roots only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_lifecycle_rollup.sessions[*].latest_event_kind` points at `start`, `checkpoint`, `snapshot`, `snapshot_soft_delete`, or `end` while the matching `latest_*_root` field is missing. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit latest-event root wording instead of accepting a copied or hand-edited lifecycle row whose headline event has no matching Merkle-root anchor.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_missing_latest_event_root_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_missing_latest_event_root_in_session_continuity_sidecar -q` failed first (`10` subtest failures), then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 666 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive per-session latest-event root coherence validation for existing `zerker.snapshot_continuity.v1` sidecars.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded `latest_status_root` coherence contract so a raw continuity sidecar cannot claim a latest lifecycle event/root pair while the rollup-level latest status root points somewhere else, without widening restore mechanics or sidecar authenticity.

## 2026-07-04 - raw snapshot latest-status-root coherence validation

- Scope: bounded L2 slice on impossible per-session raw snapshot continuity latest-root rollup rows only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_lifecycle_rollup.sessions[*].latest_status_root` does not equal the event-specific `latest_*_root` implied by the row's `latest_event_kind`. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit latest-status-root coherence wording instead of accepting a copied or hand-edited rollup row whose headline latest lifecycle event points at a different Merkle root than the rollup-level latest status root.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_mismatched_latest_status_root_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_mismatched_latest_status_root_in_session_continuity_sidecar -q` failed first (`10` subtest failures), then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 682 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive per-session `latest_status_root` coherence validation for existing `zerker.snapshot_continuity.v1` sidecars.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded `latest_lifecycle_id` coherence contract so a raw continuity sidecar cannot claim a latest lifecycle event while the generic rollup-level lifecycle id points at another event, without widening restore mechanics or sidecar authenticity.

## 2026-07-05 - raw snapshot latest-lifecycle-id coherence validation

- Scope: bounded L2 slice on impossible per-session raw snapshot continuity lifecycle ids only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose rollup-level `latest_lifecycle_id` does not equal the event-specific latest identifier implied by `session_lifecycle_rollup.sessions[*].latest_event_kind`. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit latest-lifecycle-id coherence wording instead of accepting a copied or hand-edited row whose generic lifecycle id points at another event.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_mismatched_latest_lifecycle_id_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_mismatched_latest_lifecycle_id_in_session_continuity_sidecar -q` failed first (`10` subtest failures), then passed (`Ran 2 tests`); the adjacent six-test continuity cluster covering root, latest-status-root, and latest-lifecycle-id paths passed (`Ran 6 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 688 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive per-session `latest_lifecycle_id` coherence validation for existing `zerker.snapshot_continuity.v1` sidecars.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded rollup-level `latest_event_created_at` coherence contract only if operators still need stricter raw-sidecar honesty before sidecar authenticity work.

## 2026-07-05 - raw snapshot lifecycle receipt-provenance total coherence validation

- Scope: bounded L2 slice on aggregate raw snapshot continuity receipt-provenance totals only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose aggregate `session_lifecycle_rollup.verified_receipt_count`, `.failed_receipt_count`, or `.linked_treeship_artifact_count` disagrees with the summed per-session lifecycle receipt counts. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` with explicit receipt-provenance total wording instead of accepting copied or hand-edited continuity summaries that overstate or understate lifecycle proof coverage.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_incoherent_lifecycle_receipt_provenance_totals_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_incoherent_lifecycle_receipt_provenance_totals_in_session_continuity_sidecar -q` failed first (`6` subtest failures), then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 701 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive aggregate lifecycle receipt-provenance total validation for existing `zerker.snapshot_continuity.v1` sidecars.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded non-negative receipt-provenance-count contract on the same raw continuity sidecar path, without widening restore mechanics or sidecar authenticity.

## 2026-07-06 - raw snapshot latest-start token-budget-hint validation

- Scope: bounded L2 slice on raw snapshot continuity token-budget hints only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, or runner context packing.
- Files touched: `zerker_memory/cli.py`, `tests/test_cli_onboarding.py`.
- Behavior changed: `validate_snapshot_continuity_payload(...)` now rejects `zerker.snapshot_continuity.v1` sidecars whose `session_lifecycle_rollup.sessions[*].latest_start_token_budget_hint.context_budget_tokens` is negative. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now keep `Session continuity verify: failed` instead of accepting impossible negative context-budget history in the continuity summary operators use at context boundaries.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_negative_latest_start_context_budget_hint_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_negative_latest_start_context_budget_hint_in_session_continuity_sidecar -q` failed first (`2` failures), then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/cli.py tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 714 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema; additive token-budget-hint validation only for existing `zerker.snapshot_continuity.v1` sidecars.
- Blockers: sidecar validation still proves local structure plus snapshot hash/root coherence only; there is still no signature or receipt-backed authenticity check for the sidecar itself.
- Next safe slice: add one similarly bounded type/coherence contract for malformed `latest_start_token_budget_hint` payloads, or move to sidecar authenticity work without widening any lifecycle write path.

## 2026-07-06 - raw snapshot malformed latest-start token-budget-hint contracts

- Scope: bounded L2 tests/docs slice on malformed raw snapshot continuity token-budget-hint payloads only; did not change lifecycle write semantics, restore mechanics, snapshot JSON schema, handoff manifests, sidecar generation, or runner context packing.
- Files touched: `tests/test_cli_onboarding.py`.
- Behavior changed: none. Raw `snapshot verify <snapshot> --summary-only` and raw `restore <snapshot> --summary-only` now have direct regression coverage proving the existing validator rejects `zerker.snapshot_continuity.v1` sidecars when `session_lifecycle_rollup.sessions[*].latest_start_token_budget_hint` is not an object or when `context_budget_tokens` is a string or boolean instead of an integer.
- Tests: focused `python3 -m unittest tests.test_cli_onboarding.CliOnboardingTest.test_snapshot_verify_summary_only_reports_malformed_latest_start_token_budget_hint_payloads_in_session_continuity_sidecar tests.test_cli_onboarding.CliOnboardingTest.test_restore_snapshot_summary_only_reports_malformed_latest_start_token_budget_hint_payloads_in_session_continuity_sidecar -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_cli_onboarding.py` passed; required `python3 -m unittest tests.test_cli_onboarding tests.test_store tests.test_runner -q` passed (`Ran 724 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: no new schema or receipt path; additive regression coverage only for the existing raw `zerker.snapshot_continuity.v1` validator.
- Blockers: sidecar authenticity is still trust-on-local-file presence plus structural/hash/root validation only; there is still no signature or receipt-backed authenticity proof for the sidecar itself.
- Next safe slice: add one similarly bounded malformed/coherence contract on another raw continuity headline field, or move to sidecar authenticity without widening lifecycle write paths.
