# Temporal KG Lane Log

## 2026-07-06T13:01:18Z - L1 temporal-kg - Codex L1 worker

- Scope: regression-locked the already-shipped unfiltered no-search `query_at(..., current_resolution="all")` identity envelope at the field level, keeping the slice local-store-only and tests/docs-only before any schema work.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; the existing default no-search point-in-time identity view is now durably covered to prove the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` chain keeps explicit `learned_at`, `valid_from`, `valid_to`, `superseded_at`, `superseded_by_ids`, and `serial` fields, while visible current winners in the unfiltered snapshot still carry `current_resolution="selected"` and the superseded predecessor does not.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", scope="project")` now has direct store and runner passthrough coverage proving the unrelated billing-exporter `Alice` fact stays current with `learned_at=valid_from=2024-01-15T00:00:00Z`, the stale owner fact keeps `valid_to=superseded_at=2024-02-01T00:00:00Z` with `temporal_resolution_kind="supersession"`, and the current `Alice Chen` fact stays current with `learned_at=valid_from=2024-02-01T00:00:00Z`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_default_no_search_view_preserves_identity_bitemporal_fields tests.test_runner.RunnerTest.test_build_context_preserves_query_at_default_no_search_identity_bitemporal_fields -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 337 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 171 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains intentionally narrow and local-first; there is still no dedicated `query_at(...)` receipt wrapper or schema-backed bi-temporal edge layer behind the additive default no-search envelope.
- Next safe slice: add one similarly bounded regression for one remaining unfiltered point-in-time ordering or selection contract only if a real consumer still needs that metadata made explicit before schema work.

## 2026-07-06T09:03:45Z - L1 temporal-kg - Codex L1 worker

- Scope: made the existing no-search `query_at(..., current_resolution="selected")` identity snapshot keep explicit selection metadata, keeping the slice local-store-only and limited to current-selection receipts plus focused regressions.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: the no-search selected-current point-in-time view now keeps `selected_ids`, `selection_strategy="current_only_v1"`, `selection_reason="default-current-only"`, `current_ordering`, and `history_ordering` instead of dropping that metadata outside the search-scoped path. This now holds both after `Status page owner is Alice Chen.` becomes current and before that successor becomes active.
- Focused fixtures: the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity chain now has direct no-search coverage proving `query_at("2024-01-20T00:00:00Z", scope="project", current_resolution="selected")` keeps the current `Alice` fact and unrelated billing-exporter `Alice` fact explicitly selected with receipt-visible ordering metadata while the future `Alice Chen` fact stays outside the selected graph; `build_context(...)` is regression-locked to preserve the same no-search selected-current contract in `zerker.memory_context.v1`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_no_search_selected_current_scope_preserves_identity_bitemporal_fields tests.test_store.MemoryStoreTest.test_query_at_no_search_selected_current_scope_keeps_pre_activation_identity_metadata_explicit tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_selected_scope_identity_bitemporal_fields tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_selected_scope_pre_activation_identity_metadata -q` passed (`Ran 4 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 336 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 170 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains intentionally narrow and local-first; there is still no dedicated `query_at(...)` receipt wrapper or schema-backed bi-temporal edge layer behind the additive selected-current envelope.
- Next safe slice: keep L1 bounded and make one remaining no-search `query_at(...)` envelope explicit only if another already-shipped point-in-time subset still drops receipt or ordering metadata before schema work.

## 2026-07-06T04:59:31Z - L1 temporal-kg - Codex L1 worker

- Scope: made the existing no-search `query_at(..., superseded_only=True)` empty identity guard explicit before supersession takes effect, keeping the slice local-store-only and limited to temporal selection metadata plus focused regressions.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: the empty no-search superseded envelope now keeps the same explicit filtered-view contract as the other temporal subsets even when nothing is yet superseded. Before `Status page owner is Alice Chen.` becomes active, `query_at("2024-01-20T00:00:00Z", scope="project", superseded_only=True)` now returns `selected_ids=[]`, `selection_strategy="superseded_only_v1"`, `selection_reason="explicit-superseded-only-filter"`, and an applied empty `history_ordering` payload instead of leaving that pre-supersession identity guard partially implicit.
- Focused fixtures: the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity chain now has direct no-search coverage proving the January pre-supersession snapshot leaves both the current owner fact and the unrelated billing-exporter `Alice` fact visible only through `current_history_receipt_metadata.omitted_*`, while the future `Alice Chen` fact remains outside the base history/current receipt split until supersession actually takes effect; `build_context(...)` is regression-locked to preserve that same empty superseded contract in `zerker.memory_context.v1`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_no_search_superseded_only_stays_empty_before_identity_supersession_takes_effect -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_superseded_identity_empty_predecessor_guard -q` passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 333 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 167 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains intentionally narrow and local-first; there is still no dedicated `query_at(...)` receipt wrapper or schema-backed bi-temporal edge layer behind the additive empty superseded envelope.
- Next safe slice: add one similarly bounded no-search field-level regression for another filtered identity guard only if a consumer still needs a remaining pre/post-transition envelope made explicit before broader schema work.

## 2026-07-06T00:58:19Z - L1 temporal-kg - Codex L1 worker

- Scope: regression-locked the already-shipped no-search `query_at(..., future_only=True)` populated identity envelope at the field level without changing SQLite schema, temporal projection rules, receipt wrappers, runtime context shape, or external graph dependencies.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; the existing no-search future snapshot is now durably covered so the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` successor fact keeps explicit `learned_at=valid_from=2024-02-01T00:00:00Z`, `valid_to=None`, `superseded_at=None`, `unlearned_at=None`, `status_at_query="future"`, and the expected omitted current-vs-history receipt split instead of leaving that populated no-search field mapping implicit.
- Focused fixtures: `query_at("2024-01-20T00:00:00Z", scope="project", future_only=True)` now has direct coverage proving the future `Status page owner is Alice Chen.` node preserves the explicit bi-temporal future fields above while the learned `Release freeze owner is Mallory.`, the current `Status page owner is Alice.`, and the unrelated current `Alice owns the billing exporter.` fact remain visible only through `current_history_receipt_metadata.omitted_*`; `build_context(...)` is regression-locked to preserve that same no-search future-node field mapping unchanged, including the intentionally omitted `query_current_candidate_rank`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_no_search_future_only_surfaces_current_vs_history_receipt_metadata tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_identity_snapshot_controls_for_future_subset -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 332 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 166 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains intentionally tests/docs-only; there is still no dedicated `query_at(...)` receipt wrapper or schema-backed bi-temporal edge layer behind the additive future envelope.
- Next safe slice: add one similarly bounded field-level regression for one remaining already-shipped no-search point-in-time envelope that still relies mostly on receipt-shape coverage before widening into schema or explicit graph work.

## 2026-07-05T21:01:06Z - L1 temporal-kg - Codex L1 worker

- Scope: regression-locked the already-shipped no-search `query_at(..., learned_only=True)` populated envelope at the field level without changing SQLite schema, temporal projection rules, receipt wrappers, runtime context shape, or external graph dependencies.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; the existing no-search learned snapshot is now durably covered so the quarantined/proposed `Status page owner is Alice Chen.` fact keeps explicit `learned_at` with `valid_from=None`, `valid_to=None`, `unlearned_at=None`, and the expected omitted current/history receipt split instead of leaving that no-search learned-state mapping implicit.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", scope="project", learned_only=True)` now has direct coverage proving the learned `Status page owner is Alice Chen.` node preserves `learned_at=2024-02-01T00:00:00Z`, `valid_from=None`, `valid_to=None`, `status_at_query="quarantined"`, and `temporal_state="learned"` while the current `Status page owner is Alice.` fact plus the unrelated current `Alice owns the billing exporter.` fact remain visible only through `current_history_receipt_metadata.omitted_*`; `build_context(...)` is regression-locked to preserve the same no-search learned bi-temporal fields unchanged.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_no_search_learned_only_preserves_bitemporal_fields tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_learned_scope_bitemporal_fields -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` failed on the unrelated overlapping retrieval test `tests.test_store.MemoryStoreTest.test_original_history_budget_packing_keeps_earliest_and_latest_current_anchors` (`AssertionError: None != 'earliest-history-anchor-pair-reserved'`); `python3 -m unittest tests.test_runner -q` failed on the matching unrelated retrieval test `tests.test_runner.RunnerTest.test_build_context_original_history_budget_records_reserved_anchor_pair_exclusion_reason` (`AssertionError: None != 'earliest-history-anchor-pair-reserved'`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: required full-store and full-runner verification are currently blocked by an overlapping L3 retrieval failure already present in the dirty tree, so this L1 slice stays tests/docs-only and does not widen into `zerker_memory/store.py` or retrieval reservation logic.
- Next safe slice: add one similarly bounded no-search field-level regression for another already-shipped filtered envelope such as `future_only=True` only if a consumer still needs explicit no-search bi-temporal field mapping before schema work.

## 2026-07-05T16:58:00Z - L1 temporal-kg - Codex L1 worker

- Scope: regression-locked the already-shipped no-search `query_at(..., unlearned_only=True)` populated envelope at the field level without changing SQLite schema, temporal projection rules, receipt wrappers, runtime context shape, or external graph dependencies.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; the existing no-search unlearned snapshot is now durably covered so revoked, forgotten, and rejected facts keep explicit `learned_at`, `valid_from`, `valid_to`, and `unlearned_at` bi-temporal fields instead of leaving that no-search field mapping implicit.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", scope="project", unlearned_only=True)` now has direct coverage proving the revoked `Status page owner is Alice.` fact keeps `learned_at=valid_from=2024-01-01T00:00:00Z` with `valid_to=unlearned_at=2024-02-10T00:00:00Z`, the forgotten `Temporary routing snack is ramen.` fact keeps `learned_at=valid_from=2024-01-02T00:00:00Z` with `valid_to=unlearned_at=2024-02-11T00:00:00Z`, and the rejected `Release freeze owner is Mallory.` fact keeps `learned_at=2024-01-03T00:00:00Z`, `valid_from=None`, and `valid_to=unlearned_at=2024-02-12T00:00:00Z`; `build_context(...)` is regression-locked to preserve the same no-search unlearned bi-temporal fields unchanged.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_unlearned_temporal_subset tests.test_runner.RunnerTest.test_build_context_preserves_unlearned_selection_metadata_for_query_at_subset -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` failed on the unrelated overlapping retrieval test `tests.test_store.MemoryStoreTest.test_chronology_relation_budget_packing_keeps_selected_support_chain_when_it_fits` (`AssertionError: None != 'chronology-support-chain-reserved'`); `python3 -m unittest tests.test_runner -q` passed (`Ran 163 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: required full-store verification is currently blocked by an overlapping L3 retrieval failure already present in the dirty tree, so this L1 slice stays tests/docs-only and does not widen into `zerker_memory/store.py`.
- Next safe slice: add one similarly bounded no-search field-level regression for the populated `learned_only=True` envelope only if a consumer still needs the null-`valid_from` learned-state mapping explicit before schema work.

## 2026-07-05T12:57:19Z - L1 temporal-kg - Codex L1 worker

- Scope: regression-locked the already-shipped no-search `query_at(..., current_resolution="abstained")` contradiction envelope at the field level without changing SQLite schema, temporal projection rules, receipt wrappers, runtime context shape, or external graph dependencies.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; the existing no-search abstained-current contradiction snapshot is now durably covered so both unresolved `Incident owner is Alex.` and `Incident owner is Priya.` facts keep explicit `learned_at` and `valid_from` bi-temporal fields plus contradiction metadata while the unrelated current billing-exporter `Alice` fact remains visible only through the omitted base history/current receipt envelope.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", scope="project", current_resolution="abstained")` now has direct coverage proving the no-search path keeps both unresolved current contenders in `history_temporal_graph`/`current_temporal_graph`/`abstained_temporal_graph`, preserves `learned_at=valid_from=2024-02-01T00:00:00Z`, `current_resolution="abstained"`, and `temporal_resolution_kind="contradiction"` with `lexical-current-conflict`, and keeps the unrelated billing-exporter `Alice` fact only in `current_history_receipt_metadata.omitted_*`; `build_context(...)` is regression-locked to preserve the same no-search contradiction fields unchanged.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_no_search_abstained_scope_preserves_contradiction_bitemporal_fields tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_abstained_scope_contradiction_bitemporal_fields -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 328 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 162 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains intentionally tests/docs-only; there is still no dedicated `query_at(...)` receipt wrapper or schema-backed bi-temporal edge layer behind the additive abstained-current envelope.
- Next safe slice: add one similarly bounded no-search contradiction or identity-empty regression only if a consumer still needs more explicit bi-temporal field mapping before schema work.

## 2026-07-05T08:57:20Z - L1 temporal-kg - Codex L1 worker

- Scope: regression-locked the already-shipped no-search `query_at(..., current_resolution="dropped")` contradiction envelope at the field level without changing SQLite schema, temporal projection rules, receipt wrappers, runtime context shape, or external graph dependencies.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: no runtime behavior changed; the existing no-search dropped-current contradiction snapshot is now durably covered so the losing `Deploy target is Render.` fact keeps explicit `learned_at` and `valid_from` bi-temporal fields plus contradiction metadata while the winning `Deploy target is Railway.` fact and unrelated current scope facts remain visible only through the omitted base history/current receipt envelope.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", scope="project", current_resolution="dropped")` now has direct coverage proving the no-search path keeps the dropped loser in `history_temporal_graph`/`current_temporal_graph`/`dropped_current_temporal_graph`, preserves `learned_at=valid_from=2024-02-01T00:00:00Z`, `current_resolution="dropped"`, and `temporal_resolution_kind="contradiction"` with `lexical-current-conflict`, and keeps both the winning deploy-target fact plus the unrelated billing-exporter `Alice` fact only in `current_history_receipt_metadata.omitted_*`; `build_context(...)` is regression-locked to preserve the same no-search contradiction fields unchanged.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_no_search_dropped_current_scope_preserves_contradiction_bitemporal_fields tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_dropped_scope_contradiction_bitemporal_fields -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 325 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 161 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains intentionally tests/docs-only; there is still no dedicated `query_at(...)` receipt wrapper or schema-backed bi-temporal edge layer behind the additive dropped-current envelope.
- Next safe slice: add one similarly bounded no-search field-level regression for the remaining `current_resolution="abstained"` contradiction surface only if a consumer now needs the same explicit bi-temporal mapping before schema work.

## 2026-07-05T04:58:49Z - L1 temporal-kg - Codex L1 worker

- Scope: regression-locked the already-shipped no-search `query_at(..., current_resolution="selected")` scope envelope without changing SQLite schema, temporal projection rules, receipt wrappers, runtime context shape, or external graph dependencies.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; the existing no-search selected-current scope snapshot is now durably covered so the resolved `Status page owner is Alice.` -> `Status page owner is Alice Chen.` fixture keeps explicit `learned_at` and `valid_from` fields on the surviving current `Alice Chen` node while the unrelated current billing-exporter `Alice` fact remains part of the same scope-wide selected/current graph and the stale `Alice` owner stays visible only through the omitted base-history receipt envelope.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", scope="project", current_resolution="selected")` now has direct coverage proving the no-search path keeps both current facts in `history_temporal_graph`/`current_temporal_graph`/`selected_temporal_graph`, preserves `learned_at=valid_from=2024-01-15T00:00:00Z` for the unrelated billing-exporter `Alice` fact and `learned_at=valid_from=2024-02-01T00:00:00Z` for `Alice Chen`, and keeps the stale `Status page owner is Alice.` node out of the selected graph but present in `current_history_receipt_metadata.omitted_history_memory_ids`; `build_context(...)` is regression-locked to preserve the same no-search scope-wide bi-temporal graph fields unchanged.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_no_search_selected_current_scope_preserves_identity_bitemporal_fields tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_selected_scope_identity_bitemporal_fields -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 324 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 160 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains intentionally tests/docs-only; there is still no dedicated `query_at(...)` receipt wrapper or schema-backed bi-temporal edge layer behind the additive envelope.
- Next safe slice: add one similarly bounded field-level regression for another already-shipped no-search current-resolution surface such as `current_resolution="dropped"` or `current_resolution="abstained"` only if a consumer now needs explicit bi-temporal field mapping there before schema work.

## 2026-07-05T00:55:34Z - L1 temporal-kg - Codex L1 worker

- Scope: regression-locked the already-shipped no-search `query_at(..., superseded_only=True)` bi-temporal identity envelope without changing SQLite schema, temporal projection rules, receipt wrappers, runtime context shape, or external graph dependencies.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; the existing no-search superseded identity snapshot is now durably covered so the resolved `Status page owner is Alice.` -> `Status page owner is Alice Chen.` fixture keeps explicit `learned_at`, `valid_from`, `valid_to`, and `superseded_at` bi-temporal fields aligned with the current-vs-history receipt envelope instead of leaving that no-search field mapping implicit.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", scope="project", superseded_only=True)` now has direct coverage proving the stale `Alice` node preserves `learned_at=2024-01-01T00:00:00Z`, `valid_from=2024-01-01T00:00:00Z`, and `valid_to=superseded_at=2024-02-01T00:00:00Z` while the unrelated current billing-exporter `Alice` fact stays visible only through the omitted base history/current receipt envelope; `build_context(...)` is regression-locked to preserve the same no-search bi-temporal graph fields unchanged.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_no_search_superseded_only_surfaces_current_vs_history_receipt_metadata tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_superseded_identity_receipt_metadata -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 321 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 158 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains intentionally tests/docs-only; there is still no dedicated `query_at(...)` receipt wrapper or schema-backed bi-temporal edge layer behind the additive envelope.
- Next safe slice: add one similarly bounded no-search field-level regression for another existing temporal subset such as `future_only=True` or one of the identity-empty `learned_only`/`unlearned_only` views only if a consumer now needs those explicit bi-temporal fields before schema work.

## 2026-07-04T20:56:21Z - L1 temporal-kg - Codex L1 worker

- Scope: regression-locked the already-shipped no-search `query_at(..., learned_only=True)` and `query_at(..., unlearned_only=True)` empty identity guards without changing SQLite schema, temporal projection rules, receipt wrappers, runtime context shape, or external graph dependencies.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: no runtime behavior changed; the existing no-search filtered point-in-time envelopes are now durably covered so `current_history_receipt_metadata.filter=learned_only|unlearned_only` keeps explicit base/included/omitted history/current id splits even when the resolved `Status page owner is Alice.` -> `Status page owner is Alice Chen.` fixture leaves the filtered result empty and an unrelated current `Alice owns the billing exporter.` fact is only visible through the omitted base envelope.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", scope="project", learned_only=True)` and `query_at("2024-02-20T00:00:00Z", scope="project", unlearned_only=True)` now stay empty for the cross-session identity successor while still exposing the full no-search base history `[billing exporter Alice, stale Alice, current Alice Chen]` plus base current `[billing exporter Alice, current Alice Chen]` splits, and `build_context(...)` is regression-locked to preserve that payload unchanged.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_no_search_learned_only_stays_empty_for_identity_supersession_history tests.test_store.MemoryStoreTest.test_query_at_no_search_unlearned_only_stays_empty_for_identity_supersession_history tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_identity_snapshot_controls_for_learned_empty_subset tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_identity_snapshot_controls_for_unlearned_empty_subset -q` passed (`Ran 4 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py docs/CONTINUOUS_BUILD/temporal-kg.log.md docs/SWARM_OPERATION_TRACKER.md` passed; `python3 -m unittest tests.test_store -q` failed on the pre-existing unrelated hybrid-retrieval `test_search_with_meta_passive_update_current_queries_backfill_generic_change_anchor_support` (`2` `update_current_support` errors); `python3 -m unittest tests.test_runner -q` failed on the matching unrelated hybrid-retrieval `test_passive_update_current_context_backfills_generic_change_anchor_support` (`2` failures missing the expected support memory); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: required full-suite verification is currently blocked by overlapping L3 hybrid-retrieval failures already present in the dirty tree, so this L1 slice stays tests/docs-only and does not widen into retrieval implementation files.
- Next safe slice: once the overlapping passive-update retrieval failure is cleared, add one similarly bounded regression around receipt-visible `learned_at`/`unlearned_at` preservation for an explicit no-search identity subset only if that temporal seam still lacks direct coverage.

## 2026-07-04T16:54:31Z - L1 temporal-kg - Codex L1 worker

- Scope: regression-locked the already-shipped no-search `query_at(..., future_only=True)` current-vs-history receipt envelope and runtime passthrough without changing SQLite schema, temporal projection rules, receipt wrappers, runtime context shape, or external graph dependencies.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no-search future-only point-in-time snapshots are now durably covered so `current_history_receipt_metadata.filter=future_only` keeps explicit base/included/omitted history/current id splits even without a lexical `search_query`, and the existing `build_context(...)` passthrough is regression-locked to preserve that payload unchanged for both the populated pre-active `Status page owner is Alice.` -> `Status page owner is Alice Chen.` fixture and the empty post-activation identity guard.
- Focused fixtures: `query_at("2024-01-20T00:00:00Z", scope="project", future_only=True)` now stays explicit about omitted learned/current same-name context while still surfacing only the future `Alice Chen` candidate, and `query_at("2024-02-20T00:00:00Z", scope="project", future_only=True)` remains empty while still exposing the stale/current omitted envelope instead of hiding the current-vs-history split.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_no_search_future_only_surfaces_current_vs_history_receipt_metadata tests.test_store.MemoryStoreTest.test_query_at_no_search_future_only_stays_empty_when_identity_successor_is_already_current -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_identity_snapshot_controls_for_future_subset tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_identity_snapshot_controls_for_future_empty_subset -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 315 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 154 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains intentionally tests/docs-only; there is still no dedicated `query_at(...)` receipt wrapper, and no-search filtered receipt coverage is still incomplete for the remaining sibling surfaces.
- Next safe slice: add one similarly bounded no-search filtered receipt regression such as `unlearned_only=True` only if a real consumer needs that contract before receipt-wrapper or schema work.

## 2026-07-04T08:53:59Z - L1 temporal-kg - Codex L1 worker

- Scope: extended the existing additive `current_history_receipt_metadata` contract to the default no-search `query_at(...)` snapshot without changing SQLite schema, temporal projection rules, receipt wrappers, runtime context shape, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: successful unfiltered `query_at("2024-02-20T00:00:00Z", scope="project")` snapshots now carry `current_history_receipt_metadata` with `filter=current_resolution:all` plus explicit base/included/omitted history/current id splits, and the existing `build_context(...)` passthrough is regression-locked to preserve that payload unchanged for the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity fixture while still keeping the unrelated billing-exporter Alice fact in the default scope-wide current/history envelope.
- Focused fixtures: the no-search point-in-time identity snapshot now exposes the same explicit receipt envelope that search-scoped `query_at(...)` already had, and the runtime `zerker.memory_context.v1` payload preserves that no-search envelope unchanged.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_default_no_search_view_surfaces_current_vs_history_receipt_metadata_for_identity_snapshot -q` failed first with missing `current_history_receipt_metadata`, then passed; `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_default_no_search_identity_receipt_metadata -q` failed first with the same missing key, then passed; `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 309 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 150 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper or schema-backed bi-temporal edge layer behind the additive envelope.
- Next safe slice: add one bounded dedicated `query_at(...)` receipt wrapper only if a real consumer needs a first-class receipt object before schema or external graph work.

## 2026-07-04T04:55:05Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive current-vs-history receipt-metadata contract to the existing unfiltered search-scoped `query_at(...)` point-in-time path, without changing SQLite schema, temporal projection rules, write receipts, runtime context shape, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: successful unfiltered `query_at(..., search_query=...)` snapshots now carry `current_history_receipt_metadata` with `filter=current_resolution:all` plus explicit base/included/omitted history/current id splits, and the existing `build_context(...)` passthrough is regression-locked to preserve that payload unchanged for the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity fixture.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", ..., search_query="status page owner")` now explicitly records the default current-vs-history envelope instead of exposing only `history_memory_ids` and `current_memory_ids`; the same runtime `zerker.memory_context.v1` payload now keeps that unfiltered envelope unchanged while still excluding the unrelated same-name billing-exporter Alice fact.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_default_view_surfaces_current_vs_history_receipt_metadata_for_identity_snapshot -q` failed first with missing `current_history_receipt_metadata`, then passed; `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_default_identity_receipt_metadata -q` failed first with the same missing key, then passed; `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 306 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 147 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, and unfiltered point-in-time snapshots without `search_query` still do not emit the same explicit current-vs-history receipt envelope.
- Next safe slice: only if a real consumer needs it, extend the same additive `current_resolution:all` receipt envelope to one more bounded unfiltered `query_at(...)` surface such as the no-search snapshot before any receipt-wrapper, schema, or external graph work.

## 2026-07-04T00:53:51Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive current-vs-history receipt-metadata contract to the existing `query_at(..., include_abstained_current=False)` point-in-time path, without changing SQLite schema, temporal projection rules, write receipts, runtime context shape, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: hidden-abstained point-in-time snapshots now carry `current_history_receipt_metadata` with explicit base/included/omitted history/current id splits whenever unresolved current contradictions are filtered out by `include_abstained_current=False`, and the existing `build_context(...)` passthrough is regression-locked to preserve that payload unchanged for the hidden `Incident owner is Alex.` versus `Incident owner is Priya.` contradiction fixture.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", ..., include_abstained_current=False)` now explicitly records both abstained contenders as omitted from the base history/current envelope instead of hiding them without a receipt-visible current-vs-history split; the runtime `zerker.memory_context.v1` temporal payload now preserves the same omitted-id envelope unchanged.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_hide_abstained_current_envelopes_while_preserving_conflict_metadata -q` failed first with missing `current_history_receipt_metadata`, then passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_hidden_query_at_abstained_snapshot_controls_for_current_contradiction -q` passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 302 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 146 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, and the default unfiltered point-in-time view still does not expose a parallel current-vs-history receipt envelope.
- Next safe slice: only if a real consumer needs it, add one bounded `query_at(...)` receipt wrapper or one similarly additive unfiltered current-vs-history receipt envelope before any schema or external graph work.

## 2026-07-03T20:53:02Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive current-vs-history receipt-metadata contract to the existing `query_at(..., future_only=True)` point-in-time path, without changing SQLite schema, temporal projection rules, write receipts, runtime context shape, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: future-only point-in-time snapshots now carry `current_history_receipt_metadata` with explicit `base_*`, `included_*`, and `omitted_*` history/current id splits, and the existing `build_context(...)` passthrough is regression-locked to preserve that payload unchanged for both the populated pre-active future fixture and the empty `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity guard.
- Focused fixtures: `query_at("2024-01-20T00:00:00Z", ..., future_only=True)` now explicitly records the contemporaneous current `Status page owner is Alice.` fact as omitted from both the base history and base current sets while the future `Status page owner is Alice Chen.` candidate continues to flow through the existing future selection/order surface; the same future-only filter on the resolved `Alice` -> `Alice Chen` identity path now stays empty but still records stale `Alice` plus current `Alice Chen` as omitted instead of hiding that split entirely.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_future_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_future_only_stays_empty_when_identity_successor_is_already_current tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_future_subset tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_future_empty_subset -q` failed first with missing `current_history_receipt_metadata`, then passed (`Ran 4 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 301 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 145 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, and the new future-only receipt split intentionally describes the base current/history envelope rather than duplicating future ids into a second receipt channel.
- Next safe slice: only if a real consumer needs it, add one bounded `query_at(...)` receipt wrapper or similarly additive future-envelope receipt surface on top of the existing local temporal projection before any schema or external graph work.

## 2026-07-03T16:50:36Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive current-vs-history receipt-metadata contract to the existing `query_at(..., unlearned_only=True)` point-in-time path, without changing SQLite schema, temporal projection rules, write receipts, runtime context shape, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: unlearned-only point-in-time snapshots now carry `current_history_receipt_metadata` with explicit `base_*`, `included_*`, and `omitted_*` history/current id splits, and the existing `build_context(...)` passthrough is regression-locked to preserve that payload unchanged for both the populated revoked/forgotten/rejected fixture and the empty `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity guard.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", ..., unlearned_only=True)` now explicitly shows revoked `Alex`, forgotten `ramen`, and rejected `Mallory` candidates included in the filtered history receipt view while the contemporaneous current `Status page owner is Alice Chen.` fact is recorded as omitted from both the base history and base current sets; the same unlearned-only filter on the resolved `Alice` -> `Alice Chen` identity path now stays empty but still records both base history ids and the base current `Alice Chen` id as omitted instead of hiding that split entirely.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_unlearned_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_unlearned_only_stays_empty_for_identity_supersession_history tests.test_runner.RunnerTest.test_build_context_preserves_unlearned_selection_metadata_for_query_at_subset tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_unlearned_empty_subset -q` failed first with missing `current_history_receipt_metadata`, then passed (`Ran 4 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 299 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 143 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, and the explicit current-vs-history receipt split still does not cover the remaining `future_only=True` filtered view.
- Next safe slice: only if a real consumer needs it, extend the same additive receipt-metadata split to exactly one remaining non-current point-in-time filter such as `future_only=True` before any schema or external graph work.

## 2026-07-03T12:49:51Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive current-vs-history receipt-metadata contract to the existing `query_at(..., learned_only=True)` point-in-time path, without changing SQLite schema, temporal projection rules, write receipts, runtime context shape, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: learned-only point-in-time snapshots now carry `current_history_receipt_metadata` with explicit `base_*`, `included_*`, and `omitted_*` history/current id splits, and the existing `build_context(...)` passthrough is regression-locked to preserve that payload unchanged for both the populated learned-candidate fixture and the empty `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity guard.
- Focused fixtures: `query_at("2024-01-20T00:00:00Z", ..., learned_only=True)` now explicitly shows the learned `Release freeze owner is Mallory.` candidate included in the filtered history receipt view while the contemporaneous current `Status page owner is Alice.` fact is recorded as omitted from both the base history and base current sets; the same learned-only filter on the resolved `Alice` -> `Alice Chen` identity path now stays empty but still records both base history ids and the base current `Alice Chen` id as omitted instead of hiding that split entirely.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_learned_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_learned_only_stays_empty_for_identity_supersession_history tests.test_runner.RunnerTest.test_build_context_preserves_bitemporal_identity_metadata_for_learned_only_query_envelopes tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_learned_empty_subset -q` passed (`Ran 4 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 299 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 143 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, and the explicit current-vs-history receipt split still does not cover the remaining non-current filtered views such as `unlearned_only=True` or `future_only=True`.
- Next safe slice: only if a real consumer needs it, extend the same additive receipt-metadata split to exactly one remaining non-current point-in-time filter such as `unlearned_only=True` before any schema or external graph work.

## 2026-07-03T08:49:33Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive current-vs-history receipt-metadata contract to the existing `query_at(..., superseded_only=True)` point-in-time path, without changing SQLite schema, temporal projection rules, write receipts, runtime context shape, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: superseded-only point-in-time snapshots now carry `current_history_receipt_metadata` with explicit `base_*`, `included_*`, and `omitted_*` history/current id splits, and the existing `build_context(...)` passthrough is regression-locked to preserve that payload unchanged for the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity supersession fixture.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", ..., superseded_only=True)` now explicitly shows stale `Alice` included in the filtered history receipt view while the current `Alice Chen` winner is recorded as omitted from both the base history and base current sets, instead of leaving the superseded-only path without any receipt-visible current-vs-history split.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_superseded_temporal_subset -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_superseded_history_subset -q` passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 298 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 142 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, and the explicit current-vs-history receipt split still covers only selected-current, abstained-current, dropped-current, and superseded-only filtered point-in-time views rather than every temporal subset.
- Next safe slice: only if a real consumer needs it, extend the same additive receipt-metadata split to one other non-current point-in-time filter such as `learned_only=True`, `unlearned_only=True`, or `future_only=True` before any schema or external graph work.

## 2026-07-03T04:47:30Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive current-vs-history receipt-metadata contract to the existing `query_at(..., current_resolution="dropped")` point-in-time path, without changing SQLite schema, temporal projection rules, write receipts, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: dropped-current point-in-time snapshots now carry `current_history_receipt_metadata` with explicit `base_*`, `included_*`, and `omitted_*` history/current id splits, and `build_context(...)` is regression-locked to preserve that payload unchanged for both the populated `Deploy target is Render.` versus `Deploy target is Railway.` dropped-current contradiction fixture and the empty `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity guard.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", ..., current_resolution="dropped")` now explicitly shows the dropped `Render` loser included in the filtered history/current receipt view while the selected current `Railway` winner is recorded as omitted; the same dropped-only filter on the resolved `Alice` -> `Alice Chen` identity path now stays empty but still records both base history ids and the base current `Alice Chen` id as omitted instead of hiding that split entirely.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_dropped_current_conflict_subset tests.test_store.MemoryStoreTest.test_query_at_dropped_resolution_stays_empty_for_resolved_identity_history -q` failed first with missing `current_history_receipt_metadata`, then passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_dropped_snapshot_controls_for_current_conflict tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_dropped_empty_subset -q` failed first with the same missing key, then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 297 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 141 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, and the explicit current-vs-history receipt split still covers only the selected-current, abstained-current, and dropped-current point-in-time filters rather than every filtered temporal view.
- Next safe slice: only if a real consumer needs it, extend the same additive receipt-metadata split to one other point-in-time filter outside current-resolution handling before any schema or external graph work.

## 2026-07-03T00:46:19Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive current-vs-history receipt-metadata contract to the existing `query_at(..., current_resolution="abstained")` point-in-time path, without changing SQLite schema, temporal projection rules, write receipts, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: abstained-current point-in-time snapshots now carry `current_history_receipt_metadata` with explicit `base_*`, `included_*`, and `omitted_*` history/current id splits, and `build_context(...)` is regression-locked to preserve that payload unchanged for both the populated unresolved `Incident owner is Alex.` versus `Incident owner is Priya.` contradiction fixture and the empty `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity guard.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", ..., current_resolution="abstained")` now explicitly shows the unrelated current `Runbook owner is Alice.` fact omitted from the filtered current/history receipt view while the abstained `Alex`/`Priya` contenders remain included; the same abstained filter on the resolved `Alice` -> `Alice Chen` identity path now stays empty but still records both base history ids and the base current `Alice Chen` id as omitted instead of hiding that split entirely.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_abstained_current_contradiction_subset tests.test_store.MemoryStoreTest.test_query_at_abstained_resolution_stays_empty_for_resolved_identity_history -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_abstained_snapshot_controls_for_current_contradiction tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_abstained_empty_subset -q` passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 297 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 141 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, and the explicit current-vs-history receipt split still covers only the selected-current and abstained-current point-in-time filters rather than every filtered temporal view.
- Next safe slice: only if a real consumer needs it, extend the same additive receipt-metadata split to one other point-in-time filter such as `current_resolution="dropped"` before any schema or external graph work.

## 2026-07-02T20:47:27Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive current-vs-history receipt-metadata contract to the existing `query_at(..., current_resolution="selected")` point-in-time path, without changing SQLite schema, temporal projection rules, write receipts, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `zerker_memory/runner.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: selected-current point-in-time snapshots now carry `current_history_receipt_metadata` with `base_*`, `included_*`, and `omitted_*` history/current id splits, and `build_context(...)` preserves that payload unchanged for both the populated `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity fixture and the empty unresolved `Incident owner is Alex.` versus `Incident owner is Priya.` contradiction guard.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", ..., search_query="status page owner", current_resolution="selected")` now explicitly shows stale `Alice` omitted from the filtered history while current `Alice Chen` remains included in both current and history receipt views; the same selected-only filter on the unresolved `Alex`/`Priya` contradiction now stays empty but still reports both base current/history contenders as omitted instead of hiding that split entirely.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_selected_current_identity_subset tests.test_store.MemoryStoreTest.test_query_at_selected_resolution_stays_empty_for_unresolved_current_contradiction -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_selected_current_subset tests.test_runner.RunnerTest.test_build_context_preserves_query_at_selected_snapshot_receipt_metadata_for_empty_current_conflict -q` passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py zerker_memory/runner.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 296 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 140 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, and the new current-vs-history receipt split exists only on the selected-current point-in-time path rather than across every filtered temporal view.
- Next safe slice: only if a real consumer needs it, extend the same additive receipt-metadata split to one other point-in-time filter such as `current_resolution="abstained"` before any schema or external graph work.

## 2026-07-02T16:46:35Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive dropped-current ordering metadata contract to the existing `query_at(..., current_resolution="dropped")` point-in-time path, without changing SQLite schema, temporal projection rules, receipt wrappers, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: dropped-current point-in-time snapshots now carry explicit `selection_strategy=dropped_only_v1`, `selection_reason=explicit-dropped-current-filter`, `selected_ids`, `current_ordering`, and `history_ordering`; `build_context(...)` preserves that payload unchanged for both the populated dropped-conflict subset and the empty Alice-versus-Alice-Chen identity guard.
- Focused fixtures: the resolved `Deploy target is Render.` versus `Deploy target is Railway.` contradiction now locks `current_ordering.selected_current_rankings=[Render]` plus `considered_current_rankings=[Render, Railway]` at `query_at("2024-02-20T00:00:00Z", ..., current_resolution="dropped")`, while the resolved `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity path now stays empty but still surfaces the same explicit dropped-only ordering stubs.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_dropped_current_conflict_subset tests.test_store.MemoryStoreTest.test_query_at_dropped_resolution_stays_empty_for_resolved_identity_history -q` failed first with incomplete dropped-current contender ordering, then passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_dropped_snapshot_controls_for_current_conflict tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_dropped_empty_subset -q` failed first with the same ordering gap, then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 295 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 138 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, no point-in-time current-vs-history receipt metadata split, and no schema-backed bi-temporal edge layer behind same-name disambiguation.
- Next safe slice: only if a real consumer needs it, add one similarly bounded receipt-visible wrapper or metadata contract for another current-vs-history point-in-time view before any schema or external graph work.

## 2026-07-01T11:30:40Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive history-ordering metadata contract to the existing `query_at(..., future_only=True)` point-in-time path, without changing SQLite schema, temporal projection rules, receipt wrappers, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: future-only point-in-time snapshots now carry explicit `history_ordering` metadata with `basis=historical_selection_rank`, `source=temporal_history_selection`, and `reason=explicit-future-only-filter`; `build_context(...)` preserves that payload unchanged for both populated pre-active future snapshots and the empty Alice-versus-Alice-Chen post-activation guard.
- Focused fixtures: the cross-session `Status page owner is Alice.` -> future `Status page owner is Alice Chen.` snapshot now locks deterministic `history_ordering.selected_history_rankings=[Alice Chen]` at `query_at("2024-01-20T00:00:00Z", ..., future_only=True)`, while the same filtered query at `2024-02-20T00:00:00Z` now stays empty but still surfaces the same explicit future-only ordering stub after `Alice Chen` becomes current.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_future_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_future_only_stays_empty_when_identity_successor_is_already_current -q` failed first with missing `history_ordering`, then passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_future_subset tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_future_empty_subset -q` failed first with the same missing key, then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 293 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 134 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, no current-vs-history receipt metadata split for point-in-time snapshots, and no schema-backed bi-temporal edge layer behind same-name disambiguation.
- Next safe slice: only if a real consumer needs it, add one similarly bounded receipt-visible wrapper or metadata contract for point-in-time current-vs-history snapshots before any schema or external graph work.

## 2026-07-01T07:28:42Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive history-ordering metadata contract to the existing `query_at(..., learned_only=True)` point-in-time path, without changing SQLite schema, temporal projection rules, receipt wrappers, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: learned-only point-in-time snapshots now carry explicit `history_ordering` metadata with `basis=historical_selection_rank`, `source=temporal_history_selection`, and `reason=explicit-learned-only-filter`; `build_context(...)` preserves that payload unchanged for both populated learned-history snapshots and the empty Alice-versus-Alice-Chen identity guard.
- Focused fixtures: the agent-proposed `Status page owner is Alice Chen.` learned candidate now locks deterministic `history_ordering.selected_history_rankings=[Alice Chen]` at `query_at("2024-01-20T00:00:00Z", ..., learned_only=True)`, while the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity guard now stays empty but still surfaces the same explicit learned-only ordering stub.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_learned_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_learned_only_stays_empty_for_identity_supersession_history -q` failed first with missing `history_ordering`, then passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_bitemporal_identity_metadata_for_learned_only_query_envelopes tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_learned_empty_subset -q` failed first with the same missing key, then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 292 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 133 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, no matching ordering payload on the future-only filter, and no schema-backed bi-temporal edge layer behind same-name disambiguation.
- Next safe slice: only if a real consumer needs it, add one similarly bounded ordering payload for another non-current filtered view such as `query_at(..., future_only=True)` before any receipt-wrapper or schema work.

## 2026-07-01T03:31:42Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive history-ordering metadata contract to the existing `query_at(..., unlearned_only=True)` point-in-time path, without changing SQLite schema, temporal projection rules, receipt wrappers, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: unlearned-only point-in-time snapshots now carry explicit `history_ordering` metadata with `basis=historical_selection_rank`, `source=temporal_history_selection`, and `reason=explicit-unlearned-only-filter`; `build_context(...)` preserves that payload unchanged for both populated non-active history snapshots and the empty Alice-versus-Alice-Chen identity guard.
- Focused fixtures: the revoked `Release checklist owner was Alex.`, forgotten `Temporary outage snack was ramen.`, and rejected `Release freeze owner is Mallory.` non-active set now locks deterministic `history_ordering.selected_history_rankings=[Alex, ramen, Mallory]` at `query_at("2024-02-20T00:00:00Z", ..., unlearned_only=True)`, while the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity guard now stays empty but still surfaces the same explicit unlearned-only ordering stub.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_unlearned_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_unlearned_only_stays_empty_for_identity_supersession_history -q` failed first with missing `history_ordering`, then passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_unlearned_selection_metadata_for_query_at_subset tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_unlearned_empty_subset -q` failed first with the same missing key, then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 292 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 133 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, no matching ordering payload on the learned-only or future-only filters, and no schema-backed bi-temporal edge layer behind same-name disambiguation.
- Next safe slice: only if a real consumer needs it, add one similarly bounded ordering payload for another non-current filtered view such as `query_at(..., learned_only=True)` or `query_at(..., future_only=True)` before any receipt-wrapper or schema work.

## 2026-06-30T23:30:11Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive abstained-current selection/order metadata contract to the existing `query_at(..., current_resolution="abstained")` point-in-time path, without changing SQLite schema, temporal projection rules, receipt wrappers, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: abstained-current point-in-time snapshots now carry explicit `selection_strategy=abstained_only_v1`, `selection_reason=explicit-abstained-current-filter`, `selected_ids`, `current_ordering`, and `history_ordering`; `build_context(...)` preserves that payload unchanged for both populated contradiction snapshots and empty resolved-identity guards.
- Focused fixtures: the same-timestamp `Incident owner is Alex.` vs `Incident owner is Priya.` contradiction now locks `selected_ids` to the existing `abstained_current_memory_ids` conflict order plus explicit current-conflict abstention ordering metadata at `query_at("2024-02-20T00:00:00Z", ..., current_resolution="abstained")`; the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` abstained filter stays empty but now still surfaces the same explicit filter metadata and empty ordering stubs.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_abstained_current_contradiction_subset tests.test_store.MemoryStoreTest.test_query_at_abstained_resolution_stays_empty_for_resolved_identity_history -q` failed first with missing `selection_strategy`/ordering metadata, then passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_abstained_snapshot_controls_for_current_contradiction tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_abstained_empty_subset -q` failed first with the same missing keys, then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 291 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 132 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no `query_at(...)` receipt wrapper, no dedicated history-ordering payload for the other non-current filters, and no schema-backed bi-temporal edge layer behind contradiction vs supersession.
- Next safe slice: only if a real consumer needs it, add one similarly bounded ordering payload for another existing filtered point-in-time view such as `query_at(..., unlearned_only=True)` before any receipt-wrapper or schema work.

## 2026-06-30T19:26:49Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive unlearned-selection metadata contract to the existing `query_at(..., unlearned_only=True)` point-in-time path, without changing SQLite schema, temporal projection rules, receipt wrappers, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: unlearned-only point-in-time snapshots now carry explicit `selection_strategy=unlearned_only_v1`, `selection_reason=explicit-unlearned-only-filter`, and `selected_ids`; `build_context(...)` preserves that payload unchanged for both populated unlearned-history snapshots and empty identity-guard snapshots.
- Focused fixtures: the revoked `Release checklist owner was Alex.`, forgotten `Temporary outage snack was ramen.`, and rejected `Release freeze owner is Mallory.` non-active set now locks `selected_ids=[Alex, ramen, Mallory]` at `query_at("2024-02-20T00:00:00Z", ..., unlearned_only=True)`, while the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity guard now stays empty but still surfaces the same explicit unlearned-only filter metadata; the active `Status page owner is Alice Chen.` fact stays excluded.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_unlearned_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_unlearned_only_stays_empty_for_identity_supersession_history -q` failed first with missing `selected_ids`/selection metadata, then passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_unlearned_selection_metadata_for_query_at_subset tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_unlearned_empty_subset -q` failed first with the same missing keys, then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 291 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 131 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `unlearned_ordering` payload, no `query_at(...)` receipt wrapper, and no schema-backed bi-temporal edge layer behind same-name disambiguation.
- Next safe slice: only if a real consumer needs it, add one similarly bounded point-in-time selection-metadata contract for another non-current subset such as `query_at(..., current_resolution="abstained")` or a dedicated `unlearned_ordering` payload before any receipt-wrapper or schema work.

## 2026-06-30T15:25:20Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive learned-selection metadata contract to the existing `query_at(..., learned_only=True)` point-in-time path, without changing SQLite schema, temporal projection rules, receipt wrappers, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: learned-only point-in-time snapshots now carry explicit `selection_strategy=learned_only_v1`, `selection_reason=explicit-learned-only-filter`, and `selected_ids`; `build_context(...)` preserves that payload unchanged for both populated and empty learned-only identity snapshots.
- Focused fixtures: the agent-proposed `Status page owner is Alice Chen.` learned candidate now locks `selected_ids=[Alice Chen]` at `query_at("2024-02-20T00:00:00Z", ..., learned_only=True)`, while the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity guard now stays empty but still surfaces the same explicit learned-only filter metadata; the unrelated same-name billing-exporter Alice fact stays excluded.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_learned_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_learned_only_stays_empty_for_identity_supersession_history -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_bitemporal_identity_metadata_for_learned_only_query_envelopes tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_learned_empty_subset -q` passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 289 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 129 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `learned_ordering` payload, no `query_at(...)` receipt wrapper, and no schema-backed bi-temporal edge layer behind same-name disambiguation.
- Next safe slice: only if a real consumer needs it, add one similarly bounded point-in-time selection-metadata contract for another non-current subset such as `query_at(..., unlearned_only=True)` before any receipt-wrapper or schema work.

## 2026-06-30T11:26:07Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive future-selection metadata contract to the existing `query_at(..., future_only=True)` point-in-time path, without changing SQLite schema, temporal projection rules, receipt wrappers, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `future_only` point-in-time snapshots now carry explicit `selection_strategy=future_only_v1`, `selection_reason=explicit-future-only-filter`, and `selected_ids`; `build_context(...)` preserves that payload unchanged for both populated and empty future-only identity snapshots.
- Focused fixtures: the cross-session `Status page owner is Alice.` -> future `Status page owner is Alice Chen.` identity snapshot now locks `selected_ids=[Alice Chen]` at `query_at("2024-01-20T00:00:00Z", ..., future_only=True)`, while the post-activation `query_at("2024-02-20T00:00:00Z", ..., future_only=True)` guard now stays empty but still surfaces the same explicit filter metadata; the unrelated same-name billing-exporter Alice fact stays excluded.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_future_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_future_only_stays_empty_when_identity_successor_is_already_current -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_future_subset tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_future_empty_subset -q` passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 287 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 127 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `future_ordering` payload, no `query_at(...)` receipt wrapper, and no schema-backed bi-temporal edge layer behind same-name disambiguation.
- Next safe slice: only if a real consumer needs it, add one similarly bounded point-in-time selection-metadata contract for another non-current subset such as `query_at(..., learned_only=True)` before any receipt-wrapper or schema work.

## 2026-06-30T03:23:18Z - L1 temporal-kg - Codex L1 worker

- Scope: forwarded one remaining additive bi-temporal receipt field into `zerker.memory_context.v1`, without changing store projection, retrieval selection, point-in-time query semantics, receipt generation, or SQLite schema.
- Files touched: `zerker_memory/runner.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `build_context(...)` now preserves the full receipt/query `temporal_graph` map alongside the existing subset graphs, so runtime consumers can inspect the same explicit `learned_at`, `valid_from`, `valid_to`, `superseded_at`, `superseded_by_ids`, `unlearned_at`, and `serial` envelopes already present in local temporal receipts.
- Focused fixture: the existing Alice-versus-Alice-Chen cross-session history fixture now regression-locks the top-level runtime `temporal_graph` itself, proving `Status page owner is Alice.` stays `superseded` with `valid_to=superseded_at=2024-02-01T00:00:00Z`, `Status page owner is Alice Chen.` stays `current` with open-ended validity, and the unrelated same-name billing-exporter Alice fact stays excluded.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_full_bitemporal_identity_temporal_graph -q` passed (`Ran 1 test`); `git diff --check -- zerker_memory/runner.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q`; `python3 -m unittest tests.test_runner -q`; `python3 -m zerker_memory eval`.
- Artifacts/receipts: none.
- Blockers: this is still a runtime passthrough slice only; there is still no dedicated `query_at(...)` receipt wrapper, no new history-only runtime contract on filtered point-in-time queries, and no schema-backed graph edge layer behind identity disambiguation.
- Next safe slice: if a real consumer needs it, lock one similarly bounded `build_context(...)` passthrough contract for a filtered point-in-time history view such as `query_at(..., superseded_only=True)` before any receipt-wrapper or schema work.

## 2026-06-30T07:27:01Z - L1 temporal-kg - Codex L1 worker

- Scope: added one additive history-ordering metadata contract to the existing `query_at(..., superseded_only=True)` point-in-time path, without changing SQLite schema, temporal selection logic, receipt wrappers, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: successful superseded-only point-in-time snapshots now carry explicit `history_ordering` metadata with `basis=historical_selection_rank`, `source=temporal_history_selection`, and `reason=explicit-superseded-only-filter`; `build_context(...)` preserves that payload unchanged because it already forwards `history_ordering`.
- Focused fixtures: the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity snapshot now locks `history_ordering.selected_history_rankings=[{"memory_id": Alice, "rank": 1}]` on the superseded-only path while excluding the unrelated same-name billing-exporter Alice fact; the no-parent-link `Deploy target is Staging.` -> `Deploy target changed to Production.` update-history fixture now locks the same superseded-only ordering contract for explicit-update history.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_superseded_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_superseded_only_keeps_explicit_update_history_without_current_winner -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_superseded_history_subset -q` passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 285 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 125 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a query-surface metadata slice only; there is still no dedicated `query_at(...)` receipt wrapper, no comparable ordering contract on other filtered history-only views, and no schema-backed bi-temporal edge layer behind same-name disambiguation.
- Next safe slice: only if a real consumer needs it, add one similarly bounded point-in-time ordering contract for another filtered history view such as `query_at(..., future_only=True)` or `query_at(..., learned_only=True)` before any receipt-wrapper or schema work.

## 2026-06-29T23:25:45Z - L1 temporal-kg - Codex L1 worker

- Scope: exposed one bounded point-in-time selection-metadata contract on the existing `query_at(..., current_resolution="selected")` identity path, without changing SQLite schema, receipt wrappers, runner logic, retrieval selection, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: successful selected-current `query_at(...)` snapshots now surface additive `selection_strategy`, `selection_reason`, `selected_ids`, `current_ordering`, and `history_ordering` metadata derived from the existing local temporal selector, so point-in-time consumers can read the same current-only ordering stub shape that receipt/runtime paths already use.
- Focused fixture: a `query_at("2024-02-20T00:00:00Z", search_query="status page owner", current_resolution="selected")` snapshot now keeps `selected_ids=[Alice Chen]`, `selection_strategy=current_only_v1`, `selection_reason=default-current-only`, `current_ordering.selected_current_rankings=[{"memory_id": Alice Chen, "rank": 2}]`, and an explicit empty `history_ordering` stub while the stale `Status page owner is Alice.` fact and the unrelated same-name `Alice owns the billing exporter.` fact stay excluded.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_selected_current_identity_subset -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_selected_current_subset -q` passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 283 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 124 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a narrow point-in-time metadata slice; there is still no `query_at(...)` receipt wrapper, no matching history-focused ordering surface on other point-in-time filters, and no schema-backed bi-temporal graph edge layer behind same-name disambiguation.
- Next safe slice: only if a real consumer needs it, add one similarly bounded point-in-time ordering contract for a history-focused identity filter such as `query_at(..., superseded_only=True)` before widening into receipt wrappers or schema work.

## 2026-06-29T15:22:28Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded runtime current-only identity ordering passthrough contract on the existing Alice-versus-Alice-Chen receipt path, without changing store logic, runner behavior, query semantics, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `zerker.memory_context.v1` is now explicitly regression-tested to preserve the shipped current-only `current_ordering` payload plus the empty `history_ordering` stub for the cross-session identity guard path.
- Focused fixture: a `current status page owner` receipt now stays explicit through runtime context: only `Status page owner is Alice Chen.` remains selected, `current_ordering` keeps the baseline `retrieval_rank` metadata, `history_ordering` stays present but unapplied with empty rankings, and both the stale `Status page owner is Alice.` fact and the unrelated same-name `Alice owns the billing exporter.` fact stay excluded from `selected_temporal_graph`.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_current_only_identity_ordering_metadata -q` passed (`Ran 1 test`); `git diff --check -- tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 279 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 122 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a contract-only runtime slice; there is still no point-in-time `query_at(...)` receipt wrapper, no schema-backed bi-temporal graph edge layer, and no first-class entity anchor model behind same-name disambiguation.
- Next safe slice: only if a real consumer needs it, lock one similarly bounded runtime passthrough contract for the companion current-only point-in-time identity snapshot before widening into receipt wrappers or schema work.

## 2026-06-29T11:23:01Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded current-vs-history receipt/runtime ordering contract on the existing Alice-versus-Alice-Chen identity fixture, without changing store logic, runner behavior, query semantics, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; existing `retrieval.temporal.current_ordering` and `retrieval.temporal.history_ordering` metadata are now explicitly regression-tested on the shipped current-vs-history identity path, and `zerker.memory_context.v1` is now explicitly regression-tested to preserve that ordering metadata unchanged for the history-focused receipt path.
- Focused fixture: the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` receipt path now locks current-only metadata (`current_only_v1`, baseline `current_ordering`, empty `history_ordering`) plus history metadata (`historical_preferred_v1`, baseline `current_ordering`, applied `history_ordering`) while the unrelated same-name `Alice owns the billing exporter.` fact stays excluded; runtime context now preserves the same `current_ordering` and `history_ordering` payloads unchanged when built from the history-focused receipt.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_temporal_contract_current_vs_history_keeps_identity_disambiguation -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_current_vs_history_ordering_metadata -q` passed (`Ran 1 test`); `git diff --check -- tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 277 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 120 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a contract-only metadata slice; there is still no point-in-time receipt wrapper for `query_at(...)`, no schema-backed bi-temporal graph edge layer, and no first-class entity anchor model behind same-name disambiguation.
- Next safe slice: only if a runtime consumer needs it, lock one similarly bounded runner passthrough contract for the companion current-only identity receipt so baseline `current_ordering` plus the empty `history_ordering` stub stay explicit end to end before widening into receipt wrappers or schema work.

## 2026-06-29T07:18:55Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded runtime unlearned-only point-in-time identity passthrough contract on top of the existing `query_at(..., unlearned_only=True)` snapshot surface, without changing store logic, runner behavior, query semantics, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `zerker.memory_context.v1` is now explicitly regression-tested to preserve additive point-in-time `unlearned_only=True` snapshot controls plus the existing empty non-active subset graphs for the cross-session identity guard path.
- Focused fixture: a `query_at("2024-02-20T00:00:00Z", search_query="status page owner", unlearned_only=True)` snapshot now stays explicit through runtime context: `history_memory_ids`, `current_memory_ids`, `future_memory_ids`, `superseded_memory_ids`, `unlearned_memory_ids`, `learned_memory_ids`, and every temporal subset graph remain empty while the supersession pair `Status page owner is Alice.` -> `Status page owner is Alice Chen.` and the unrelated same-name `Alice owns the billing exporter.` fact stay excluded.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_unlearned_empty_subset -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_unlearned_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_unlearned_only_stays_empty_for_identity_supersession_history tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_unlearned_empty_subset -q` passed (`Ran 3 tests`); `git diff --check -- tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 276 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 118 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a contract-only runtime slice; unlearned-only point-in-time snapshots still do not have a dedicated receipt wrapper, a populated runtime non-active envelope path from live retrieval, or any schema-backed bi-temporal graph layer behind same-name disambiguation.
- Next safe slice: only if a runtime consumer needs it, lock one similarly bounded passthrough contract for a populated `query_at(..., unlearned_only=True)` non-active subset before widening into receipt wrappers or schema work.

## 2026-06-29T03:17:54Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded runtime future-only point-in-time identity passthrough contract on top of the existing `query_at(..., future_only=True)` snapshot surface, without changing store logic, runner behavior, query semantics, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `zerker.memory_context.v1` is now explicitly regression-tested to preserve additive point-in-time `future_only=True` snapshot controls plus the existing future-envelope metadata for the cross-session identity path.
- Focused fixture: a `query_at("2024-01-20T00:00:00Z", search_query="status page owner", future_only=True)` snapshot now stays explicit through runtime context: only the future `Status page owner is Alice Chen.` envelope remains in `future_temporal_graph`, while the current `Status page owner is Alice.` fact, the learned `Release freeze owner is Mallory.` fact, and the unrelated same-name `Alice owns the billing exporter.` fact stay excluded and the future envelope keeps `valid_from`, `superseded_by_ids`, `temporal_state`, and the query-control metadata itself.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_future_subset -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_future_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_future_only_stays_empty_when_identity_successor_is_already_current tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_future_subset -q` passed (`Ran 3 tests`); `git diff --check -- tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 273 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 116 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a contract-only runtime slice; future-only point-in-time snapshots still do not have a dedicated receipt wrapper, explicit ordering metadata beyond the existing filtered temporal payload, or any schema-backed bi-temporal graph layer behind same-name disambiguation.
- Next safe slice: only if a runtime consumer needs it, lock one similarly bounded passthrough contract for the existing `query_at(..., unlearned_only=True)` or another filtered identity view before widening into receipt wrappers or schema work.

## 2026-06-28T23:16:37Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded runtime hidden-contradiction point-in-time passthrough contract on top of the existing `query_at(..., include_abstained_current=False)` snapshot surface, without changing store logic, runner behavior, query semantics, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `zerker.memory_context.v1` is now explicitly regression-tested to preserve hidden-current point-in-time snapshot controls plus the existing unresolved-current contradiction metadata when abstained envelopes are intentionally suppressed.
- Focused fixture: a `query_at("2024-02-20T00:00:00Z", search_query="incident owner", include_abstained_current=False)` snapshot now stays explicit through runtime context: `history_temporal_graph`, `current_temporal_graph`, `selected_temporal_graph`, `abstained_temporal_graph`, and `dropped_current_temporal_graph` all remain empty while `abstained_current_memory_ids`, `abstention`, and the `lexical-current-conflict` conflict set still preserve the hidden `Incident owner is Alex.` and `Incident owner is Priya.` contradiction metadata, and the unrelated `Runbook owner is Alice.` fact stays excluded.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_hidden_query_at_abstained_snapshot_controls_for_current_contradiction -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_hidden_query_at_abstained_snapshot_controls_for_current_contradiction tests.test_runner.RunnerTest.test_build_context_preserves_query_at_abstained_snapshot_controls_for_current_contradiction tests.test_store.MemoryStoreTest.test_query_at_can_hide_abstained_current_envelopes_while_preserving_conflict_metadata -q` passed (`Ran 3 tests`); `git diff --check -- tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 271 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 114 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a contract-only runtime slice; hidden-current point-in-time contradictions still do not have a dedicated receipt wrapper, explicit ordering metadata beyond the existing filtered temporal payload, or any schema-backed bi-temporal graph layer for contradiction vs supersession.
- Next safe slice: only if a runtime consumer needs it, lock one similarly bounded passthrough contract for a point-in-time hidden-current resolved path before widening into receipt wrappers or schema work.

## 2026-06-28T19:17:18Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded runtime contradiction-focused point-in-time passthrough contract on top of the existing `query_at(..., current_resolution="abstained")` snapshot surface, without changing store logic, runner behavior, query semantics, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `zerker.memory_context.v1` is now explicitly regression-tested to preserve additive point-in-time `current_resolution="abstained"` snapshot controls plus the existing contradiction envelope metadata for the shipped unresolved-current conflict path.
- Focused fixture: a `query_at("2024-02-20T00:00:00Z", search_query="incident owner", current_resolution="abstained")` snapshot now stays explicit through runtime context: only the conflicting `Incident owner is Alex.` and `Incident owner is Priya.` envelopes remain in `history_temporal_graph`, `current_temporal_graph`, and `abstained_temporal_graph`, while the unrelated `Runbook owner is Alice.` fact stays excluded and both contradiction envelopes keep `current_resolution=abstained`, `temporal_resolution_kind=contradiction`, `temporal_resolution_reasons=["lexical-current-conflict"]`, and the existing abstention/conflict-set metadata.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_abstained_snapshot_controls_for_current_contradiction -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_abstained_snapshot_controls_for_current_contradiction tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_superseded_history_subset tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_abstained_current_contradiction_subset -q` passed (`Ran 3 tests`); `git diff --check -- tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 270 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 112 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a contract-only runtime slice; point-in-time abstained snapshots still do not surface a dedicated receipt wrapper or ordering metadata beyond the existing filtered temporal payload, and there is still no schema-backed bi-temporal graph edge layer behind contradiction vs supersession.
- Next safe slice: only if a runtime consumer needs it, lock one similarly bounded passthrough contract for `query_at(..., include_abstained_current=False)` so hidden-current contradiction views stay explicit end to end before widening into receipt wrappers or schema work.

## 2026-06-28T15:15:00Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded runtime superseded-history point-in-time passthrough contract on top of the existing `query_at(..., superseded_only=True)` snapshot surface, without changing store logic, runner behavior, query semantics, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `zerker.memory_context.v1` is now explicitly regression-tested to preserve additive point-in-time `superseded_only` snapshot controls plus the existing supersession envelope metadata for the cross-session identity path.
- Focused fixture: a `query_at("2024-02-20T00:00:00Z", search_query="status page owner", superseded_only=True)` snapshot now stays explicit through runtime context: only the stale `Status page owner is Alice.` envelope remains in `history_temporal_graph` and `superseded_temporal_graph`, while the current `Status page owner is Alice Chen.` fact and the unrelated same-name `Alice owns the billing exporter.` fact stay excluded and the stale envelope keeps `valid_from`, `valid_to`, `superseded_at`, `superseded_by_ids`, `temporal_resolution_kind`, and `temporal_resolution_reasons`.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_superseded_history_subset -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_superseded_history_subset tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_selected_current_subset tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_superseded_temporal_subset -q` passed (`Ran 3 tests`); `git diff --check -- tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 268 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 110 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a contract-only runtime slice; there is still no first-class entity anchor model, no new receipt wrapper for point-in-time history filters, and no schema-backed bi-temporal graph edge layer behind same-name disambiguation.
- Next safe slice: only if a runtime consumer needs it, lock one similarly bounded contradiction-focused `query_at(..., current_resolution="abstained")` runtime contract before widening into schema or typed-edge work.

## 2026-06-28T11:14:55Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded runtime point-in-time query-control passthrough contract on top of the existing `query_at(...)` snapshot surface, without changing store logic, query semantics, schema, retrieval selection, or external graph dependencies.
- Files touched: `zerker_memory/runner.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `zerker.memory_context.v1` now preserves additive point-in-time snapshot controls when runtime context is built from `query_at(...)`, forwarding `query_at`, `scope`, `search_query`, `include_abstained_current`, `current_resolution`, `learned_only`, `unlearned_only`, `superseded_only`, and `future_only` beside the existing temporal subset graphs.
- Focused fixture: a `query_at("2024-02-20T00:00:00Z", search_query="status page owner", current_resolution="selected")` snapshot now stays explicit through runtime context: only the cross-session current `Status page owner is Alice Chen.` envelope remains in `history_temporal_graph`, `current_temporal_graph`, and `selected_temporal_graph`, while the stale `Status page owner is Alice.` fact and the unrelated same-name `Alice owns the billing exporter.` fact stay excluded and the selected-current filter metadata itself remains visible to the consumer.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_selected_current_subset -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_selected_current_subset tests.test_runner.RunnerTest.test_build_context_preserves_bitemporal_identity_metadata_for_learned_only_query_envelopes tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_selected_current_identity_subset -q` passed (`Ran 3 tests`); `git diff --check -- zerker_memory/runner.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 266 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 108 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a runtime-only contract slice; there is still no first-class entity anchor model, no new receipt-side `query_at(...)` wrapper beyond the existing temporal payloads, and no schema-backed bi-temporal graph edge layer behind same-name disambiguation.
- Next safe slice: only if a runtime consumer needs it, add one similarly bounded contract for a history-focused `query_at(...)` filter such as `superseded_only=True` or `current_resolution="abstained"` before widening into schema or typed-edge work.

## 2026-06-28T07:13:58Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded runtime learned-only bi-temporal identity passthrough contract on top of the existing `query_at(..., learned_only=True)` projection, without changing store logic, runner behavior, query semantics, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `zerker.memory_context.v1` is now explicitly regression-tested to preserve point-in-time learned-only `history_temporal_graph` / `learned_temporal_graph` envelope metadata for the existing cross-session identity path.
- Focused fixture: a learned-only `Status page owner is Alice Chen.` proposed memory now stays isolated from the current `Status page owner is Alice.` fact and the unrelated same-name `Alice owns the billing exporter.` fact when a `query_at(..., learned_only=True)` snapshot is passed through runtime context, while preserving `learned_at`, `valid_from`, `valid_to`, `superseded_at`, `superseded_by_ids`, `status_at_query`, and `temporal_state`.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_bitemporal_identity_metadata_for_learned_only_query_envelopes -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_learned_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_learned_only_stays_empty_for_identity_supersession_history tests.test_runner.RunnerTest.test_build_context_preserves_bitemporal_identity_metadata_for_learned_only_query_envelopes -q` passed (`Ran 3 tests`); `git diff --check -- tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 264 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 106 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a contract-only runtime slice; there is still no first-class entity anchor model, no live inject path for learned-only envelopes, and no new schema-backed bi-temporal edge layer behind same-name disambiguation.
- Next safe slice: only if a runtime consumer needs more than this passthrough, add one similarly bounded point-in-time future/learned query receipt bridge before any schema or typed-edge work.

## 2026-06-28T03:13:36Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded runtime current-vs-future bi-temporal identity contract on the existing receipt/context projection, without changing store logic, runner behavior, query surfaces, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `zerker.memory_context.v1` is now explicitly regression-tested to preserve receipt-side `learned_at`, `valid_from`, `valid_to`, `superseded_at`, `superseded_by_ids`, `status_at_query`, and `temporal_state` metadata across `current_temporal_graph`, `future_temporal_graph`, and `selected_temporal_graph` for the existing current-vs-future identity path.
- Focused fixture: the cross-session `Status page owner is Alice.` -> future `Status page owner is Alice Chen.` identity fixture now locks those bi-temporal fields end to end in runtime context while excluding the unrelated same-name `Alice owns the billing exporter.` fact from the current, future, and selected subset graphs.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_bitemporal_identity_metadata_for_current_vs_future_envelopes -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_bitemporal_identity_metadata_for_current_vs_history_envelopes tests.test_runner.RunnerTest.test_build_context_preserves_bitemporal_identity_metadata_for_current_vs_future_envelopes -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 262 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 104 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a contract-only runtime slice; there is still no first-class entity anchor model, no learned-only runtime identity contract, and no new point-in-time query or receipt surface behind same-name disambiguation.
- Next safe slice: only if a real runtime consumer needs it, add one similarly bounded learned-only identity envelope passthrough contract before any schema or typed-edge work.

## 2026-06-27T23:12:03Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded runtime current-vs-history bi-temporal identity contract on the existing receipt/context projection, without changing store logic, runner behavior, query surfaces, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `zerker.memory_context.v1` is now explicitly regression-tested to preserve receipt-side `learned_at`, `valid_from`, `valid_to`, `superseded_at`, `superseded_by_ids`, and `temporal_resolution_kind` metadata across `history_temporal_graph` and `selected_temporal_graph` for the existing current-vs-history identity path.
- Focused fixture: the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity fixture now locks those bi-temporal fields end to end in runtime context while excluding the unrelated same-name `Alice owns the billing exporter.` fact from both the selected and history subset graphs.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_bitemporal_identity_metadata_for_current_vs_history_envelopes -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_current_vs_history_temporal_envelopes tests.test_runner.RunnerTest.test_build_context_preserves_temporal_serial_metadata_for_current_vs_history_envelopes tests.test_runner.RunnerTest.test_build_context_preserves_bitemporal_identity_metadata_for_current_vs_history_envelopes -q` passed (`Ran 3 tests`); `git diff --check -- tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 260 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 102 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a contract-only runtime slice; there is still no first-class entity anchor model behind same-name disambiguation, and no new point-in-time query or receipt surface was added in this run.
- Next safe slice: only if a real runtime consumer needs more than the shared current-vs-history path, add one similarly bounded field-level contract for learned/future identity envelopes before any schema or typed-edge work.

## 2026-06-27T19:12:08Z - L1 temporal-kg - Codex L1 worker

- Scope: added one read-only point-in-time learned-history filter on top of the existing temporal projection, without changing schema, receipt/runtime payloads, retrieval selection, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.query_at(..., learned_only=True)` now returns only the already-projected proposed/quarantined envelopes and companion subset graphs, so callers can read non-active learned facts directly from `temporal_graph` / `learned_temporal_graph` instead of reconstructing them from mixed current/history snapshots.
- Focused fixtures: one mixed-state fixture now locks a learned-only snapshot that keeps only proposed `Release freeze owner is Mallory.` while excluding the current `Status page owner is Alice.` and future `Status page owner is Alice Chen.` successor; the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity fixture now locks the empty learned-only guard so same-name current/superseded history does not leak into proposed/quarantined-only views.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_learned_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_learned_only_stays_empty_for_identity_supersession_history tests.test_store.MemoryStoreTest.test_query_at_rejects_superseded_only_conflicting_filters tests.test_store.MemoryStoreTest.test_query_at_rejects_unlearned_only_current_resolution_combination -q` passed (`Ran 4 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 258 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 100 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains query-surface-only and covers only local proposed/quarantined history; there is still no first-class graph/entity anchor model or matching receipt/runtime focused learned-only subset outside `query_at(...)`.
- Next safe slice: only if a real consumer needs it, mirror this same learned-only subset into receipt/runtime temporal metadata before widening into schema or typed-edge work.

## 2026-06-27T15:10:50Z - L1 temporal-kg - Codex L1 worker

- Scope: added one read-only point-in-time future-only filter on top of the existing temporal projection, without changing schema, receipt/runtime payloads, retrieval selection, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.query_at(..., future_only=True)` now returns only the already-projected pre-active future envelopes and companion subset graphs, so callers can read scheduled-but-not-yet-valid facts directly from `temporal_graph` / `future_temporal_graph` instead of reconstructing them from mixed current/history snapshots.
- Focused fixtures: the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity fixture now locks a future-only snapshot at `2024-01-20T00:00:00Z` that keeps only the pre-active `Alice Chen` successor while excluding both the current `Alice` owner and the unrelated billing-exporter Alice fact; the mixed-state `Release freeze owner is Mallory.` + `Status page owner is Alice.` + `Status page owner is Alice Chen.` fixture now proves learned/current entries stay out of the same future-only surface.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_future_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_future_only_stays_empty_when_identity_successor_is_already_current tests.test_store.MemoryStoreTest.test_query_at_rejects_superseded_only_conflicting_filters -q` passed (`Ran 3 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 255 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 100 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains query-surface-only and covers only future pre-active state; there is still no first-class graph/entity anchor model or matching focused filter for learned-only snapshots.
- Next safe slice: only if a point-in-time caller needs it, add one similarly bounded `learned_only` filter before widening into schema or typed-edge work.

## 2026-06-27T11:09:09Z - L1 temporal-kg - Codex L1 worker

- Scope: added one read-only point-in-time superseded-history filter on top of the existing temporal projection, without changing schema, receipt/runtime payloads, retrieval selection, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.query_at(..., superseded_only=True)` now returns only the already-projected superseded envelopes and companion subset graphs, so callers can read stale-only history directly from `temporal_graph` / `superseded_temporal_graph` instead of reconstructing it from mixed current/history snapshots.
- Focused fixtures: the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity fixture now locks a superseded-only snapshot that keeps the stale Alice owner plus `superseded_by_ids=[Alice Chen]` while excluding the unrelated billing-exporter Alice fact; the no-parent-link `Deploy target is Staging.` -> `Deploy target changed to Production.` fixture now locks the same superseded-only path for explicit-update history.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_superseded_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_superseded_only_keeps_explicit_update_history_without_current_winner tests.test_store.MemoryStoreTest.test_query_at_rejects_superseded_only_conflicting_filters -q` passed (`Ran 3 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 253 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 100 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains query-surface-only and covers only superseded history; there is still no first-class graph/entity anchor model or matching point-in-time focused filter for pre-active `learned` / `future` states.
- Next safe slice: only if a point-in-time caller needs it, add one similarly bounded `learned` or `future` focused filter before widening into schema or typed-edge work.

## 2026-06-27T07:08:32Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded query-surface-only filtered subset-map contract on top of the existing point-in-time temporal projection, without changing schema, receipt projection, runner behavior, retrieval selection, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.query_at(...)` now materializes additive `selected_temporal_graph`, `abstained_temporal_graph`, and `dropped_current_temporal_graph` subset maps from the already-filtered point-in-time `temporal_graph`, so point-in-time consumers can read explicit current-winner, contradiction-only, and dropped-loser envelopes without reconstructing them from ids or reusing receipt-only terminology.
- Focused fixtures: the same-timestamp `Incident owner is Alex.` versus `Incident owner is Priya.` contradiction now locks the filtered `abstained_temporal_graph` with `serial=1` then `serial=2`; the resolved `Status page owner is Alice.` versus `Status page owner is Alice Chen.` identity fixture now locks `selected_temporal_graph` to only `Alice Chen`; the resolved `Deploy target is Render.` versus `Deploy target is Railway.` conflict now locks `dropped_current_temporal_graph` to only the dropped `Render` envelope at `serial=1`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_abstained_current_contradiction_subset tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_selected_current_identity_subset tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_dropped_current_conflict_subset -q` passed (`Ran 3 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 247 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 99 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: point-in-time history/current/future subset maps are now aligned with filtered current-resolution subset maps, but query-time contradiction/supersession still ride on the existing lexical conflict plus parent-link contracts rather than typed graph edges or persisted bi-temporal tables.
- Next safe slice: if a real caller needs it, add one similarly bounded query-time identity/disambiguation fixture that proves `selected_temporal_graph` stays stable across same-name cross-session current conflicts before any schema or graph-edge work.

## 2026-06-27T03:08:08Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one additional bounded point-in-time contract for already-shipped filtered `query_at(...)` current-conflict views, without changing store projection logic, runner behavior, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `query_at(..., current_resolution="abstained"|"dropped")` is now explicitly regression-tested to preserve the same query-visible `serial` ordering already present in the shared temporal projection, so point-in-time consumers can rely on filtered contradiction/current-loser snapshots too.
- Focused fixtures: the same-timestamp `Incident owner is Alex.` versus `Incident owner is Priya.` contradiction now locks `serial=1` and `serial=2` through the filtered abstained-only `temporal_graph` and `current_temporal_graph`; the resolved `Deploy target is Render.` versus `Deploy target is Railway.` fixture now locks the dropped-only snapshot at `serial=1` while preserving the full-snapshot selected-current comparison at `serial=2`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_abstained_current_contradiction_subset tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_dropped_current_conflict_subset -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 246 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 98 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: filtered `query_at(...)` snapshots still do not materialize dedicated `abstained_temporal_graph` or `dropped_current_temporal_graph` keys the way inject receipts/runtime context do, but this run intentionally stayed contract-only on the existing filtered `temporal_graph` plus `current_temporal_graph` surface.
- Next safe slice: only if point-in-time consumers need more than the filtered shared graph, add one bounded query-surface subset-map contract for abstained or dropped filtered snapshots before any schema or typed-edge work.

## 2026-06-26T23:06:01Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one additional bounded runtime-context contract for already-shipped temporal `serial` envelopes on current-conflict subsets, without changing store projection logic, runner behavior, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `zerker.memory_context.v1` serial passthrough is now explicitly regression-tested for both abstained-current contradiction envelopes and dropped-current deterministic-resolution envelopes, so runtime consumers can rely on the same receipt-emitted ordering across current-conflict subsets too.
- Focused fixtures: the same-timestamp `Incident owner is Alex.` versus `Incident owner is Priya.` contradiction now locks `serial` passthrough through `current_temporal_graph` and `abstained_temporal_graph`; the resolved `Deploy target is Render.` versus `Deploy target is Railway.` fixture now locks the lower dropped-current `serial` and higher selected-current `serial` through `current_temporal_graph`, `dropped_current_temporal_graph`, and `injected_temporal_graph`.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_temporal_serial_metadata_for_abstained_current_conflict tests.test_runner.RunnerTest.test_build_context_preserves_temporal_serial_metadata_for_dropped_current_conflict -q` passed (`Ran 2 tests`); `git diff --check -- tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 244 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 97 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: point-in-time filtered contradiction views still do not have dedicated `serial` assertions on the `query_at(..., current_resolution="abstained"|"dropped")` surface, but that should remain deferred unless a real caller needs explicit ordering guarantees there.
- Next safe slice: if point-in-time consumers start reading filtered contradiction-only views directly, add one similarly bounded `query_at(...)` serial assertion for `abstained` or `dropped` snapshots before any schema or typed-edge work; otherwise keep the lane at the current contract-only stopping point.

## 2026-06-26T19:05:10Z - L1 temporal-kg - Codex L1 worker

- Scope: locked one bounded runtime-context contract for already-shipped temporal `serial` envelopes, without changing store projection logic, runner behavior, schema, retrieval selection, or external graph dependencies.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; `zerker.memory_context.v1` serial passthrough is now explicitly regression-tested across current-vs-history temporal envelopes so runtime consumers can rely on the same query-time ordering already present in inject receipts.
- Focused fixtures: the existing identity-disambiguation fixture keeps unrelated `Alice owns the billing exporter.` out of selection while `Status page owner is Alice.` still keeps a lower query-visible `serial` than `Status page owner is Alice Chen.`; the same receipt-emitted ordering is now locked in `history_temporal_graph`, `selected_temporal_graph`, and `injected_temporal_graph` inside `build_context(...)`.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_temporal_serial_metadata_for_current_vs_history_envelopes -q` passed (`Ran 1 test`); `git diff --check -- tests/test_runner.py docs/CONTINUOUS_BUILD/temporal-kg.log.md docs/SWARM_OPERATION_TRACKER.md docs/CURRENT_STATE.md docs/BUILD_LOG.md` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 243 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 94 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: there is still no dedicated runner-context `serial` fixture for contradiction/dropped-current temporal envelopes, but that should stay deferred unless a real runtime consumer needs serial-aware ordering outside the current-vs-history path.
- Next safe slice: if runtime consumers start reading contradiction or dropped-current subsets directly, add one similarly bounded `serial` passthrough assertion for those envelopes before any schema or typed-edge work; otherwise stop here and preserve the narrow contract-only scope.

## 2026-06-26T15:08:53Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded point-in-time ordering and metadata slice inside the existing shared temporal projection, without changing schema, retrieval modes, runtime context code, or external graph dependencies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: temporal envelopes now expose additive monotonic `serial` metadata derived only from events visible at the projection timestamp, and `query_at(...)` same-timestamp supersession/restatement ordering now uses query-time event metadata instead of live `updated_at` or later event sequences. Later revocations no longer perturb earlier point-in-time current-vs-history snapshots, and inject receipts inherit the same `serial` field because they reuse the shared temporal projection.
- Focused fixtures: the promoted `Release checklist owner is Alice Chen.` fixture now locks `serial=1` before promotion and `serial=2` after promotion; the same-timestamp `Incident owner is Alice.` vs `Incident owner is Alice Chen.` restatement fixture now stays stable at `2024-01-20T00:00:00Z` even after a later `REVOKED` event is added for `Alice`; the revoked `Status page owner is Alice.` fixture now proves `serial` advances from the observed event to the later revocation only when the query timestamp crosses that revocation.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_projects_learned_and_valid_time_from_existing_events tests.test_store.MemoryStoreTest.test_query_at_uses_query_time_serial_for_same_timestamp_restatement_ordering tests.test_store.MemoryStoreTest.test_query_at_unlearned_history_surfaces_revoked_resolution_reason tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_surfaces_explicit_state_group_subset_graphs tests.test_store.MemoryStoreTest.test_query_at_surfaces_explicit_state_group_subset_graphs -q` passed (`Ran 5 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 239 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 92 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: `serial` is now explicit on query and receipt temporal envelopes, but there is still no dedicated runtime-context fixture that locks serial-aware passthrough for consumers reading only `zerker.memory_context.v1`.
- Next safe slice: if a real runtime consumer needs serial-aware temporal ordering, add one bounded `build_context(...)` passthrough assertion for the shared temporal-envelope `serial` field before any schema or typed-edge work.

## 2026-06-26T11:04:40Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded additive query-surface-only state-group subset layer on top of the existing local `query_at(...)` projection, without changing persistence, retrieval receipts, runtime context, resolver ordering, or schema.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.query_at(...)` now materializes explicit `history_temporal_graph`, `current_temporal_graph`, `future_temporal_graph`, `superseded_temporal_graph`, `unlearned_temporal_graph`, and `learned_temporal_graph` subset maps from the already-filtered point-in-time projection, so callers can read current-vs-history and non-active slices directly instead of reconstructing them from `temporal_graph` plus id lists.
- Focused fixtures: one mixed-state `owner` snapshot now locks `learned` `Release freeze owner is Mallory.`, superseded `Status page owner is Alice.`, current `Status page owner is Alice Chen.`, and future `Roadmap owner is Priya.` into separate query-time subset graphs; the existing non-active fixtures now also prove `unlearned_only=True` collapses to only `unlearned_temporal_graph`, while the `Alice` -> `Alice Chen` identity guard keeps every subset graph empty when no non-active history exists.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_surfaces_explicit_state_group_subset_graphs tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_unlearned_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_unlearned_only_stays_empty_for_identity_supersession_history -q` passed (`Ran 3 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 231 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 91 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: live inject receipts and `zerker.memory_context.v1` still do not populate the same non-active subset from ordinary retrieval because deprecated/revoked/forgotten memories remain intentionally excluded there.
- Next safe slice: if a real consumer needs non-active provenance outside `query_at(...)`, add one similarly bounded receipt/runtime surface that explicitly populates `unlearned_temporal_graph` for rejected/revoked/forgotten histories before any schema or typed-edge work.

## 2026-06-26T07:04:40Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded read-only non-active query surface on top of the existing local temporal projection, without changing persistence, retrieval receipts, runtime context, resolver ordering, or schema.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.query_at(..., unlearned_only=True)` now returns only rejected/revoked/forgotten point-in-time envelopes from the existing bi-temporal projection, clears current/conflict subsets, and rejects incompatible `current_resolution != "all"` combinations instead of silently mixing non-active and current-only filter semantics.
- Focused fixtures: one mixed unlearned snapshot now locks revoked `Status page owner is Alice.`, forgotten `Temporary routing snack is ramen.`, and rejected `Release freeze owner is Mallory.` as the only visible envelopes on `2024-02-20T00:00:00Z`, while current `Status page owner is Alice Chen.` stays excluded; one identity guard fixture proves the `Alice` -> `Alice Chen` supersession timeline stays empty under `unlearned_only=True` so superseded history is not mistaken for non-active memory.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_unlearned_temporal_subset tests.test_store.MemoryStoreTest.test_query_at_unlearned_only_stays_empty_for_identity_supersession_history tests.test_store.MemoryStoreTest.test_query_at_rejects_unlearned_only_current_resolution_combination -q` passed (`Ran 3 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 230 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 91 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: live inject receipts and `zerker.memory_context.v1` still do not surface the same unlearned-only subset because retrieval intentionally excludes non-active memories; this run adds the query surface only.
- Next safe slice: if a real consumer needs non-active receipt/runtime provenance, add one similarly bounded explicit receipt-side `unlearned_temporal_graph` surface for rejected/revoked/forgotten memories before any schema or typed-edge work.

## 2026-06-26T03:04:35Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded additive receipt/runtime temporal-state subset layer on top of the existing temporal id groups and envelopes, without changing resolver behavior, query semantics, retrieval candidate selection, or schema.
- Files touched: `zerker_memory/store.py`, `zerker_memory/runner.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: inject receipts now persist explicit `history_temporal_graph`, `current_temporal_graph`, `future_temporal_graph`, `superseded_temporal_graph`, `learned_temporal_graph`, and `unlearned_temporal_graph` subsets derived from the existing shared temporal projection, and `zerker.memory_context.v1` now forwards the same subset maps unchanged when present.
- Focused fixtures: one mixed-state receipt fixture now locks a single `owner` query where quarantined `Release freeze owner is Mallory.` stays visible as `learned`, `Status page owner is Alice.` stays visible as `superseded`, `Status page owner is Alice Chen.` stays visible as `current`, and future-dated `Roadmap owner is Priya.` stays visible as `future` through explicit state-group subset graphs; one runner passthrough fixture now locks the same subset graphs in `build_context(...)`, including manual `unlearned_temporal_graph` passthrough for future non-active consumers.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_surfaces_explicit_state_group_subset_graphs -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_explicit_temporal_state_group_graphs tests.test_runner.RunnerTest.test_build_context_preserves_unlearned_temporal_id_groups_when_present -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 227 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 91 tests`); `python3 -m zerker_memory eval` passed (`11/11`); `git diff --check -- zerker_memory/store.py zerker_memory/runner.py tests/test_store.py tests/test_runner.py` passed.
- Artifacts/receipts: none.
- Blockers: ordinary inject receipts still do not populate `unlearned_temporal_graph` from live retrieval because deprecated/revoked/forgotten memories remain excluded by design, so that subset is explicit and forward-compatible but usually empty unless a dedicated non-active receipt/query surface is added.
- Next safe slice: if temporal consumers still need more explicit non-current provenance, add one bounded non-active receipt/query surface that intentionally populates `unlearned_temporal_graph` from rejected/revoked/forgotten memories before any schema or typed-edge work.

## 2026-06-25T23:02:59Z - L1 temporal-kg - Codex L1 worker

- Scope: forwarded one remaining additive temporal metadata slice from existing inject receipts into `zerker.memory_context.v1`, without changing store projection, retrieval behavior, or schema.
- Files touched: `zerker_memory/runner.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: runtime memory context now preserves top-level `future_memory_ids`, `learned_memory_ids`, and `unlearned_memory_ids` whenever those id groups are already present in `retrieval.temporal`, instead of dropping them during `build_context(...)`.
- Focused fixtures: one real runner fixture now proves a quarantined `Release freeze owner is Mallory.` candidate stays visible as `learned` while a future-dated `Roadmap owner is Priya.` candidate stays visible as `future` in the same context; one receipt-level passthrough fixture locks `unlearned_memory_ids=["mem_rejected"]` so future consumers can rely on the same additive runtime field when non-active temporal receipts are surfaced.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_learned_and_future_temporal_id_groups tests.test_runner.RunnerTest.test_build_context_preserves_unlearned_temporal_id_groups_when_present -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 223 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 90 tests`); `python3 -m zerker_memory eval` passed (`11/11`); `git diff --check -- zerker_memory/runner.py tests/test_runner.py` passed.
- Artifacts/receipts: none.
- Blockers: ordinary inject receipts still do not surface rejected/revoked/forgotten envelopes because retrieval excludes non-active memories by design, so `unlearned_memory_ids` remains a forward-compatible runtime field unless a dedicated non-active receipt/query consumer is added.
- Next safe slice: only if a runtime or receipt consumer needs it, add one similarly bounded explicit unlearned-envelope subset or non-active receipt surface before any schema or typed-edge work.

## 2026-06-25T16:38:00Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded schema-free regression fixture for an already-shipped `query_at(...)` temporal envelope path, covering rejected memories that become `deprecated` without ever becoming valid/current.
- Files touched: `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`.
- Behavior changed: no runtime behavior changed; the new fixture locks the existing local bi-temporal mapping where a quarantined fact is `learned` before rejection and then becomes `temporal_state=unlearned` with `status_at_query=deprecated`, `unlearned_at`, `valid_to`, and `temporal_resolution_reasons=["deprecated"]` after rejection.
- Focused fixture: `Release freeze owner is Mallory.` stays `learned` with `valid_from=None` while quarantined on `2024-01-20T00:00:00Z`, then becomes rejected/deprecated unlearned history on `2024-02-20T00:00:00Z` after a `REJECTED` event on `2024-02-01T00:00:00Z`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_unlearned_history_surfaces_deprecated_resolution_reason_for_rejected_memory -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` passed (`Ran 221 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 88 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this run intentionally stayed test-first and query-surface-only; rejected/deprecated envelopes are still not surfaced in inject receipts or runtime context because retrieval excludes non-active memories by design.
- Next safe slice: if broader unlearned coverage is still useful, add one similarly bounded revoked-vs-deprecated receipt-mapping comparison only when a receipt/runtime consumer actually needs non-active temporal envelopes.

## 2026-06-25T15:03:45Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded read-only temporal-envelope reason for existing unlearned history states in `query_at(...)`, without changing persistence, retrieval, receipt surfaces, runtime context, or schema.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: point-in-time temporal envelopes now mark `temporal_state=unlearned` histories with additive `temporal_resolution_kind=unlearned` and a status-specific `temporal_resolution_reasons` value derived from the existing local lifecycle state, such as `revoked` or `forgotten`. Current, superseded, contradiction, and receipt/runtime temporal contracts are unchanged.
- Focused fixtures: a revoked `Status page owner is Alice.` fact now stays current before revocation and then becomes an unlearned history envelope with `status_at_query=revoked`, `unlearned_at`, and `temporal_resolution_reasons=["revoked"]`; a forgotten `Temporary routing snack is ramen.` fact now becomes an unlearned history envelope with `temporal_resolution_reasons=["forgotten"]`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_unlearned_history_surfaces_revoked_resolution_reason tests.test_store.MemoryStoreTest.test_query_at_unlearned_history_surfaces_forgotten_resolution_reason -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 218 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 88 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: inject receipts and `zerker.memory_context.v1` still do not surface unlearned envelopes because retrieval excludes deprecated/revoked/forgotten memories by design, and typed graph edges for unlearning/revocation still do not exist.
- Next safe slice: add one bounded deprecated/rejected unlearned-history fixture if broader status coverage is worth locking, or stop before widening into retrieval/receipt surfaces until there is a real need to surface unlearned facts outside `query_at(...)`.

## 2026-06-25T11:03:43Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded read-only temporal-envelope discriminator so receipts, `query_at(...)`, and runtime context can distinguish contradiction-driven envelopes from supersession/history envelopes without changing resolver behavior, persistence, or schema.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: temporal envelopes now carry additive `temporal_resolution_kind` plus `temporal_resolution_reasons`. Current conflict envelopes use `contradiction` with the existing lexical conflict reason, while superseded history envelopes use `supersession` with the existing local supersession basis such as parent-child lineage. `zerker.memory_context.v1` forwards the same metadata through the existing temporal subset graphs because it already passes receipt envelopes through unchanged.
- Focused fixtures: the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity/supersession fixture now marks the stale `Alice` envelope as `temporal_resolution_kind=supersession`; the same-timestamp `Incident owner is Alex.` versus `Incident owner is Priya.` contradiction now marks both abstained current envelopes as `temporal_resolution_kind=contradiction`; and the resolved `Deploy target is Render.` versus `Deploy target is Railway.` conflict now marks both the dropped loser and selected winner as contradiction-driven rather than forcing callers to infer that from multiple fields.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_temporal_contract_current_vs_history_keeps_identity_disambiguation tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_surfaces_current_conflict_resolution_metadata tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_preserves_dropped_current_subset_metadata tests.test_store.MemoryStoreTest.test_query_at_projects_parent_supersession_without_merging_unrelated_alice_identity tests.test_store.MemoryStoreTest.test_query_at_surfaces_current_conflict_resolution_metadata_without_erasing_current_state -q` passed (`Ran 5 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_current_vs_history_temporal_envelopes tests.test_runner.RunnerTest.test_build_context_preserves_current_conflict_abstention_without_injected_memories tests.test_runner.RunnerTest.test_build_context_preserves_dropped_current_conflict_subset -q` passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 213 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 88 tests`); `python3 -m zerker_memory eval` passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` passed.
- Artifacts/receipts: none.
- Blockers: the new metadata makes contradiction-vs-supersession explicit, but the reasons still come from the existing local lexical/lineage contracts rather than typed graph edges, and there is still no dedicated surfaced reason for unlearned/revoked state transitions.
- Next safe slice: add one bounded temporal-envelope reason for unlearned/revoked history, or stop before any schema work until typed contradiction/supersession edges are actually required.

## 2026-06-25T07:01:27Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded receipt/runtime temporal subset so resolved current losers stay visible beyond `query_at(...)`, without changing temporal resolution logic, persistence, or schema.
- Files touched: `zerker_memory/store.py`, `zerker_memory/runner.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: inject receipts now persist `retrieval.temporal.dropped_current_temporal_graph` for locally outranked current memories, and `zerker.memory_context.v1` now forwards the same subset beside the existing selected/abstained/injected/withheld/budget-dropped temporal envelopes.
- Focused fixtures: the resolved `Deploy target is Render.` versus `Deploy target is Railway.` authority-conflict fixture now keeps the dropped `Render` current envelope receipt-visible and runtime-visible with `current_resolution=dropped`, while the winning `Railway` memory remains injected as the selected current envelope.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_preserves_dropped_current_subset_metadata tests.test_runner.RunnerTest.test_build_context_preserves_dropped_current_conflict_subset -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_dropped_current_conflict_subset tests.test_store.MemoryStoreTest.test_query_at_dropped_resolution_stays_empty_for_resolved_identity_history tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_preserves_dropped_current_subset_metadata tests.test_runner.RunnerTest.test_build_context_preserves_dropped_current_conflict_subset -q` passed (`Ran 4 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 213 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 88 tests`); `python3 -m zerker_memory eval` passed (`11/11`); `git diff --check -- zerker_memory/store.py zerker_memory/runner.py tests/test_store.py tests/test_runner.py` passed.
- Artifacts/receipts: none.
- Blockers: dropped current losers are now explicit in receipts and runtime context, but contradiction/supersession still ride on the existing lexical/local temporal contract rather than typed graph edges, and there is still no explicit receipt split between supersession and contradiction beyond the shared envelope metadata.
- Next safe slice: add one bounded contradiction-vs-supersession receipt discriminator or similarly small read-only temporal metadata refinement before any schema or graph-edge work.

## 2026-06-25T03:03:33Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded read-only `query_at(...)` filter so point-in-time snapshots can return only resolved-but-dropped current envelopes without changing persistence, receipt shape, or schema.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.query_at(..., current_resolution="dropped")` now returns only locally outranked current entries, visible temporal envelopes, and the matching resolved current-conflict sets for the requested timestamp while leaving `current_resolution="all"`, `"abstained"`, and `"selected"` behavior unchanged. Parent-linked superseded history still stays out of this dropped-only surface.
- Focused fixtures: the resolved `Deploy target is Render.` versus `Deploy target is Railway.` authority conflict now returns only the dropped `Render` current envelope in dropped mode with the winning `Railway` id preserved in the conflict metadata; the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity/supersession timeline now proves the dropped-only view stays empty when history is superseded rather than merely outranked.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_dropped_current_conflict_subset tests.test_store.MemoryStoreTest.test_query_at_dropped_resolution_stays_empty_for_resolved_identity_history -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_abstained_current_contradiction_subset tests.test_store.MemoryStoreTest.test_query_at_abstained_resolution_stays_empty_for_resolved_identity_history tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_selected_current_identity_subset tests.test_store.MemoryStoreTest.test_query_at_selected_resolution_stays_empty_for_unresolved_current_contradiction tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_dropped_current_conflict_subset tests.test_store.MemoryStoreTest.test_query_at_dropped_resolution_stays_empty_for_resolved_identity_history tests.test_store.MemoryStoreTest.test_query_at_rejects_hidden_abstained_resolution_filter_combination -q` passed (`Ran 7 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 210 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 87 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: point-in-time callers can now isolate abstained, selected, and dropped current subsets, but receipt/runtime temporal envelopes still have no dedicated dropped-current subset and contradiction/supersession remain modeled through the existing lexical/local temporal contract rather than typed graph edges.
- Next safe slice: add one bounded receipt/runtime `dropped_current_temporal_graph` subset so locally outranked current losers stay visible beyond `query_at(...)` without widening into schema or external graph work.

## 2026-06-24T23:01:50Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded read-only `query_at(...)` filter so point-in-time snapshots can return only resolved usable current envelopes without changing persistence, receipt shape, or schema.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.query_at(..., current_resolution="selected")` now returns only resolved current entries and temporal envelopes for the requested timestamp while leaving default `current_resolution="all"` behavior unchanged. Contradictory same-timestamp current claims now correctly yield an empty selected-only snapshot instead of leaking abstained contradictions into the usable-current view.
- Focused fixtures: the resolved `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity/supersession timeline now returns only the current `Alice Chen` envelope even when the facts come from different sessions; the same-timestamp `Incident owner is Alex.` versus `Incident owner is Priya.` contradiction now proves the selected-only view stays empty when no current winner exists.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_selected_current_identity_subset tests.test_store.MemoryStoreTest.test_query_at_selected_resolution_stays_empty_for_unresolved_current_contradiction -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_abstained_current_contradiction_subset tests.test_store.MemoryStoreTest.test_query_at_abstained_resolution_stays_empty_for_resolved_identity_history tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_selected_current_identity_subset tests.test_store.MemoryStoreTest.test_query_at_selected_resolution_stays_empty_for_unresolved_current_contradiction tests.test_store.MemoryStoreTest.test_query_at_rejects_hidden_abstained_resolution_filter_combination -q` passed (`Ran 5 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 206 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 86 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: point-in-time callers can now ask for all, abstained-only, or selected-only current views, but there is still no symmetric dropped-current projection and contradiction/supersession remain modeled through the existing lexical/local temporal contract rather than typed graph edges.
- Next safe slice: add one bounded `query_at(timestamp, current_resolution="dropped"|...)` read-only filter so point-in-time callers can inspect locally outranked current losers before any schema or graph-edge work.

## 2026-06-24T18:58:57Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded read-only contradiction-only `query_at(...)` filter so point-in-time snapshots can return only unresolved abstained-current envelopes without changing persistence, receipt shape, or schema.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.query_at(..., current_resolution="abstained")` now returns only abstained current contradiction entries, temporal envelopes, and conflict sets for the requested timestamp while preserving `abstained_current_memory_ids` and `abstention` metadata; default `current_resolution="all"` behavior is unchanged. The API now also rejects `include_abstained_current=False` when `current_resolution="abstained"` to avoid an empty contradictory contract.
- Focused fixtures: the same-timestamp `Incident owner is Alex.` versus `Incident owner is Priya.` contradiction now yields a contradiction-only snapshot with both abstained current envelopes visible; the resolved `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity/supersession fixture now proves the same filter stays empty when no abstained contradiction exists.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_abstained_current_contradiction_subset tests.test_store.MemoryStoreTest.test_query_at_abstained_resolution_stays_empty_for_resolved_identity_history tests.test_store.MemoryStoreTest.test_query_at_rejects_hidden_abstained_resolution_filter_combination tests.test_store.MemoryStoreTest.test_query_at_can_hide_abstained_current_envelopes_while_preserving_conflict_metadata tests.test_store.MemoryStoreTest.test_query_at_abstained_filter_keeps_resolved_identity_history_visible -q` passed (`Ran 5 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 203 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 86 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: `query_at(...)` can now isolate abstained contradictions, but there is still no symmetric read-only filter for resolved current winners or dropped current losers, and contradiction history still rides on the existing lexical conflict contract rather than typed contradiction edges.
- Next safe slice: add one bounded `query_at(timestamp, current_resolution="selected"|...)` read-only filter so point-in-time callers can request the resolved usable current subset through the same schema-free contract.

## 2026-06-24T14:58:35Z - L1 temporal-kg - Codex L1 worker

- Scope: added one bounded read-only `query_at(...)` filter contract so point-in-time snapshots can hide unresolved abstained-current envelopes without changing the underlying temporal projection, receipt shape, or schema.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `MemoryStore.query_at(..., include_abstained_current=False)` now filters abstained-current memories out of visible `entries`, `temporal_graph`, and visible history/current id lists, while preserving `abstained_current_memory_ids`, `conflict_sets`, and `abstention` metadata so contradiction evidence stays queryable. Default `include_abstained_current=True` behavior is unchanged.
- Focused fixtures: the same-timestamp `Incident owner is Alex.` versus `Incident owner is Priya.` contradiction now yields an empty usable snapshot with preserved abstention metadata when the filter is disabled; the `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity/supersession fixture stays unchanged under the same flag.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_surfaces_current_conflict_resolution_metadata_without_erasing_current_state tests.test_store.MemoryStoreTest.test_query_at_can_hide_abstained_current_envelopes_while_preserving_conflict_metadata tests.test_store.MemoryStoreTest.test_query_at_abstained_filter_keeps_resolved_identity_history_visible tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_surfaces_current_conflict_resolution_metadata -q` passed (`Ran 4 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 200 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 86 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: `query_at(...)` can now hide abstained-current envelopes, but there is still no dedicated read-only surface for returning only the abstained contradiction subset, and contradiction history still rides on the existing lexical conflict contract rather than typed contradiction edges.
- Next safe slice: add one bounded `query_at(timestamp, current_resolution=\"abstained\"|...)` or equivalent read-only contradiction-only filter before any schema or graph-edge work.

## 2026-06-24T10:56:45Z - L1 temporal-kg - Codex L1 worker

- Scope: filled the remaining schema-free contradiction-envelope gap by carrying abstained current-memory temporal envelopes into stored receipts and generated runtime context, without changing temporal selection logic or persistence shape.
- Files touched: `zerker_memory/store.py`, `zerker_memory/runner.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: `retrieval.temporal` now includes `abstained_temporal_graph` for unresolved current conflicts, and `zerker.memory_context.v1` now forwards that same subset beside the existing selected/injected/withheld/budget-dropped temporal envelopes.
- Focused fixture: same-timestamp `Incident owner is Alex.` versus `Incident owner is Priya.` now keeps per-memory `temporal_state=current`, `current_resolution=abstained`, and `current_conflict_reasons=["lexical-current-conflict"]` visible even when nothing is injected.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_surfaces_current_conflict_resolution_metadata tests.test_store.MemoryStoreTest.test_query_at_surfaces_current_conflict_resolution_metadata_without_erasing_current_state -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_current_conflict_abstention_without_injected_memories -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` passed (`Ran 195 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 85 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: `query_at(...)` still has no explicit status/filter surface for "only abstained current envelopes at timestamp", and contradiction history remains grouped by the existing lexical conflict contract rather than typed graph edges.
- Next safe slice: add one bounded `query_at(timestamp, include_abstained_current=True|False)` or equivalent read-only filter contract before any schema or graph-edge work.

## 2026-06-24T07:02:53Z - L1 temporal-kg - Codex L1 worker

- Scope: locked the no-injection current-conflict runtime contract by proving `runner.build_context()` preserves abstention and conflict metadata for competing current claims, without changing store logic or schema.
- Files touched: `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; the new regression proves `zerker.memory_context.v1` keeps `temporal.selection_strategy`, `selection_reason`, `current_memory_ids`, `abstained_current_memory_ids`, `conflict_sets`, `abstention`, and empty selected/injected temporal subsets when current claims conflict and nothing is injected.
- Focused fixture: same-timestamp `Incident owner is Alex.` versus `Incident owner is Priya.` now has an explicit runner-context contract for `current_conflict_abstained_v1`, preserving the `lexical-current-conflict` envelope even with `context["memories"] == []`.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_current_conflict_abstention_without_injected_memories -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` passed (`Ran 194 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 84 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: runtime context still does not expose the full per-memory abstained `temporal_graph`; current-conflict cases keep ids, `conflict_sets`, and `abstention`, but not complete abstained envelopes.
- Next safe slice: add one schema-free runtime surface for abstained current envelopes, or stop until a stronger `query_at(timestamp, status=...)` or contradiction-history contract is required.

## 2026-06-24T02:59:31Z - L1 temporal-kg - Codex L1 worker

- Scope: carried the existing bi-temporal receipt subsets through `runner.build_context()` so runtime memory context preserves current/history and omitted-memory envelopes without any store/schema change.
- Files touched: `zerker_memory/runner.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`, `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: generated `zerker.memory_context.v1` payloads now include `temporal` metadata with `selection_strategy`, `selection_reason`, selected/current/history/superseded ids, current-conflict summaries, and the existing `selected_temporal_graph`, `injected_temporal_graph`, `withheld_temporal_graph`, and `budget_dropped_temporal_graph` subset envelopes from the stored receipt.
- Focused fixtures: one `Alice` vs `Alice Chen` history fixture proves runtime context keeps the superseded/current pair and leaves an unrelated `Alice` fact out of the selected temporal envelope; one omitted-subset fixture proves a denied `Status page owner is Alice Chen.` fact stays `current` in `withheld_temporal_graph` while `Incident owner was Alex.` remains injected superseded history and `Incident owner is Priya.` stays visible as a budget-dropped current envelope.
- Tests: `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_current_vs_history_temporal_envelopes tests.test_runner.RunnerTest.test_build_context_preserves_temporal_omitted_subset_envelopes tests.test_runner.RunnerTest.test_build_context_separates_instructional_and_recall_memory_and_surfaces_budget_receipts -q` passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 191 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 82 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: contradiction/abstention metadata is now forwarded into runtime context, but there is still no focused runner fixture proving a no-injection current-conflict snapshot survives into `temporal.conflict_sets` / `temporal.abstention`.
- Next safe slice: add one bounded runner-context contradiction fixture for competing current claims, or stop before any schema/query-surface work until a stricter `query_at(timestamp, status=...)` contract is needed.

## 2026-06-23T22:54:22Z - L1 temporal-kg - Codex L1 worker

- Scope: added receipt-visible temporal subset envelopes for omitted memories so `inject(...)` preserves current-vs-history metadata for `withheld` and `budget_dropped` facts without any schema change.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`, `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: `retrieval.temporal` now carries `withheld_temporal_graph` and `budget_dropped_temporal_graph` subsets derived from the shared temporal projection alongside the existing `selected_temporal_graph` and `injected_temporal_graph`. Omitted memories now keep their `learned_at`, `valid_from`, `valid_to`, `superseded_at`, `unlearned_at`, `status_at_query`, `temporal_state`, and current-conflict metadata in the stored receipt/`why(...)` payload.
- Focused fixtures: one deny-label fixture proves a current `Status page owner is Alice Chen.` fact stays visible as a withheld current envelope; one tight-budget chronology fixture proves `Incident owner was Alex.` remains injected as superseded history while `Incident owner is Priya.` is preserved as a budget-dropped current envelope.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_preserves_withheld_current_subset_metadata tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_preserves_budget_dropped_current_subset_metadata -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 189 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 79 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: omitted subsets are now receipt-visible, but there is still no dedicated `query_at(...)` filter surface for "only omitted at this receipt time," and runner context still exposes omission lists rather than temporal envelopes.
- Next safe slice: add one focused contradiction/history omitted-subset fixture for cross-provenance abstention, or deliberately stop before any schema work until a stricter `query_at(timestamp, status=...)` contract is needed.

## 2026-06-23T18:59:24Z - L1 temporal-kg - Codex L1 worker

- Scope: added contradiction-aware current-conflict metadata to the shared event-lineage temporal projection used by both `query_at(...)` and receipt envelopes, while keeping the slice schema-free and local-store-only.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`, `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: `_project_temporal_state(...)` now returns `resolved_current_memory_ids`, `dropped_current_memory_ids`, `abstained_current_memory_ids`, top-level `conflict_sets` / `abstention`, and per-memory `current_resolution` / `current_conflict_reasons` alongside the existing bi-temporal envelope. Inject receipts now mirror the resolved/dropped/abstained current-id subsets inside `retrieval.temporal` while reusing the same `temporal_graph`.
- Focused fixtures: one receipt test proves same-timestamp `Incident owner is Alex.` versus `Incident owner is Priya.` stays `temporal_state=current` but is marked `current_resolution=abstained` with no resolved current ids in the receipt projection; one `query_at(...)` test proves the same contradiction is explicit at a point in time without erasing the underlying current-state history.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_surfaces_current_conflict_resolution_metadata tests.test_store.MemoryStoreTest.test_query_at_surfaces_current_conflict_resolution_metadata_without_erasing_current_state -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 79 tests`); `python3 -m zerker_memory eval` passed (`11/11`); `python3 -m unittest tests.test_store -q` currently fails on unrelated existing lifecycle-compaction errors because `tests.test_store.MemoryStoreTest.test_soft_delete_session_snapshot_payload_preserves_receipt_visible_summary` and `tests.test_store.MemoryStoreTest.test_session_snapshots_reports_soft_deleted_payload_without_returning_snapshot_json` call a missing `MemoryStore.soft_delete_session_snapshot_payload`.
- Artifacts/receipts: none.
- Blockers: the required full store suite is currently red in the dirty tree on the unrelated L2 session-snapshot soft-delete contract; query-specific cross-provenance history abstention and withheld/budget-dropped temporal subset receipts are still not part of the shared projection.
- Next safe slice: either land the missing L2 session-snapshot soft-delete method outside this lane so the required broad store suite is green again, or keep L1 bounded and add receipt-visible withheld/budget-dropped temporal subsets now that contradiction metadata exists in the shared projection.

## 2026-06-23T14:54:48Z - L1 temporal-kg - Codex L1 worker

- Scope: surfaced receipt-visible temporal envelopes on `inject`/`why` by reusing the existing event-lineage `query_at(...)` projection, while keeping the slice schema-free and local-store-only.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`, `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: `retrieval.temporal` now persists `temporal_projection_at`, candidate `temporal_graph`, projected `history_memory_ids` / `current_memory_ids` / `future_memory_ids` / `superseded_memory_ids` / `unlearned_memory_ids` / `learned_memory_ids`, plus `selected_temporal_graph` and `injected_temporal_graph` subsets carrying `learned_at`, `valid_from`, `valid_to`, `superseded_at`, `unlearned_at`, `status_at_query`, and `temporal_state`. `why(action_id)` now returns the same receipt-visible envelope data because it reads the persisted retrieval payload back unchanged.
- Focused fixtures: one receipt test proves `Status page owner is Alice.` becomes a superseded history envelope while `Status page owner is Alice Chen.` stays current and an unrelated `Alice` fact stays out of the receipt; another proves a promoted `Release checklist owner is Alice Chen.` memory preserves distinct `learned_at` and `valid_from` timestamps inside the inject receipt.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_temporal_contract_current_vs_history_keeps_identity_disambiguation tests.test_store.MemoryStoreTest.test_temporal_receipt_projection_preserves_learned_vs_valid_time_for_promoted_memory -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 182 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 79 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: contradiction-aware abstention and cross-provenance competing facts still rely on the existing conflict-set receipt path rather than an explicit bi-temporal contradiction envelope; withheld/budget-dropped memories still do not have dedicated temporal subset receipts.
- Next safe slice: add contradiction-aware abstention metadata to the shared temporal projection without widening into schema changes, or decide whether withheld/budget-dropped temporal subsets should be the next receipt-focused temporal contract.

## 2026-06-22 - coordinator

- Scope: seeded lane for bi-temporal graph memory, point-in-time queries, supersession, and entity disambiguation.
- Files touched: lane log only.
- Behavior changed: none.
- Tests: not applicable.
- Artifacts/receipts: none.
- Blockers: worker must map existing temporal metadata before schema changes.
- Next safe slice: document current temporal fields and add a focused `query_at` contract test.

## 2026-06-22T18:49:55Z - L1 temporal-kg - Codex L1 worker

- Scope: mapped the existing temporal memory surface and added a narrow current-vs-history contract fixture before any schema migration.
- Files touched: `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`.
- Behavior changed: no runtime behavior changed; the new test locks existing retrieval behavior for an `Alice` -> `Alice Chen` supersession while keeping an unrelated `Alice` memory out of both current and history receipts.
- Existing temporal inventory: persisted memory fields are `created_at`, `updated_at`, `expires_at`, `status`, and `parents_json`; event, retrieval, and write receipts also carry `created_at`. Retrieval derives `temporal_state` as `current`, `superseded`, `expired`, or `revoked`, marks `superseded_by_candidate`, and records `selected_current_ids`, `selected_superseded_ids`, `stale_ids`, conflict sets, and injection order under `retrieval["temporal"]`. There are no explicit persisted `valid_from`, `valid_to`, `learned_at`, `superseded_at`, `unlearned_at`, or `query_at(timestamp)` API fields yet.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_temporal_contract_current_vs_history_keeps_identity_disambiguation -q` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 158 tests`).
- Artifacts/receipts: none.
- Blockers: explicit bi-temporal graph memory still needs schema/API design; current implementation is retrieval-time lifecycle resolution over timestamps, parents, status, and receipts, not durable valid-time/transaction-time graph edges.
- Next safe slice: add the first failing `query_at(timestamp)` contract around the same supersession fixture, then implement the smallest local-store point-in-time API using existing `created_at`/`updated_at` and parent lineage before adding new graph tables.

## 2026-06-22T22:55:26Z - L1 temporal-kg - Codex L1 worker

- Scope: shipped the first local `query_at(timestamp)` projection for explicit bi-temporal envelopes using existing event history plus parent lineage, without any schema migration.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`, `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: `MemoryStore.query_at(...)` now returns derived `learned_at`, `valid_from`, `valid_to`, `superseded_at`, `unlearned_at`, `status_at_query`, and point-in-time temporal state (`current`, `superseded`, `learned`, `future`) for a scope-local snapshot, with optional lexical narrowing plus parent/child lineage closure.
- Focused fixtures: one test proves quarantined memory can be `learned` before promotion and `current` after promotion because `learned_at` and `valid_from` differ; another proves `Status page owner is Alice` becomes superseded by `Status page owner is Alice Chen` on `2024-02-01T00:00:00Z` while an unrelated `Alice` memory stays separate across both snapshots.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_projects_learned_and_valid_time_from_existing_events tests.test_store.MemoryStoreTest.test_query_at_projects_parent_supersession_without_merging_unrelated_alice_identity -q` passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 163 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 74 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: the projection currently uses event status transitions plus parent lineage only; explicit same-subject update/restatement supersession and receipt-visible point-in-time metadata are still deferred.
- Next safe slice: port the existing explicit-update and same-provenance restatement supersession rules from retrieval receipts into `query_at`, then decide whether `inject`/`why` should expose the same derived temporal envelopes.

## 2026-06-23T06:53:18Z - L1 temporal-kg - Codex L1 worker

- Scope: extended `query_at(timestamp)` so explicit same-subject update memories can supersede older facts without parent links, while keeping the slice local-store-only and schema-free.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`, `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: point-in-time temporal envelopes now apply the existing lexical explicit-update rule inside `query_at`, using existing event sequence order as the deterministic tiebreaker when `valid_from` timestamps match. Older facts now move to `superseded` with derived `superseded_at`, `valid_to`, and `superseded_by_ids` even when no parent edge exists.
- Focused fixture: `Deploy target is Staging.` versus `Deploy target changed to Production.` at the same timestamp now resolves to only the update memory in `current_memory_ids`, with the older fact preserved in history as superseded.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_explicit_update_supersedes_older_same_subject_memory_without_parent_link -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_projects_learned_and_valid_time_from_existing_events tests.test_store.MemoryStoreTest.test_query_at_projects_parent_supersession_without_merging_unrelated_alice_identity tests.test_store.MemoryStoreTest.test_query_at_explicit_update_supersedes_older_same_subject_memory_without_parent_link -q` passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 173 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 77 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: `query_at` still does not project same-provenance subject-lookup restatement supersession or receipt-surface point-in-time metadata, so retrieval receipts remain the richer path for those explanations.
- Next safe slice: port same-provenance restatement supersession into `query_at`, then decide whether `inject`/`why` should expose the same derived temporal envelopes directly.

## 2026-06-23T10:52:17Z - L1 temporal-kg - Codex L1 worker

- Scope: extended `query_at(timestamp)` so same-provenance plain restatements can supersede older facts without parent links or explicit update phrasing, while keeping the slice local-store-only and schema-free.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`, `docs/SWARM_OPERATION_TRACKER.md`.
- Behavior changed: point-in-time temporal envelopes now apply the existing lexical subject/relation grouping for same-provenance restatements inside `query_at`. Within one group, the latest `valid_from` wins; equal `valid_from` falls back to existing event sequence order as the local deterministic serial. Older facts now move to `superseded` with derived `superseded_at`, `valid_to`, and `superseded_by_ids` even when no parent edge exists.
- Focused fixture: `Incident owner is Alice.` is superseded by `Incident owner is Alice Chen.` on `2024-02-15T00:00:00Z`, while unrelated `Runbook owner is Alice.` stays current across the same snapshots.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_same_provenance_restatement_supersedes_older_subject_fact_without_parent_link tests.test_store.MemoryStoreTest.test_query_at_explicit_update_supersedes_older_same_subject_memory_without_parent_link tests.test_store.MemoryStoreTest.test_query_at_projects_parent_supersession_without_merging_unrelated_alice_identity -q` passed (`Ran 3 tests`); `python3 -m unittest tests.test_store -q` passed (`Ran 176 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 78 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: `query_at` still does not surface contradiction-aware abstention or receipt-visible current-vs-history temporal envelopes on `inject`/`why`, so retrieval receipts remain the richer explanation path for those cases.
- Next safe slice: expose the same derived temporal envelopes directly in `inject`/`why` receipts before widening into contradiction handling or schema changes.

## 2026-07-04T12:53:31Z - L1 temporal-kg - Codex L1 worker

- Scope: regression-locked one already-shipped no-search `query_at(..., superseded_only=True)` current-vs-history receipt surface without changing store logic, runner logic, SQLite schema, receipt wrappers, or external graph dependencies.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/temporal-kg.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: no runtime behavior changed; the existing no-search superseded-only point-in-time snapshot and `build_context(...)` passthrough are now durably covered to prove `current_history_receipt_metadata.filter=superseded_only` preserves explicit base/included/omitted history/current id splits even when there is no lexical `search_query`.
- Focused fixtures: `query_at("2024-02-20T00:00:00Z", scope="project", superseded_only=True)` now stays regression-locked for the cross-session `Status page owner is Alice.` -> `Status page owner is Alice Chen.` identity chain while the unrelated current `Alice owns the billing exporter.` fact remains omitted from the filtered temporal graph but visible in the base omitted history/current envelope; the matching `zerker.memory_context.v1` payload is locked to preserve that same no-search receipt metadata unchanged.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_query_at_no_search_superseded_only_surfaces_current_vs_history_receipt_metadata -q` passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_preserves_query_at_no_search_superseded_identity_receipt_metadata -q` passed (`Ran 1 test`); `git diff --check -- tests/test_store.py tests/test_runner.py` passed; `python3 -m unittest tests.test_store -q` passed (`Ran 311 tests`); `python3 -m unittest tests.test_runner -q` passed (`Ran 151 tests`); `python3 -m zerker_memory eval` passed (`11/11`).
- Artifacts/receipts: none.
- Blockers: this remains a tests-and-docs-only coverage slice; there is still no dedicated `query_at(...)` receipt wrapper or schema-backed bi-temporal edge layer behind the additive envelope.
- Next safe slice: only if a real consumer needs another no-search filtered point-in-time receipt contract, regression-lock one similarly bounded surface such as `future_only=True` or `unlearned_only=True` before any receipt-wrapper or schema work.
