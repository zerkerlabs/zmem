# Hybrid Retrieval Lane Log

## 2026-07-06T13:01:58Z - target-history pair-fit budgets now lock the no-decoy packing boundary

- Scope: shipped one bounded L3 local-first regression-lock slice that closes the queued `target_history_support_preferred_v1` overflow question without widening retrieval or packing behavior.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: no retrieval behavior changed. The reproduced exact-pair budget for `What did Blue Finch deploy to before it moved to Production?` still injects only the explicit support/current pair, keeps `packing.budget_dropped=[]`, and leaves the generic current decoy entirely outside `packing.candidate_priorities`; the decoy remains explainable through the existing `temporal.selection_exclusions` receipt/context path.
- Tests: focused boundary coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_before_target_history_exact_pair_budget_keeps_generic_decoy_out_of_packing tests.test_runner.RunnerTest.test_before_target_history_context_exact_pair_budget_keeps_generic_decoy_out_of_packing -q` -> passed (`Ran 2 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 338 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 172 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema migration, no `MemoryStore.search()` return-shape change, and no retrieval implementation change. This run locks the current `selected_memories` packing boundary as the reason a generic target-history decoy cannot become a packing-visible drop on the exact-pair budget path.
- Blockers: there is still no reproduced `target_history_support_preferred_v1` case where a non-selected generic current decoy survives into packing. Under the current local path, temporal selection excludes that decoy before policy and packing see it.
- Next safe slice: pivot away from widening target-history packing and back to one bounded local FTS/BM25 ranking slice, starting with richer receipt-visible baseline ordering metadata and deterministic same-phrase tie handling.

## 2026-07-06T09:04:01Z - target-history pair-over-budget drops now keep blocked-pair reservation metadata

- Scope: shipped one bounded L3 local-first explainability slice that makes the reproduced `target_history_support_preferred_v1` pair-over-budget seam explicit without changing ranking, routing, or fallback selection.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: when `What did Blue Finch deploy to before it moved to Production?` selects the explicit support/current pair but that pair itself exceeds the context budget, `packing.reservation` now keeps additive blocked-pair metadata (`blocked_detail`, `blocked_target_current_id`, `blocked_target_support_ids`, `blocked_pair_ids`, `blocked_pair_tokens`, `blocked_pair_excess_tokens`) and the selected pair members now carry `reservation_exclusion_reason=target-history-support-pair-blocked` plus per-member role metadata instead of leaving the dropped selected current fact as an opaque `reason=context-budget` loss.
- Tests: focused pair coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_before_target_history_budget_drop_records_blocked_selected_pair_metadata tests.test_runner.RunnerTest.test_before_target_history_context_budget_drop_records_blocked_selected_pair_metadata -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 336 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 170 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema migration and no `MemoryStore.search()` return-shape change; additive explainability now keeps the blocked reservation visible in persisted `retrieval_json`, `packing.candidate_priorities`, `packing.budget_dropped`, and `zerker.memory_context.v1`. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` returned no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice intentionally stays on the reproduced pair-over-budget seam only. The adjacent overflow case where the selected support/current pair fits but an extra anchor does not remains unverified.
- Next safe slice: probe one `target_history_support_preferred_v1` overflow case where the selected support/current pair fits but an extra anchor does not, and only extend fallback or packing metadata if that concrete retrieval miss reproduces.

## 2026-07-06T05:01:53Z - target-history exclusions now distinguish alternate support evidence from generic current decoys

- Scope: shipped one bounded L3 local-first explainability slice that sharpens the existing `target_history_support_preferred_v1` exclusion contract without changing ranking or packing outcomes.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: multi-support target-history prompts such as `What did Blue Finch deploy to before it moved to Production?` now keep alternate explicit support evidence separate from generic current decoys inside `temporal.selection_exclusions`. Unselected support evidence now carries `reason=target-history-support-candidate-not-selected`, `detail=strongest-explicit-target-support-selected`, and `candidate_role=history-support`, while generic current decoys keep `reason=target-history-current-anchor-not-selected` with additive `candidate_role=generic-current`. The same additive distinction now survives through stored receipts and `zerker.memory_context.v1`.
- Tests: added focused store and runner coverage for the reproduced multi-support seam, then ran `python3 -m unittest tests.test_store.MemoryStoreTest.test_before_target_history_selection_exclusions_distinguish_support_candidates_from_generic_current_anchors tests.test_runner.RunnerTest.test_before_target_history_context_preserves_support_vs_generic_selection_exclusion_roles -q` -> failed pre-fix (`2` failures), then passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 334 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 168 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema migration and no `MemoryStore.search()` return-shape change; additive explainability now keeps `temporal.selection_exclusions[*].candidate_role` alongside the existing target-history reason/detail/pair metadata in both persisted `retrieval_json` and runtime context. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` returned no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice intentionally stops at exclusion classification. A reproduced adjacent multi-support path still shows `packing.reservation.blocked_reason=reservation-exceeds-budget` plus an opaque `context-budget` drop when the selected support/current pair itself exceeds the budget, but fixing that would widen packing semantics beyond this run.
- Next safe slice: add explicit pair-blocked reservation metadata for the reproduced `target_history_support_preferred_v1` case where the selected support/current pair itself exceeds budget, but only after locking one focused local repro.

## 2026-07-06T01:00:50Z - target-history selection exclusions now survive into runtime context

- Scope: shipped one bounded L3 local-first explainability slice that preserves the existing `target_history_support_preferred_v1` temporal exclusion contract through `zerker.memory_context.v1`.
- Files touched: `zerker_memory/runner.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: target-history prompts such as `What did Blue Finch deploy to before it moved to Production?` still retrieve the generic current decoy `Blue Finch changed after freeze.` but inject only the explicit support/current pair, and `temporal.selection_exclusions` now survives into the runtime context file so that retrieved-but-not-injected decoy keeps its existing `target-history-current-anchor-not-selected` reason plus selected target pair ids where agents actually read context. This slice intentionally does not invent a false `budget_dropped` or `reservation_exclusion` contract for `target_history_support_chain_v1`, because the reproduced path never reaches packing with a dropped decoy.
- Tests: added a focused runner assertion on the existing target-history support-pair fixture, then ran `python3 -m unittest tests.test_runner.RunnerTest.test_before_target_history_context_prefers_explicit_support_pair_over_generic_current_anchor -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/runner.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 332 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 166 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema migration and no `MemoryStore.search()` return-shape change; additive explainability now keeps `temporal.selection_exclusions[*].reason`, `.detail`, `.selected_target_current_id`, `.selected_target_support_ids`, and `.selected_target_pair_ids` visible in both stored receipts and runtime context. Focused stale-target scan on `zerker_memory/runner.py` and `tests/test_runner.py` returned no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice intentionally stays on runtime-context passthrough only. The reproduced `target_history_support_chain_v1` path does not create a packing-visible dropped decoy; the generic current anchor is excluded earlier by temporal selection.
- Next safe slice: probe one `target_history_support_preferred_v1` overflow case where the selected support/current pair fits but an extra anchor does not, and only extend reservation fallback or packing metadata if that concrete retrieval miss reproduces.

## 2026-07-05T21:02:03Z - earliest-history anchor-pair decoys now carry explicit reservation-exclusion receipts

- Scope: shipped one bounded L3 local-first retrieval slice that adds the missing receipt-visible exclusion contract for the applied `earliest_history_anchor_pair_v1` reservation path.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: earliest-history queries such as `what was the original deploy target` still keep the same injected reserved pair `Deploy target is Heroku.` plus `Deploy target changed to Production.` under the exact two-memory budget, but the dropped middle superseded decoy `Deploy target changed to Render.` now carries additive `packing.candidate_priorities[*].reservation_exclusion_reason=earliest-history-anchor-pair-reserved` plus matching `reservation_exclusion` detail, and the same metadata now flows through `packing.budget_dropped` and runtime context receipts for that reserved-away stale decoy.
- Tests: added focused store and runner assertions on the reproduced budget-decoy case, then ran `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; the first targeted focused run surfaced the new runner failure on missing exclusion metadata, then `python3 -m unittest tests.test_store.MemoryStoreTest.test_original_history_budget_packing_keeps_earliest_and_latest_current_anchors tests.test_runner.RunnerTest.test_build_context_original_history_budget_records_reserved_anchor_pair_exclusion_reason -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 331 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 166 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema migration and no `MemoryStore.search()` return-shape change; additive explainability now includes `reservation_exclusion_reason`, `reservation_exclusion.detail`, `reservation_exclusion.selected_stale_id`, `reservation_exclusion.selected_current_id`, and `reservation_exclusion.selected_pair_ids` on the reserved-away superseded decoy in both `packing.candidate_priorities` and `packing.budget_dropped`. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` returned no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice intentionally stops at the applied `earliest_history_anchor_pair_v1` path on the multi-update original-history budget seam. The adjacent `target_history_support_chain_v1` family still has no reproduced packing-visible decoy contract beyond the existing temporal `selection_exclusions`.
- Next safe slice: reproduce whether `target_history_support_chain_v1` yields a packing-visible dropped current decoy beyond its existing temporal `selection_exclusions`; if it does, extend the same additive reservation-exclusion contract there without widening routing or schema scope.

## 2026-07-05T16:59:00Z - chronology support-chain decoys now carry explicit reservation-exclusion receipts

- Scope: shipped one bounded L3 local-first retrieval slice that adds the missing receipt-visible exclusion contract for the applied `chronology_relation_support_chain_v1` reservation path.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: chronology relation queries such as `when did the api gateway point at change then` still keep the same injected reserved chain `API gateway points to canary.` plus `API gateway points to production.` plus `API gateway points changed after migration.` under the exact three-memory budget, but the dropped middle superseded decoy `API gateway points to staging.` now carries additive `packing.candidate_priorities[*].reservation_exclusion_reason=chronology-support-chain-reserved` plus matching `reservation_exclusion` detail, and the same metadata now flows through `packing.budget_dropped` and runtime context receipts for that reserved-away stale decoy.
- Tests: added focused store and runner assertions on the reproduced budget-decoy case, then ran `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store.MemoryStoreTest.test_chronology_relation_budget_packing_keeps_selected_support_chain_when_it_fits tests.test_runner.RunnerTest.test_build_context_chronology_budget_records_reserved_support_chain_exclusion_reason -q` -> failed pre-fix (`2` failures), then passed post-fix (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 329 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 164 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema migration and no `MemoryStore.search()` return-shape change; additive explainability now includes `reservation_exclusion_reason`, `reservation_exclusion.detail`, `reservation_exclusion.selected_stale_id`, `reservation_exclusion.selected_current_id`, `reservation_exclusion.selected_support_ids`, and `reservation_exclusion.selected_chain_ids` on the reserved-away superseded decoy in both `packing.candidate_priorities` and `packing.budget_dropped`. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice intentionally stops at the applied `chronology_relation_support_chain_v1` path. The adjacent `target_history_support_chain_v1` reservation family still does not emit the same explicit support-chain-beat exclusion contract.
- Next safe slice: extend the same additive reservation-exclusion receipt contract to `target_history_support_chain_v1` only if a reproduced budget-dropped superseded or no-superseded decoy still lacks an explicit support-chain-beat reason there.

## 2026-07-05T12:59:19Z - update-history anchor-pair decoys now carry explicit reservation-exclusion receipts

- Scope: shipped one bounded L3 local-first retrieval slice that adds the missing receipt-visible exclusion contract for the applied `update_history_anchor_pair_v1` reservation path.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: update-history queries such as `what did the deploy target change from` still keep the same injected reserved pair `Deploy target changed to Render.` plus `Deploy target changed to Production.` under the exact two-memory budget, but the dropped older superseded decoy `Deploy target is Heroku.` now carries additive `packing.candidate_priorities[*].reservation_exclusion_reason=update-history-anchor-pair-reserved` plus matching `reservation_exclusion` detail, and the same metadata now flows through `packing.budget_dropped` and runtime context receipts for that reserved-away stale decoy.
- Tests: added focused store and runner assertions on the reproduced budget-decoy case, then ran `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store.MemoryStoreTest.test_update_history_budget_packing_keeps_previous_and_latest_current_anchors tests.test_runner.RunnerTest.test_build_context_update_history_budget_records_reserved_anchor_pair_exclusion_reason -q` -> failed pre-fix (`2` failures), then passed post-fix (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 328 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 163 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema migration and no `MemoryStore.search()` return-shape change; additive explainability now includes `reservation_exclusion_reason`, `reservation_exclusion.detail`, `reservation_exclusion.selected_stale_id`, `reservation_exclusion.selected_current_id`, and `reservation_exclusion.selected_pair_ids` on the reserved-away superseded decoy in both `packing.candidate_priorities` and `packing.budget_dropped`. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice intentionally stops at the pair-beat exclusion contract for the applied `update_history_anchor_pair_v1` path. Adjacent `chronology_relation_support_chain_v1` reservation families still do not emit the same explicit stale-current support-chain exclusion contract.
- Next safe slice: extend the same additive reservation-exclusion receipt contract to `chronology_relation_support_chain_v1` only if a reproduced budget-dropped superseded decoy still lacks an explicit support-chain-beat reason there.

## 2026-07-05T08:59:43Z - recent-history support-chain decoys now carry explicit reservation-exclusion receipts

- Scope: shipped one bounded L3 local-first retrieval slice that adds the missing receipt-visible exclusion contract for the applied recent-history relation support-chain reservation path.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: recent-history relation queries such as `what did the api gateway point at before` still keep the same injected reserved chain `API gateway points to staging.` plus `API gateway points to production.` plus `API gateway points changed after migration.` under the exact three-memory budget, but the dropped older superseded decoy `API gateway points to canary.` now carries additive `packing.candidate_priorities[*].reservation_exclusion_reason=history-support-chain-reserved` plus matching `reservation_exclusion` detail, and the same metadata now flows through `packing.budget_dropped` and runtime context receipts for that reserved-away stale decoy.
- Tests: added focused store and runner assertions on the reproduced budget-decoy case, then ran `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store.MemoryStoreTest.test_recent_history_relation_budget_packing_keeps_selected_support_chain_when_it_fits tests.test_runner.RunnerTest.test_recent_history_relation_context_budget_keeps_selected_support_chain_when_it_fits -q` -> failed pre-fix (`2` failures), then passed post-fix (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 326 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 161 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema migration and no `MemoryStore.search()` return-shape change; additive explainability now includes `reservation_exclusion_reason`, `reservation_exclusion.detail`, `reservation_exclusion.selected_stale_id`, `reservation_exclusion.selected_current_id`, `reservation_exclusion.selected_support_ids`, and `reservation_exclusion.selected_chain_ids` on the reserved-away superseded decoy in both `packing.candidate_priorities` and `packing.budget_dropped`. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice intentionally stops at the applied `history_anchor_pair_v1` support-chain path. Adjacent `update_history_anchor_pair_v1` and `chronology_relation_support_chain_v1` reservation families still do not emit the same reservation-exclusion contract.
- Next safe slice: extend the same additive reservation-exclusion receipt contract to `update_history_anchor_pair_v1` only if a reproduced budget-dropped superseded decoy still lacks an explicit stale-current pair-beat reason there.

## 2026-07-05T04:56:12Z - current-side update-support decoys now carry explicit reservation-exclusion receipts

- Scope: shipped one bounded L3 local-first retrieval slice that adds the missing receipt-visible exclusion contract for current-side update-support decoys displaced by the reserved relation-plus-support pair.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: passive current-side update queries such as `what did db use change to` now keep the same injected pair and budget outcome as the prior reservation slice, but the reserved-away current decoy now carries additive `packing.candidate_priorities[*].reservation_exclusion_reason=update-current-support-pair-reserved` plus matching `reservation_exclusion` detail, and the same metadata now flows through `packing.budget_dropped` and runtime context receipts for the dropped decoy.
- Tests: added focused store and runner assertions on the reproduced budget-decoy case, then ran `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store.MemoryStoreTest.test_passive_update_current_pair_budget_prefers_explicit_relation_plus_support_anchor tests.test_runner.RunnerTest.test_passive_update_current_context_pair_budget_prefers_explicit_relation_plus_support_anchor -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 323 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 159 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema migration and no `MemoryStore.search()` return-shape change; additive explainability now includes `reservation_exclusion_reason`, `reservation_exclusion.detail`, `reservation_exclusion.selected_current_id`, `reservation_exclusion.selected_support_ids`, and `reservation_exclusion.selected_pair_ids` on the reserved-away current decoy in both `packing.candidate_priorities` and `packing.budget_dropped`. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice intentionally stops at the current-side `update_current_support_pair_v1` reservation path. Other support-chain reservation families still do not emit the same reservation-exclusion contract.
- Next safe slice: extend the same additive reservation-exclusion receipt contract to one adjacent support-chain reservation family such as `target_history_support_chain_v1` only if a reproduced budget-decoy case still lacks an explicit pair-beat reason.

## 2026-07-05T01:00:23Z - current-side relation-update support pairs now reserve the explicit current fact plus support anchor

- Scope: shipped one bounded L3 local-first retrieval slice that turns the previously logged current-side passive update-support backfill into a tight-budget packing contract.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: passive current-side update queries such as `what did db use change to` now set `temporal.injection_strategy=update_current_relation_support_anchor_first_v1`, keep the explicit relation fact as `selected_current_anchor_id`, reserve `packing.reservation.strategy=update_current_support_pair_v1`, and inject the explicit current relation fact plus the selected generic update-support anchor together when both fit, even if a higher-authority current decoy would otherwise win the knapsack on score alone.
- Tests: added focused store and runner coverage for the reproduced budget-decoy case, then ran `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store.MemoryStoreTest.test_passive_update_current_pair_budget_prefers_explicit_relation_plus_support_anchor tests.test_runner.RunnerTest.test_passive_update_current_context_pair_budget_prefers_explicit_relation_plus_support_anchor -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 322 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 159 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema migration and no `MemoryStore.search()` return-shape change; additive explainability now includes `temporal.injection_reason`, `temporal.selected_relation_current_id`, `temporal.selected_relation_support_ids`, `packing.reservation.fallback_requested_ids`, and `reserved_by_strategy` on candidate priorities plus budget-dropped entries. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, and `docs/CURRENT_STATE.md` found no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice intentionally stops at pair reservation. Higher-ranked current decoys displaced by the reservation still surface only as `budget_dropped` entries rather than a dedicated temporal exclusion reason on the current-side update-support path.
- Next safe slice: add one bounded receipt-visible exclusion contract for current-side update-support decoys so reserved-away current candidates explain why the relation-plus-support pair beat them before any broader current-query generalization.

## 2026-07-04T21:08:00Z - Current-side relation update queries now backfill generic change support locally

- Scope: shipped one bounded L3 local-first retrieval slice that mirrors the existing passive update-history support-anchor path on the current-side relation-update route.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `what did db use change to` / `what did ui require change to` now append the generic update support anchor already present in local memory, expose it through additive `retrieval.update_current_support` plus `query_lookup.update.support_*` metadata, and keep that support memory visible in `temporal.selected_ids` and runtime context alongside the explicit current relation fact instead of silently dropping the update anchor on the current-side path.
- Tests: added focused store and runner coverage for passive current-side update relation support backfill, then ran `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store.MemoryStoreTest.test_search_with_meta_passive_update_current_queries_backfill_generic_change_anchor_support tests.test_runner.RunnerTest.test_passive_update_current_context_backfills_generic_change_anchor_support -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 320 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 158 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema migration and no `MemoryStore.search()` return-shape change; additive receipt explainability now includes `retrieval.update_current_support.selected_candidate_ids`, `query_lookup.update.support_candidate_ids`, `query_lookup.update.support_relation_terms`, and `query_lookup.update.support_subject_term`. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, and `docs/CURRENT_STATE.md` found no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice intentionally stops at retrieval and explainability. Current-side relation-update support pairs still rely on default current ordering and have no dedicated budget reservation or injection-preference contract when only a very small context window fits.
- Next safe slice: add one bounded current-side relation-update pair-budget reservation contract so the explicit current relation fact plus its generic update support anchor stay together under tight budgets.

## 2026-07-04T16:53:46Z - current-side change/move pair budgets now lock the canonical hybrid pair

- Scope: regression-locked one bounded L3 local-first retrieval slice for the already-present current-side deploy target/destination `change to` / `move to` hybrid path under an exact two-memory budget.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: no retrieval implementation changed in this run. `what did the deploy target change to` and `where did the deploy destination move to` now have durable pair-budget coverage proving the baseline-only hybrid path keeps exactly the canonical current pair `Deploy destination is Production` plus the explicit update anchor when only those two memories fit, instead of silently drifting back to a weaker lexical-only packing choice.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_update_current_deploy_target_change_move_pair_budget_keeps_canonical_hybrid_pair_with_baseline_only -q` -> passed (`Ran 1 test` with both `change` and `move` subcases); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_update_current_deploy_target_change_move_pair_budget_keeps_canonical_hybrid_pair -q` -> passed (`Ran 1 test` with both subcases); `git diff --check -- tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 316 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 155 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema or payload change; receipt explainability is unchanged and now explicitly locked for the pair-budget path through `retrieval.hybrid`, `baseline_ranking.hybrid_semantic_signal_applied`, `packing.injected_ids`, `packing.candidate_priorities`, and empty `packing.budget_dropped`.
- Blockers: this remains verification-only for the current-side pair-budget seam and intentionally avoids the already-dirty overlapping retrieval implementation file. The remaining `deploy destination change to` / `deploy target move to` siblings are still not durably locked by this run.
- Next safe slice: add one matching pair-budget regression for the remaining current-side `deploy destination change to` / `deploy target move to` siblings only if that exact seam still matters more than a newly reproduced retrieval miss elsewhere.

## 2026-07-04T12:57:08Z - current-side switch/update pair budgets now lock the canonical hybrid pair

- Scope: regression-locked one bounded L3 local-first retrieval slice for the already-present current-side deploy target/destination `switch to` / `update to` hybrid path under an exact two-memory budget.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: no retrieval implementation changed in this run. `what did the deploy target switch to` and `what did the deploy destination update to` now have durable pair-budget coverage proving the baseline-only hybrid path keeps exactly the canonical current pair `Deploy destination is Production` plus the explicit update anchor when only those two memories fit, instead of silently drifting back to a weaker lexical-only packing choice.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_update_current_deploy_target_family_pair_budget_keeps_canonical_hybrid_pair_with_baseline_only -q` -> passed (`Ran 1 test` with both `switch` and `update` subcases); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_update_current_deploy_target_family_pair_budget_keeps_canonical_hybrid_pair -q` -> passed (`Ran 1 test` with both subcases); `git diff --check -- tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 312 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 152 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema or payload change; receipt explainability is unchanged and now explicitly locked for the pair-budget path through `retrieval.hybrid`, `baseline_ranking.hybrid_semantic_signal_applied`, `packing.injected_ids`, `packing.candidate_priorities`, and empty `packing.budget_dropped`.
- Blockers: this remains verification-only for the current-side pair-budget seam and intentionally avoids the already-dirty overlapping retrieval implementation file. Adjacent `change to` / `move to` pair-budget siblings still are not durably locked by this run.
- Next safe slice: add one matching pair-budget regression for the adjacent current-side `change to` / `move to` family only if that exact seam still matters more than a newly reproduced retrieval miss elsewhere.

## 2026-07-04T08:53:17Z - current-side deploy-target switch/update variants now have durable regression locks

- Scope: regression-locked one bounded L3 local-first retrieval slice for the already-present current-side `switch to` / `update to` deploy target-family hybrid path.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: no retrieval implementation changed in this run. `what did the deploy target switch to`, `what did the deploy destination switch to`, `what did the deploy target update to`, and `what did the deploy destination update to` are now durably covered as part of the same receipt-visible current-direction canonical hybrid path as the earlier `change to` / `move to` family: they stay on `selected_search_basis=update-subject-core` with `selected_search_query=deploy target`, keep the explicit update anchor, strip `switch|update` plus `to` from the semantic probe, introduce the direct current fact `Deploy destination is Production`, and drop the weaker lexical `Deploy target docs mention Production` decoy.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_update_current_deploy_target_query_uses_canonical_hybrid_backfill_without_switch_to_noise tests.test_store.MemoryStoreTest.test_update_current_deploy_destination_query_uses_canonical_hybrid_backfill_without_update_to_noise tests.test_runner.RunnerTest.test_update_current_deploy_target_context_ignores_switch_to_during_hybrid_backfill tests.test_runner.RunnerTest.test_update_current_deploy_destination_context_ignores_update_to_during_hybrid_backfill -q` -> passed (`Ran 4 tests`).
- Artifacts/receipts: no schema, payload-shape, or `MemoryStore.search()` return-shape change. This run only hardens explainability expectations already exposed in `query_lookup.update`, `retrieval.hybrid.effective_query`, `semantic_probe.ignored_query_terms`, lexical keep/drop ids, and temporal selected ids.
- Blockers: this remains verification-only for the current-direction `switch` / `update` verb family on top of already-dirty overlapping retrieval implementation files. Tight-budget packing for this family is still not regression-locked.
- Next safe slice: add one dedicated tight-budget regression for the current-side deploy target/destination `change|move|switch|update ... to` family so the direct fact stays injected when only the canonical hybrid pair fits.

## 2026-07-04T04:54:59Z - current-side deploy target update prompts now use canonical hybrid backfill

- Scope: upgraded one bounded L3 local-first retrieval slice from reproduced current-side deploy-target update misses into the existing canonical hybrid semantic backfill path.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `what did the deploy target change to`, `what did the deploy destination change to`, `where did the deploy target move to`, and `where did the deploy destination move to` no longer keep the weaker lexical-only candidate set `Deploy target docs mention Production` plus `Deploy target changed to Production.` while omitting the direct current fact `Deploy destination is Production`. These current-direction `update-subject-core` prompts now reuse a canonical hybrid override with `effective_query=deploy target`, `effective_query_terms=["deploy","target"]`, and receipt-visible ignored query noise from `update.matched_terms` plus `update.direction_terms`, so hybrid backfill introduces the direct current fact ahead of the lexical decoy while preserving the explicit update anchor, `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape.
- Tests: ad hoc pre-fix repro via `python3 - <<'PY' ... store.inject(...) ... PY` showed all four current-side prompts on `search_mode=fts` with `hybrid_applied=false`, `hybrid_reason=semantic-candidates-already-lexical`, and the direct `Deploy destination is Production` fact omitted from retrieved ids; focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_update_current_deploy_target_query_uses_canonical_hybrid_backfill_without_change_to_noise tests.test_store.MemoryStoreTest.test_update_current_deploy_destination_query_uses_canonical_hybrid_backfill_without_move_to_noise -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 306 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 147 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema or payload change; additive `retrieval.hybrid.effective_query`, `effective_query_terms`, `required_terms`, `semantic_probe.ignored_query_terms`, `lexical_candidate_ids`, `introduced_candidate_ids`, `kept_lexical_candidate_ids`, and `dropped_lexical_candidate_ids` now make this current-direction hybrid override explainable without a migration. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, and `docs/CURRENT_STATE.md` found no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets in the changed active surfaces.
- Blockers: this slice is intentionally narrow to current-side deploy target/destination update prompts using the `change` and `move` verb families. Adjacent `switched to` / `updated to` variants and a dedicated tight-budget regression for this current-direction family remain separate slices.
- Next safe slice: extend the same canonical current-direction hybrid override to `switched to` / `updated to` deploy target or destination prompts, and lock the tight-budget case only if one of those current-side variants still lets the lexical update anchor crowd out the direct fact.

## 2026-07-04T00:51:55Z - current deployment-signoff plain `on point` siblings now have durable regression coverage

- Scope: regression-locked one bounded L3 local-first retrieval slice for the already-correct plain current `on point for deployment signoff` / `sign-off` siblings on the existing `deployment approver` phrase-alias route.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: no retrieval logic changed in this run. `Who is on point for deployment signoff now?` and `Who is on point for deployment sign-off now?` already stayed on `search_mode=fts`, kept `lookup_basis=role-relation-on-point`, selected `selected_search_basis=current-subject-core-phrase-alias`, selected `selected_search_query=deployment approver`, exposed the exact `deployment signoff owner` / `deployment sign off owner` phrase bridge through `current.search_alias_variants`, and injected only the direct current fact `Deployment approver is Priya.`. This run makes that route durable with explicit store/runner regressions while keeping `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape unchanged.
- Tests: ad hoc pre-edit spot repro via `python3 - <<'PY' ... store.inject(...) ... PY` confirmed both phrasings already used the correct `role-relation-on-point` route; focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_deployment_signoff_on_point_queries_use_relation_phrase_alias_route tests.test_runner.RunnerTest.test_current_deployment_signoff_on_point_question_context_uses_relation_phrase_alias_route -q` -> passed (`Ran 2 tests`); `git diff --check -- tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 302 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 146 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no schema or payload changes; receipt explainability stays the same and is now explicitly locked for these plain signoff siblings. Focused stale-target scan on `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, and `docs/CURRENT_STATE.md` returned no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: none for this regression-lock slice in the current tree; the previously noted future-only temporal suite failures are no longer present after the adjacent temporal lane fix already landed.
- Next safe slice: regression-lock the remaining current deployment-signoff owner-wrapper siblings such as `Who owns deployment signoff now?` and `Who is in charge of deployment signoff now?` before widening beyond this signoff wording family.

## 2026-07-03T20:51:58Z - current deployment-signoff `person on point` wrappers now keep the explicit relation route

- Scope: upgraded one bounded L3 local-first retrieval slice from reproduced current `person on point for deployment signoff` receipt-planning drift into the existing `deployment approver` phrase-alias route.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `Who is the person on point for deployment signoff now?` and `Who is the person on point for deployment sign-off now?` already answered correctly, but they no longer degrade to `lookup_basis=current-term`. Both now stay on `search_mode=fts`, keep `lookup_basis=role-relation-on-point`, upgrade to `selected_search_basis=current-subject-core-phrase-alias`, select `selected_search_query=deployment approver`, expose the exact `deployment signoff owner` / `deployment sign off owner` phrase bridge through `current.search_alias_variants`, and still inject only the direct current fact `Deployment approver is Priya.`. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_deployment_signoff_person_on_point_queries_use_relation_phrase_alias_route tests.test_runner.RunnerTest.test_current_deployment_signoff_person_on_point_question_context_uses_relation_phrase_alias_route -q` -> failed pre-fix (`4` failures), then passed post-fix (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> failed on pre-existing unrelated `tests.test_store.MemoryStoreTest.test_query_at_can_focus_only_future_temporal_subset` (`Ran 301 tests`, `1` failure); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> failed on pre-existing unrelated `tests.test_runner.RunnerTest.test_build_context_preserves_query_at_identity_snapshot_controls_for_future_subset` (`Ran 145 tests`, `1` failure); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.lookup_basis=role-relation-on-point`, unchanged `current.search_alias_variants`, `selected_search_basis`, `selected_search_query`, and `temporal.selected_ids` keep this person-wrapper signoff route explainable without a schema migration. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` returned no active stale public repo, raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: mandated full retrieval suites are currently blocked by unrelated pre-existing future-only temporal receipt/context failures outside this slice: `test_query_at_can_focus_only_future_temporal_subset` and `test_build_context_preserves_query_at_identity_snapshot_controls_for_future_subset`.
- Next safe slice: regression-lock the adjacent plain current sibling `Who is on point for deployment signoff now?` / `Who is on point for deployment sign-off now?` so the now-aligned explicit `on-point` route is durable without the extra `person` wrapper.

## 2026-07-03T16:51:21Z - current deployment-signoff `handles` phrasings now infer owner before the local phrase-alias route

- Scope: upgraded one bounded L3 local-first retrieval slice from reproduced current `handles deployment signoff` misses into the existing `deployment approver` phrase-alias route.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `Who handles deployment signoff now?` and `Who handles deployment sign-off now?` no longer fall into `search_mode=semantic` and over-retrieve the decoy `Deployment contact doc mentions Priya.`. Both now stay on `search_mode=fts`, keep `lookup_basis=current-term`, infer `current.role_inferred=owner`, upgrade to `selected_search_basis=current-subject-core-phrase-alias`, select `selected_search_query=deployment approver`, record the exact `handles deployment signoff owner` / `handles deployment sign off owner` bridges under `current.search_alias_variants`, and inject only the direct current fact `Deployment approver is Priya.`. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: direct pre-fix `search_with_meta(...)` spot repro showed both phrasings on `search_mode=semantic` with the decoy retrieved; focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_deployment_signoff_handles_queries_infer_owner_role_before_phrase_alias tests.test_runner.RunnerTest.test_current_deployment_signoff_handles_question_context_infers_owner_role_before_phrase_alias -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 300 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 144 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.current.role_inferred`, `role_inference_reason`, `search_alias_variants`, `selected_search_basis`, `selected_search_query`, and unchanged `temporal.selected_ids` keep this signoff person-role route explainable without a schema migration. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, and this lane log returned no active stale public repo, stale raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice is intentionally narrow to current `handles deployment signoff` phrasings. Adjacent signoff person-role siblings such as `Who is on point for deployment signoff now?` and current owner-wrapper phrasings still rely on spot probes rather than locked regressions.
- Next safe slice: regression-lock one adjacent current deployment-signoff person-role sibling, preferably `Who is on point for deployment signoff now?`, before widening beyond the signoff wording family.

## 2026-07-03T12:48:17Z - current deployment-signoff noun-wrapper now stays on the local phrase-alias route

- Scope: upgraded one bounded L3 local-first retrieval slice from the reproduced current noun-wrapper miss `Who is responsible for deployment signoff now?` into a narrow `deployment approver` phrase-alias bridge.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: the reproduced noun-wrapper query no longer falls through to `search_mode=fallback` and over-injects the decoy `Deployment contact doc mentions Priya.`. It now stays on `search_mode=fts`, keeps `lookup_basis=role-relation-responsible`, upgrades to `selected_search_basis=current-subject-core-phrase-alias`, selects `selected_search_query=deployment approver`, records the exact `deployment signoff owner -> deployment approver` bridge under `current.search_alias_variants`, and injects only the direct current fact `Deployment approver is Priya.` without semantic rescue or fallback routing. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_deployment_signoff_responsible_query_uses_phrase_alias_search_variant tests.test_runner.RunnerTest.test_current_deployment_signoff_responsible_question_context_uses_phrase_alias_search_variant -q` -> failed pre-fix (`2` failures), then passed post-fix (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 299 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 143 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.current.search_alias_variants`, `selected_search_basis`, `selected_search_query`, and `semantic_rescue.applied=false` keep this noun-wrapper local-first route explainable without a schema migration. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` returned no matches for stale public repo or installer targets.
- Blockers: this slice is intentionally narrow to the reproduced `responsible ... signoff` noun-wrapper. Adjacent `signoff` siblings such as `Who owns deployment signoff now?`, `Who is in charge of deployment signoff now?`, and spaced `Who is responsible for deployment sign off now?` now spot-probe correctly on the same route, but they are not yet locked as durable regressions.
- Next safe slice: regression-lock one adjacent current deployment-signoff noun-wrapper sibling, preferably `Who owns deployment signoff now?` or the spaced `Who is responsible for deployment sign off now?`, before widening beyond the sign-off wording family.

## 2026-07-03T08:48:27Z - current deployment sign-off phrasings now stay on the local phrase-alias route

- Scope: upgraded one bounded L3 local-first retrieval slice from reproduced current deployment sign-off misses `Who signs off on deployment now?` and `Who signs off on deployments now?` into narrow `deployment approver` phrase-alias bridges.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: both reproduced current sign-off phrasings no longer fall through to `search_mode=semantic` plus a decoy over-injection or to `search_mode=none`. They now stay on `search_mode=fts`, keep `lookup_basis=current-term`, upgrade to `selected_search_basis=current-subject-core-phrase-alias`, select `selected_search_query=deployment approver`, record the exact `signs off deployment` and `signs off deployments` bridges under `current.search_alias_variants`, and inject only the direct current fact `Deployment approver is Priya.` without a semantic-rescue detour. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_deployment_sign_off_queries_use_phrase_alias_search_variant tests.test_runner.RunnerTest.test_current_deployment_sign_off_question_context_uses_phrase_alias_search_variant -q` -> failed pre-fix (`4` failures), then passed post-fix (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 298 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 142 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.current.search_alias_variants`, `selected_search_basis`, `selected_search_query`, and `semantic_rescue.applied=false` keep this sign-off local-first route explainable without a schema migration. Focused stale-target scan `rg -n 'zerkerlabs/zerker-memory|raw\\.githubusercontent\\.com/.*/zerker-memory|github\\.com/.*/zerker-memory|rezkerlabs' zerker_memory/store.py tests/test_store.py tests/test_runner.py` returned no matches.
- Blockers: this slice is intentionally narrow to the reproduced current sign-off verb phrasings. The adjacent noun-wrapper wording `Who is responsible for deployment signoff now?` still routes through fallback search and over-retrieves the decoy `Deployment contact doc mentions Priya.`.
- Next safe slice: reproduce and fix one current deployment-signoff noun-wrapper variant such as `Who is responsible for deployment signoff now?` before widening beyond the sign-off wording family.

## 2026-07-03T00:46:12Z - singular current `deployment` approval phrasings now stay on the local phrase-alias route

- Scope: upgraded one bounded L3 local-first retrieval slice from the adjacent singular current deployment-approval misses `Who approves deployment now?` and `Who is the approver for deployment now?` into narrow `deployment approver` phrase-alias bridges.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: the reproduced singular current phrasings no longer fall through to `search_mode=semantic` / `selected_search_basis=current-term` and over-retrieve the decoy `Deployment contact doc mentions Priya.`. Both queries now stay on `search_mode=fts`, keep `lookup_basis=current-term`, upgrade to `selected_search_basis=current-subject-core-phrase-alias`, select `selected_search_query=deployment approver`, record the exact `approves deployment` and `approver for deployment` bridges under `current.search_alias_variants`, and inject only the direct current fact `Deployment approver is Priya.` without a semantic-rescue detour. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_singular_deployment_approval_queries_use_phrase_alias_search_variant tests.test_runner.RunnerTest.test_current_singular_deployment_approval_question_context_uses_phrase_alias_search_variant -q` -> failed pre-fix (`4` failures), then passed post-fix (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 297 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 141 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.current.search_alias_variants`, `selected_search_basis`, `selected_search_query`, and `semantic_rescue.applied=false` keep this singular local-first route explainable without a schema migration. Focused stale-target scan `rg -n 'zerkerlabs/zerker-memory|raw\\.githubusercontent\\.com/.*/zerker-memory|github\\.com/.*/zerker-memory|rezkerlabs' zerker_memory/store.py tests/test_store.py tests/test_runner.py` returned no matches.
- Blockers: this slice is intentionally narrow to the reproduced singular current `deployment` approval phrasings. The adjacent article-bearing wording `Who approves the deployment now?` now rides the same alias path locally, but it is not yet locked as a durable regression.
- Next safe slice: regression-lock one article-bearing singular current deployment-approval variant such as `Who approves the deployment now?` before widening beyond the deployment-approval wording family.

## 2026-07-02T20:46:06Z - current `who approves deployments` phrasing now stays on the local phrase-alias route

- Scope: upgraded one bounded L3 local-first retrieval slice from the explicitly queued broader current approval verb miss `Who approves deployments now?` into one narrow `deployment approver` phrase-alias bridge.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: the reproduced current approval verb wording no longer falls through to `search_mode=none` / `selected_search_basis=raw-query`. `Who approves deployments now?` now stays on `search_mode=fts`, keeps `lookup_basis=current-term`, upgrades to `selected_search_basis=current-subject-core-phrase-alias`, selects `selected_search_query=deployment approver`, records the phrase bridge under `current.search_alias_variants` with `canonical_query=approves deployments`, and injects the direct current fact `Deployment approver is Priya.` without a semantic-rescue detour. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_who_approves_deployments_query_uses_phrase_alias_search_variant tests.test_runner.RunnerTest.test_current_who_approves_deployments_question_context_uses_phrase_alias_search_variant -q` -> failed pre-fix (`2` failures), then passed post-fix (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 296 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 139 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.current.search_alias_variants`, `selected_search_basis`, `selected_search_query`, and `semantic_rescue.applied=false` keep this local-first route explainable without a schema migration. Focused stale-target scan `rg -n 'zerkerlabs/zerker-memory|raw\\.githubusercontent\\.com/.*/zerker-memory|github\\.com/.*/zerker-memory|rezkerlabs' zerker_memory/store.py tests/test_store.py tests/test_runner.py` returned no matches.
- Blockers: this slice is intentionally narrow to the reproduced plural current approval verb wording. Adjacent singular deployment-approval verb phrasings or broader approval-verb normalization remain separate retrieval slices.
- Next safe slice: reproduce and fix one adjacent current deployment-approval verb variant outside the now-covered plural wording before widening into broader verb normalization or hosted-provider retrieval changes.

## 2026-07-02T16:44:42Z - current `approver for deployments` phrasing now stays on the local phrase-alias route

- Scope: upgraded one bounded L3 local-first retrieval slice from the explicitly queued adjacent current approver wording miss `Who is the approver for deployments now?` into one narrow `deployment approver` phrase-alias bridge.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: the reproduced current approver wording no longer falls through to `search_mode=semantic` / `selected_search_basis=current-term`. `Who is the approver for deployments now?` now stays on `search_mode=fts`, keeps `lookup_basis=current-term`, upgrades to `selected_search_basis=current-subject-core-phrase-alias`, selects `selected_search_query=deployment approver`, records the phrase bridge under `current.search_alias_variants`, and injects the direct current fact `Deployment approver is Priya.` without a semantic-rescue detour. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_deployment_approver_for_deployments_query_uses_phrase_alias_search_variant tests.test_runner.RunnerTest.test_current_deployment_approver_for_deployments_question_context_uses_phrase_alias_search_variant -q` -> failed pre-fix (`2` failures), then passed post-fix (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 295 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 136 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.current.search_alias_variants`, `selected_search_basis`, `selected_search_query`, and `semantic_rescue.applied=false` keep this local-first route explainable without a schema migration. Focused stale-target scan `rg -n 'zerkerlabs/zerker-memory|raw\\.githubusercontent\\.com/.*/zerker-memory|github\\.com/.*/zerker-memory|rezkerlabs' zerker_memory/store.py tests/test_store.py tests/test_runner.py` returned no matches.
- Blockers: this slice is intentionally narrow to the reproduced current `approver for deployments` wording. The broader verb phrasing `Who approves deployments now?` still has no lexical bridge and remains a separate retrieval slice.
- Next safe slice: fix one next reproduced local-first current deployment-approval wording miss, preferably `Who approves deployments now?`, before widening into broader verb or hosted-provider retrieval changes.

## 2026-07-01T13:10:50Z - reordered deployment-approval contact current phrasing now stays on the local phrase-alias route

- Scope: upgraded one bounded L3 local-first retrieval slice from the explicitly queued reordered current contact miss `Who is the approval contact for deployments now?` into a narrow `deployment approver` phrase-alias expansion.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: reordered current contact wording with `deployments` plural no longer falls through to `search_mode=semantic` / `selected_search_basis=current-term`. The reproduced query `Who is the approval contact for deployments now?` now stays on `search_mode=fts`, keeps `lookup_basis=current-term`, upgrades to `selected_search_basis=current-subject-core-phrase-alias`, selects `selected_search_query=deployment approver`, records the phrase alias under `current.search_alias_variants`, and injects the direct current fact `Deployment approver is Priya.` instead of a semantic-rescue decoy. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_reordered_deployment_approval_contact_query_uses_phrase_alias_search_variant tests.test_runner.RunnerTest.test_current_reordered_deployment_approval_contact_question_context_uses_phrase_alias_search_variant -q` -> failed pre-fix (`2` failures), then passed post-fix (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 294 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 135 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.current.search_alias_variants`, `selected_search_basis`, `selected_search_query`, `semantic_rescue.applied=false`, and the unchanged temporal current-selection metadata keep this reordered local-first route explainable without a schema migration. Focused stale-target scan `rg -n 'zerkerlabs/zerker-memory|raw\\.githubusercontent\\.com/.*/zerker-memory|github\\.com/.*/zerker-memory|rezkerlabs' zerker_memory/store.py tests/test_store.py tests/test_runner.py` returned no matches.
- Blockers: this slice is intentionally narrow to reordered current contact phrasing that still clearly means `deployment approver`. Adjacent current approver phrasings such as `Who is the approver for deployments now?` still depend on semantic rescue, and `Who approves deployments now?` still has no lexical bridge.
- Next safe slice: fix one next reproduced local-first current deployment-approval alias miss, preferably the adjacent approver wording family so `Who is the approver for deployments now?` can stay on the same FTS phrase-alias route before considering broader verb normalization.

## 2026-07-01T08:42:07Z - current deployment-approval person phrasings now infer the owner role before the phrase-alias route

- Scope: upgraded one bounded L3 local-first retrieval slice from a reproduced current-query normalization miss into a narrow owner-role inference reuse on the existing `deployment approver` phrase-alias path.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `Who handles deployment approvals now?` no longer falls back to semantic rescue even when `Deployment approver is Priya.` is already present. Current `who`-wrapper person queries now reuse the same deployment-approval owner-role inference already used on the history/update paths, so this repro stays on `search_mode=fts`, keeps `lookup_basis=current-term`, upgrades to `selected_search_basis=current-subject-core-phrase-alias`, selects `selected_search_query=deployment approver`, records `current.role_inferred=owner`, and still injects the direct current fact ahead of the sibling `changed to ...` anchor. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_deployment_approval_person_query_infers_owner_role_before_phrase_alias tests.test_runner.RunnerTest.test_current_deployment_approval_person_question_context_infers_owner_role_before_phrase_alias -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md docs/SWARM_OPERATION_TRACKER.md docs/BUILD_LOG.md docs/CURRENT_STATE.md` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 293 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 134 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.current.role_inferred`, `role_inference_reason`, `selected_search_basis`, `selected_search_query`, `retrieval.temporal.selection_reason`, and `selected_ids` now keep this local-first route explainable without a schema migration. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo, stale raw-installer, stale GitHub repo, or `rezkerlabs` targets.
- Blockers: this slice is intentionally narrow to current `who`-wrapper deployment-approval person phrasings that can reuse the existing owner-role inference. Reordered contact phrasings such as `Who is the approval contact for deployments now?` still sit outside this exact route and should be treated as a separate retrieval slice if they prove worth fixing.
- Next safe slice: fix one next reproduced local-first current-query normalization miss, preferably a contact word-order or deployment/deployments alias variant that still falls back or uses semantic rescue instead of the existing FTS phrase-alias route.

## 2026-06-30T20:37:07Z - plural deployment-approval no-overview owner-wrapper variants are now regression-locked on the phrase-alias multi-hop route

- Scope: shipped one bounded L3 local-first retrieval regression-lock slice only, covering the remaining plural no-overview owner-wrapper deployment-approval variants on the already-landed `deployment approver` phrase-alias plus multi-hop path.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior locked: `who owns deployment approvals rollback policy notes`, `who is on point for deployment approvals rollback policy notes`, `who is responsible for deployment approvals rollback policy notes`, and `who is in charge of deployment approvals rollback policy notes` are now all regression-locked to keep `search_mode=fts`, wrapper-specific `lookup_basis`, wrapper-specific `selected_search_basis=...-phrase-alias`, `selected_search_query=deployment approver`, `activation_reason=fts-direct-subject-compound-query`, and the same explicit approver-plus-rollback injection under a no-overview budget. This run did not widen `zerker_memory/store.py`, `MemoryStore.search()` return shape, SQLite schema, or receipt payload shape.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_no_overview_deployment_approval_owner_wrapper_compound_queries_use_phrase_alias_parent_for_multi_hop -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_no_overview_deployment_approval_owner_wrapper_compound_queries_use_phrase_alias_parent -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 291 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 131 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no new receipt fields were needed because the existing additive retrieval metadata already keeps the route explainable through `lookup_basis`, `selected_search_basis`, `selected_search_query`, `multi_hop.activation_reason`, subquery queries, retrieved ids, injected ids, and `packing.budget_dropped`.
- Blockers: this slice is intentionally regression-only. Singular `deployment approval ...` wrapper variants and adjacent history/update wrapper phrasings were spot-probed locally and behaved correctly, but they are not yet locked as durable regressions.
- Next safe slice: move to the next reproduced retrieval-quality miss outside this now-covered plural owner-wrapper family, preferably a concrete tight-budget, temporal/update, or ranking misorder repro where explainable receipts still need stronger locking or a small local-first behavior change.

## 2026-06-30T16:35:51Z - no-overview `responsible for deployment approvals rollback policy notes` now reuses the owner-wrapper phrase-alias route for multi-hop recovery

- Scope: upgraded one bounded L3 local-first retrieval slice from the previously queued no-overview owner-wrapper miss `who is responsible for deployment approvals rollback policy notes` into a narrow multi-hop auto-enable gate fix plus regression lock.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: owner-wrapper compound queries that already resolve through `lookup_basis=role-relation-responsible` and `selected_search_basis=role-relation-responsible-phrase-alias` now remain eligible for the existing owner/deployment multi-hop planner instead of being rejected before decomposition because the selected FTS basis was the phrase alias. With only `Deployment approver is ...` plus rollback facts present, the no-overview wrapper query `who is responsible for deployment approvals rollback policy notes` now stays on `search_mode=fts`, keeps `selected_search_query=deployment approver`, auto-enables `activation_reason=fts-direct-subject-compound-query`, and injects the specific approver plus rollback facts without changing `MemoryStore.search()` return shape, SQLite schema, or receipt payload shape.
- Tests: focused regression coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_no_overview_responsible_deployment_approval_compound_query_uses_phrase_alias_parent_for_multi_hop tests.test_runner.RunnerTest.test_build_context_no_overview_responsible_deployment_approval_compound_query_uses_phrase_alias_parent -q` -> passed (`Ran 2 tests`) after first reproducing the pre-fix failure (`2` failures); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md docs/SWARM_OPERATION_TRACKER.md docs/BUILD_LOG.md docs/CURRENT_STATE.md` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 291 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 130 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.lookup_basis`, `selected_search_basis`, `selected_search_query`, `retrieval.multi_hop.activation_reason`, `retrieval.multi_hop.subqueries[*].query`, `retrieved_memory_ids`, `injected_memory_ids`, and `retrieval.packing.budget_dropped` now keep the no-overview responsible-wrapper route explainable without a schema migration. Focused stale-target scan on the changed retrieval code/tests found no active stale public repo, stale raw-installer, `rezkerlabs`, or stale GitHub repo targets; older references remain confined to long-lived historical state/log text.
- Blockers: this fix is intentionally narrow to owner-relation compound wrappers that already resolve onto the deployment-approver phrase alias. The adjacent no-overview `who owns...`, `who is on point for...`, and `who is in charge of...` wrapper variants are not yet regression-locked on the no-overview path.
- Next safe slice: lock the remaining no-overview owner-wrapper variants to the same phrase-alias multi-hop route, and only widen beyond that family if one of those concrete wrappers still falls back or misorders the rollback fact under budget.

## 2026-06-30T12:33:43Z - plural deployment-approval owner no-overview queries now promote `deployment approver` before the no-match fallback

- Scope: upgraded one bounded L3 local-first retrieval slice from the previously queued no-overview plural `deployment approvals owner rollback policy notes` miss into a narrow direct-subject phrase-alias parent lookup plus regression lock.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: direct-subject owner rollback lookup now appends the existing deployment-approval owner phrase alias as a later FTS parent candidate, so when only `Deployment approver is ...` plus rollback facts exist the plural query `deployment approvals owner rollback policy notes` no longer falls through to `search_mode=none` / `activation_reason=no-lexical-match-compound-query`. It now resolves through `selected_search_query=deployment approver`, `selected_search_basis=direct-subject-phrase-alias`, stays on `fts-direct-subject-compound-query`, keeps `deployment approver`, `deployment approver is`, `deployment approvals rollback policy`, and `rollback policy is` receipt-visible, and still injects the specific owner plus rollback facts without an overview memory present. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused owner retrieval/runtime coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_deployment_approval_owner_query_auto_enables_multi_hop_and_recovers_specific_facts tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_deployment_approval_owner_query_without_overview_uses_phrase_alias_parent tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_deployment_approval_owner_query_recovers_specific_rollback_fact tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_deployment_approval_owner_query_without_overview_uses_phrase_alias_parent -q` -> passed (`Ran 4 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 289 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 128 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.selected_search_query`, `selected_search_basis`, `semantic_aliases.search_alias_variants`, `retrieval.multi_hop.activation_reason`, `retrieval.multi_hop.subqueries[*].query`, `retrieved_memory_ids`, and `injected_memory_ids` now keep the no-overview plural owner variant explainable on the FTS phrase-alias route without a schema migration. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo, stale raw-installer, `rezkerlabs`, or stale GitHub repo targets.
- Blockers: this widening stays intentionally narrow to the direct-subject owner family using the existing deployment-approval phrase alias; owner-wrapper no-overview queries and non-deployment owner families are still unverified.
- Next safe slice: add the matching no-overview owner-wrapper regression for `who is responsible for deployment approvals rollback policy notes`, and only widen direct-subject owner phrase-alias parent selection further if that concrete wrapper miss still falls back to no-match routing.

## 2026-06-30T04:33:49Z - plural deployment-approval contact rollback queries now reuse the FTS direct-subject alias route

- Scope: upgraded one bounded L3 local-first retrieval slice from the previously queued adjacent plural `deployment approvals contact rollback policy notes` miss into a narrow direct-subject lexical-number-alias reuse plus regression lock.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: direct-subject alias expansion now extends the existing local `approval`/`approvals` lexical alias reuse to the deployment-approval contact rollback family, so the plural direct-subject query `deployment approvals contact rollback policy notes` now finds the existing singular overview through FTS instead of dropping into `search_mode=none` / `activation_reason=no-lexical-match-compound-query`. The query now resolves with `selected_search_query=deployment approval contact rollback policy notes`, `selected_search_basis=direct-subject-alias`, keeps `deployment approver`, `deployment approver is`, `deployment approvals rollback policy`, and `rollback policy is` as receipt-visible subqueries, and still injects the specific contact plus rollback facts ahead of the overview under tight budget packing. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused decomposition/search/runtime coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_direct_subject_phrase_alias_planner_for_deployment_approval_contact_query tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_deployment_approval_contact_query_auto_enables_multi_hop_and_recovers_specific_facts tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_deployment_approval_contact_query_recovers_specific_rollback_fact -q` -> passed (`Ran 3 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md docs/SWARM_OPERATION_TRACKER.md docs/BUILD_LOG.md docs/CURRENT_STATE.md` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 285 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 125 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.selected_search_query`, `selected_search_basis`, `semantic_aliases.search_alias_variants`, `retrieval.multi_hop.activation_reason`, `retrieval.multi_hop.subqueries[*].query`, `retrieved_memory_ids`, `injected_memory_ids`, and `retrieval.packing.budget_dropped[*]` now keep the plural contact variant explainable on the FTS alias route without a schema migration. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo or raw-installer targets; older stale strings remain confined to historical tracker/build-state text.
- Blockers: this fix stays intentionally narrow to the overview-backed direct-subject plural contact compound. If only `Deployment approver is ...` plus rollback facts exist and no singular `Deployment approval contact rollback policy notes.` overview exists, the same query still falls back to `search_mode=none` / `activation_reason=no-lexical-match-compound-query`.
- Next safe slice: widen the same plural contact family so direct-subject phrase-alias FTS can promote `deployment approver` before the no-match compound fallback when the overview is absent, and only widen it if the focused no-overview repro plus receipt coverage stays local-first and explainable.

## 2026-06-30T00:32:20Z - plural deployment-approval owner rollback queries now stay on the FTS direct-subject path

- Scope: upgraded one bounded L3 local-first retrieval slice from the previously logged but still-uncovered plural `deployment approvals owner rollback policy notes` variant into a narrow lexical alias fix plus regression lock.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: subject-core search variants now treat `approval` and `approvals` as lexical aliases, so the plural direct-subject owner query `deployment approvals owner rollback policy notes` now finds the existing singular overview through FTS instead of dropping into `search_mode=none` / `activation_reason=no-lexical-match-compound-query`. The query now stays on the same local `fts-direct-subject-compound-query` route as the singular variant, keeps `deployment approver`, `deployment approver is`, `deployment approvals rollback policy`, and `rollback policy is` as receipt-visible subqueries, and still injects the specific approver plus rollback facts ahead of the overview under tight budget packing. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused decomposition/search/runtime coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_direct_subject_phrase_alias_for_deployment_approval_owner_query tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_deployment_approval_owner_query_auto_enables_multi_hop_and_recovers_specific_facts tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_deployment_approval_owner_query_recovers_specific_rollback_fact -q` -> passed (`Ran 3 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md docs/SWARM_OPERATION_TRACKER.md docs/BUILD_LOG.md docs/CURRENT_STATE.md` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 284 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 124 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.selected_search_query`, `selected_search_basis`, `retrieval.multi_hop.activation_reason`, `retrieval.multi_hop.subqueries[*].query`, `retrieved_memory_ids`, `injected_memory_ids`, and `retrieval.packing.budget_dropped[*]` now keep the plural variant explainable on the same lexical route as the singular query without a schema migration. Focused stale-target scans on `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, and `docs/CURRENT_STATE.md` found no active stale public repo or raw-installer targets.
- Blockers: the earlier `target_history_support_preferred_v1` overflow follow-up was stale for the current code path; manual repro with pair-sized `What did Blue Finch deploy to before it moved to Production?` budgets still shows `target_history_support_chain_v1` already requesting only the support/current pair, so there is no extra-anchor fallback to widen on that path without a different reproduced miss.
- Next safe slice: benchmark one adjacent plural direct-subject phrase-alias family such as `deployment approvals contact rollback policy notes`, and only widen lexical alias normalization further if that concrete query still falls back to no-match routing or lets the overview crowd out explicit facts.

## 2026-06-29T16:30:11Z - recent-history tight budgets now fall back to previous plus latest current before dropping support

- Scope: upgraded one bounded L3 local-first retrieval slice from a concrete recent-history target-query packing miss into a narrow reservation fallback for `what was the previous deploy target`.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: when a `history_anchor_pair_v1` or sibling earliest/update-history support-chain reservation would overflow the context budget but the selected stale-plus-latest-current anchor pair still fits, `pack_memory_context(...)` now applies that smaller pair instead of abandoning reservation entirely. The concrete `what was the previous deploy target` miss now injects `Render + Production` and budget-drops `Heroku + changed after CAB review` instead of injecting `Render + Heroku` and dropping the latest current fact. Receipts stay explainable through additive `retrieval.packing.reservation.fallback_requested_ids`, `fallback_applied`, and `fallback_reason` metadata without changing `MemoryStore.search()` return shape, SQLite schema, or receipt payload shape.
- Tests: focused overflow store coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_recent_history_budget_packing_falls_back_to_previous_and_latest_current_when_support_chain_exceeds_budget -q` -> passed (`Ran 1 test`); focused runtime-context guard `python3 -m unittest tests.test_runner.RunnerTest.test_recent_history_context_budget_falls_back_to_previous_and_latest_current_when_support_chain_exceeds_budget -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 281 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 123 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.packing.reservation.fallback_requested_ids`, `fallback_applied`, `fallback_reason`, `applied_ids`, `retrieved_memory_ids`, `injected_memory_ids`, and `retrieval.packing.budget_dropped[*]` now make the overflow fallback explicit instead of leaving the dropped current fact unexplained. Focused stale-target scans on `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, and `docs/CURRENT_STATE.md` found no active stale public repo or raw-installer targets.
- Blockers: this fallback is intentionally narrow to history-anchor reservations that already selected a stale/current answer pair plus extra support; chronology relation support chains and any future broader reservation families still keep their current overflow behavior until a concrete miss justifies widening this heuristic.
- Next safe slice: probe one chronology relation-support overflow case where the stale-plus-current answer pair fits but the extra support anchor does not, and only extend the same fallback if that concrete budget miss reproduces.

## 2026-06-29T12:31:41Z - mixed owner-plus-contact two-fact budgets now reserve role facts before rollback

- Scope: upgraded one bounded L3 local-first retrieval slice from the previously identified mixed owner-plus-contact tight-budget gap into a narrow two-fact packing reservation for `status page owner routing contact rollback policy notes` plus `status page owner routing escalation contact rollback policy notes`.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `pack_memory_context(...)` now applies a narrow `mixed_owner_contact_role_pair_v1` reservation when direct-subject multi-hop retrieval sees the mixed owner-plus-contact-plus-rollback family with both owner/maintainer and escalation-contact fact subqueries present. That fixes the concrete `196`-token gap where both `owner+rollback` and `owner+contact` fit but the previous knapsack chose rollback because it carried the top fusion rank. Receipts now keep `owner+contact` injected, budget-drop `rollback+overview`, and expose the reservation decision in `retrieval.packing.reservation` without changing `MemoryStore.search()` return shape, SQLite schema, or receipt payload shape.
- Tests: focused tight-budget store coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_owner_phrase_alias_queries_reserve_owner_and_contact_under_two_fact_budget -q` -> passed (`Ran 1 test`); focused tight-budget runtime-context guard `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_direct_subject_owner_phrase_alias_queries_reserve_owner_and_contact_under_two_fact_budget -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 279 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 121 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.packing.reservation`, `candidate_priorities[*].reserved_by_strategy`, `retrieved_memory_ids`, `injected_memory_ids`, and `retrieval.packing.budget_dropped[*]` now make the two-fact role-preservation decision explicit without a schema migration. Focused stale-target scans on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo or raw-installer targets.
- Blockers: this reservation stays intentionally narrow to direct-subject mixed owner-plus-contact rollback compounds with owner/maintainer plus contact role facts present; it does not introduce a broader role-priority model or widen temporal/current/history variants of the same family.
- Next safe slice: only widen this reservation to another mixed multi-role family if a concrete miss shows a requested role fact still losing to ancillary rollback facts under a two-memory budget.

## 2026-06-29T08:31:24Z - direct-subject mixed owner-plus-contact rollback queries now keep contact facts ahead of the overview

- Scope: upgraded one bounded L3 local-first retrieval slice from the previously flagged direct-subject mixed owner-plus-phrase-alias benchmark miss into a narrow owner-plus-contact planner and tie-break fix for `status page owner routing contact rollback policy notes` plus `status page owner routing escalation contact rollback policy notes`.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: direct-subject owner-plus-rollback multi-hop plans now add the existing local phrase-alias contact probe when the same query also carries `routing contact` or `routing escalation contact`, and deterministic baseline ranking now prefers a subquery-introduced specific fact over a parent-only overview when multi-hop fusion evidence ties. That keeps `status page owner`, `status page maintainer`, `status page escalation contact`, `status page rollback policy`, and `rollback policy is` explainable in receipts for these mixed queries, so the owner/contact/rollback facts rank ahead of the overview instead of letting the overview crowd out the contact fact on exact-match lexical score alone. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused mixed owner-plus-contact retrieval coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_owner_phrase_alias_queries_recover_owner_contact_and_rollback_facts -q` -> passed (`Ran 1 test`); focused budgeted runtime-context guard `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_direct_subject_owner_phrase_alias_queries_drop_overview_before_specific_facts -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 277 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 119 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.subqueries[*].query`, `retrieval.multi_hop.candidate_attribution`, candidate `multi_hop_fusion_rank` / `introduced_by_subquery_id`, `retrieved_memory_ids`, `injected_memory_ids`, and `retrieval.packing.budget_dropped[*]` now make the mixed owner-plus-contact tie-break explainable without a schema migration. Focused stale-target scans on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo or raw-installer targets.
- Blockers: this slice stays intentionally narrow to direct-subject mixed owner-plus-phrase-alias rollback compounds and equal-score overview tie-breaks; it does not yet lock a tighter two-fact packing contract or widen temporal/current/history variants of the same family.
- Next safe slice: lock one adjacent tight-budget packing contract for the same mixed owner-plus-phrase-alias family so owner plus contact facts stay ahead of rollback plus overview when only two memories fit, and only widen role-priority packing if a concrete miss still contradicts that behavior.

## 2026-06-29T04:30:07Z - owner-wrapper phrase-alias rollback queries now trim contact qualifiers before FTS and multi-hop

- Scope: upgraded one bounded L3 local-first retrieval slice from the previously flagged adjacent owner-plus-phrase-alias benchmark miss into a narrow owner-wrapper normalization fix for `status page routing contact rollback policy notes` and `status page routing escalation contact rollback policy notes`.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: owner-family wrapper queries such as `who owns the status page routing contact rollback policy notes`, `who is on point for the status page routing escalation contact rollback policy notes`, and adjacent `responsible` / `in charge` variants now trim the trailing phrase-alias qualifier from the owner-wrapper subject before building both the primary FTS lookup and the local `multi_hop` owner planner. That keeps the wrapped owner path explainable and local-first while switching from dead-end probes like `status page routing escalation contact owner` to the existing specific-fact path `status page owner`, `status page maintainer`, `status page rollback policy`, and `rollback policy is`. The concrete retrieval gain is that wrapped owner queries now recover the owner plus rollback facts and budget-drop the overview instead of falling back to semantic rescue or promoting the overview when the owner fact exists. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused owner-wrapper phrase-alias coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_owner_relation_phrase_alias_normalization_for_status_page_contact_query tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_owner_relation_phrase_alias_normalization_for_status_page_escalation_contact_query tests.test_store.MemoryStoreTest.test_compound_fts_owner_relation_status_page_phrase_alias_queries_recover_owner_and_skip_generic_decoys -q` -> passed (`Ran 3 tests`); focused runtime-context guard `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_owner_relation_status_page_phrase_alias_queries_recover_owner_and_skip_generic_subject_only_decoys -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 276 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 117 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.selected_search_query`, `retrieval.multi_hop.subqueries[*].query`, `retrieved_memory_ids`, `injected_memory_ids`, and `retrieval.packing.budget_dropped[*]` now stay explainable on these wrapped owner-plus-phrase-alias families without a schema migration. Focused stale-target scans on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo or raw-installer targets.
- Blockers: this owner-wrapper fix stays intentionally narrow to phrase-alias qualifiers riding inside owner-family wrapper subjects; it does not yet widen direct-subject mixed owner-plus-contact overview compounds or broader phrase-alias ranking unless a concrete miss proves those still crowd out a needed owner/contact fact.
- Next safe slice: benchmark one adjacent direct-subject mixed owner-plus-phrase-alias overview family such as `status page owner routing escalation contact rollback policy notes`, and only add explicit dual-role subqueries if that concrete miss still causes either owner or contact facts to lose to the overview under tight budget packing.

## 2026-06-29T00:25:22Z - escalation-contact phrase-alias rollback contract now locks the adjacent no-decoy path

- Scope: upgraded one bounded L3 local-first retrieval slice from a concrete adjacent benchmark probe into contract coverage for the already-correct `status page routing escalation contact rollback policy notes` phrase-alias rollback family.
- Files touched: `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: no store or runner retrieval semantics changed. The shipped local `multi_hop` path is now regression-locked to keep using `status page escalation contact`, `status page rollback policy`, and `rollback policy is` for `status page routing escalation contact rollback policy notes`, while continuing to skip the generic `status page` subquery so unrelated subject-only facts like `Status page dashboard is public.` stay out of retrieved results and tight-budget context packing. `MemoryStore.search()` return shape, SQLite schema, and receipt payload shape stay unchanged.
- Tests: focused escalation-contact coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_direct_subject_phrase_alias_planner_for_escalation_contact_compound_query tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_phrase_alias_escalation_contact_query_skips_generic_subject_only_decoys -q` -> passed (`Ran 2 tests`); focused runtime-context guard `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_direct_subject_phrase_alias_escalation_contact_queries_skip_generic_subject_only_decoys -q` -> passed (`Ran 1 test`); `git diff --check -- tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 273 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 115 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: this additive contract slice keeps `retrieval.multi_hop.subqueries[*].query`, `retrieved_memory_ids`, and `retrieval.packing.budget_dropped[*]` explainable on the adjacent escalation-contact family too without a schema migration. Focused stale-target scans on the changed retrieval code/tests found no active stale public launch targets; older stale strings remain confined to historical long-lived log text.
- Blockers: this slice locks the already-shipped adjacent phrase-alias path only; owner-plus-phrase-alias rollback compounds and broader non-owner planners still keep their current coverage unless a concrete miss proves they reintroduce generic owner or subject-only noise.
- Next safe slice: benchmark one adjacent owner-plus-phrase-alias rollback family such as `status page owner routing contact rollback policy notes`, and only widen the owner-intent planner if that concrete miss still retrieves or budget-drops generic owner/subject-only facts.

## 2026-06-28T20:25:55Z - phrase-alias rollback plans now skip generic subject-only decoys

- Scope: upgraded one L3 local-first retrieval slice from owner-only bare-subject suppression to the adjacent subject-anchored non-owner phrase-alias rollback family.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `_direct_subject_phrase_alias_multi_hop_plans(...)` no longer emits the bare `direct_subject_anchor` query such as `status page` once the same phrase-alias rollback path already emits `status page escalation contact`, `status page rollback policy`, and `rollback policy is`. `status page routing contact rollback policy notes` stays on the same local `multi_hop` route under additive `activation_reason=fts-direct-subject-compound-query`, but unrelated subject-only facts like `Status page dashboard is public.` stop surfacing as retrieved or budget-dropped noise on that path. This slice is intentionally narrow: it removes the decoy branch without claiming a broader overview-vs-contact rerank change.
- Tests: focused phrase-alias planner/noise coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_direct_subject_phrase_alias_planner_for_lowercase_compound_query tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_phrase_alias_query_skips_generic_subject_only_decoys -q` -> passed (`Ran 2 tests`); focused runtime-context guard `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_direct_subject_phrase_alias_queries_skip_generic_subject_only_decoys tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_direct_subject_phrase_alias_query_recovers_specific_rollback_fact -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 271 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 113 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.subqueries[*].query`, `retrieved_memory_ids`, and `retrieval.packing.budget_dropped[*]` stay cleaner on this phrase-alias rollback family because the bare subject branch is no longer planned, while `MemoryStore.search()` return shape, SQLite schema, and runtime-context payload shape stay unchanged.
- Blockers: this suppression is intentionally limited to subject-anchored direct-subject phrase-alias rollback compounds; allow-empty-anchor alias families and broader non-owner planners still keep their current routing unless a benchmark-grounded miss shows the same subject-only noise pattern.
- Next safe slice: benchmark one adjacent subject-anchored phrase-alias rollback variant such as `status page routing escalation contact rollback policy notes`, and only widen this suppression if another alias variant still retrieves or budget-drops unrelated subject-only facts.

## 2026-06-28T16:24:29Z - owner-plus-rollback multi-hop plans now skip generic subject-only decoys

- Scope: upgraded one L3 local-first retrieval slice from owner-wrapper/direct-subject rollback recall that still re-expanded a bare subject anchor into the narrower specific-facts-only multi-hop path.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `_direct_subject_owner_intent_multi_hop_plans(...)` no longer adds the generic subject-only probe such as `status page` once the same owner-plus-rollback compound path already emits `status page owner`, `status page maintainer`, `status page rollback policy`, and `rollback policy is`. That keeps `status page owner rollback policy notes` plus owner-wrapper variants such as `who is responsible for the status page rollback policy notes` on the same local `multi_hop` route and the same additive `activation_reason=fts-direct-subject-compound-query`, but it stops unrelated subject-only facts like `Status page dashboard is public.` from being retrieved, budget-dropped, or receipt-visible noise on this family.
- Tests: focused owner-compound anchor suppression `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_direct_subject_owner_planner_for_lowercase_compound_query tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_owner_query_skips_generic_subject_only_decoys tests.test_store.MemoryStoreTest.test_compound_fts_owner_relation_status_page_queries_skip_generic_subject_only_decoys -q` -> passed (`Ran 3 tests`); focused runtime-context guard `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_owner_relation_status_page_queries_skip_generic_subject_only_decoys tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_direct_subject_query_recovers_specific_rollback_fact -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 270 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 111 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.subqueries[*].query`, `candidate_ids`, `retrieved_memory_ids`, and `retrieval.packing.budget_dropped[*]` now stay cleaner on owner-plus-rollback compounds because the generic subject-only branch is no longer planned, while `MemoryStore.search()` return shape, SQLite schema, and runtime-context payload shape stay unchanged.
- Blockers: this suppression is intentionally narrow to owner-plus-rollback multi-hop plans; other compound families still keep their current subject-anchor behavior unless a benchmark-grounded miss shows the same subject-only noise pattern.
- Next safe slice: benchmark one adjacent non-owner rollback family that still emits a bare subject anchor, such as `status page routing contact rollback policy notes`, and only widen this suppression if that concrete miss still retrieves or budget-drops unrelated subject-only facts.

## 2026-06-28T12:25:20Z - owner-relation rollback wrappers now recover approver plus rollback facts

- Scope: upgraded one L3 local-first retrieval slice from literal direct-subject rollback compounds only to owner-family role-wrapper rollback compounds on the same explainable multi-hop path.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `decompose_multi_hop_query(..., query_lookup=...)` now normalizes owner-family role wrappers such as `who owns deployment approvals rollback policy notes`, `who is on point for deployment approvals rollback policy notes`, `who is responsible for deployment approvals rollback policy notes`, and `who is in charge of deployment approvals rollback policy notes` into the existing direct-subject owner planner. That route emits `deployment approver`, `deployment approver is`, `deployment approvals rollback policy`, and `rollback policy is`, and it short-circuits the generic wrapper-noise `history_safe_terms` branch on this path so `MemoryStore.search_with_meta(...)` still auto-enables the same local `multi_hop` route under additive `activation_reason=fts-direct-subject-compound-query` while the specific approver plus rollback facts beat the generic overview in both retrieval and tight-budget packing.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_owner_relation_normalization_for_deployment_approval_responsible_query tests.test_store.MemoryStoreTest.test_compound_fts_owner_relation_deployment_approval_queries_auto_enable_multi_hop_and_recover_specific_facts tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_owner_relation_deployment_approval_queries_recover_specific_rollback_fact -q` -> passed (`Ran 3 tests`); focused owner-wrapper expansion `python3 -m unittest tests.test_store.MemoryStoreTest.test_compound_fts_owner_relation_deployment_approval_queries_auto_enable_multi_hop_and_recover_specific_facts tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_owner_relation_deployment_approval_queries_recover_specific_rollback_fact -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 268 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 109 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.auto_enabled=true`, `activation_reason=fts-direct-subject-compound-query`, `retrieval.multi_hop.subqueries[*].candidate_ids`, `filtered_parent_candidate_ids`, and `retrieval.multi_hop.fusion.ranked_candidate_ids` now explain why the approver and rollback facts were retrieved, injected, and kept ahead of the overview on owner-wrapper queries too without changing `MemoryStore.search()` shape, SQLite schema, or runtime-context payload shape.
- Blockers: this normalization is intentionally limited to owner-family `role-relation-*` rollback compounds; non-owner wrappers and any remaining wrapper families still keep their prior routing unless a concrete miss or benchmark proves the same wrapper-noise suppression is needed.
- Next safe slice: benchmark one adjacent non-deployment owner-wrapper rollback family such as `who is responsible for the status page rollback policy notes` and only widen wrapper normalization further if a concrete miss still lets wrapper noise or a generic overview crowd out explicit facts.

## 2026-06-28T08:24:13Z - deployment-approval-owner direct-subject compounds now recover approver plus rollback facts

- Scope: upgraded one L3 local-first retrieval slice from alias-only deployment-approval-contact rollback handling to the deployment-approval-owner phrase-alias rollback family on the same direct-subject path.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `decompose_multi_hop_query(..., query_lookup=...)` now lets the existing direct-subject owner planner prefer the already-shipped deployment-approval phrase alias when the query is `deployment approval owner rollback policy notes`. That route now emits `deployment approver`, `deployment approver is`, `deployment approvals rollback policy`, and `rollback policy is`, and it skips the generic `deployment approval` anchor re-vote on this family, so `MemoryStore.search_with_meta(...)` still auto-enables the same local `multi_hop` path under additive `activation_reason=fts-direct-subject-compound-query` while the specific approver plus rollback facts beat the generic overview in both retrieval and tight-budget packing.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_direct_subject_phrase_alias_for_deployment_approval_owner_query tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_deployment_approval_owner_query_auto_enables_multi_hop_and_recovers_specific_facts -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_deployment_approval_owner_query_recovers_specific_rollback_fact -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 266 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 107 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.auto_enabled=true`, `activation_reason=fts-direct-subject-compound-query`, `retrieval.multi_hop.subqueries[*].candidate_ids`, `filtered_parent_candidate_ids`, and `retrieval.multi_hop.fusion.ranked_candidate_ids` now show why the approver and rollback facts were retrieved, injected, and kept ahead of the overview on this owner phrase-alias path too without changing `MemoryStore.search()` shape, SQLite schema, or runtime-context payload shape.
- Blockers: this owner-path narrowing is intentionally limited to deployment-approval rollback compounds that already imply the shipped `deployment approver` phrase alias; other owner phrase-alias families still keep their prior routing unless a concrete miss or benchmark proves they need the same anchor suppression.
- Next safe slice: benchmark one adjacent deployment-approval owner phrase-alias variant such as plural `deployment approvals owner rollback policy notes` and only widen this planner if that concrete miss still shows a generic overview or no-match path crowding out explicit facts.

## 2026-06-28T04:26:25Z - deployment-approval-contact direct-subject compounds now recover approver plus rollback facts

- Scope: upgraded one L3 local-first retrieval slice from anchored-only phrase-alias direct-subject rollback handling to the alias-only deployment-approval-contact rollback family the tracker called out next.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `decompose_multi_hop_query(..., query_lookup=...)` now lets the existing phrase-alias direct-subject planner fire for `deployment approval contact rollback policy notes` even when there is no pre-alias subject anchor, but only for the deployment-approval-contact family. That route now emits `deployment approver`, `deployment approver is`, `deployment approvals rollback policy`, and `rollback policy is`, so `MemoryStore.search_with_meta(...)` auto-enables the existing local `multi_hop` path under the same additive `activation_reason=fts-direct-subject-compound-query` and gives the specific approver plus rollback facts enough deterministic fusion weight to beat the generic overview.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_direct_subject_phrase_alias_planner_for_deployment_approval_contact_query tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_deployment_approval_contact_query_auto_enables_multi_hop_and_recovers_specific_facts -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_deployment_approval_contact_query_recovers_specific_rollback_fact -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 264 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 105 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.auto_enabled=true`, `activation_reason=fts-direct-subject-compound-query`, `retrieval.multi_hop.subqueries[*].candidate_ids`, `filtered_parent_candidate_ids`, and `retrieval.multi_hop.fusion.ranked_candidate_ids` now explain why the approver and rollback facts were retrieved, injected, and kept ahead of the overview on this alias-only path too without changing `MemoryStore.search()` shape, SQLite schema, or runtime-context payload shape.
- Blockers: this planner widening stays intentionally narrow to alias-only deployment-approval-contact rollback compounds; other alias-only or non-rollback direct-subject families still keep their prior routing unless a concrete miss or benchmark proves they need the same treatment.
- Next safe slice: benchmark one additional alias-only already-FTS-backed direct-subject rollback family without a pre-alias anchor and only generalize this planner if that concrete miss still shows a generic overview crowding out explicit facts.

## 2026-06-28T00:22:25Z - direct-subject phrase-alias rollback compounds now recover alias-specific facts

- Scope: upgraded one L3 local-first retrieval slice from literal-owner direct-subject compound handling to one additional already-FTS-backed phrase-alias direct-subject rollback family.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `decompose_multi_hop_query(..., query_lookup=...)` now adds a narrow planner for lowercase direct-subject phrase-alias rollback prompts such as `status page routing contact rollback policy notes`, emitting a subject-plus-alias fact branch like `status page escalation contact`, a subject-anchor rollback branch, and the existing `rollback policy is` fact branch. `MemoryStore.search_with_meta(...)` now auto-enables the existing local `multi_hop` path for that family under the same additive `activation_reason=fts-direct-subject-compound-query`, and the existing receipt-visible parent-overview suppression now keeps the generic overview from re-voting when those alias-specific contact and rollback facts are already present.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_direct_subject_phrase_alias_planner_for_lowercase_compound_query tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_phrase_alias_query_auto_enables_multi_hop_and_recovers_specific_facts -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_direct_subject_phrase_alias_query_recovers_specific_rollback_fact -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 262 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 103 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.auto_enabled=true`, `activation_reason=fts-direct-subject-compound-query`, and `retrieval.multi_hop.subqueries[*].filtered_parent_candidate_ids` / `filtered_parent_candidate_reason=prefer-subquery-introduced-specific-facts` now explain retrieved, injected, and budget-dropped decisions on this phrase-alias path too without changing `MemoryStore.search()` shape, SQLite schema, or runtime-context payload shape.
- Blockers: this planner stays intentionally narrow to direct-subject phrase-alias rollback compounds; non-rollback alias families and other non-owner direct-subject intent shapes still keep their prior routing unless a concrete miss or benchmark proves they need the same treatment.
- Next safe slice: benchmark one additional already-FTS-backed non-owner direct-subject compound family such as `deployment approval contact ...` and extend the same planner/filter only if that concrete miss still shows a generic overview crowding out explicit facts.

## 2026-06-27T20:25:00Z - lowercase direct-subject FTS compounds now recover owner plus rollback facts

- Scope: upgraded one L3 local-first retrieval slice from identifier/entity-intent/deploy-target-only already-FTS-backed compound handling to one additional lowercase direct-subject owner-plus-rollback family.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `decompose_multi_hop_query(..., query_lookup=...)` now adds a narrow planner for lowercase `direct-subject` / `direct-subject-alias` owner-plus-rollback prompts such as `status page owner rollback policy notes`, emitting owner/maintainer fact branches plus subject-anchor and rollback-policy branches. `MemoryStore.search_with_meta(...)` now auto-enables the existing local `multi_hop` path for that family under additive `activation_reason=fts-direct-subject-compound-query`, and the same receipt-visible parent-overview suppression now keeps the generic overview from re-voting when explicit owner and rollback facts are already present, so those facts survive ranking and tight-budget packing ahead of the overview.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_direct_subject_owner_planner_for_lowercase_compound_query tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_query_auto_enables_multi_hop_and_recovers_specific_facts -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_direct_subject_query_recovers_specific_rollback_fact -q` -> passed (`Ran 1 test`); adjacent guard coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_compound_fts_entity_intent_query_auto_enables_multi_hop_and_recovers_specific_fact tests.test_store.MemoryStoreTest.test_compound_fts_deploy_target_query_auto_enables_multi_hop_and_recovers_specific_facts tests.test_store.MemoryStoreTest.test_simple_fts_owner_query_does_not_auto_enable_multi_hop_without_identifier_branch tests.test_store.MemoryStoreTest.test_simple_subject_query_does_not_auto_enable_multi_hop tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_entity_intent_query_recovers_specific_rollback_fact tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_deploy_target_query_recovers_target_and_rollback_facts -q` -> passed (`Ran 6 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 260 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 101 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.auto_enabled=true`, `activation_reason=fts-direct-subject-compound-query`, and `retrieval.multi_hop.subqueries[*].filtered_parent_candidate_ids` / `filtered_parent_candidate_reason=prefer-subquery-introduced-specific-facts` now make retrieved, injected, and budget-dropped decisions explainable on this route too without changing `MemoryStore.search()` shape, SQLite schema, or runtime-context payload shape.
- Blockers: this new planner/filter is intentionally narrow to lowercase direct-subject owner-plus-rollback compounds; current-term phrase-alias compounds and other non-owner direct-subject intent families still keep their prior routing unless a concrete miss or benchmark proves they need the same treatment.
- Next safe slice: benchmark one additional already-FTS-backed direct-subject phrase-alias or non-owner compound family and extend the same planner/filter only if that concrete miss still shows a generic overview crowding out explicit facts.

## 2026-06-27T16:21:01Z - identifier-gated FTS compounds now suppress generic overview duplicates

- Scope: upgraded one L3 local-first retrieval slice from entity-intent/deploy-target-only overview suppression to the remaining identifier-gated already-FTS-backed compound path.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `MemoryStore.search_with_meta(...)` now applies the existing receipt-visible parent-overview suppression to `activation_reason=fts-identifier-compound-query`, covering identifier and entity-intent subqueries plus the broad entity/title anchor filter on that path. Broad FTS prompts such as `Project Atlas owner DeployWindow rollback policy notes` still auto-enable the same local identifier-gated multi-hop route, but the `DeployWindow` and `Project Atlas owner|rollback|policy` branches now stop re-voting for the generic overview when the explicit rollback and owner facts are already present, so those specific facts survive ranking and packing ahead of the overview under the same budget.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_compound_fts_identifier_query_auto_enables_multi_hop_and_introduces_missing_fact tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_identifier_query_recovers_specific_rollback_fact -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 256 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 100 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.subqueries[*].filtered_parent_candidate_ids` and `filtered_parent_candidate_reason=prefer-subquery-introduced-specific-facts` now stay visible on the identifier-gated route too, so retrieved, injected, and budget-dropped outcomes remain explainable without changing `MemoryStore.search()` shape, SQLite schema, or runtime-context payload shape.
- Blockers: this specificity filter is still intentionally narrow to the shipped identifier, entity-intent, and deploy-target already-FTS-backed compound families; other lowercase direct-subject planners still keep their prior ranking unless a concrete miss or benchmark proves they need the same suppression.
- Next safe slice: benchmark one more already-FTS-backed lowercase direct-subject compound family and extend the same receipt-visible overview suppression only if that concrete miss shows broad overviews still crowd out explicit facts.

## 2026-06-27T12:19:20Z - entity-intent FTS compounds now drop generic overview duplicates

- Scope: upgraded one L3 local-first retrieval slice from entity-intent auto-routing that still over-weighted parent overviews to receipt-visible parent-duplicate suppression for already-FTS-backed direct-subject entity-intent compounds.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `MemoryStore.search_with_meta(...)` now suppresses parent-only overview duplicates from entity-intent compound subquery rankings on the existing `activation_reason=fts-entity-intent-compound-query` path. Broad FTS prompts such as `Project Atlas owner rollback policy notes` still auto-enable the same local `multi_hop` planner, but the broad `Project Atlas` anchor and the `Project Atlas owner|rollback|policy` subqueries now stop re-voting for the generic overview when the specific owner/rollback facts are already present. That moves the explicit rollback fact plus owner fact ahead of the overview under the same budget while leaving ordinary `Who owns Project Atlas?` lookups and the earlier deploy-target-specific path unchanged.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_compound_fts_entity_intent_query_auto_enables_multi_hop_and_recovers_specific_fact tests.test_store.MemoryStoreTest.test_simple_fts_owner_query_does_not_auto_enable_multi_hop_without_identifier_branch tests.test_store.MemoryStoreTest.test_compound_fts_deploy_target_query_auto_enables_multi_hop_and_recovers_specific_facts tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_entity_intent_query_recovers_specific_rollback_fact tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_deploy_target_query_recovers_target_and_rollback_facts -q` -> passed (`Ran 5 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 253 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 100 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.subqueries[*].filtered_parent_candidate_ids` and `filtered_parent_candidate_reason=prefer-subquery-introduced-specific-facts` now also expose which broad entity-intent subquery candidates were withheld so rollback/owner-specific facts can survive retrieved, injected, and budget-dropped decisions without changing `MemoryStore.search()` shape, SQLite schema, or runtime-context payload shape.
- Blockers: this specificity filter is intentionally narrow to `fts-entity-intent-compound-query`; identifier-gated FTS compounds still keep their prior parent/subquery weighting unless a benchmark or real miss proves the same overview suppression is needed there too.
- Next safe slice: extend the same receipt-visible parent-overview suppression to one additional already-FTS-backed compound family only if a benchmark or real-query miss shows identifier-gated compounds still let a broad overview crowd out explicit facts.

## 2026-06-27T08:25:29Z - planner-aware auto multi-hop for deploy-target FTS compounds

- Scope: upgraded one L3 local-first retrieval slice from title/entity-only already-FTS-backed auto multi-hop to one additional lowercase planner-backed compound family, `direct-deploy-target-core`.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `decompose_multi_hop_query(..., query_lookup=...)` now adds a planner-aware path for `direct-deploy-target-core`, so broad FTS prompts such as `deploy target rollback policy notes` emit structured deploy-target fact subqueries plus a rollback-policy fact branch instead of returning no subqueries. `MemoryStore.search_with_meta(...)` now auto-enables the existing local `multi_hop` path for that already-FTS-backed family under additive `activation_reason=fts-direct-deploy-target-compound-query`, and subquery rankings now filter parent-only overview hits when the subquery introduced more specific candidates. That keeps the explicit `Deploy target is Production.` and `Rollback policy is canary first for deploy target Production.` facts ahead of the generic overview under the same budget while leaving plain `deploy target` lookups on the prior single-pass path.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_decomposition_uses_direct_deploy_target_planner_for_compound_query tests.test_store.MemoryStoreTest.test_compound_fts_deploy_target_query_auto_enables_multi_hop_and_recovers_specific_facts tests.test_store.MemoryStoreTest.test_simple_deploy_target_query_does_not_auto_enable_multi_hop tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_deploy_target_query_recovers_target_and_rollback_facts -q` -> passed (`Ran 4 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 250 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 100 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.subqueries[*].filtered_parent_candidate_ids` and `filtered_parent_candidate_reason=prefer-subquery-introduced-specific-facts` now make the withheld broad-overview suppression explicit without changing `MemoryStore.search()` shape, SQLite schema, or runtime-context payload shape.
- Blockers: this new planner-aware decomposition/filtering is intentionally narrow to `direct-deploy-target-core`; other lowercase already-FTS-backed direct-subject planners still use their prior routing unless a benchmark or real miss proves the same parent-duplicate suppression is needed.
- Next safe slice: extend the same receipt-visible structured-fact decomposition and parent-duplicate suppression to one additional lowercase already-FTS-backed direct-subject basis only if a benchmark or real-query miss shows the deploy-target planner was not the last high-leverage local gap.

## 2026-06-27T04:20:43Z - auto multi-hop routing for FTS title-entity compound queries

- Scope: upgraded one L3 local-first retrieval routing slice from identifier-only already-FTS-backed auto multi-hop to conservative title-entity compound auto-routing for direct-subject FTS queries.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `decompose_multi_hop_query(...)` now excludes title-entity tokens from consuming the entity-intent budget, so direct subject prompts such as `Project Atlas owner rollback policy notes` produce the intended `Project Atlas owner`, `Project Atlas rollback`, and `Project Atlas policy` branches instead of stopping after `Project Atlas owner`. `MemoryStore.search_with_meta(...)` now auto-enables the existing local `multi_hop` path for already-FTS-backed direct-subject and direct-subject-alias queries when the caller did not explicitly configure multi-hop, the decomposer finds a multi-word `entity_or_title` or `quoted_phrase`, and at least two `entity_intent_pair` subqueries exist. Receipts now expose additive `retrieval.multi_hop.auto_enabled=true` with `activation_reason=fts-entity-intent-compound-query`, so broad FTS prompts can recover the missing rollback fact by default while ordinary `Who owns Project Atlas?` lookups and existing temporal/current FTS planners still stay on their prior single-pass paths.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_compound_fts_entity_intent_query_auto_enables_multi_hop_and_recovers_specific_fact tests.test_store.MemoryStoreTest.test_simple_fts_owner_query_does_not_auto_enable_multi_hop_without_identifier_branch tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_entity_intent_query_recovers_specific_rollback_fact -q` -> passed (`Ran 3 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md docs/SWARM_OPERATION_TRACKER.md docs/BUILD_LOG.md docs/CURRENT_STATE.md` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 247 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 99 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive multi-hop receipt metadata now distinguishes `fts-entity-intent-compound-query` from the earlier fallback/no-match, semantic-rescue, and identifier-gated FTS auto-routing paths without changing `MemoryStore.search()` shape, SQLite schema, or runtime-context payload shape; the richer entity-intent decomposition remains visible through `retrieval.multi_hop.subqueries`, injected context, and budget-dropped metadata.
- Blockers: this conservative widening only covers already-FTS-backed direct-subject/direct-subject-alias compound queries with multi-word entity or quoted-phrase anchors plus at least two entity-intent pairs; non-entity FTS compounds and the existing temporal/current lookup planners still rely on their prior routing unless a concrete miss proves the broader gate is worth the recall-noise risk.
- Next safe slice: extend the same receipt-visible conservative auto-routing discipline to one additional already-FTS-backed direct-subject compound shape only if a benchmark or real-query miss shows the new multi-word title-entity gate is still too narrow.

## 2026-06-27T00:20:43Z - auto multi-hop routing for FTS identifier compound queries

- Scope: upgraded one L3 local-first retrieval routing slice from fallback/none/semantic-only automatic multi-hop to conservative automatic multi-hop for already-FTS-backed compound queries with explicit identifier branches.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `MemoryStore.search_with_meta(...)` now auto-enables the existing local `multi_hop` path when the caller did not explicitly configure it, the parent search mode is `fts`, the built-in decomposer finds at least two subqueries, and at least one subquery is an `identifier` branch. Exact broad FTS prompts such as `Project Atlas owner DeployWindow rollback policy notes` now widen into deterministic parent-plus-subquery RRF by default, so the specific `DeployWindow rollback policy is canary first for Project Atlas.` fact is retrieved and can survive packing beside the broad overview instead of leaving the receipt/context with only the overview hit. Receipts now expose additive `retrieval.multi_hop.auto_enabled=true` with `activation_reason=fts-identifier-compound-query`, while ordinary FTS owner lookups such as `Who owns Project Atlas?` still keep multi-hop disabled.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_compound_fts_identifier_query_auto_enables_multi_hop_and_introduces_missing_fact tests.test_store.MemoryStoreTest.test_simple_fts_owner_query_does_not_auto_enable_multi_hop_without_identifier_branch tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_identifier_query_recovers_specific_rollback_fact -q` -> passed (`Ran 3 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 246 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 98 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.multi_hop.activation_reason=fts-identifier-compound-query` now distinguishes this already-FTS-backed widening from existing fallback/no-match and semantic-rescue auto-routing without changing `MemoryStore.search()` shape, SQLite schema, or runtime-context payload shape; the same decision remains visible through `retrieval_json`, injected context, and budget-dropped metadata.
- Blockers: this closes only one conservative FTS-backed compound gap; already-FTS-backed compound queries without an identifier branch still use the existing single-pass FTS behavior unless a concrete benchmark or real query miss proves the gate is too narrow.
- Next safe slice: extend the same receipt-visible conservative auto-routing discipline to one additional already-FTS-backed compound-query shape only if a benchmark or real-query miss shows the identifier-gated path is no longer the highest-leverage remaining local retrieval gap.

## 2026-06-26T20:22:03Z - observation-support anchor pruning for history queries

- Scope: upgraded one L3 local-first retrieval quality slice from keep-all observation anchors to deterministic anchor-chain trimming for multi-anchor history-observation queries.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `history_observation_support_v1` now keeps the earliest supporting anchor plus the strongest/latest anchor instead of retaining every mutation-term anchor candidate. Receipts now expose additive `ordered_anchor_candidate_ids`, `selected_anchor_candidate_ids`, `excluded_anchor_candidate_ids`, and `anchor_selection_strategy`, while `retrieval.temporal.selection_exclusions` and `history_ordering.considered_history_rankings` show which extra anchor was withheld from the selected chain. Concrete local history prompts such as `Who handled the status page before the shift?` now keep `Avery covered the overnight rotation.` plus the two informative shift/handoff anchors under the same budget that previously let a lower-signal extra anchor compete.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_history_shift_question_trims_low_signal_extra_anchor_and_records_exclusion -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_history_shift_context_trims_low_signal_extra_anchor_before_budget_pressure -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 244 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 95 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive receipt metadata now records why multi-anchor observation-support chains were trimmed before packing without changing `MemoryStore.search()` shape, SQLite schema, or runtime-context shape; excluded anchors stay explainable through `query_lookup.history.observation_support` and `retrieval.temporal.history_ordering`.
- Blockers: this improves only the fallback/semantic `who ... before ... shift` observation-support planner; already-FTS-backed history paths and other multi-anchor support planners still rely on their existing anchor sets unless a concrete miss justifies the same trimming discipline.
- Next safe slice: extend the same receipt-visible anchor-chain trimming or reservation discipline to the next concrete multi-anchor history path only if a benchmark or real query shows an FTS-backed history/support chain still loses quality or budget stability.

## 2026-06-26T16:17:36Z - auto multi-hop routing for compound semantic rescue queries

- Scope: upgraded one L3 local-first retrieval quality slice from fallback/no-match-only auto multi-hop routing to the existing local semantic rescue planner for concrete compound misses.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `MemoryStore.search_with_meta(...)` now auto-enables the existing local `multi_hop` path when the caller did not explicitly configure it, the parent search mode is `semantic`, and the built-in decomposer finds at least two subqueries. Compound semantic-rescue prompts such as `Who is responsible for Project Atlas plus its DeployWindow rollback plan?` now widen into deterministic parent-plus-subquery RRF by default, so the rollback fact is retrieved and the generic overview can become the budget-dropped memory instead of the only combined answer-bearing context.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_compound_semantic_query_auto_enables_multi_hop_and_introduces_missing_fact -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_semantic_budget_prefers_specific_facts_over_generic_overview -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 242 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 93 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed.
- Artifacts/receipts: `retrieval.multi_hop` now uses additive `activation_reason=semantic-compound-query` alongside `auto_enabled=true`, so receipts can distinguish semantic-rescue auto-routing from fallback/no-match auto-routing and explicit operator-configured decomposition without changing `MemoryStore.search()` shape, packing schema, or runtime-context payload shape.
- Blockers: this closes one concrete semantic-rescue compound gap only; already-FTS-backed compound queries still do not auto-enable multi-hop, and benchmark-honest routing still depends on the current decomposer heuristics rather than a broader adaptive router contract.
- Next safe slice: extend the same receipt-visible conservative auto-routing discipline to already-FTS-backed compound queries only if a concrete benchmark or real-query miss proves that default FTS compound routing is now the highest-leverage remaining local retrieval gap.

## 2026-06-26T12:16:46Z - auto multi-hop routing for compound fallback/no-match queries

- Scope: upgraded one L3 local-first retrieval quality slice from opt-in-only multi-hop decomposition to conservative automatic multi-hop routing for obvious compound queries that would otherwise stay in fallback or no-match baseline search.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `MemoryStore.search_with_meta(...)` now auto-enables the existing local `multi_hop` path only when the caller did not explicitly configure it, the parent lexical search mode is `fallback` or `none`, and the built-in decomposer finds at least two subqueries. Compound prompts such as `What is the Project Atlas owner DeployWindow rollback policy?` and `Project Atlas owner DeployWindow rollback policy` now route through deterministic parent-plus-subquery RRF by default, while simple queries such as `who owns the status page` still keep multi-hop disabled.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_obvious_compound_fallback_query_auto_enables_multi_hop tests.test_store.MemoryStoreTest.test_compound_no_match_query_auto_enables_multi_hop_without_explicit_config tests.test_store.MemoryStoreTest.test_simple_subject_query_does_not_auto_enable_multi_hop -q` -> passed (`Ran 3 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_budget_prefers_two_specific_hops_over_generic_overview -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 236 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 92 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: `retrieval.multi_hop` now surfaces additive `auto_enabled` plus `activation_reason` metadata, so receipts can distinguish explicit operator-configured decomposition from conservative default routing without changing `MemoryStore.search()` shape, packing schema, or runtime-context payload shape.
- Blockers: automatic multi-hop still does not trigger for already-FTS-backed compound queries, and benchmark-honest routing still depends on the existing decomposer heuristics rather than a broader adaptive router contract.
- Next safe slice: extend the same receipt-visible conservative auto-routing discipline to one additional local-first planner only if a concrete benchmark or real-query miss shows that fallback/none compound auto multi-hop is no longer the highest-leverage gap.

## 2026-06-26T08:14:50Z - chronology history ordering receipts

- Scope: upgraded one L3 local-first retrieval explainability slice from support-pair-only history ordering to chronology/timeline selection ordering.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `retrieval.temporal.history_ordering` now also applies to `chronological_timeline_v1`; chronology receipts now expose `basis=chronological_timeline_selection_rank`, `source=temporal_chronological_timeline_selection`, ordered `selected_history_rankings`, and additive `considered_history_rankings` for explicit change timelines and relation-current support chains, and the same metadata keeps forwarding through `zerker.memory_context.v1` without changing `MemoryStore.search()` shape, packing, schema, CLI, or Phase 1 surfaces.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_benchmark_chronology_query_injects_explicit_change_event_before_timeline_support tests.test_store.MemoryStoreTest.test_chronology_relation_query_injects_plain_current_relation_before_generic_current_anchor -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_benchmark_chronology_context_puts_explicit_change_event_before_timeline_support tests.test_runner.RunnerTest.test_chronology_relation_context_prefers_plain_current_relation_before_generic_anchor -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 232 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 91 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed.
- Artifacts/receipts: chronology receipts and `zerker.memory_context.v1` now keep one additive selected-vs-considered ordering view for timeline chains, so chronology consumers no longer need to reconstruct history ordering from `selected_ids`, temporal fusion, and injection order separately.
- Blockers: chronology ordering is now explicit, but current-only current sibling exclusions outside timeline/history paths still remain on the `current_ordering` side rather than the `history_ordering` side by design.
- Next safe slice: add one similarly bounded chronology/timeline omitted-sibling fixture if a real selection path starts dropping extra current candidates, or return to higher-leverage retrieval work such as deterministic local fusion/routing improvements without widening into hosted providers.

## 2026-06-26T04:17:55Z - support-pair history ordering receipts

- Scope: upgraded one L3 local-first retrieval explainability slice from selected-history-only ordering receipts to support-pair history strategies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `retrieval.temporal.history_ordering` now also applies to `target_history_support_preferred_v1` and `history_observation_support_v1`; target-history receipts now expose `basis=target_history_support_selection_rank`, `source=temporal_target_history_support_selection`, and ordered omitted current siblings in `considered_history_rankings`, while observation-support receipts now expose `basis=history_observation_support_selection_rank`, `source=temporal_history_observation_support_selection`, and deterministic omitted support-candidate order backed by additive `query_lookup.history.observation_support.ordered_support_candidate_ids`. The same ordering metadata stays forwarded through `zerker.memory_context.v1`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_before_target_history_prefers_explicit_support_pair_over_generic_current_anchor tests.test_store.MemoryStoreTest.test_history_shift_question_prefers_person_rotation_support_over_generic_handled_observation tests.test_runner.RunnerTest.test_before_target_history_context_prefers_explicit_support_pair_over_generic_current_anchor tests.test_runner.RunnerTest.test_history_shift_context_prefers_person_rotation_support_over_generic_handled_observation -q` -> passed (`Ran 4 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 227 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 91 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed.
- Artifacts/receipts: support-pair history receipts and `zerker.memory_context.v1` now keep one additive selected-vs-considered ordering view for selected support/current anchors plus omitted current or support siblings, so these paths no longer require reconstructing that order from `selected_ids`, `selection_exclusions`, and observation-support lookup metadata separately.
- Blockers: `chronological_timeline_v1` still does not expose the same dedicated selected/considered history-ordering contract for timeline-style current/history/support chains, so chronology receipts still rely on `selected_ids`, fusion, and injection order alone.
- Next safe slice: extend additive `history_ordering` metadata to chronology/timeline selection paths so selected timeline anchors and omitted siblings share the same receipt-visible ordering contract without widening into packing changes or hosted-provider dependencies.

## 2026-06-26T00:15:40Z - selected history ordering receipts

- Scope: upgraded one L3 local-first retrieval explainability slice from abstained-history-only ordering receipts to explicit selected-history ordering receipts.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `retrieval.temporal.history_ordering` now also applies to `historical_preferred_v1` and `earliest_history_preferred_v1`; selected-history receipts now expose `basis=historical_selection_rank` or `basis=earliest_history_selection_rank`, strategy-specific `source`, ordered `selected_history_rankings`, and additive `considered_history_rankings` so selected stale/current history chains stay receipt-visible before packing and inside forwarded runtime context.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_update_history_deploy_target_query_uses_canonical_hybrid_backfill_without_change_from_noise tests.test_store.MemoryStoreTest.test_original_owner_query_uses_earliest_history_alias_search_variant_and_prefers_earliest_state -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_original_history_question_context_orders_multi_update_chain_from_earliest -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 224 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 90 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: selected-history receipts and `zerker.memory_context.v1` now keep one upstream ordering view for shipped earliest-history and update-history selections, so consumers no longer need to infer selected history order only from `selected_ids`.
- Blockers: support-pair history strategies such as `target_history_support_preferred_v1` and `history_observation_support_v1` still do not expose the same dedicated history-ordering view for excluded or support-sibling paths.
- Next safe slice: extend the same additive `history_ordering` contract to support-pair history strategies so selected supports plus omitted current siblings share one receipt-visible ordering view without widening into packing or hosted-provider work.

## 2026-06-25T20:14:26Z - history abstention ordering receipts

- Scope: upgraded one L3 local-first retrieval explainability slice from current-only abstention ordering receipts to winnerless history-conflict abstentions.
- Files touched: `zerker_memory/store.py`, `zerker_memory/runner.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `retrieval.temporal.history_ordering` now applies to `history_conflict_abstained_v1`; abstained history contradictions now expose `basis=history_conflict_abstention_rank`, `source=temporal_history_conflict_abstention`, empty `selected_history_rankings`, and additive `considered_history_rankings` that preserve the deterministic abstained-history consideration order before packing and in forwarded runtime context.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_temporal_wrapped_relation_history_abstains_on_cross_provenance_conflict -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_temporal_wrapped_relation_history_context_abstains_on_cross_provenance_conflict -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 223 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 88 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: abstained history receipts and `zerker.memory_context.v1` now keep the same additive ordering contract as the shipped direct-current abstention path, so winnerless history contradictions no longer require reconstructing considered order only from `conflict_sets`.
- Blockers: this closes `history_conflict_abstained_v1` only; selected history paths such as `historical_preferred_v1` and `earliest_history_preferred_v1` still do not expose one dedicated history-ordering receipt view for selected versus omitted siblings.
- Next safe slice: extend additive `history_ordering` metadata to selected history strategies so selected stale/current anchors and omitted siblings share one receipt-visible ordering contract without changing retrieval or packing behavior.

## 2026-06-25T16:12:30Z - abstained current ordering receipts

- Scope: upgraded one L3 local-first retrieval explainability slice from resolved-only direct-current ordering receipts to winnerless current-conflict abstentions.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `retrieval.temporal.current_ordering` now also applies to `current_conflict_abstained_v1`; abstained current contradictions now expose `basis=current_conflict_abstention_rank`, `source=temporal_current_conflict_abstention`, empty `selected_current_rankings`, and additive `considered_current_rankings` that preserve the deterministic abstained-current consideration order before packing and in forwarded runtime context.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_same_subject_current_conflict_abstains_when_cross_provenance_tie_stays_unresolved tests.test_runner.RunnerTest.test_build_context_preserves_current_conflict_abstention_without_injected_memories -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 218 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 88 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: abstained direct-current receipts and `zerker.memory_context.v1` now keep the same additive current-ordering contract as the resolved and update-preferred paths, so winnerless contradictions no longer require reconstructing considered order only from `conflict_sets`.
- Blockers: history-side abstentions still do not expose an equivalent considered-order receipt view, so `history_conflict_abstained_v1` reconstructions still depend on `conflict_sets` plus temporal envelopes alone.
- Next safe slice: add additive history-side abstention ordering metadata for `history_conflict_abstained_v1` without changing selected histories, packing policy, or hosted-provider dependencies.

## 2026-06-25T12:16:28Z - non-history current ordering receipts

- Scope: upgraded one L3 local-first retrieval explainability slice from `current_only_v1`-only ordering receipts to the two remaining non-history current-selection strategies.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `retrieval.temporal.current_ordering` now applies to `current_update_preferred_v1` and `current_conflict_resolved_v1` in addition to `current_only_v1`; update-preferred receipts now expose `basis=current_update_preference_rank`, conflict-resolved receipts now expose `basis=current_conflict_resolution_rank`, and both strategies now publish additive `considered_current_rankings` so the selected winner plus excluded current siblings stay visible upstream of packing and in forwarded runtime context without a schema migration.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_same_subject_current_conflict_prefers_higher_authority tests.test_store.MemoryStoreTest.test_current_target_query_prefers_update_fact_over_release_note_decoy tests.test_runner.RunnerTest.test_build_context_preserves_dropped_current_conflict_subset tests.test_runner.RunnerTest.test_current_target_context_prefers_update_fact_over_release_note_decoy -q` -> passed (`Ran 4 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 214 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 88 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed.
- Artifacts/receipts: `retrieval.temporal.current_ordering` now publishes strategy-specific `basis`, `source`, and `reason` for the surviving update-preferred and resolved-conflict current winners, while additive `considered_current_rankings` shows which current candidates stayed selected versus excluded before context packing.
- Blockers: `current_conflict_abstained_v1` still does not emit a matching `current_ordering` view for winnerless current contradictions, so abstained current sets still require `conflict_sets` plus temporal envelopes to reconstruct considered order.
- Next safe slice: extend the same additive `current_ordering` contract to `current_conflict_abstained_v1` so winnerless current conflicts keep considered-order explainability without widening into packing changes or hosted-provider work.

## 2026-06-25T08:14:30Z - current-only semantic pass-through receipts

- Scope: upgraded one L3 local-first retrieval explainability slice from packing-only semantic basis visibility to upstream `current_only_v1` temporal ordering metadata.
- Files touched: `zerker_memory/store.py`, `zerker_memory/runner.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: `retrieval.temporal.current_ordering` now states when `current_only_v1` selection order is a plain retrieval-rank baseline versus an explicit `reranker_rank`, `embedding_rank`, or `hybrid_semantic_rank` pass-through, and `zerker.memory_context.v1` now forwards the same dict so runtime consumers do not need packing metadata to distinguish semantic-vs-temporal current ordering.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_embedding_overlay_budget_uses_embedding_rank_packing_basis tests.test_store.MemoryStoreTest.test_provider_reranker_budget_drop_surfaces_outrank_metadata tests.test_store.MemoryStoreTest.test_current_deploy_target_budget_baseline_only_surfaces_hybrid_outrank_metadata tests.test_runner.RunnerTest.test_build_context_preserves_provider_reranker_budget_drop_metadata -q` -> passed (`Ran 4 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 213 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 88 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py zerker_memory/runner.py tests/test_store.py tests/test_runner.py` -> passed.
- Artifacts/receipts: `retrieval.temporal.current_ordering` now publishes `applied`, `pass_through`, `basis`, `source`, `reason`, and ordered `selected_current_rankings` without a schema migration; the same metadata now survives into runtime context.
- Blockers: default-current pass-through is now explicit, but `current_update_preferred_v1` and `current_conflict_resolved_v1` still do not surface the same upstream semantic-vs-temporal ordering explanation.
- Next safe slice: extend the same additive `current_ordering` contract to `current_update_preferred_v1` and `current_conflict_resolved_v1` when semantic overlays, rather than temporal/history logic, decide the surviving current order.

## 2026-06-25T04:23:10Z - default-current semantic packing basis

- Scope: upgraded one L3 local-first packing slice from generic default-current temporal packing labels to explicit semantic-overlay packing basis for embedding, reranker, and hybrid-semantic ordering.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: when `retrieval.temporal.selection_strategy=current_only_v1` is only acting as the default current-memory wrapper, `pack_memory_context(...)` now uses `reranker_rank`, `embedding_rank`, or `hybrid_semantic_rank` as the explicit packing basis instead of collapsing those default-current selections into generic `temporal_selection_rank`; stronger temporal/history paths such as temporal fusion, multi-hop fusion, and temporal injection ordering keep their existing precedence.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_embedding_overlay_budget_uses_embedding_rank_packing_basis tests.test_store.MemoryStoreTest.test_provider_reranker_budget_drop_surfaces_outrank_metadata tests.test_store.MemoryStoreTest.test_current_deploy_target_budget_baseline_only_surfaces_hybrid_outrank_metadata tests.test_store.MemoryStoreTest.test_hybrid_backfill_baseline_prefers_semantic_state_when_embedding_and_reranker_disabled tests.test_runner.RunnerTest.test_build_context_preserves_provider_reranker_budget_drop_metadata tests.test_runner.RunnerTest.test_build_context_current_deploy_target_budget_prefers_semantic_backfill_state_without_embedding_or_reranker -q` -> passed (`Ran 6 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 211 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 87 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed.
- Artifacts/receipts: `retrieval.packing.priority_model` now reports `reranker_rank_score_authority_current_v1`, `embedding_rank_score_authority_current_v1`, or `hybrid_semantic_rank_score_authority_current_v1` when those overlays drive default-current packing; `candidate_priorities` and `budget_dropped` now keep the same explicit semantic `packing_rank_basis` instead of hiding those reorderings behind `temporal_selection_rank`.
- Blockers: `retrieval.temporal.current_only_v1` still looks generic upstream of packing, so callers must infer from packing metadata when semantic overlays, rather than temporal/history logic, decided the current-order pass-through.
- Next safe slice: propagate the same default-current semantic pass-through explanation into `retrieval.temporal` so receipts can state when hybrid/embedding/reranker ordering, not temporal history logic, chose the current-set order.

## 2026-06-25T00:19:17Z - semantic overlay outrank receipts

- Scope: upgraded one L3 local-first retrieval explainability slice from hybrid-only outrank receipts to a shared semantic-overlay contract for embedding and reranker reorders.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: local embedding reorders now record `retrieval.embedding.promoted_candidate_ids` / `outranked_candidate_ids` plus candidate-level `embedding_rank_delta` and `embedding_outranked_reason`; reranker passes now emit the same contract through `retrieval.reranker`, and policy-withheld plus budget-dropped receipts now keep the same embedding/reranker rank breadcrumbs instead of dropping overlay context at the policy/packing boundary.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_deterministic_embedding_overlay_can_reorder_candidates tests.test_store.MemoryStoreTest.test_embedding_overlay_does_not_bypass_policy_withheld_memory tests.test_store.MemoryStoreTest.test_provider_reranker_only_sends_active_subset_and_preserves_non_active_slots tests.test_store.MemoryStoreTest.test_provider_reranker_budget_drop_surfaces_outrank_metadata tests.test_runner.RunnerTest.test_build_context_preserves_provider_reranker_budget_drop_metadata -q` -> passed (`Ran 5 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 207 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 87 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: `retrieval.embedding` and `retrieval.reranker` now publish promoted and outranked candidate ids; candidate receipts add `embedding_rank_delta` / `reranker_rank_delta` with local-vs-provider outrank reasons; `withheld` entries now retain overlay rank metadata; and packing candidate priorities plus budget drops carry the same overlay rank/promoted/outranked fields without a schema migration.
- Blockers: overlay outranks are now visible, but `pack_memory_context(...)` still does not use `embedding_rank` or `reranker_rank` as an explicit packing-rank basis, so promoted semantic winners can still lose under tight non-temporal budgets for priority reasons that are visible but not yet optimized.
- Next safe slice: decide and test when embedding/reranker rank should become a first-class `packing_rank_basis` so promoted semantic winners can survive tight non-temporal budgets without weakening temporal/history packing rules.

## 2026-06-24T20:15:50Z - hybrid semantic backfill outrank receipts

- Scope: upgraded one L3 local-first baseline-only hybrid semantic backfill slice from opaque lexical replacement/rank flips to explicit outranked and exclusion receipts across retrieval candidates and budget drops.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: baseline-only hybrid prompts such as `who owns the status page` now keep replaced weak lexical anchors visible through `retrieval["hybrid"]["selection_exclusions"]`; hybrid rank-preserving prompts such as `what is the deploy target` now label lexical anchors that stayed retrieved but lost to semantic backfill with `hybrid_semantic_rank`, `hybrid_rank_delta`, `hybrid_outranked_by_semantic_backfill`, and `hybrid_outranked_reason`; and budget-dropped receipts now carry the same hybrid outrank fields even when a temporal packing basis still wins.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_owner_question_hybrid_semantic_backfill_replaces_weak_fts_mention tests.test_store.MemoryStoreTest.test_current_deploy_target_budget_baseline_only_surfaces_hybrid_outrank_metadata -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_current_deploy_target_budget_prefers_semantic_backfill_state_without_embedding_or_reranker -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 204 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 86 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md docs/SWARM_OPERATION_TRACKER.md docs/BUILD_LOG.md docs/CURRENT_STATE.md` -> passed.
- Artifacts/receipts: `retrieval.hybrid.fusion` now records `promoted_candidate_ids` and `outranked_candidate_ids`; `retrieval.hybrid.selection_exclusions` captures replaced lexical candidates with `pre_hybrid_rank` plus local semantic score/overlap; and candidate plus packing receipts now surface `hybrid_semantic_rank`, `hybrid_rank_delta`, `hybrid_outranked_by_semantic_backfill`, and `hybrid_outranked_reason` without a schema migration.
- Blockers: baseline-only hybrid semantic backfill is now explainable, but non-hybrid embedding/reranker reorderings still do not emit the same candidate-level promoted/outranked contract.
- Next safe slice: extend the same promoted/outranked receipt contract to local embedding/reranker overlays so non-temporal semantic reorders beyond hybrid backfill stay explicit in retrieved, injected, withheld, and budget-dropped receipts.

## 2026-06-24T16:13:16Z - multi-hop fusion packing exclusions alignment

- Scope: upgraded one L3 local-first graph-backed retrieval slice from baseline-only multi-hop RRF ordering to explicit multi-hop fusion-aware packing and outranked-candidate receipts.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: graph-backed multi-hop prompts such as `What is the Project Atlas owner DeployWindow rollback policy?` now keep the deterministic parent-plus-subquery RRF contract visible through packing, so budgeted receipts use `packing_rank_basis=multi_hop_fusion_rank` when that merge wins; candidates and budget-dropped items now also mark when a memory was outranked by fusion instead of only showing a generic context-budget drop.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_budget_prefers_two_specific_hops_over_generic_overview tests.test_runner.RunnerTest.test_build_context_multi_hop_budget_prefers_two_specific_hops_over_generic_overview -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 200 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 86 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md docs/SWARM_OPERATION_TRACKER.md docs/BUILD_LOG.md docs/CURRENT_STATE.md` -> passed.
- Artifacts/receipts: `retrieval.multi_hop.merge` now records `promoted_candidate_ids` and `outranked_candidate_ids`; candidate receipts add `multi_hop_rank_delta`, `multi_hop_promoted_by_fusion`, `multi_hop_outranked_by_fusion`, and `multi_hop_outranked_reason`; and packing receipts now surface `multi_hop_fusion_rank`, `multi_hop_outranked_reason`, and `packing_rank_basis=multi_hop_fusion_rank` for this graph-backed merge path without a schema migration.
- Blockers: this closes multi-hop graph-backed merge explainability only; hybrid semantic backfill and other non-temporal fusion paths still do not emit the same dedicated outranked-candidate contract.
- Next safe slice: extend the same candidate-level outranked/exclusion contract to baseline-only hybrid semantic backfill so weaker lexical anchors are explicit in retrieved, injected, withheld, and budget-dropped receipts.

## 2026-06-24T12:10:19Z - recent-history relation pair RRF packing alignment

- Scope: upgraded one L3 local-first temporal retrieval slice from injection-only plain recent relation-history support ordering to deterministic relation-pair fusion that also survives tight context budgets.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: plain recent relation-history prompts such as `what did the api gateway point at before` now add the explicit stale/relation-current pair into temporal `reciprocal_rank_fusion_v1`, so a longer answer-bearing current relation can outrank a higher-authority generic `changed after ...` support anchor in `retrieval["candidates"]`; when that new `temporal_history_relation_pair_rrf_score_v1` signal is active, packing now follows the fused order so the current relation stays injected and the generic anchor becomes the budget-dropped memory.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_recent_history_relation_rrf_promotes_explicit_current_relation_over_high_authority_generic_anchor -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_recent_history_relation_context_rrf_promotes_explicit_current_relation_over_high_authority_generic_anchor -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 198 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 86 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed.
- Artifacts/receipts: `retrieval.temporal.fusion` now records `signal=temporal_history_relation_pair_rrf_score_v1`, `basis=history_relation_pair`, and `source_rankings.temporal_history_relation_pair`; candidates expose the new contributor in `temporal_fusion_sources`; and packing receipts now surface `packing_rank_basis=temporal_fusion_rank` plus `temporal_fusion_rank` for this recent-history relation path, keeping injected vs budget-dropped decisions explicit without a schema migration.
- Blockers: this closes plain recent relation-history current/support ordering only; graph candidate merges and other non-temporal hybrid fusion paths still do not share the same deterministic relation-pair budget contract.
- Next safe slice: extend the same deterministic fusion plus packing contract to graph-backed candidate merges while preserving candidate-by-candidate withheld, injected, and budget-dropped visibility.

## 2026-06-24T08:11:57Z - earliest-history relation pair RRF packing alignment

- Scope: upgraded one L3 local-first temporal retrieval slice from injection-only original/earliest relation-history support ordering to deterministic relation-pair fusion that also survives tight context budgets.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: original/earliest relation-history prompts such as `what was the original deploy target` now add the explicit stale/relation-current pair into temporal `reciprocal_rank_fusion_v1`, so a longer answer-bearing current relation can outrank a higher-authority generic `changed after ...` support anchor in `retrieval["candidates"]`; when that new `temporal_earliest_relation_pair_rrf_score_v1` signal is active, packing now follows the fused order so the current relation stays injected and the generic anchor becomes the budget-dropped memory.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_original_target_history_rrf_promotes_explicit_current_relation_over_high_authority_generic_anchor -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_original_target_history_context_rrf_promotes_explicit_current_relation_over_high_authority_generic_anchor -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 195 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 85 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: `retrieval.temporal.fusion` now records `signal=temporal_earliest_relation_pair_rrf_score_v1`, `basis=earliest_relation_pair`, and `source_rankings.temporal_earliest_relation_pair`; candidates expose the new source in `temporal_fusion_sources`; and packing receipts now surface `packing_rank_basis=temporal_fusion_rank` plus `temporal_fusion_rank` for this earliest-history relation-pair path, keeping injected vs budget-dropped decisions explicit without a schema migration.
- Blockers: this closes original/earliest relation current/support ordering only; plain recent-history relation-support paths and future graph candidate merges still rely on their current heuristics.
- Next safe slice: extend the same deterministic relation-pair fusion plus budget contract to plain recent-history relation current/support queries while preserving candidate-by-candidate withheld, injected, and budget-dropped visibility.

## 2026-06-24T04:20:24Z - update-history relation pair RRF packing alignment

- Scope: upgraded one L3 local-first temporal retrieval slice from injection-only update-history relation support ordering to deterministic relation-pair fusion that also survives tight context budgets.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: update-history relation prompts such as `what did the api gateway point at change from` now add the explicit stale/relation-current pair into temporal `reciprocal_rank_fusion_v1`, so a longer answer-bearing current relation can outrank a higher-authority generic `changed after ...` support anchor in `retrieval["candidates"]`; when that new `temporal_update_relation_pair_rrf_score_v1` signal is active, packing now follows the fused order so the current relation stays injected and the generic anchor becomes the budget-dropped memory.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_update_history_relation_rrf_promotes_explicit_current_relation_over_high_authority_generic_anchor -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_update_history_relation_context_rrf_promotes_explicit_current_relation_over_high_authority_generic_anchor -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 192 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 83 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed.
- Artifacts/receipts: `retrieval.temporal.fusion` now records `signal=temporal_update_relation_pair_rrf_score_v1`, `basis=update_relation_pair`, and `source_rankings.temporal_update_relation_pair`; candidates expose the new source in `temporal_fusion_sources`; and packing receipts now surface `packing_rank_basis=temporal_fusion_rank` plus `temporal_fusion_rank` only for this relation-pair fusion path, keeping injected vs budget-dropped decisions explicit without a schema migration.
- Blockers: this closes update-history relation current/support ordering only; earliest/original target-history relation-support paths and future graph candidate merges still rely on their current heuristics.
- Next safe slice: extend the same deterministic relation-pair fusion plus budget contract to earliest/original target-history relation current/support queries while preserving candidate-by-candidate withheld, injected, and budget-dropped visibility.

## 2026-06-24T00:31:40Z - update-history stale/current pair RRF ordering

- Scope: upgraded one L3 local-first temporal retrieval slice from packing-only `update-history` anchor preference to deterministic stale/current pair merge ordering.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: plain `update-history` queries with a generic `changed ...` anchor now add the explicit stale/current update pair into temporal `reciprocal_rank_fusion_v1`, so the current answer-bearing update fact can outrank a higher-authority generic change anchor directly in `retrieval["candidates"]` while the stale predecessor still stays first for history-style questions.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_update_history_rrf_promotes_explicit_current_over_high_authority_generic_anchor -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_update_history_context_rrf_promotes_explicit_current_over_high_authority_generic_anchor -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 191 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 80 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: `retrieval.temporal.fusion` now records `signal=temporal_update_pair_rrf_score_v1`, `basis=update_pair`, and `source_rankings.temporal_update_pair`; candidates now expose the new source in `temporal_fusion_sources`, and `retrieval.baseline_ranking.temporal_fusion_signal` distinguishes update-pair ordering from the earlier support-chain and mutation-anchor modes.
- Blockers: this closes plain update-history stale/current pair ordering only; update-history relation current/support queries and future graph candidate merges still use their earlier heuristics.
- Next safe slice: extend the same deterministic temporal fusion contract to update-history relation current/support queries while keeping candidate-by-candidate withheld, injected, and budget-dropped visibility explicit.

## 2026-06-23T20:09:57Z - chronology mutation RRF baseline ordering

- Scope: upgraded one L3 local-first temporal retrieval slice from packing-only chronology mutation preference to deterministic baseline merge ordering.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: chronology mutation queries now add explicit mutation-anchor rankings into temporal `reciprocal_rank_fusion_v1`, so answer-bearing `changed to ...` memories can outrank a higher-authority stale timeline support fact directly in `retrieval["candidates"]` instead of only being recovered later by packing.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_chronology_mutation_rrf_promotes_explicit_change_events_over_high_authority_support tests.test_store.MemoryStoreTest.test_benchmark_chronology_query_injects_explicit_change_event_before_timeline_support -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 187 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 79 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md docs/SWARM_OPERATION_TRACKER.md docs/BUILD_LOG.md docs/CURRENT_STATE.md` -> passed.
- Artifacts/receipts: `retrieval.temporal.fusion` now records `signal=temporal_mutation_rrf_score_v1`, `basis=mutation_anchor`, and `source_rankings.temporal_mutation_anchor`, while `retrieval.baseline_ranking.temporal_fusion_signal` now distinguishes mutation-anchor ordering from the earlier support-chain ordering.
- Blockers: this closes chronology mutation ordering only; plain update-history stale/current pairs and future graph candidate merges still use their earlier heuristics.
- Next safe slice: extend the same deterministic temporal fusion contract to plain update-history stale/current pairs while keeping candidate-by-candidate withheld, injected, and budget-dropped visibility explicit.

## 2026-06-23T16:09:20Z - temporal support-chain RRF baseline ordering

- Scope: upgraded one L3 local-first temporal retrieval slice from explicit support-chain selection only to deterministic support-chain merge ordering.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: support-chain temporal queries now fuse the pre-temporal baseline ranking, temporal selection order, and temporal injection order with deterministic `reciprocal_rank_fusion_v1` before the final non-reranked ordering step, so explicit support/current pairs can outrank a generic current anchor in `retrieval["candidates"]` instead of being visible only through later packing rules.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_before_target_history_prefers_explicit_support_pair_over_generic_current_anchor -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_before_target_history_context_prefers_explicit_support_pair_over_generic_current_anchor -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 182 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 79 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: `retrieval.temporal.fusion` now records the selected baseline/selection/injection RRF summary, candidates carry `temporal_fusion_rank`, `temporal_fusion_score`, `temporal_fusion_sources`, and `temporal_fusion_source_count`, and `retrieval.baseline_ranking` now states when `temporal_support_rrf_score_v1` changed the final non-reranked order while preserving explicit `selection_exclusions`.
- Blockers: this improves support-chain temporal merges only; chronology mutation-only timelines, plain update-history stale/current pairs, and future graph candidate merges still use their earlier heuristics.
- Next safe slice: extend the same deterministic fusion contract to chronology mutation timelines or graph candidate merges while keeping selection exclusions and withheld/budget-dropped visibility explicit.

## 2026-06-23T12:07:27Z - multi-hop RRF baseline merge ordering

- Scope: upgraded one L3 local-first multi-hop ranking slice from candidate union only to actual deterministic merge ordering.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: multi-hop retrieval now fuses the parent ranking plus subquery rankings with deterministic `reciprocal_rank_fusion_v1`, then lets baseline ordering honor `multi_hop_fusion_score` before older lexical-only signals. Under tight budgets, hop-specific facts can now outrank a generic overview that happened to win the parent lexical pass.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_multi_hop_rrf_promotes_subquery_supported_specific_fact_in_baseline_search tests.test_store.MemoryStoreTest.test_multi_hop_budget_prefers_two_specific_hops_over_generic_overview -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_multi_hop_budget_prefers_two_specific_hops_over_generic_overview -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 179 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 79 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed.
- Artifacts/receipts: `retrieval.multi_hop.fusion` now records the selected parent/subquery RRF ranking, candidates carry `multi_hop_fusion_rank`, `multi_hop_fusion_score`, `multi_hop_fusion_sources`, and `multi_hop_fusion_source_count`, and `retrieval.baseline_ranking` now states when `multi_hop_rrf_score_v1` actually affected non-reranked order.
- Blockers: this improves local multi-hop merge ordering only; graph/temporal candidate merges still rely on their existing heuristics, and multi-hop receipts still do not label generic overview memories with a dedicated outranked exclusion reason.
- Next safe slice: extend the same deterministic RRF ordering contract to temporal/graph candidate merges while keeping candidate-by-candidate exclusion visibility explicit.

## 2026-06-23T08:03:19Z - baseline-only hybrid semantic ordering

- Scope: closed a config-specific L3 ordering gap for hybrid semantic backfill when local embedding and reranking are both explicitly disabled.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: baseline ranking now honors candidate `semantic_backfill_score` before the older lexical lookup-fact score path, so baseline-only local configs keep the same stronger semantic target ordering as the default local reranker path. Receipts now expose `retrieval.baseline_ranking.hybrid_semantic_signal` and `hybrid_semantic_signal_applied` whenever that semantic ordering signal is in play.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_hybrid_backfill_baseline_prefers_semantic_state_when_embedding_and_reranker_disabled tests.test_store.MemoryStoreTest.test_current_deploy_target_budget_prefers_semantic_backfill_state_with_baseline_only -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_current_deploy_target_budget_prefers_semantic_backfill_state_without_embedding_or_reranker -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 175 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 78 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed.
- Artifacts/receipts: candidate-level `semantic_backfill_score` stays visible, and `retrieval.baseline_ranking` now tells you when non-reranked hybrid order was decided by `semantic_backfill_score_v1`.
- Blockers: baseline receipts still do not show a per-candidate semantic margin or an explicit "semantic-backfill-outranked" exclusion reason for weaker lexical anchors; multi-hop and temporal merge ranking still use their own ordering heuristics.
- Next safe slice: add receipt-visible hybrid semantic margin/exclusion metadata for baseline-only configs so outranked lexical anchors are explicit candidate-by-candidate without relying on reranker metadata.

## 2026-06-23T04:09:04Z - hybrid semantic budget rerank

- Scope: fixed a direct-current hybrid retrieval ranking bug that surfaced under tight context budgets.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: when `fts_semantic_backfill_v1` applies on the default local reranker path, deterministic reranking now uses each candidate's `semantic_backfill_score` instead of pure query-term counts. Receipts now expose `reranker.local_strategy`, `local_score`, `local_lexical_score`, and `hybrid_semantic_score`, so a direct semantic state fact can outrank a weaker lexical update/support anchor and stay injected under one-memory budgets.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_deploy_target_budget_prefers_semantic_backfill_state_over_lexical_update_anchor -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_current_deploy_target_budget_prefers_semantic_backfill_state -q` -> passed (`Ran 1 test`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 169 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 77 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md docs/SWARM_OPERATION_TRACKER.md docs/BUILD_LOG.md docs/CURRENT_STATE.md` -> passed.
- Artifacts/receipts: `retrieval.reranker` now explains whether `hybrid_semantic_backfill_score_v1` or lexical term matching decided local rerank order, and `retrieval.packing.budget_dropped` now makes the displaced lexical anchor explicit in the direct-query budget path.
- Blockers: this improves the default local reranker path only; baseline ordering when reranking is explicitly disabled, plus multi-hop/graph fusion ordering, still use older ranking signals.
- Next safe slice: make baseline ranking honor the same hybrid semantic signal when reranking is explicitly disabled so candidate order stays aligned across local retrieval modes.

## 2026-06-23T02:58:51Z - hybrid backfill RRF receipt summary

- Scope: closed a narrow L3 fusion explainability gap for existing FTS + semantic backfill retrieval.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/CURRENT_STATE.md`, `docs/BUILD_LOG.md`.
- Behavior changed: when `fts_semantic_backfill_v1` applies, `retrieval.hybrid.fusion` now records a deterministic `reciprocal_rank_fusion_v1` summary of the actual selected lexical/semantic sources, plus the considered lexical and semantic candidate rankings. Selected candidates also carry `fusion_rank`, `fusion_score`, and `fusion_sources`.
- Tests: `python3 -m unittest tests.test_store -k hybrid_semantic_backfill -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 168 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: hybrid receipts now distinguish considered candidates from selected fusion contributors, so dropped lexical decoys remain visible without being credited as selected-source evidence.
- Blockers: this is receipt metadata only; it does not yet reorder retrieval through RRF, fuse graph traversal candidates, or add hosted vector provider calls.
- Next safe slice: extend the same fusion summary to multi-hop merge receipts or add a small deterministic conflict-resolution fixture before changing ranking behavior.

## 2026-06-23T02:42:10Z - target-history selection exclusions

- Scope: closed the next L3 explainability gap for no-superseded target-history prompts.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`.
- Behavior changed: `retrieval.temporal.selection_exclusions` now records current anchors that were retrieved but not selected by `target_history_support_preferred_v1`, and each excluded candidate carries `temporal_selection_exclusion` plus `temporal_selection_exclusion_reason`.
- Tests: `python3 -m unittest tests.test_store.MemoryStoreTest.test_before_target_history_prefers_explicit_support_pair_over_generic_current_anchor tests.test_runner.RunnerTest.test_before_target_history_context_prefers_explicit_support_pair_over_generic_current_anchor -q` -> passed (`Ran 2 tests`); `python3 -m unittest tests.test_store -q` -> passed (`Ran 168 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 76 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: generic anchors such as `Blue Finch changed after freeze.` can still appear in `retrieved_memory_ids`, but receipts now mark them as `target-history-current-anchor-not-selected` with the selected support/current pair ids.
- Blockers: this labels no-superseded target-history selection exclusions only; other temporal strategies still expose non-selection mostly through rank/selection fields rather than dedicated exclusion objects.
- Next safe slice: decide whether to generalize `selection_exclusions` to relation-history and chronology strategies, or switch to L2 checkpoint/root contracts if lifecycle remains the higher-priority lane.

## 2026-06-22 - coordinator

- Scope: seeded lane for BM25/FTS, dense providers, graph candidates, RRF, context packing, and receipt-visible retrieval decisions.
- Files touched: lane log only.
- Behavior changed: none.
- Tests: not applicable.
- Artifacts/receipts: none.
- Blockers: existing retrieval automation is active; future slices must avoid overlapping unmerged work.
- Next safe slice: make the next retrieval automation write here and to the orchestrator.

## 2026-06-22 - support-chain reservation

- Scope: extended L3 context packing from stale/current pair reservation to explicit stale/current/support chain reservation for history, update-history, and chronology relation prompts.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`.
- Behavior changed: packing now reserves `selected_current_anchor_id` plus explicit support anchors from temporal receipts; chronology relation prompts now emit `chronology_relation_support_chain_v1`; when the chain fits, older stale states are budget-dropped before the selected support anchor.
- Tests: `python3 -m unittest tests.test_store -q`, `python3 -m unittest tests.test_policy -q`, `python3 -m unittest tests.test_runner -q`, `python3 -m zerker_memory eval`.
- Artifacts/receipts: `retrieval.packing.reservation.requested_ids`/`applied_ids`, `candidate_priorities[].reserved_by_strategy`, and `budget_dropped[].reserved_by_strategy` now show support-chain reservation decisions directly.
- Blockers: `target_history_support_preferred_v1` still lacks the same budget reservation, so target-history stale/current/support chains can still lose support under pressure.
- Next safe slice: extend the same explicit support-chain reservation to target-history prompts while keeping budget-dropped receipts explicit.

## 2026-06-22T21:22:00Z - L3 hybrid-retrieval - live coordinator verification

- Scope: verified the latest retrieval drop as part of the accumulated live-session dirty tree.
- Files touched: lane log only.
- Behavior changed: none in this coordinator entry.
- Tests: `python3 -m unittest tests.test_bench tests.test_store tests.test_runner tests.test_policy tests.test_workspaces tests.test_cli_onboarding tests.test_snapshot tests.test_consolidation -q` -> passed (`Ran 503 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: no new external artifacts.
- Blockers: the working tree now contains multiple verified lanes plus generated/runtime folders. Checkpoint or split reviewed work before adding another broad retrieval slice.
- Next safe slice: checkpoint the reviewed support-chain reservation, then extend target-history support reservation in a fresh narrow L3 run.

## 2026-06-23T00:08:02Z - target-history support pair selection

- Scope: fixed the no-superseded target-history path so `before ... moved to ...` queries keep the explicit support/current pair even when a generic current anchor is also retrieved.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`.
- Behavior changed: `target_history_support_preferred_v1` now accepts larger current candidate sets, prefers the explicit history-cued support memory over generic current anchors, surfaces `selected_target_current_id` plus `selected_target_support_ids` in temporal receipts, and marks the pair through `packing.reservation.strategy = target_history_support_chain_v1` when it fits the budget.
- Tests: `python3 -m unittest tests.test_store -q`, `python3 -m unittest tests.test_policy -q`, `python3 -m unittest tests.test_runner -q`, `python3 -m zerker_memory eval`.
- Artifacts/receipts: target-history receipts now show the chosen support/current pair explicitly; generic current anchors can still appear in `retrieved_memory_ids` without being injected.
- Blockers: extra target-history current anchors are now excluded deterministically, but temporal receipts still do not label those excluded anchors with a dedicated withheld reason.
- Next safe slice: add receipt-visible exclusion metadata for extra target-history current anchors so generic notes are explicitly marked as retrieved-but-not-selected.

## 2026-06-29T20:30:51Z - chronology support-chain overflow now falls back to stale plus current

- Scope: upgraded one bounded L3 local-first retrieval slice from a reproduced chronology relation-support budget miss into a narrow reservation fallback for `when did the api gateway point at change then`.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: when `chronology_relation_support_chain_v1` selects a stale/current/support reservation that overflows the context budget but the stale-plus-current answer pair still fits, `pack_memory_context(...)` now applies that smaller pair instead of abandoning reservation and letting the generic knapsack drop the current relation. The concrete chronology miss now injects `API gateway points to canary.` plus `API gateway points to production.` and budget-drops `API gateway points to staging.` plus `API gateway points changed after migration.` Receipts stay explainable through additive `retrieval.packing.reservation.fallback_requested_ids`, `fallback_applied`, and `fallback_reason` metadata without changing `MemoryStore.search()` return shape, SQLite schema, or receipt payload shape.
- Tests: focused chronology overflow store coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_chronology_relation_budget_packing_falls_back_to_stale_and_current_when_support_chain_exceeds_budget -q` -> passed (`Ran 1 test`); focused runtime-context guard `python3 -m unittest tests.test_runner.RunnerTest.test_chronology_relation_context_budget_falls_back_to_stale_and_current_when_support_chain_exceeds_budget -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 283 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 124 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: chronology overflow receipts now surface additive `retrieval.packing.reservation.fallback_requested_ids`, `fallback_applied`, `fallback_reason`, `applied_ids`, `retrieved_memory_ids`, `injected_memory_ids`, and `retrieval.packing.budget_dropped[*]`, so operators can see that the support anchor was withheld for budget while the stale/current answer pair stayed injected. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, and `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md` found no active stale public repo or raw-installer targets.
- Blockers: this fallback is intentionally limited to `chronology_relation_support_chain_v1`; `target_history_support_chain_v1` and any broader support-family overflow behavior still keep their current overflow behavior until a concrete miss justifies widening the heuristic.
- Next safe slice: probe one `target_history_support_preferred_v1` overflow fixture where the selected support/current pair fits but an extra anchor does not, and only extend the same fallback if that concrete budget miss reproduces.

## 2026-06-30T08:33:41Z - no-overview plural deployment-approval contact rollback queries now use a phrase-alias FTS parent

- Scope: upgraded one bounded L3 local-first retrieval slice from the previously queued no-overview plural `deployment approvals contact rollback policy notes` miss into a narrow direct-subject phrase-alias parent lookup plus regression lock.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: when the singular overview memory is absent, the plural direct-subject contact query `deployment approvals contact rollback policy notes` no longer falls through to `search_mode=none` / `activation_reason=no-lexical-match-compound-query`. Direct-subject alias lookup now appends the existing allow-empty deployment-approval phrase alias as a later FTS parent, so the query resolves via `selected_search_query=deployment approver` with `selected_search_basis=direct-subject-phrase-alias`, stays on `fts-direct-subject-compound-query`, keeps `deployment approver`, `deployment approver is`, `deployment approvals rollback policy`, and `rollback policy is` receipt-visible, and injects the contact plus rollback facts without changing `MemoryStore.search()` return shape, SQLite schema, or provider behavior.
- Tests: focused retrieval coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_deployment_approval_contact_query_auto_enables_multi_hop_and_recovers_specific_facts tests.test_store.MemoryStoreTest.test_compound_fts_direct_subject_deployment_approval_contact_query_without_overview_uses_phrase_alias_parent -q` -> passed (`Ran 2 tests`); focused runtime coverage `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_deployment_approval_contact_query_recovers_specific_rollback_fact tests.test_runner.RunnerTest.test_build_context_auto_multi_hop_fts_deployment_approval_contact_query_without_overview_uses_phrase_alias_parent -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 287 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 126 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: additive `retrieval.query_lookup.selected_search_query`, `selected_search_basis`, `semantic_aliases.search_alias_variants`, `retrieval.multi_hop.activation_reason`, `retrieval.multi_hop.subqueries[*].query`, `retrieved_memory_ids`, and `injected_memory_ids` now explain the no-overview phrase-alias rescue without a schema migration. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo or raw-installer targets; older legacy target strings remain confined to historical tracker/build-log text.
- Blockers: this widening stays intentionally narrow to the direct-subject contact family with the existing allow-empty deployment-approval phrase alias. The symmetric plural owner no-overview family is still unverified on this path.
- Next safe slice: add the matching no-overview regression for `deployment approvals owner rollback policy notes`, and only widen direct-subject owner phrase-alias parent selection if that concrete miss still falls back to no-match routing.

## 2026-07-01T00:45:06Z - direct current restatements now beat matching changed-to anchors

- Scope: upgraded one bounded L3 local-first retrieval slice from a reproduced current-query wording miss into deterministic explicit-update current-value restatement preference.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: when an explicit update anchor such as `Deployment approver changed to Priya.` and an exact current restatement such as `Deployment approver is Priya.` share the same subject/current value, deterministic explicit-update conflict assembly now keeps the direct current fact current instead of injecting the mutation phrasing on current deployment-approval wrapper queries. Receipts surface that choice through `retrieval.temporal.conflict_sets[*].matching_current_value_ids` and `resolution_strategy=explicit_update_current_value_restatement_prefers_direct_fact_v1` without changing `MemoryStore.search()` return shape, SQLite schema, or provider behavior.
- Tests: focused retrieval coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_deployment_approval_on_point_query_prefers_direct_current_fact_over_update_anchor tests.test_runner.RunnerTest.test_build_context_current_deployment_approval_now_prefers_direct_fact_over_update_anchor -q` -> passed (`Ran 2 tests`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 292 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 133 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: `retrieval.temporal.conflict_sets[*]` now shows the matching direct-current restatement ids plus the new resolution strategy so operators can see why the direct fact stayed current while the update anchor became stale. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo or raw-installer targets.
- Blockers: this path is still explained through temporal conflict metadata rather than a dedicated candidate-level exclusion field.
- Next safe slice: add receipt-visible exclusion metadata for direct-current-over-update restatement conflicts, or move to the next reproduced local-first current/update ranking miss if a higher-leverage repro appears first.

## 2026-07-01T04:39:49Z - skipped explicit-update anchors now surface as selection exclusions

- Scope: closed the next bounded L3 explainability gap for direct-current-over-update current queries.
- Files touched: `zerker_memory/store.py`, `tests/test_store.py`, `tests/test_runner.py`, `docs/CONTINUOUS_BUILD/hybrid-retrieval.log.md`, `docs/SWARM_OPERATION_TRACKER.md`, `docs/BUILD_LOG.md`, `docs/CURRENT_STATE.md`.
- Behavior changed: when `current_only_v1` keeps a direct current restatement such as `Deployment approver is Priya.` over a sibling update anchor such as `Deployment approver changed to Priya.`, receipts now add `retrieval.temporal.selection_exclusions[*]` only for the skipped update anchor with `reason=explicit-update-anchor-not-selected` and `detail=direct-current-restatement-selected`. Candidate metadata mirrors that through `temporal_selection_exclusion` and `temporal_selection_exclusion_reason` without changing `MemoryStore.search()` return shape, SQLite schema, or provider behavior.
- Tests: focused retrieval coverage `python3 -m unittest tests.test_store.MemoryStoreTest.test_current_deployment_approval_on_point_query_prefers_direct_current_fact_over_update_anchor -q` -> passed (`Ran 1 test`); focused runtime coverage `python3 -m unittest tests.test_runner.RunnerTest.test_build_context_current_deployment_approval_now_prefers_direct_fact_over_update_anchor -q` -> passed (`Ran 1 test`); `git diff --check -- zerker_memory/store.py tests/test_store.py tests/test_runner.py` -> passed; `python3 -m unittest tests.test_store -q` -> passed (`Ran 292 tests`); `python3 -m unittest tests.test_policy -q` -> passed (`Ran 7 tests`); `python3 -m unittest tests.test_runner -q` -> passed (`Ran 133 tests`); `python3 -m zerker_memory eval` -> passed (`11/11`).
- Artifacts/receipts: the deployment-approval current repro now keeps `retrieval.temporal.conflict_sets[*]` for the conflict decision and a separate candidate-level exclusion object for the skipped update anchor, while the older stale fact remains unexcluded because it is historical evidence rather than the displaced current-value update anchor. Focused stale-target scan on `zerker_memory/store.py`, `tests/test_store.py`, and `tests/test_runner.py` found no active stale public repo or raw-installer targets.
- Blockers: this exclusion contract is intentionally narrow to explicit-update conflicts resolved by a direct current restatement; other current-only non-selection paths still rely on conflict-set or ordering metadata instead of a dedicated exclusion object.
- Next safe slice: move to the next reproduced local-first current/update ranking miss, and only widen this exclusion contract if another concrete skipped-anchor path appears.
